<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Web前端爱好者</title>
  <subtitle>愿景成为一名全栈工程师</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="https://cnt1992.github.io/"/>
  <updated>2017-04-05T01:41:37.000Z</updated>
  <id>https://cnt1992.github.io/</id>
  
  <author>
    <name>SkyCai</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>前端工程师全栈之路之部署环境搭建篇</title>
    <link href="https://cnt1992.github.io/2017/04/05/how-to-deploy-a-new-website/"/>
    <id>https://cnt1992.github.io/2017/04/05/how-to-deploy-a-new-website/</id>
    <published>2017-04-04T16:00:07.000Z</published>
    <updated>2017-04-05T01:41:37.000Z</updated>
    
    <content type="html"><![CDATA[<p>又是一年清明雨上，去年写<a href="http://cnt1992.xyz/2016/04/08/upgrade-nginx-to-http2/" target="_blank" rel="external">前端工程师学习Nginx实践配置HTTP2.0篇</a>的时候心想着有钱能买个服务器，一年过去了，我终于有了一个腾讯云的服务器！！！（虽然只有一个月免费试用期… ）。</p>
<p>这篇文章想要偏向实践兼顾理论地系统性介绍如何在一个全新的Linux服务器上部署一个Node应用，我们知道Node依然很火，同时也越来越多全栈情怀的工程师诞生，这篇文章以<code>Nginx + Node + Mongodb</code>的技术架构来介绍如何从0开始一步步部署一个应用。</p>
<p>首先，想要实践的童鞋得先有一台Linux服务器，土豪直接去<strong>腾讯云</strong>或者<strong>阿里云</strong>等云平台购买，像我优雅的舍不得花钱的申请一个月的免费试用玩玩。</p>
<a id="more"></a>
<h2 id="一点点知识储备"><a href="#一点点知识储备" class="headerlink" title="一点点知识储备"></a>一点点知识储备</h2><p>有了云服务器之后，第一步是登录到这台服务器，最简单的命令就是打开一个<strong>终端</strong>，然后输入：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">$ ssh 账号@服务器IP</div><div class="line">## 输入密码之后直接回车</div></pre></td></tr></table></figure>
<p>登录上去之后只有一个命令行界面，那么需要懂一些基本的<strong>Linux</strong>命令了，这里列举一下我们将要用到的命令（诸如：ls/cat/cd/vi这些命令就不介绍了），具体使用过程也可以回头看：</p>
<ul>
<li>ps：显示当前进程的状态，我们经常需要查看某个进程是否开启着，例如想查看所有进程信息：<code>ps -ef</code>，关于参数介绍可以直接输入命令行<code>ps --help all</code>查看具体含义</li>
<li>grep：过滤或者搜索特定的字符，例如结合<code>ps</code>命令我们想过滤出<strong>nginx</strong>的进程，那么可以用：<code>ps -ef | grep nginx</code></li>
<li>wget/curl：这两个货都可以用来下载指定URL的内容，比如下载二进制安装包，用法上有一些差别，同时功能上也有一些区别，例如下载某个URL的内容：<code>wget URL</code>/<code>curl -O URL</code></li>
<li>tar：解压命令，我们一般下载了二进制压缩包之后需要解压进行编译，假如解压xxx.tar.gz包直接用：<code>tar zxvf xxx.tar.gz</code></li>
<li>yum：一个包管理器，可以理解为Mac平台下的brew，或者是Node下的npm，这货提供了查找，增加，删除软件包(.rpm)的功能，例如我们在安装<strong>Nginx</strong>之前需要安装一个<strong>openssl</strong>库，那么可以直接使用：<code>yum -y install openssl</code>进行安装</li>
</ul>
<p>值得一提的一点是，我们不可能记住所有命令提供的参数，这时候要么网上搜索，要么直接就用Linux自带的<code>man</code>命令（当然全英的，英文也是程序员必备技能）。</p>
<p>一切准备就绪，<del>撸</del>搞起来。</p>
<blockquote>
<p>我们所有操作的基础路径在<code>/usr/local/src</code>，也就是下载的二进制包都会放在这里统一管理，请先进入该目录，若不想用这个目录下面有些路径需要对应调整</p>
</blockquote>
<h2 id="安装Nginx"><a href="#安装Nginx" class="headerlink" title="安装Nginx"></a>安装Nginx</h2><p>其实我们完全可以不用<strong>Nginx</strong>，无非就是<strong>Node</strong>应用起来之后在访问的时候需要多一个端口号而已。但为了不暴露我们实际应用的端口号，用<strong>Nginx</strong>来做反向代理是非常有必要的，<strong>Nginx</strong>的介绍可以翻看我一年前写的文章：<a href="http://cnt1992.xyz/2016/03/18/simple-intro-to-nginx/" target="_blank" rel="external">传送门1</a>/<a href="http://cnt1992.xyz/2016/04/08/upgrade-nginx-to-http2/" target="_blank" rel="external">传送门2</a>。在安装<strong>Nginx</strong>之前得先安装一些必要的编译工具及库文件：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">yum -y install make zlib zlib-devel gcc-c++ libtool openssl openssl-devel</div></pre></td></tr></table></figure>
<blockquote>
<p>上面安装的库列表如果有一个出错，那么后面的库就不会自动安装，所以如果你看到命令行有出错并且提示哪个库，就排查掉那个库再继续安装</p>
</blockquote>
<p>紧接着安装<strong>PCRE</strong>，这家伙提供了Rewrite功能：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">## 先下载二进制包</div><div class="line">wget http://downloads.sourceforge.net/project/pcre/pcre/8.35/pcre-8.35.tar.gz</div><div class="line"></div><div class="line">## 解压</div><div class="line">tar zxvf pcre-8.35.tar.gz</div><div class="line"></div><div class="line">## 进入安装目录编译安装</div><div class="line">cd pcre-8.35</div><div class="line">./configure</div><div class="line">make &amp;&amp; make install</div></pre></td></tr></table></figure>
<p>现在就可以正式安装<strong>Nginx</strong>了，跟上面安装同样的方法：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">## 先返回上一级目录</div><div class="line">cd ..</div><div class="line"></div><div class="line">## 下载二进制包</div><div class="line">wget http://nginx.org/download/nginx-1.10.3.tar.gz(截止本文最新的稳定版本)</div><div class="line"></div><div class="line">## 解压</div><div class="line">tar zxvf nginx-1.10.3.tar.gz</div><div class="line"></div><div class="line">## 进入安装目录编译安装</div><div class="line">cd nginx-1.10.3</div><div class="line">./configure --prefix=/usr/local/nginx --with-http_stub_status_module --with-http_ssl_module --with-pcre=/usr/local/src/pcre-8.35</div><div class="line">make &amp;&amp; make install</div></pre></td></tr></table></figure>
<p>安装成功之后可以执行 <code>/usr/local/nginx/sbin/nginx -v</code>查看版本，但我们不想每次都输那么一长串命令，所以可以简化一些命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">## 编辑~/.bashrc文件</div><div class="line">vi ~/.bashrc</div><div class="line"></div><div class="line">## 在~/.bashrc文件找到alias直接输入o在下一行加入下面命令：</div><div class="line">alias nginx=&apos;/usr/local/nginx/sbin/nginx&apos;</div><div class="line"></div><div class="line">## 然后按一下`esc`键，再`shift+;`，输入`x`保存退出，然后还要让配置文件生效：</div><div class="line">source ~/.bashrc</div></pre></td></tr></table></figure>
<h2 id="配置Nginx"><a href="#配置Nginx" class="headerlink" title="配置Nginx"></a>配置Nginx</h2><p>安装好了<strong>Nginx</strong>之后我们还需要配置一下，这里就直接命令带过：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">## 创建www组跟www用户用于nginx的进程</div><div class="line">/usr/sbin/groupadd www </div><div class="line">/usr/sbin/useradd -g www www</div></pre></td></tr></table></figure>
<p>编辑<strong>Nginx</strong>配置文件（文件路径在<code>/usr/local/nginx/conf/nginx.conf</code>），我们先只改user，也就是第一行被注释的改成：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">user www www;</div></pre></td></tr></table></figure>
<p>其它的等下再改，保存退出之后启动<strong>Nginx</strong>，输入下面命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">## 配置文件检查</div><div class="line">nginx -t</div><div class="line"></div><div class="line">## 上面检查没问题之后启动</div><div class="line">nginx</div></pre></td></tr></table></figure>
<p>这个时候直接浏览器输入你的IP就可以访问到<strong>Nginx</strong>的欢迎界面了。</p>
<h2 id="安装Node"><a href="#安装Node" class="headerlink" title="安装Node"></a>安装Node</h2><p>在安装<strong>Node</strong>之前，其实我推荐先安装<a href="https://github.com/creationix/nvm" target="_blank" rel="external">nvm</a>，<strong>Node</strong>版本控制器，可以安装指定版本的<strong>Node</strong>同时又可以随时切换不同版本，只需要一条命令自动安装：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">wget -qO- https://raw.githubusercontent.com/creationix/nvm/v0.33.1/install.sh | bash</div></pre></td></tr></table></figure>
<p>上面命令成功之后，执行以下<code>source ~/.bashrc</code>让<strong>nvm</strong>命令生效，然后我们就可以愉快地安装最新稳定版的<strong>Node</strong>了：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">nvm install v6.10.1</div></pre></td></tr></table></figure>
<p>由于我们是第一次安装了<strong>Node</strong>，所以<strong>nvm</strong>会自动将这个版本的<strong>Node</strong>作为默认版本，这时可以输入<code>node -v</code>/<code>npm -v</code>看看我们安装的版本。</p>
<h2 id="安装Mongodb"><a href="#安装Mongodb" class="headerlink" title="安装Mongodb"></a>安装Mongodb</h2><p>听说用<strong>Node</strong>的都很喜欢用<strong>Mongodb</strong>数据库？那么我们这个例子也安装一下：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">## 下载二进制包</div><div class="line">wget https://fastdl.mongodb.org/linux/mongodb-linux-x86_64-3.0.6.tgz</div><div class="line"></div><div class="line">## 解压</div><div class="line">tar -zxvf mongodb-linux-x86_64-3.0.6.tgz </div><div class="line"></div><div class="line">## 直接将解压包移动到指定目录</div><div class="line">mv  mongodb-linux-x86_64-3.0.6/ /usr/local/mongodb</div></pre></td></tr></table></figure>
<p><strong>Mongodb</strong>可执行的命令放在<code>/usr/local/mongodb/bin/</code>下面，为了方便不带这么长的前缀，可以将路径添加到<strong>PATH</strong>里面：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">export PATH=/usr/local/mongodb/bin:$PATH</div></pre></td></tr></table></figure>
<p>紧接着创建数据库目录，执行下面命令：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">mkdir -p /data/db</div></pre></td></tr></table></figure>
<blockquote>
<p>-p参数会自动级联创建文件夹，也就是data文件夹不存在先创建data文件夹再创建db文件夹</p>
</blockquote>
<p>当然，如果你不想用默认的数据库目录，也可以指定<code>--dbpath=xxx</code>来启动，这时启动<strong>Mongodb</strong>：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">mongod</div></pre></td></tr></table></figure>
<h2 id="上传代码"><a href="#上传代码" class="headerlink" title="上传代码"></a>上传代码</h2><blockquote>
<p>这里我们为了演示直接采用本地代码直接上传，实际项目中我们一般把代码提交到<strong>github</strong>然后利用<code>github hook</code>触发代码拉取。</p>
</blockquote>
<p>假设我们本地代码路径在<code>~/node/test</code>，那么可以直接用<code>scp</code>命令上传到我们的服务器路径<code>/node/app</code>：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">scp -r ~/node/test root@服务器IP:/node/test</div><div class="line">## 输入密码回车</div></pre></td></tr></table></figure>
<p>上传成功之后就可以用<strong>Node</strong>命令启动了，比如我的代码启动之后就下面这样子：</p>
<p><img src="/img/node-deploy/node_deploy.png" alt="node应用启动"></p>
<p>然后直接在浏览器打开：<a href="">http://服务器IP:9990</a>，就可以看到你的应用启动了！好了，结束，拜~</p>
<p>等等，作为有追求的全栈工程师，这还没完，这离我们想要的效果还有些出入：</p>
<ul>
<li>访问可不可以去掉端口号？</li>
<li>启动<strong>Node</strong>应用就一直挂着命令行?</li>
</ul>
<p>带着这两个问题，我们继续。</p>
<h2 id="安装pm2"><a href="#安装pm2" class="headerlink" title="安装pm2"></a>安装pm2</h2><p>嗯，<a href="https://github.com/Unitech/pm2" target="_blank" rel="external">pm2</a>作为<strong>Node</strong>应用管理器，在生产环境下强烈建议使用，本地开发的建议使用<a href="https://github.com/remy/nodemon" target="_blank" rel="external">nodemon</a>，能够监听文件改动并自动重启应用。</p>
<p><strong>pm2</strong>只是一个<strong>npm</strong>包，安装一下无痛感使用：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">npm i -g pm2</div></pre></td></tr></table></figure>
<p>这个时候我们就可以愉快地使用<strong>pm2</strong>来启动我们的应用了：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">pm2 start index.js</div></pre></td></tr></table></figure>
<p>更多<strong>pm2</strong>的命令可以阅读上面的链接。</p>
<h2 id="Nginx反向代理"><a href="#Nginx反向代理" class="headerlink" title="Nginx反向代理"></a>Nginx反向代理</h2><p>要去掉端口号访问很简单，只需要利用<strong>Nginx</strong>反向代理到<strong>Node</strong>应用即可，直接在<code>/usr/local/nginx/conf/nginx.conf</code>找到<code>location</code>直接添加下面：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">location /app/ &#123;</div><div class="line">    proxy_pass http://127.0.0.1:9990;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>然后执行下面命令重新加载<strong>Nginx</strong>配置文件：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><div class="line">nginx -s reload</div></pre></td></tr></table></figure>
<blockquote>
<p>如果执行上面命令出现 <code>nginx: [error] invalid PID number &quot;&quot; in &quot;/usr/local/nginx/logs/nginx.pid&quot;</code>错误提示，那么先执行以下<code>nginx -c /usr/local/nginx/conf/nginx.conf</code>即可</p>
</blockquote>
<p>这时就可以愉快的用 <a href="">http://IP/app</a> 访问应用了。</p>
]]></content>
    
    <summary type="html">
    
      越来越多追求全栈的工程师情怀，本文偏实践兼顾理论的介绍全栈工程师入门级别如何部署一个应用
    
    </summary>
    
      <category term="教程" scheme="https://cnt1992.github.io/categories/%E6%95%99%E7%A8%8B/"/>
    
    
      <category term="Nginx Node Linux 腾讯云" scheme="https://cnt1992.github.io/tags/Nginx-Node-Linux-%E8%85%BE%E8%AE%AF%E4%BA%91/"/>
    
  </entry>
  
  <entry>
    <title>【译】ES建议：async函数</title>
    <link href="https://cnt1992.github.io/2017/03/28/es-proposal-async-functions/"/>
    <id>https://cnt1992.github.io/2017/03/28/es-proposal-async-functions/</id>
    <published>2017-03-27T17:16:56.000Z</published>
    <updated>2017-03-27T10:00:34.000Z</updated>
    
    <content type="html"><![CDATA[<p><code>Async</code>函数是<code>Brian Terlson</code>提出的<code>ECMAScript提案</code>，目前处于<code>stage 3(candidate 候选)</code>阶段。</p>
<p>| 译者注：ECMAScript提案分三个阶段：草图(Sketch) -&gt; 提案(Proposal) -&gt; 规范(Standard)，所以<code>Async</code>很大的希望能成为规范.</p>
<p>在开始解释<code>async</code>函数之前，我想先讲一下如何通过组合<code>Promises</code>和<code>generators</code>用<strong>看起来同步的代码</strong>来控制异步操作。</p>
<h2 id="通过-Promises-和-generators-写异步代码"><a href="#通过-Promises-和-generators-写异步代码" class="headerlink" title="通过 Promises 和 generators 写异步代码"></a>通过 Promises 和 generators 写异步代码</h2><p>在众多的能用于异步的函数中，<code>Promises</code>，作为<code>ES6</code>的一部分，变得越来越受欢迎。一个例子就是客户端的<code>fetch API</code>，用来发送请求获取数据从而替代<code>XMLHttpRequest</code>。使用<code>fetch</code>看起来像下面这样：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">fetchJson</span>(<span class="params">url</span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> fetch(url)</div><div class="line">        .then(<span class="function"><span class="params">request</span> =&gt;</span> request.text())</div><div class="line">        .then(<span class="function"><span class="params">text</span> =&gt;</span> &#123;</div><div class="line">            <span class="keyword">return</span> <span class="built_in">JSON</span>.parse(text);</div><div class="line">        &#125;)</div><div class="line">        .catch(<span class="function"><span class="params">error</span> =&gt;</span> &#123;</div><div class="line">            <span class="built_in">console</span>.log(<span class="string">`ERROR: <span class="subst">$&#123;error.stack&#125;</span>`</span>);</div><div class="line">        &#125;);</div><div class="line">&#125;</div><div class="line">fetchJson(<span class="string">'http://example.com/some_file.json'</span>).then(<span class="function"><span class="params">obj</span> =&gt;</span> <span class="built_in">console</span>.log(obj));</div></pre></td></tr></table></figure>
<p><a href="https://github.com/tj/co" target="_blank" rel="external">co</a>库通过使用<code>Promises</code>和<code>generators</code>能让代码看起来更同步，上面的例子用<code>co</code>来写就是下面这样子：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">const</span> fetchJson = co.wrap(<span class="function"><span class="keyword">function</span>* (<span class="params">url</span>) </span>&#123;</div><div class="line">    <span class="keyword">try</span> &#123;</div><div class="line">        <span class="keyword">let</span> request = <span class="keyword">yield</span> fetch(url);</div><div class="line">        <span class="keyword">let</span> text = <span class="keyword">yield</span> request.text();</div><div class="line">        <span class="keyword">return</span> <span class="built_in">JSON</span>.parse(text);</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">catch</span> (error) &#123;</div><div class="line">        <span class="built_in">console</span>.log(<span class="string">`ERROR: <span class="subst">$&#123;error.stack&#125;</span>`</span>);</div><div class="line">    &#125;</div><div class="line">&#125;);</div><div class="line">fetchJson(<span class="string">'http://example.com/some_file.json'</span>).then(<span class="function"><span class="params">obj</span> =&gt;</span> <span class="built_in">console</span>.log(obj));</div></pre></td></tr></table></figure>
<p>每一次回调(一个<code>generator</code>函数)都会传递一个<code>Promises</code>给到<code>co</code>，然后回调进入暂停状态。一旦<code>Promise</code>执行完毕，<code>co</code>就会触发回调：如果<code>Promise</code>返回<code>fulfilled(成功)</code>，<code>yield</code>返回成功状态的值，如果返回<code>rejected(失败)</code>，<code>yield</code>就会抛出失败的错误。另外，<code>co</code>会将返回的回调包装成一个<code>Promise</code>(跟<code>then()</code>的用途一样)。</p>
<h2 id="Async-函数"><a href="#Async-函数" class="headerlink" title="Async 函数"></a>Async 函数</h2><p>上面的例子用<code>Async</code>函数的基本用法实现如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">async</span> <span class="function"><span class="keyword">function</span> <span class="title">fetchJson</span>(<span class="params">url</span>) </span>&#123;</div><div class="line">    <span class="keyword">try</span> &#123;</div><div class="line">        <span class="keyword">let</span> request = <span class="keyword">await</span> fetch(url);</div><div class="line">        <span class="keyword">let</span> text = <span class="keyword">await</span> request.text();</div><div class="line">        <span class="keyword">return</span> <span class="built_in">JSON</span>.parse(text);</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">catch</span> (error) &#123;</div><div class="line">        <span class="built_in">console</span>.log(<span class="string">`ERROR: <span class="subst">$&#123;error.stack&#125;</span>`</span>);</div><div class="line">    &#125;</div><div class="line">&#125;</div><div class="line">fetchJson(<span class="string">'http://example.com/some_file.json'</span>).then(<span class="function"><span class="params">obj</span> =&gt;</span> <span class="built_in">console</span>.log(obj));</div></pre></td></tr></table></figure>
<p>在实现上，<code>async</code>函数更像<code>generators</code>，但是不会转换成<code>generatos</code>函数.</p>
<p>Internally, async functions work much like generators, but they are not translated to generator functions.</p>
<h3 id="Async-函数其他的一些用法"><a href="#Async-函数其他的一些用法" class="headerlink" title="Async 函数其他的一些用法"></a>Async 函数其他的一些用法</h3><p>现在存在如下的<code>async</code>函数的变体：</p>
<ul>
<li>Async 函数声明： <code>async function foo() {}</code></li>
<li>Async 函数表达式： <code>const foo = async function() {}</code></li>
<li>Async 方法定义： <code>let obj = { async foo() {} }</code></li>
<li>Async 箭头函数：<code>const foo = async () =&gt; {};</code></li>
</ul>
<h2 id="深入阅读"><a href="#深入阅读" class="headerlink" title="深入阅读"></a>深入阅读</h2><p><a href="https://github.com/tc39/ecmascript-asyncawait" target="_blank" rel="external">Async Functions</a>(Brian Terlson)<br><a href="http://exploringjs.com/es6/ch_generators.html#sec_co-library" target="_blank" rel="external">Simplifying asynchronous computations via generators</a>(“Exploring ES6”的章节)</p>
]]></content>
    
    <summary type="html">
    
      简要介绍了异步函数进化史，同时推荐ES7的async函数，毕竟ES6的generator跟yield只是一个过渡
    
    </summary>
    
      <category term="翻译" scheme="https://cnt1992.github.io/categories/%E7%BF%BB%E8%AF%91/"/>
    
    
      <category term="ES7 async await" scheme="https://cnt1992.github.io/tags/ES7-async-await/"/>
    
  </entry>
  
  <entry>
    <title>CSS Grid Layout 不完整指南</title>
    <link href="https://cnt1992.github.io/2017/01/20/A-Complete-Guide-to-Grid/"/>
    <id>https://cnt1992.github.io/2017/01/20/A-Complete-Guide-to-Grid/</id>
    <published>2017-01-20T11:24:29.000Z</published>
    <updated>2017-03-27T09:58:44.000Z</updated>
    
    <content type="html"><![CDATA[<p>| 本文还没写完。。。</p>
<a id="more"></a>
<h2 id="介绍"><a href="#介绍" class="headerlink" title="介绍"></a>介绍</h2><p><code>CSS Grid Layout</code>，即网格布局系统，旨在用最简单的方式改变用户界面。</p>
<p>CSS发展至今，最基础也最核心的部分是布局，从一开始的用黑科技<code>float</code>进行多列布局，到后来的<code>flex</code>布局的出现，再到现在想讲的<code>Grid</code>布局系统，可以说大大加快网页布局的书写之外语义化也更加明确了。</p>
<h2 id="兼容性"><a href="#兼容性" class="headerlink" title="兼容性"></a>兼容性</h2><p>从下图看出，目前主流浏览器尚且不支持该属性（截至现在最新Chrome版本为55），所以想要体验<code>Grid</code>属性请先打开<a href="chrome://flags/" target="_blank" rel="external">chrome://flags/</a>将<code>experimental Web Platform features</code>开启。</p>
<iframe style="border: none;width: 100%;height: 520px;background: " src="https://caniuse.bitsofco.de/embed/index.html?feat=css-grid&periods=future_3,current,past_1,past_2"></iframe>

<h2 id="示例一览"><a href="#示例一览" class="headerlink" title="示例一览"></a>示例一览</h2><p>我们先从一个最经典的三列布局（左右固定宽度中间自适应）来看看如何用<code>grid</code>实现：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><div class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">html</span> <span class="attr">lang</span>=<span class="string">"en"</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">meta</span> <span class="attr">charset</span>=<span class="string">"UTF-8"</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>Document<span class="tag">&lt;/<span class="name">title</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">style</span>&gt;</span><span class="css"></span></div><div class="line">        <span class="selector-class">.container</span> &#123;</div><div class="line">            <span class="attribute">display</span>: grid;</div><div class="line">            <span class="attribute">grid-template-columns</span>: <span class="number">100px</span> auto <span class="number">150px</span>;</div><div class="line">            <span class="attribute">grid-template-areas</span>: <span class="string">"left main right"</span>;</div><div class="line">            </div><div class="line">            <span class="attribute">min-height</span>: <span class="number">400px</span>;</div><div class="line">        &#125;</div><div class="line">        <span class="selector-class">.left</span> &#123;</div><div class="line">            <span class="attribute">grid-area</span>: left;</div><div class="line">        &#125;</div><div class="line">        <span class="selector-class">.main</span> &#123;</div><div class="line">            <span class="attribute">grid-area</span>: main;</div><div class="line">        &#125;</div><div class="line">        <span class="selector-class">.right</span> &#123;</div><div class="line">            <span class="attribute">grid-area</span>: right;</div><div class="line">        &#125;</div><div class="line">    <span class="tag">&lt;/<span class="name">style</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"container"</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"left"</span>&gt;</span></div><div class="line">            left</div><div class="line">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"main"</span>&gt;</span></div><div class="line">            main</div><div class="line">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">class</span>=<span class="string">"right"</span>&gt;</span></div><div class="line">            right</div><div class="line">        <span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></div></pre></td></tr></table></figure>
<h2 id="语法"><a href="#语法" class="headerlink" title="语法"></a>语法</h2><p>1.Grid Container</p>
<p>想让一个容器变成<code>Grid</code>，只需要如下声明：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-class">.container</span> &#123;</div><div class="line">  <span class="attribute">display</span>: grid | inline-grid | subgrid;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<table>
<thead>
<tr>
<th style="text-align:center">属性</th>
<th style="text-align:center">grid</th>
<th style="text-align:center">inline-grid</th>
<th style="text-align:center">subgrid</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">说明</td>
<td style="text-align:center">块状级别grid</td>
<td style="text-align:center">内联级别grid</td>
<td style="text-align:center">自身已经是一个grid item，同时声明自己是一个grid container</td>
</tr>
</tbody>
</table>
<blockquote>
<p>注意： <code>column</code>, <code>float</code>, <code>clear</code>, 以及 <code>vertical-align</code> 对声明了<code>display: grid;</code> 的元素无效。</p>
</blockquote>
<p>2.Grid Items</p>
<h2 id="参考资源"><a href="#参考资源" class="headerlink" title="参考资源"></a>参考资源</h2><ul>
<li><a href="https://css-tricks.com/snippets/css/complete-guide-grid/" target="_blank" rel="external">complete-guide-grid</a></li>
<li><a href="https://bitsofco.de/3-new-css-features-to-learn-in-2017/" target="_blank" rel="external">3-new-css-features-to-learn-in-2017</a></li>
</ul>
]]></content>
    
    <summary type="html">
    
      介绍并实践下CSS Grid Layout
    
    </summary>
    
      <category term="教程" scheme="https://cnt1992.github.io/categories/%E6%95%99%E7%A8%8B/"/>
    
    
      <category term="CSS" scheme="https://cnt1992.github.io/tags/CSS/"/>
    
  </entry>
  
  <entry>
    <title>2016小结</title>
    <link href="https://cnt1992.github.io/2016/12/25/summary-of-my-2016/"/>
    <id>https://cnt1992.github.io/2016/12/25/summary-of-my-2016/</id>
    <published>2016-12-25T15:21:00.000Z</published>
    <updated>2016-12-25T13:31:15.000Z</updated>
    
    <content type="html"><![CDATA[<p>12月25日，西方的<strong>过年</strong>，对东方人们来说距离新的一年<strong>2017</strong>只剩<strong>5</strong>天。</p>
<p>来天猫已经快10个月，这10个月都在<code>忙 忙 忙</code>中度过，忙着开各种需求评审会，忙着跟业务方各种<del>撕逼</del>洽谈，忙着在半夜三更才能安安静静地写代码修bug。但老生常谈的是，这一年还是些许的小结值得写下来，算是给自己这一年的交代，以及给自己未来一年该怎么继续走一个思考。</p>
<a id="more"></a>
<h2 id="技术成长"><a href="#技术成长" class="headerlink" title="技术成长"></a>技术成长</h2><p>年初选择从京东跳槽过来天猫，最看重的还是天猫前端的技术，从13年双十一天猫全面切换到<code>Node</code>技术栈开始，到今年的<code>Weex</code>在双十一展现出<strong>web与native融合</strong>的不俗能力，不得不说天猫前端至少在业内还算是一个不错的团队存在。虽然外界看来，阿里系的开源框架都为了<code>KPI</code>而出现，并且不久后便夭折（当然，我自己也觉得很多东西其实都是为了<code>KPI</code>而出来的），但是对于<code>前端er</code>来说，我开始慢慢觉得：与其拒绝，不如从中学到一些东西，然后提取出好的抛弃坏的，让自己得到成长，转化成自己的技能。</p>
<p>这一年的<code>React</code>/<code>Vue</code>/<code>Angular</code>三足鼎立，似乎对于这边来说更青睐于<code>React</code>。比如对于我在的小组负责天猫交易来说，业务情况是需要管理各种状态，而且相对来说比较复杂，这样能选择的当然是<code>React</code>。经手的多个项目几乎都采用了<code>React</code>技术栈，用上<code>Flux</code>/<code>Redux</code>框架。当然，并不重复性的从<code>搭建脚手架 -&gt; 开发页面 -&gt; 联调</code>，而是在<code>Redux</code>框架的基础上封装一套适用于这边的业务场景的SDK<code>Rschema</code>，这个SDK解放了前端的重复性劳动：</p>
<ol>
<li>前端不再关注页面长什么样子（页面模块搭建依赖于JSON数据）</li>
<li>前端主要负责模块的研发（基于npm包的形式开发）</li>
</ol>
<p>不知道<code>Rschema</code>哪天是不是也会开源，但我觉得对于我所接触的业务来说是一个非常不错的SDK。（适合各自团队的才是最好的）</p>
<p>关于单页面应用，虽然<code>react-router</code>可以很好的解决浏览器后退的问题，但依然不够完美，一个路由一个<code>component</code>在理想状态下是对的，但一个<code>component</code>对应多个<code>status</code>的时候，<strong>浏览器后退</strong>似乎又成为了一个无法避免的问题所在，当你有多个<code>status</code>还需要改成多个<code>component</code>的时候真的会疯掉。发现<code>Swiper</code>这货除了手势滑动之后，其实也可以做成<strong>伪单页面应用</strong>，动态控制<code>Swiper</code>的页面从而达到新的页面从右侧滑入，左划后退，体验上还不错。但总体而言，对于单页面应用还是保持意见，虽然达到了接近原生的体验，但是首次加载的时间耗费以及如果业务很复杂的时候似乎成本有点高。</p>
<h2 id="为人处世"><a href="#为人处世" class="headerlink" title="为人处世"></a>为人处世</h2><p>出来工作两年，脾气似乎没有怎么改。自己坚信的一些东西，始终没能轻易动摇。在公司与人相处，<strong>和善</strong>是第一。但是也不能一味的退让，毕竟大家都是平等的。在京东也是，在阿里也是，一旦触及自己底线的东西坚决不退让。很多时候为了赶上线，做出来的东西都大大折扣，需求方不满意，自己也没有成就感，这种情况在我看来依然是不允许发生的。所以，能忍让的东西忍让，忍让不了的时候就必须一步不让。</p>
<p>一开始的时候，觉得应该做一个<strong>老好人</strong>，但是时间久了会发现这种对于别人来说成本很低的事情真的是很尴尬的存在。比如今天谁谁谁过来随便找你做个事情，你就满口答应下来做了，过一天谁谁谁又过来找你做东西，你又满口答应了，也是，无穷无尽的加班，只为了做这些事情（但这些事情真的是有意义的么，自己考量过么，更客观点，老大们知道么），这样子久了自己学不到什么不说，让这些找你做事的人觉得这个成本很低，所以我总觉得<strong>老好人</strong>对于我来说是坚决不会做的。</p>
<p>新参与进来的业务，总是被当做一个<code>资源</code>那样子用。下周要发布了，这周才告知到我这里。一开始毕竟新入这个业务不熟悉，所以就忍让。等到对业务稍微熟悉了，也就把这些问题抛出来要求解决。这对于我来说就是要告诉别人我不愿意被当做一个<code>资源</code>来使用，也让别人知道也让我随随便便被使唤的成本很高，虽然短期看来是得罪了人，但长远来看，对自己必须是有利的。</p>
<h2 id="未来计划"><a href="#未来计划" class="headerlink" title="未来计划"></a>未来计划</h2><p>从<strong>重构</strong>转型到<strong>大前端</strong>，虽然不再专注于<code>CSS</code>，而偏向于<code>JavaScript</code>，但是依然关注更优雅的写样式代码。未来，依然是倾向于功能实现，毕竟毕业前几年依旧是打怪升级的时间，不过想花更多时间关注所做的东西的性能。同时，也想多思考自己所做的东西，存在的价值及意义。不想只是一味的写代码，做一个码农，而应该花时间思考自己所做的事情的意义。</p>
<p>嗯，在接下来的<strong>2017</strong>年，希望少加班（当然是不可能的了…）</p>
]]></content>
    
    <summary type="html">
    
      恍恍惚惚混迹前端圈两年半，已然是年底尾声，写点东西回顾这一年自己都干了啥，顺便展望一下未来。
    
    </summary>
    
      <category term="心得体会" scheme="https://cnt1992.github.io/categories/%E5%BF%83%E5%BE%97%E4%BD%93%E4%BC%9A/"/>
    
    
      <category term="前端 总结" scheme="https://cnt1992.github.io/tags/%E5%89%8D%E7%AB%AF-%E6%80%BB%E7%BB%93/"/>
    
  </entry>
  
  <entry>
    <title>天猫前端团队招人贴</title>
    <link href="https://cnt1992.github.io/2016/12/17/tmall-recruit/"/>
    <id>https://cnt1992.github.io/2016/12/17/tmall-recruit/</id>
    <published>2016-12-17T14:02:32.000Z</published>
    <updated>2016-12-17T06:09:10.000Z</updated>
    
    <content type="html"><![CDATA[<p>急招P6或者P7的前端，base地杭州，考虑到很多小伙伴想拿完年终奖之后再跳槽，可以先面试拿到offer之后再协商入职时间哦~ 有意愿的请投简历到 <a href="mailto:cnt19920613@gmail.com" target="_blank" rel="external">cnt19920613@gmail.com</a></p>
<a id="more"></a>
<h2 id="团队"><a href="#团队" class="headerlink" title="团队"></a>团队</h2><p>天猫事业部-产品技术部-交易链路&amp;行业开发-前端，招聘<strong>资深前端工程师和前端技术专家</strong>。</p>
<p>团队在业务上对应天猫主要行业（如天猫超市、天猫国际、电器城、阿里汽车、天猫家装等等）及天猫核心的交易系统。 横向上同时会参与主导阿里、天猫前端的多项横向技术体系。</p>
<p>团队专注于以工程化手段提升开发效率，以数据智能化的驱动业务进步，构建高性能的用户交互解决方案。</p>
<h2 id="岗位描述"><a href="#岗位描述" class="headerlink" title="岗位描述:"></a>岗位描述:</h2><ol>
<li><p>业务工作上：</p>
<ol>
<li>交易业务：支撑双十一1200亿成交背后的，天猫、淘宝的核心交易流程（购物车、确认订单、退款、投诉等）的开发中；从技术的角度，协同产品经理一起梳理业务发展规划并落地。负责千万级UV的PC、Mobile页面开发，并参与到交易智能化 - 通过智能化的方式为用户提供能符合其需求的交易 - 的探索工作。</li>
<li>企业购业务：面向百亿级的业务蓝海，参与到面向企业用户的专属业务企业购中；支撑企业用户从进货、采购到使用服务的多套场景。负责从首页、频道、搜索、交易流程，到多个企业专属系统的方案设计与前端开发中。</li>
</ol>
</li>
<li><p>技术工作上：</p>
<ol>
<li>效率提升：在理解前端开发流程的基础上，结合前端实际建立或优化提升工作效率的工具；</li>
<li>方案定制：在理解产品业务的基础上，提升产品的用户体验和业务价值，技术驱动业务的发展；</li>
<li>体验优化：对具体的产品进行性能优化，实现极致的页面加载、执行和渲染时间；</li>
<li>团队协作：关注前端前沿技术研究，通过新技术服务团队和业务。</li>
</ol>
</li>
<li><p>技术细节上：</p>
<ol>
<li>团队目前前端框架使用React、Vue2.0，使用ES6、7进行具体开发工作。</li>
<li>在Web和Native融合、Web To Native方面，使用过ReactNative，目前参与Weex的建设，和使用了Weex进行开发。</li>
<li>业务几乎全部使用模块化的开发方式。协同创立，并维护着一套，支持7+阿里子公司内的近20个核心系统的，覆盖服务端、前端&amp;Native端、测试端的通用组件化解决方案RSchema。</li>
<li>服务器语言，在日常的工作中，需要较频繁使用到Node.js，偶尔涉及Jave和Ruby。</li>
</ol>
</li>
</ol>
<h2 id="岗位要求"><a href="#岗位要求" class="headerlink" title="岗位要求:"></a>岗位要求:</h2><ol>
<li>熟练使用各种前端技术，包括HTML/CSS/JavaScript/等。</li>
<li>具备跨终端的前端开发能力，在Web（PC+Mobile）/Node.js/Native App三个方向上至少精通一个方向，具备多个的更佳，鼓励在Native和Web技术融合上的探索。</li>
<li>对前端工程化与模块化开发有一定了解，并有实践经验（如RequireJS/SeaJS/KISSY等）。</li>
<li>至少熟悉一门非前端的语言（如Java/PHP/C/C++/Python/Ruby），并有实践经验。</li>
<li>具备良好的团队协作精神，能利用自身技术能力提升团队整体研发效率，提高团队影响力。</li>
<li>对前端技术有持续的热情，个性乐观开朗，逻辑性强，善于和各种背景的人合作。</li>
</ol>
<p>有意愿的小伙伴猛投简历到我邮箱吧 <a href="mailto:cnt19920613@gmail.com" target="_blank" rel="external">cnt19920613@gmail.com</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;急招P6或者P7的前端，base地杭州，考虑到很多小伙伴想拿完年终奖之后再跳槽，可以先面试拿到offer之后再协商入职时间哦~ 有意愿的请投简历到 &lt;a href=&quot;mailto:cnt19920613@gmail.com&quot;&gt;cnt19920613@gmail.com&lt;/a&gt;&lt;/p&gt;
    
    </summary>
    
    
      <category term="招聘" scheme="https://cnt1992.github.io/tags/%E6%8B%9B%E8%81%98/"/>
    
  </entry>
  
  <entry>
    <title>问题式学习Mock开发</title>
    <link href="https://cnt1992.github.io/2016/09/28/problem-based-to-learn-mock-data/"/>
    <id>https://cnt1992.github.io/2016/09/28/problem-based-to-learn-mock-data/</id>
    <published>2016-09-28T10:06:23.000Z</published>
    <updated>2016-09-28T09:30:44.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>本文适合于重构转前端开发或对前后端分离的开发模式尚且没有真正的实践的童鞋，对这套模式有深入实践并且有一套心得的大神欢迎提出更多的建议。</p>
</blockquote>
<a id="more"></a>
<h2 id="问题一：我跟后端约定好了接口的数据格式，接下来我应该怎么进行开发呢？"><a href="#问题一：我跟后端约定好了接口的数据格式，接下来我应该怎么进行开发呢？" class="headerlink" title="问题一：我跟后端约定好了接口的数据格式，接下来我应该怎么进行开发呢？"></a>问题一：我跟后端约定好了接口的数据格式，接下来我应该怎么进行开发呢？</h2><p>这是前后端分离的第一步也是很重要的一步，跟后端约定好了数据格式之后两边可以并行开发而不需要等待另外一方提供真正的接口出来，假设数据格式如下（一般数据都会是<strong>JSON</strong>格式）：</p>
<figure class="highlight json"><table><tr><td class="code"><pre><div class="line">&#123;</div><div class="line">    <span class="attr">"success"</span>: <span class="literal">true</span>,</div><div class="line">    <span class="attr">"data"</span>: [</div><div class="line">        &#123;</div><div class="line">            <span class="attr">"id"</span>: <span class="number">1</span>,</div><div class="line">            <span class="attr">"name"</span>: <span class="string">"天哥"</span></div><div class="line">        &#125;,</div><div class="line">        &#123;</div><div class="line">            <span class="attr">"id"</span>: <span class="number">2</span>,</div><div class="line">            <span class="attr">"name"</span>: <span class="string">"sky"</span></div><div class="line">        &#125;</div><div class="line">    ]</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>前端只要约定好了数据格式那就可以愉快进行开发了，假设用现成的<strong>jQuery</strong>库来开发，核心代码就可以如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">;(<span class="function"><span class="keyword">function</span>(<span class="params">$</span>) </span>&#123;</div><div class="line">    <span class="keyword">const</span> mockData = &#123;</div><div class="line">        <span class="string">"success"</span>: <span class="literal">true</span>,</div><div class="line">        <span class="string">"data"</span>: [</div><div class="line">            &#123;</div><div class="line">                <span class="string">"id"</span>: <span class="number">1</span>,</div><div class="line">                <span class="string">"name"</span>: <span class="string">"天哥"</span></div><div class="line">            &#125;,</div><div class="line">            &#123;</div><div class="line">                <span class="string">"id"</span>: <span class="number">2</span>,</div><div class="line">                <span class="string">"name"</span>: <span class="string">"sky"</span></div><div class="line">            &#125;</div><div class="line">        ]</div><div class="line">    &#125;;</div><div class="line">    $.ajax(&#123;</div><div class="line">        <span class="attr">url</span>: <span class="string">'ajax-url'</span>,</div><div class="line">        <span class="attr">type</span>: <span class="string">'GET'</span>,</div><div class="line">        <span class="attr">dataType</span>: <span class="string">'JSON'</span>,</div><div class="line">        <span class="attr">success</span>: <span class="function"><span class="keyword">function</span>(<span class="params">data</span>) </span>&#123;</div><div class="line">            <span class="comment">// 由于上面ajax请求的url并不会返回真实数据，所以会直接走到error函数去</span></div><div class="line">        &#125;,</div><div class="line">        <span class="attr">error</span>: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">            <span class="keyword">let</span> data = mockData;</div><div class="line">            <span class="keyword">if</span> (data.success === <span class="string">'true'</span>) &#123;</div><div class="line">                <span class="comment">// 其它逻辑开发</span></div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">    &#125;)</div><div class="line">&#125;)(jQuery);</div></pre></td></tr></table></figure>
<p>上面代码是<strong>Mock</strong>最简单的方法，当然会存在一些问题，接下来看。</p>
<h2 id="问题二：我的mock数据是比较多的，也写在脚本里面混在一起么？"><a href="#问题二：我的mock数据是比较多的，也写在脚本里面混在一起么？" class="headerlink" title="问题二：我的mock数据是比较多的，也写在脚本里面混在一起么？"></a>问题二：我的mock数据是比较多的，也写在脚本里面混在一起么？</h2><p>你能想到的问题大多数都要解决方案的，对于数据量相对较大而且又需要随机的，那就直接用 <a href="https://github.com/nuysoft/Mock/wiki/Getting-Started" target="_blank" rel="external">mock</a> 库，语法直接在他们的站点看挺简单的。</p>
<p><strong>Mock</strong>数据最终是不会发布的，所以为了避免后期又需要改动很多代码，建议的做法就是将这些数据移到一个<code>mock</code>文件夹，代码结构可以如下：</p>
<figure class="highlight 1c"><table><tr><td class="code"><pre><div class="line">app</div><div class="line"> <span class="string">|- index.html</span></div><div class="line"> <span class="string">|- js</span></div><div class="line">    <span class="string">|- index.js</span></div><div class="line"> <span class="string">|- mock</span></div><div class="line">    <span class="string">|- mock.js</span></div></pre></td></tr></table></figure>
<p>那么这个时候<code>index.html</code>的代码大致如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><div class="line"><span class="meta">&lt;!DOCTYPE html&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">head</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">title</span>&gt;</span>mock demo<span class="tag">&lt;/<span class="name">title</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">head</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">body</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"mock/mock.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span>    </div><div class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"js/index.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">body</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></div></pre></td></tr></table></figure>
<p><strong>mock.js</strong>的内容其实就是上面问题一的<code>const mockData = xxx</code>挪过去，而剩下的逻辑就是<code>index.js</code>的内容。</p>
<p>这里延伸一下，<code>&lt;script src=&quot;mock/mock.js&gt;&lt;/script&gt;&quot;</code> 这段代码只在开发环境下才需要引入，而在生产环境是不需要引入的，有没有办法可以自动化根据不同环境引入跟不引入呢？答案是肯定的。这块用<strong>gulp</strong>来做就比较合适了，这里不赘述，如有需要可私下继续讨论。</p>
<h2 id="问题三：我如何直接就写好接口地址并且返回数据写正常逻辑？"><a href="#问题三：我如何直接就写好接口地址并且返回数据写正常逻辑？" class="headerlink" title="问题三：我如何直接就写好接口地址并且返回数据写正常逻辑？"></a>问题三：我如何直接就写好接口地址并且返回数据写正常逻辑？</h2><p>这个时候<code>NodeJs</code>就出场了，后端接口地址给你了，只是没有数据，那就让<code>Node</code>去做这一层代理，给出一个示例：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// app.js</span></div><div class="line"><span class="meta">'use strict'</span>;</div><div class="line"></div><div class="line"><span class="keyword">const</span> http = <span class="built_in">require</span>(<span class="string">'http'</span>);</div><div class="line"><span class="keyword">const</span> fs = <span class="built_in">require</span>(<span class="string">'fs'</span>);</div><div class="line"><span class="keyword">const</span> path = <span class="built_in">require</span>(<span class="string">'path'</span>);</div><div class="line"><span class="keyword">const</span> url = <span class="built_in">require</span>(<span class="string">'url'</span>);</div><div class="line"></div><div class="line"><span class="keyword">const</span> hostname = <span class="string">'127.0.0.1'</span>;</div><div class="line"><span class="keyword">const</span> port = <span class="number">9000</span>;</div><div class="line"></div><div class="line"><span class="keyword">const</span> server = http.createServer(<span class="function">(<span class="params">req, res</span>) =&gt;</span> &#123;</div><div class="line">    <span class="keyword">let</span> params = url.parse(req.url, <span class="literal">true</span>);</div><div class="line">    <span class="keyword">let</span> content = fs.readFileSync(<span class="string">'mock/mockdata.js'</span>).toString();</div><div class="line">    <span class="keyword">if</span> (params.query &amp;&amp; params.query.callback) &#123;</div><div class="line">        <span class="keyword">let</span> str = <span class="string">`<span class="subst">$&#123;params.query.callback&#125;</span>(<span class="subst">$&#123;content&#125;</span>)`</span>;</div><div class="line">        res.end(str);</div><div class="line">    &#125; <span class="keyword">else</span> &#123;</div><div class="line">        res.end(content);</div><div class="line">    &#125;    </div><div class="line">&#125;);</div><div class="line"></div><div class="line">server.listen(port, hostname, () =&gt; &#123;</div><div class="line">    <span class="built_in">console</span>.info(<span class="string">`server listen on <span class="subst">$&#123;hostname&#125;</span>:<span class="subst">$&#123;port&#125;</span>`</span>);</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<p>这个时候只要启动 <code>node app.js</code>，再加上<code>hosts</code>配置，就可以把接口地址匹配到该服务返回数据进行开发了。</p>
]]></content>
    
    <summary type="html">
    
      前后端分离与解耦是目前开发的一个大趋势，本文通过问题式的方式来总结一下前端工程师如何通过Mock进行开发。
    
    </summary>
    
      <category term="教程" scheme="https://cnt1992.github.io/categories/%E6%95%99%E7%A8%8B/"/>
    
    
      <category term="Mock NodeJs" scheme="https://cnt1992.github.io/tags/Mock-NodeJs/"/>
    
  </entry>
  
  <entry>
    <title>项目是检验前端是否合格的标准</title>
    <link href="https://cnt1992.github.io/2016/08/26/to-be-a-qualified-front-end-developer/"/>
    <id>https://cnt1992.github.io/2016/08/26/to-be-a-qualified-front-end-developer/</id>
    <published>2016-08-25T16:07:11.000Z</published>
    <updated>2016-08-25T09:55:14.000Z</updated>
    
    <content type="html"><![CDATA[<p><em>友情提示：这不是一篇技术文章，几乎都是文字没有代码，适合刚入门前端亦或者是跟我一样觉得前端还有很多东西需要去学的童鞋</em></p>
<a id="more"></a>
<blockquote>
<p>入门前端2年多，算上各种加班的话应该是3年，一直标榜自己“全栈工程师”。然而从近一次持续了三个月的项目中感受很深，方知前端合格很难，更别谈优秀，这条路还有很多东西需要去“品”。</p>
</blockquote>
<h2 id="谈谈自己前端历程"><a href="#谈谈自己前端历程" class="headerlink" title="谈谈自己前端历程"></a>谈谈自己前端历程</h2><p>我要谈的前端，严格意义上是大前端。因为在腾讯一些公司是有<code>重构</code>岗位(现在改成<code>UI</code>部门，其实都差不多，就是负责页面的<code>HTML</code>跟<code>CSS</code>视觉效果还原)以及<code>JavaScript开发</code>岗位(负责产品的数据以及交互层次的研发)的。我之前的工作岗位是<code>前端设计师</code>，也就是<code>重构</code>，每天重复着<code>切图仔</code>的角色感觉有点厌烦了(现在挺怀念当时的日子~)，于是开始接手写一些<code>JS</code>效果，但还是比较简单的，比如轮播、导航这一类简单的交互效果。</p>
<p>再后来，折腾<code>Node</code>，玩<code>Nginx</code>，搞<code>Mysql</code>/<code>Mongo</code>，一项新的技术出来赶紧学，什么<code>Angular</code>/<code>Vue</code>/<code>React</code>统统学一遍，然后，开始标榜自己为<strong>全栈工程师</strong>。</p>
<p>就这样，现在成为了<strong>大前端</strong>的一员，我不确定这是否是未来的一种趋势，但就这样子一直在路上。直到最近这次持续了3个月的项目，多人协作下才深刻感受到：别谈优秀，合格都很难。</p>
<h2 id="一一论证"><a href="#一一论证" class="headerlink" title="一一论证"></a>一一论证</h2><p>下面我将在我所有感受的方面来一个个的<del>吐槽</del>讨论，基本上谈到的例子都是我项目中真实的例子。</p>
<p>在开始之前得先介绍一下技术背景：基于<code>React</code>的<code>Redux</code>封装的一套自己的<code>SDK</code>的基础上，写各种<code>React</code>组件然后封装到内部的<code>npm</code>模块，最终将这些模块堆砌输出到页面，目前共输入100多个组件。</p>
<h3 id="编码"><a href="#编码" class="headerlink" title="编码"></a>编码</h3><p>抛开<code>结构</code>、<code>表现</code>、<code>行为</code>分离，<code>JSX</code>也就是在<code>React</code>盛行之下才活得下来。那么写组件的时候也是在写<code>结构</code>、<code>表现</code>以及<code>行为</code>，只是把这些都放在了一个<code>.jsx</code>文件里面。然后有一次看到一个小伙伴写的组件，一套结构下来全部都是<code>div</code>/<code>span</code>，没有任何其他的标签了；还有一次有个小伙伴让我改一下我的组件，其中我用了<code>h2</code>的标签说不要加粗让改成<code>p</code>标签。</p>
<p>我们先不考虑<code>SEO</code>，一段语义化的代码就直接映射一个前端的思维。你在路边看到一排树脑海里想到<code>ul</code>，你在房间看到挂在墙上的一幅画脑海里想到<code>margin</code>/<code>padding</code>/<code>width</code>/<code>height</code>，你在看文章的时候能想到<code>h1</code>/<code>h2</code>/<code>section</code>/<code>article</code>，而不是看到任何事物都是一个<code>div</code>以言之。告别了<code>table</code>时代，迎来了<code>div</code>时代？别！</p>
<p>然后，讲完了<code>结构</code>，来讲讲<code>表现</code>，也就是<code>CSS</code>，一个好的<code>重构工程师</code>能够以最小颗粒度的<code>CSS</code>写出最优雅的样式，那么在协同的情况下，各种违和的命名就出来了，我们都知道<code>姓氏命名法</code>、<code>BEM命名法</code>，但在项目紧急的情况下就失去了这个原则？然后各种随手写出来的命名就这样子出现在代码上了，比如：<code>.right</code>、<code>.weixuanzhong</code>，恩，后面的表示未选中，还好我拼音学得好。</p>
<p>最后一点，跟百度的同学也了解过，自己之前的团队也一样，写<code>CSS</code>属性的时候都会有一套先后顺序约定，虽然要求有点高了，但是对以后代码的维护以及浏览器加速渲染CSS都很有帮助：</p>
<ol>
<li>display/position/top/left/margin/padding/ 最外层属性</li>
<li>width/height 元素自身属性</li>
<li>font族属性/color/background 元素外观属性（color跟background总是成对出现）</li>
<li>z-index等属性</li>
</ol>
<p>UI是给用户看的，但代码是我们自己写并且需要维护的，写“好”的代码很重要。</p>
<h3 id="UI交互"><a href="#UI交互" class="headerlink" title="UI交互"></a>UI交互</h3><p>无线大行天下的时代，细腻的UI给用户的感知是最明显的，当<code>ios8</code>出来的时候就伴随着<code>0.5px</code>的边框出现，而到现在为止有多少个项目能够真的把这考虑进去，最后自己在周末花了几个小时的时间给所有的组件都加上了<code>0.5px</code>的边框，当然了我只是用<code>JS</code>去判断是否<code>ios8</code>设备及以上然后打入类到html里面，安卓用户对不起了。</p>
<p>触摸屏的出现就会有触摸态的UI交互。一个按钮手指按下去的时候用户知道了么？我们作为开发习惯了在<code>chrome模拟器</code>下进行页面的开发，更看重于逻辑是否正常，忽略了这些感官最强的UI交互，一个简单的<code>:active</code>类加上去体验还是差很远。</p>
<p>学过了<code>CSS3</code>的<code>transition</code>之后，各种需要“动起来”的东西我们都很喜欢用<code>transition</code>来实现，比如底部弹出层，从<code>bottom:-100%</code>到<code>bottom:0</code>的效果加上<code>transition</code>就会有过渡上来的效果。但是，直接用<code>top</code>/<code>bottom</code>/<code>left</code>/<code>right</code>这一类的属性加上<code>transition</code>在手机上是很卡顿的，毕竟性能瓶颈在那里。所以一般动效都用<code>transform</code>的<code>translate3d</code>等属性去实现，什么<code>GPU</code>加速能用上的都用上，再碰到坑继续折腾。</p>
<h3 id="自测"><a href="#自测" class="headerlink" title="自测"></a>自测</h3><p>有多少写<code>H5</code>页面的童鞋能够用真机来进行测试的？当测试给你提页面乱位的bug的时候，第一反应打开<code>chrome模拟器</code>然后跟他截个图说没问题呀，然后心里骂道：傻逼测试。sorry，我有一次这么干了，但当我突然想起我忘记在真机上测试的时候就非常对不起测试同学。</p>
<p>自测是对自己负责任的最基本要求，你不能要求测试帮你测所有的东西，虽然测试就是在干这么一件事情。但你自己做的东西自己至少测试没问题，测试帮你发现你没有发现的问题，那才是最美好的事情。</p>
<h2 id="未完待续"><a href="#未完待续" class="headerlink" title="未完待续"></a>未完待续</h2><p>洋洋洒洒废话了一大堆，也许大家看到的时候会不屑，但这真的就出现了这些问题，作为一个<strong>合格的前端</strong>这些是最基本基本的要求，这是入门就必须要做到的。</p>
<p>由于肚子饿的关系，今天先写到这里，还有几个方面等有空继续写，当然了，都是非技术难题的方面，纯粹就最基本的要求但却最容易被忽略了的。</p>
]]></content>
    
    <summary type="html">
    
      前端入门很简单，培训机构两个月出来上岗了，但要检验一个前端是否合格，从项目中来看就够了。
    
    </summary>
    
      <category term="心得体会" scheme="https://cnt1992.github.io/categories/%E5%BF%83%E5%BE%97%E4%BD%93%E4%BC%9A/"/>
    
    
      <category term="前端" scheme="https://cnt1992.github.io/tags/%E5%89%8D%E7%AB%AF/"/>
    
  </entry>
  
  <entry>
    <title>React组件之高阶函数替换Mixins注入</title>
    <link href="https://cnt1992.github.io/2016/08/10/replace-highlevel-function-width-mixins-in-react/"/>
    <id>https://cnt1992.github.io/2016/08/10/replace-highlevel-function-width-mixins-in-react/</id>
    <published>2016-08-09T20:38:57.000Z</published>
    <updated>2016-08-09T13:10:13.000Z</updated>
    
    <content type="html"><![CDATA[<p>值此<code>七夕佳节</code>，在这里记录一下今天用React写一个倒计时组件的笔记。</p>
<a id="more"></a>
<h2 id="实现功能"><a href="#实现功能" class="headerlink" title="实现功能"></a>实现功能</h2><p>基于<code>ES6</code>用<code>React</code>实现一个倒计时组件，格式：6时6分6秒，然后每隔一秒不断倒计时。</p>
<h2 id="回顾Mixins"><a href="#回顾Mixins" class="headerlink" title="回顾Mixins"></a>回顾Mixins</h2><p>当然，完全可以直接写在一个<code>React.createClass</code>函数里面，但<code>可复用性</code>不高，所以换做在之前官方推荐的做法就是利用<strong>Mixins</strong>特性，所谓<strong>Mixins</strong>我理解就是<code>注入</code>，类似于<code>Java Spring</code>的<code>依赖注入</code>，简单代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 定义一个倒计时Mixins</span></div><div class="line"><span class="keyword">var</span> SetIntervalMixin = &#123;</div><div class="line">    <span class="attr">componentWillMount</span>: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        <span class="keyword">this</span>.intervals = [];</div><div class="line">    &#125;,</div><div class="line">    <span class="attr">setInterval</span>: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        <span class="keyword">this</span>.intervals.push(setInterval.apply(<span class="literal">null</span>, <span class="built_in">arguments</span>));</div><div class="line">    &#125;,</div><div class="line">    <span class="attr">componentWillUnmount</span>: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        <span class="keyword">this</span>.intervals.forEach(clearInterval);</div><div class="line">    &#125;</div><div class="line">&#125;;</div><div class="line"></div><div class="line"><span class="comment">// 注入Mixins到组件中</span></div><div class="line"><span class="keyword">var</span> Timer = React.createClass(&#123;</div><div class="line">    <span class="attr">mixins</span>: [SetIntervalMixin],</div><div class="line">    <span class="attr">getInitialState</span>: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        <span class="keyword">return</span> &#123;</div><div class="line">            <span class="attr">timestamp</span>: <span class="keyword">this</span>.props.timestamp</div><div class="line">        &#125;</div><div class="line">    &#125;,</div><div class="line">    <span class="attr">componentDidMount</span>: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        <span class="keyword">this</span>.setInterval(<span class="keyword">this</span>.changeState, <span class="number">1000</span>);</div><div class="line">    &#125;,</div><div class="line">    <span class="attr">changeState</span>: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        <span class="keyword">this</span>.setState(&#123;</div><div class="line">            <span class="attr">timestamp</span>: <span class="keyword">this</span>.state.timestamp - <span class="number">1000</span></div><div class="line">        &#125;)</div><div class="line">    &#125;,</div><div class="line">    <span class="attr">render</span>: <span class="function"><span class="keyword">function</span>(<span class="params"></span>) </span>&#123;</div><div class="line">        <span class="comment">//取得属性值</span></div><div class="line">        <span class="keyword">var</span> timestamp = <span class="keyword">this</span>.state.timestamp;        </div><div class="line">        <span class="keyword">return</span> <span class="xml"><span class="tag">&lt;<span class="name">p</span>&gt;</span>&#123;this.renderTime(timestamp)&#125;<span class="tag">&lt;/<span class="name">p</span>&gt;</span></span></div><div class="line">    &#125;,</div><div class="line">    renderTime(timestamp)&#123;</div><div class="line">        <span class="comment">// 省略掉时间计算转换</span></div><div class="line">        ...</div><div class="line">        return <span class="string">'XX天XX时XX分XX秒'</span>;</div><div class="line">    &#125;</div><div class="line">&#125;);</div><div class="line"></div><div class="line"><span class="comment">//渲染React元素</span></div><div class="line">ReactDOM.render(</div><div class="line">    <span class="xml"><span class="tag">&lt;<span class="name">Timer</span> <span class="attr">timestamp</span>=<span class="string">"66666666"</span>/&gt;</span> ,</span></div><div class="line">    document.querySelector("body")</div><div class="line">);</div></pre></td></tr></table></figure>
<p>用<code>Mixins</code>的最大好处就是<code>复用</code>，保证组件的最小颗粒化，同时也保证了功能相对单一。</p>
<h2 id="ES6的高阶函数"><a href="#ES6的高阶函数" class="headerlink" title="ES6的高阶函数"></a>ES6的高阶函数</h2><p>但是用<code>ES6</code>来写的话，<code>Mixins</code>显得有点无力，没有办法注入，这个时候高阶函数就派上用场了，其实<code>Redux</code>的<code>connect</code>就是利用了这一特性，改写上面的代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 注意这里传入一个React组件然后返回一个React组件</span></div><div class="line"><span class="keyword">var</span> SetIntervalMixin = <span class="function"><span class="params">ComposedComponent</span> =&gt;</span> <span class="class"><span class="keyword">class</span> <span class="keyword">extends</span> <span class="title">React</span>.<span class="title">Component</span> </span>&#123;</div><div class="line">    <span class="keyword">constructor</span>(props) &#123;</div><div class="line">        <span class="keyword">super</span>(props);</div><div class="line">        <span class="keyword">this</span>.state = &#123;</div><div class="line">            <span class="attr">timestamp</span>: <span class="keyword">this</span>.props.timestamp</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    componentDidMount() &#123;</div><div class="line">        <span class="keyword">this</span>.interval = setInterval(<span class="keyword">this</span>.changeState.bind(<span class="keyword">this</span>), <span class="number">1000</span>);    </div><div class="line">    &#125;</div><div class="line">    componentWillUnmount() &#123;</div><div class="line">        clearInterval(<span class="keyword">this</span>.interval);</div><div class="line">    &#125;</div><div class="line">    changeState() &#123;</div><div class="line">        <span class="keyword">this</span>.setState(&#123;</div><div class="line">            <span class="attr">timestamp</span>: <span class="keyword">this</span>.state.timestamp - <span class="number">1000</span></div><div class="line">        &#125;);</div><div class="line">    &#125;</div><div class="line">    render() &#123;</div><div class="line">        <span class="keyword">return</span> &lt;ComposedComponent &#123;...this.props&#125; &#123;...this.state&#125; /&gt;;</div><div class="line">    &#125;</div><div class="line">&#125;;</div><div class="line"></div><div class="line">class Timer extends React.Component &#123;</div><div class="line">    constructor(props) &#123;</div><div class="line">        super(props);        </div><div class="line">    &#125;</div><div class="line">    render() &#123;</div><div class="line">        // 注意，这里通过props取得属性值</div><div class="line">        var timestamp = this.props.timestamp;        </div><div class="line">        return &lt;p&gt;&#123;this.renderTime(timestamp)&#125;&lt;/p&gt;</div><div class="line">    &#125;</div><div class="line">    renderTime(timestamp)&#123;</div><div class="line">        // 省略掉时间计算转换</div><div class="line">        ...</div><div class="line">        return 'XX天XX时XX分XX秒';</div><div class="line">    &#125;</div><div class="line">&#125;</div><div class="line">export default SetIntervalMixin(Timer);</div><div class="line"></div><div class="line">//渲染React元素</div><div class="line">ReactDOM.render(</div><div class="line">    &lt;Timer timestamp="66666666"/&gt; ,</div><div class="line">    document.querySelector("body")</div><div class="line">);</div></pre></td></tr></table></figure>
<h2 id="总结"><a href="#总结" class="headerlink" title="总结"></a>总结</h2><p>两者方法其实我更倾向于第二种，也不单单是用<code>ES6</code>写起来舒服些，<code>函数式编程</code>以及<code>中间件</code>的思维在里面，总而言之，写组件多考虑抽取<code>可复用</code>组件出来，避免逻辑一大堆糅合在一起。</p>
<p>稍微再记一下今天碰到的一个坑：用<code>ES6</code>的<code>for...of</code>去遍历数组的时候，利用<code>Babel</code>转换在<code>chrome 49</code>以下会被转换成<code>Symbol</code>导致报错 o(╯□╰)o</p>
]]></content>
    
    <summary type="html">
    
      用ES6写React组件的时候，不用Mixins而直接用高阶函数来代替
    
    </summary>
    
      <category term="个人学习" scheme="https://cnt1992.github.io/categories/%E4%B8%AA%E4%BA%BA%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="React Mixins" scheme="https://cnt1992.github.io/tags/React-Mixins/"/>
    
  </entry>
  
  <entry>
    <title>关于React的一些实践思考与总结</title>
    <link href="https://cnt1992.github.io/2016/08/06/summary-of-react-base-on-practice/"/>
    <id>https://cnt1992.github.io/2016/08/06/summary-of-react-base-on-practice/</id>
    <published>2016-08-06T09:41:56.000Z</published>
    <updated>2016-08-08T02:39:13.000Z</updated>
    
    <content type="html"><![CDATA[<p>本文不是关于 <code>React</code> 的入门介绍教程，假设还没有对 <code>React</code> 这门技术有所了解或者没有真正在项目中实践过的童鞋可以移步 <a href="https://facebook.github.io/react/" target="_blank" rel="external">React官网</a> 或者 <a href="http://reactjs.cn/" target="_blank" rel="external">React中文网</a>，推荐看阮一峰的 <a href="http://www.ruanyifeng.com/blog/2015/03/react.html" target="_blank" rel="external">React入门实例教程</a>。对 <code>React</code> 有实际项目的应用并且也用过 <code>Redux</code>/<code>Flux</code>/<code>Reflux</code> 其中的一个或者多个框架开发过应用的童鞋欢迎共同 <del>吐槽</del><br><strong>探讨</strong> 关于 <code>React</code> 这门技术使用过程中碰到的问题以及一些总结。</p>
<p>最近这段时间用了 <code>Flux</code> 跟 <code>Redux</code> 这两个框架开发 <code>React</code> 应用，并且完全用 <code>ES6</code> 语法开发，利用 <code>Webpack</code> 作为脚手架结合 <code>Babel</code> 编译成向下兼容的脚本。在实际项目开发过程中，碰到的问题挺多，所以才有了此文。</p>
<a id="more"></a>
<h2 id="如何保护进程不因React脚本错误而终止"><a href="#如何保护进程不因React脚本错误而终止" class="headerlink" title="如何保护进程不因React脚本错误而终止"></a>如何保护进程不因React脚本错误而终止</h2><p>近些年一直被业界推崇的<code>组件化</code>开发，用<code>React</code>开发起来还是挺不错的，一个页面用多个组件堆砌起来，组件能够复用，但这个时候会碰到一个问题，在开发的过程中如果某一个组件报错了，那么这个页面就会被直接抛出错误而导致Crash，其他组件也直接不渲染出来。</p>
<p>这样子我们在控制台里面只会看到报错的内容以及官方的链接让你去解决问题，其实不能直观让我们知道是哪一个组件报错了。而且从<code>分而治之</code>的思想来看，组件与组件之间不应该直接相互干扰，一个组件报错也不应该导致其他组件直接不渲染了，那么最好的方法就是直接用 <code>装饰者模式</code> 给 <code>React</code> 带上美丽的装饰。</p>
<p>每一个<code>React</code>组件都会调用<code>createElement</code>方法生成组件，那么只需要在这个地方做文章即可，如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">import</span> React <span class="keyword">from</span> <span class="string">'react'</span>;</div><div class="line"><span class="keyword">import</span> safe <span class="keyword">from</span> <span class="string">'./Safe'</span>;</div><div class="line"><span class="keyword">let</span> _func = React.createElement;</div><div class="line"></div><div class="line">React.createElement = <span class="function"><span class="keyword">function</span> (<span class="params">...args</span>) </span>&#123;</div><div class="line">    <span class="keyword">if</span> (<span class="keyword">typeof</span> args[<span class="number">0</span>] === <span class="string">'function'</span> &amp;&amp; !args[<span class="number">0</span>]._isSafe) &#123;</div><div class="line">        safe(args[<span class="number">0</span>]);</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> _func.apply(<span class="keyword">this</span>, args);</div><div class="line">&#125;;</div></pre></td></tr></table></figure>
<p>上面代码将<code>React.createElement</code>由<code>safe</code>方法来接管，那么这个时候我们就有办法在<code>Safe.js</code>里面保证在<code>createElement</code>的时候不直接因为报错而导致页面Crash，只要把会报错的生命周期的其中两个：<code>componentWillMount</code>/<code>render</code>改写一些即可，大致如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">import</span> React <span class="keyword">from</span> <span class="string">'react'</span>;</div><div class="line"></div><div class="line"><span class="keyword">export</span> <span class="keyword">default</span> <span class="function"><span class="keyword">function</span> <span class="title">safe</span> (<span class="params">target</span>) </span>&#123;</div><div class="line">    <span class="keyword">let</span> p = target.prototype;</div><div class="line">    <span class="keyword">let</span> list = [</div><div class="line">        <span class="string">'render'</span>,</div><div class="line">        <span class="string">'componentWillMount'</span></div><div class="line">    ];</div><div class="line">    list.forEach(<span class="function"><span class="params">name</span> =&gt;</span> &#123;</div><div class="line">        <span class="keyword">if</span> (name <span class="keyword">in</span> p &amp;&amp; <span class="keyword">typeof</span> p[name] === <span class="string">'function'</span>) &#123;</div><div class="line">            <span class="keyword">let</span> _func = p[name];</div><div class="line">            p[name] = <span class="function"><span class="keyword">function</span> (<span class="params">...args</span>) </span>&#123;</div><div class="line">                <span class="keyword">try</span> &#123;</div><div class="line">                    <span class="keyword">return</span> _func.apply(<span class="keyword">this</span>, args);</div><div class="line">                &#125;</div><div class="line">                <span class="keyword">catch</span>(e) &#123;</div><div class="line">                    <span class="comment">// 这里捕捉到React渲染报错，你可以啥都不做</span></div><div class="line">                    <span class="comment">// 也可以将错误信息直接渲染在页面 </span></div><div class="line">                    <span class="comment">// ErrorResult是你自定义的错误展示组件 msg是你自定义的错误信息</span></div><div class="line">                    <span class="keyword">const</span> error = &#123;</div><div class="line">                        msg</div><div class="line">                    &#125;;</div><div class="line">                    <span class="keyword">return</span> (</div><div class="line">                        <span class="xml"><span class="tag">&lt;<span class="name">ErrorResult</span> &#123;<span class="attr">error</span>&#125; &#123;<span class="attr">...this.props</span>&#125; /&gt;</span></span></div><div class="line">                    )</div><div class="line">                &#125;</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">    &#125;);</div><div class="line">    target._isSafe = true;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这种<code>Safe</code>机制极大帮助我们快速知道哪个组件有问题，减少定位问题组件的时间成本，上面这种方案也是我们项目基于<code>Redux</code>的基础上封装成的基础<code>SDK</code>，基于这个<code>SDK</code>上进行页面的组件堆砌。</p>
<h2 id="关于传值与传址的问题"><a href="#关于传值与传址的问题" class="headerlink" title="关于传值与传址的问题"></a>关于传值与传址的问题</h2><p>这个其实<code>Javascript</code>里面已经说得非常清楚，简单复习一些<strong>传值</strong>与<strong>传址</strong>的区别，一个简单的Demo即可：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 传值是Javascript基本数据类型（数字、字符串、布尔值）被操作的过程</span></div><div class="line"><span class="comment">// 实际上是拷贝了一份存在另一个变量里面，相互不干扰</span></div><div class="line"><span class="keyword">var</span> a = <span class="number">666</span>, </div><div class="line">    b = a;</div><div class="line">b = <span class="number">555</span>;</div><div class="line"><span class="built_in">console</span>.log(a);  <span class="comment">// 666</span></div><div class="line"></div><div class="line"><span class="comment">// 而传址在Javascript中主要指针对引用类型(对象、数组、函数)的值的操作过程</span></div><div class="line"><span class="comment">// 虽然也拷贝了一份存在另一个变量里面，但副本指针指向跟原本同一个位置</span></div><div class="line"><span class="keyword">var</span> c = <span class="keyword">new</span> <span class="built_in">Object</span>(),</div><div class="line">    d = c;</div><div class="line">c.name = <span class="string">'SkyCai'</span>;</div><div class="line"><span class="built_in">console</span>.log(d.name);   <span class="comment">// 'SkyCai'</span></div></pre></td></tr></table></figure>
<p>简单描述一下我在项目需要实现的需求：无线端页面往下拉到差不多最底部的时候触发一次异步请求拿到下一页的数据，但由于我们的整个页面数据是由<code>SDK</code>进行管控的，这个时候就需要将请求拿到的数据跟之前的数据进行合并之后触发一次<code>SDK</code>提供的<code>updateStore</code>方法去更新<code>store</code>里面的数据，然后<code>增量更新</code>页面数据从而渲染。</p>
<p>简单看下我一开始写下的核心代码逻辑：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 用于存储页面合并之后的数据</span></div><div class="line"><span class="keyword">var</span> pageData = &#123;&#125;;</div><div class="line"></div><div class="line"><span class="comment">// 模拟加载第一页数据</span></div><div class="line">loadData(<span class="number">1</span>);</div><div class="line"></div><div class="line"><span class="comment">// 模拟加载第二页数据</span></div><div class="line">loadData(<span class="number">2</span>);</div><div class="line"></div><div class="line"><span class="comment">// 模拟加载第三页数据</span></div><div class="line">loadData(<span class="number">3</span>);</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">loadData</span>(<span class="params">page</span>) </span>&#123;</div><div class="line">    <span class="keyword">let</span> promise = xxx; <span class="comment">// promise请求</span></div><div class="line">    promise.then(<span class="function"><span class="keyword">function</span>(<span class="params">res</span>)</span>&#123;</div><div class="line">        <span class="comment">// res是一个对象</span></div><div class="line">        <span class="keyword">if</span> (res.success) &#123;</div><div class="line">            <span class="keyword">if</span> (!pageData.data) &#123;</div><div class="line">                <span class="comment">// 初次请求直接就赋值给pageData</span></div><div class="line">                pageData = res.data;</div><div class="line">                <span class="comment">// 初次请求直接渲染页面</span></div><div class="line">                renderPage(pageData);          </div><div class="line">            &#125; <span class="keyword">else</span> &#123;</div><div class="line">                <span class="comment">// 后面的请求就将请求回来的数据合并到之前保存的数据里面</span></div><div class="line">                <span class="built_in">Object</span>.assign(pageData, res.data);</div><div class="line">                <span class="comment">// 后面合并数据之后触发updateStore更新store数据</span></div><div class="line">                updateStore(pageData);</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">    &#125;,<span class="function"><span class="keyword">function</span>(<span class="params">err</span>)</span>&#123;</div><div class="line">        <span class="comment">// err handle</span></div><div class="line">    &#125;);    </div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>乍看之下因为没什么问题了，But，任凭你怎么刷新页面永远都是第一次渲染出来，后面的就不管你了。</p>
<p>首先，犯了一个很低级的错误，关于<code>cloneDeep</code>的问题，翻一下 <a href="https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/assign" target="_blank" rel="external">MDN文档</a> 可以知道 <code>Object.assign(target, ...sources)</code> 是浅拷贝的，也就是我上面第二次请求回来的数据合并之后并不是正确的数据，因为<code>res.data</code>里面还有多层<code>object</code>，这个容易解决，写一个<code>cloneDeep</code>方法实现即可，这里就不再贴出代码。</p>
<p>合并之后的数据正确了，但是触发<code>updateStore</code>的时候也碰到一个问题，那就是第一次触发<code>updateStore</code>的时候能够正确拿到数据并且渲染第二页的数据，但是再次触发的时候就不渲染第三页的数据了。这就是提到的关于<strong>传值</strong>跟<strong>传址</strong>的问题，当第一次触发<code>updateStore</code>的时候传了一个<code>object</code>过去，后面再合并数据之后同样传了一个<code>object</code>过去，但这个<code>object</code>是基于上一个<code>object</code>的基础上复制的，所以导致了指向同一个指针，最简单粗暴的方法就是：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 简单粗暴的让pageData变成值传递过去</span></div><div class="line">updateStore(<span class="built_in">JSON</span>.parse(<span class="built_in">JSON</span>.stringify(pageData)));</div></pre></td></tr></table></figure>
<h2 id="尽量避免写依赖函数名的代码"><a href="#尽量避免写依赖函数名的代码" class="headerlink" title="尽量避免写依赖函数名的代码"></a>尽量避免写依赖函数名的代码</h2><p>啥意思？接着上面那个问题，<code>updateStore</code>实际上是触发了<code>updateStoreAction</code>对<code>store</code>进行更新的，来看一下<code>updateStoreAction</code>的核心代码逻辑：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">updateStoreData</span>(<span class="params">data</span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> &#123;</div><div class="line">        <span class="attr">type</span> : <span class="string">'UPDATESTORE'</span>,</div><div class="line">        <span class="attr">data</span> : data</div><div class="line">    &#125;;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="keyword">let</span> updateStoreAction = <span class="function"><span class="keyword">function</span> <span class="title">updateStoreAction</span>(<span class="params">data</span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> <span class="function">(<span class="params">dispatch, getState</span>) =&gt;</span> &#123;        </div><div class="line">        dispatch(updateStoreData(data));</div><div class="line">    &#125;;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="keyword">export</span> <span class="keyword">default</span> updateStoreAction;</div></pre></td></tr></table></figure>
<p>上面代码逻辑还是很清晰的，如果接受到<code>UPDATESTORE</code>的<code>type</code>时就<code>dispatch</code>出去<code>updateStoreData</code>，这个时候这个<code>dispatch</code>走到<code>SDK</code>里面的一段逻辑：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 上面的updateStoreAction会传入到Config.actionMiddlewareList</span></div><div class="line"><span class="keyword">let</span> middleware = Config.actionMiddlewareList || [];</div><div class="line">middleware.forEach(<span class="function"><span class="keyword">function</span>(<span class="params">mw</span>) </span>&#123;</div><div class="line">    <span class="keyword">let</span> funcName = mw.name; <span class="comment">// 这里拿出函数名字，也就是updateStoreAction</span></div><div class="line">    <span class="keyword">if</span>(!funcName) &#123;</div><div class="line">        funcName = mw.toString().match(<span class="regexp">/^function\s*([^\s(]+)/</span>)[<span class="number">1</span>];</div><div class="line">    &#125;</div><div class="line">    ModularizationActions[funcName] = mw;</div><div class="line">&#125;);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这个时候看到<code>SDK</code>里面有一段代码依赖于函数名字了，那么问题就来了，在本地开启<code>webpack-dev-server</code>进行开发的时候没啥问题，但是提交生产环境的代码上去之后问题就来了。</p>
<p>这个“锅”让<code>webpack</code>来背比较好，看下面一段配置：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><div class="line"><span class="attr">plugins:</span> <span class="string">[</span></div><div class="line">    <span class="string">...</span></div><div class="line">    <span class="string">new</span> <span class="string">webpack.optimize.UglifyJsPlugin(&#123;</span></div><div class="line"><span class="attr">      compress:</span> <span class="string">&#123;</span></div><div class="line"><span class="attr">        warnings:</span> <span class="literal">false</span></div><div class="line">      <span class="string">&#125;,</span></div><div class="line"><span class="attr">      minimize:</span> <span class="literal">true</span></div><div class="line">    <span class="string">&#125;),</span></div><div class="line">    <span class="string">...</span></div><div class="line"><span class="string">]</span></div></pre></td></tr></table></figure>
<p>生产环境下我们会压缩JS代码，但<code>webpack</code>官方文档没有详细对<code>UglifyJsPlugin</code>的参数进行解释，含糊的说默认情况下不进行开启<strong>代码混淆</strong>，也就是你不声明的话就不混淆，但实际上默认是进行了代码混淆，有兴趣的童鞋可以试试看<code>build</code>之后的代码。</p>
<p>基本解决方法当然是在<code>webpack</code>配置上将参数配好，增加一个参数：</p>
<figure class="highlight yaml"><table><tr><td class="code"><pre><div class="line"><span class="attr">plugins:</span> <span class="string">[</span></div><div class="line">    <span class="string">...</span></div><div class="line">    <span class="string">new</span> <span class="string">webpack.optimize.UglifyJsPlugin(&#123;</span></div><div class="line"><span class="attr">      compress:</span> <span class="string">&#123;</span></div><div class="line"><span class="attr">        warnings:</span> <span class="literal">false</span></div><div class="line">      <span class="string">&#125;,</span></div><div class="line"><span class="attr">      mangle:</span> <span class="literal">false</span><span class="string">,</span> <span class="string">//</span> <span class="string">代码不进行混淆</span></div><div class="line"><span class="attr">      minimize:</span> <span class="literal">true</span></div><div class="line">    <span class="string">&#125;),</span></div><div class="line">    <span class="string">...</span></div><div class="line"><span class="string">]</span></div></pre></td></tr></table></figure>
<p>但是我们仔细思考一下，怪<code>webpack</code>？作为基础<code>SDK</code>依赖于函数名进行操作其实是非常不合理的，底层通用脚手架必然承当更大的使命，所以当然要对依赖函数名字的代码进行重构才行，这才是解决问题本质的方法。</p>
<h2 id="关于Redux-Flux-Reflux框架选型思考"><a href="#关于Redux-Flux-Reflux框架选型思考" class="headerlink" title="关于Redux/Flux/Reflux框架选型思考"></a>关于Redux/Flux/Reflux框架选型思考</h2><p>经过对这三个框架实际项目的实践来看，个人而言，难易程度以此为：Redux &gt; Flux &gt; Reflux。当然这只是个人见解，不同的人使用起来感触都有所不同。</p>
<p>盗图(若侵权请联系我删除)来对比一下这三个框架的区别：</p>
<ul>
<li>Flux</li>
</ul>
<p><code>Flux</code>是<code>Facebook</code>官方实现的一套框架，基本上整个流程都已经有了，但是感觉操作起来还是挺麻烦，你每次增加一个功能都得改很多个文件，准确来说它应该是一种模式，基于此模式才有了Reflux跟Redux等优秀框架的出现。</p>
<p><img src="/img/think-of-react/flux.png" alt="flux流程图"></p>
<ul>
<li>Reflux</li>
</ul>
<p>流程相对简单，操作起来也非常方便。</p>
<p><img src="/img/think-of-react/reflux.png" alt="flux流程图"></p>
<ul>
<li>Redux</li>
</ul>
<p>这个社区应该是最多人在用了的，<code>单一数据源</code>/<code>Store是只读的</code>/<code>使用纯函数来进行修改</code> 这三个原则是<code>Redux</code>的最大特点。但就是由于<code>单一数据源</code>，所有的数据都存储在同一个<code>object tree</code>里面导致了调试很麻烦，跟<code>Flux</code>一样，增加一个功能你也得改很多个文件。</p>
<p><img src="/img/think-of-react/redux.jpeg" alt="redux流程图"></p>
<p>所以根据不同业务场景使用恰当的框架才是最正确的解锁方式，不要为了使用框架而使用框架。而且也不要盲目地看这个好用这个，根据业务定制最好的框架。而且，你不可能实现一个简简单单的需求就动辄用一个那么大的框架进来吧？</p>
<h2 id="写在后面"><a href="#写在后面" class="headerlink" title="写在后面"></a>写在后面</h2><p><code>React</code>出来也一年多，涵盖了<code>web</code>/<code>IOS</code>/<code>Android</code>三端，社区也很火。有人看好有人看衰，有人说前端越来越浮躁，又有人喊web标准一去不复返。但我觉得框架亦或是工具只是为我们提供便利加速开发的，是我们在使用它们而不是它们在控制我们。所以，适合就用，不适合就不用，没有必要跟大众走。</p>
<p>嗯，用<code>ES6</code>写<code>React</code>还是爽！</p>
]]></content>
    
    <summary type="html">
    
      结合近段时间使用React开发项目碰到的疑惑总结一下自己对于React的一些实践思考以及总结
    
    </summary>
    
      <category term="心得体会" scheme="https://cnt1992.github.io/categories/%E5%BF%83%E5%BE%97%E4%BD%93%E4%BC%9A/"/>
    
    
      <category term="React Redux Flux" scheme="https://cnt1992.github.io/tags/React-Redux-Flux/"/>
    
  </entry>
  
  <entry>
    <title>天猫2017届前端开发实习生内推</title>
    <link href="https://cnt1992.github.io/2016/07/07/tmall-recruit/"/>
    <id>https://cnt1992.github.io/2016/07/07/tmall-recruit/</id>
    <published>2016-07-06T21:33:38.000Z</published>
    <updated>2016-07-06T14:36:40.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="关于天猫"><a href="#关于天猫" class="headerlink" title="关于天猫"></a>关于天猫</h2><p>关于天猫我就不多加以介绍，直接 <a href="https://www.zhihu.com/question/33589154" target="_blank" rel="external">搬砖</a>，欢迎有兴趣的同学砸简历给我</p>
<blockquote>
<p>点击右边发送简历 →_→ <a href="mailto:cnt19920613@gmail.com" target="_blank" rel="external">cnt19920613@gmail.com</a></p>
</blockquote>
<h2 id="岗位描述Job-Description"><a href="#岗位描述Job-Description" class="headerlink" title="岗位描述Job Description"></a>岗位描述Job Description</h2><p>在这里，你将： </p>
<ul>
<li>有机会与视觉设计师一起，美化产品线Web功能的设计， </li>
<li>有机会与交互设计师一起，完成产品线Web功能的开发与实现；</li>
<li>有机会做全栈式开发,</li>
<li>有机会做移动端开发， </li>
<li>有机会做性能、架构等方面的改进与优化， </li>
<li>还有机会从一个产品的设计到发布由自己亲手打造； </li>
</ul>
<p>你还将加入到我们业内最前沿Web技术的研究与开发中。</p>
<h2 id="岗位要求Qualifications"><a href="#岗位要求Qualifications" class="headerlink" title="岗位要求Qualifications"></a>岗位要求Qualifications</h2><ul>
<li>我们希望你，可以熟练使用各种 Web 前端技术，包括HTML(5)/CSS(3)/Javascript 等，并有相关的项目开发经验或成果； </li>
<li>我们希望你，用过git，grunt或gulp等工具，最好有自己的github仓库； </li>
<li>我们希望你，有基于Ajax 应用的开发经验，有NodeJS/Java开发经验，或者有移动端开发经验；深刻理解 Web 标准，对可用性、可访问性等相关知识有实际的了解； 对算法、数据结构、建模有一定了解； </li>
<li>我们希望你，关注新事物、新技术，有较强的学习能力，喜欢挑战；并且，个性乐观开朗，逻辑性强，善于和各种背景的人合作。 </li>
</ul>
<h2 id="工作地点Location"><a href="#工作地点Location" class="headerlink" title="工作地点Location"></a>工作地点Location</h2><p>杭州市(Hangzhou),北京市(Beijing)</p>
<a id="more"></a>
]]></content>
    
    <summary type="html">
    
      天猫2017届前端开发实习生内推开始啦，欢迎砸简历过来~
    
    </summary>
    
      <category term="招聘" scheme="https://cnt1992.github.io/categories/%E6%8B%9B%E8%81%98/"/>
    
    
      <category term="天猫 前端开发" scheme="https://cnt1992.github.io/tags/%E5%A4%A9%E7%8C%AB-%E5%89%8D%E7%AB%AF%E5%BC%80%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>Sublime Text 注册码 / Axure rp 8注册码</title>
    <link href="https://cnt1992.github.io/2016/04/28/sublime-license-code/"/>
    <id>https://cnt1992.github.io/2016/04/28/sublime-license-code/</id>
    <published>2016-04-28T10:22:38.000Z</published>
    <updated>2017-02-13T02:30:46.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>请不要举报我，用别人的注册码主要是因为<strong>Qiong</strong></p>
</blockquote>
<h2 id="Sublime-Text-3-X-高于Build-309x版本适用"><a href="#Sublime-Text-3-X-高于Build-309x版本适用" class="headerlink" title="Sublime Text 3.X (高于Build 309x版本适用)"></a>Sublime Text 3.X (高于Build 309x版本适用)</h2><blockquote>
<p>截止当前时间最新版本的都适用</p>
</blockquote>
<figure class="highlight lsl"><table><tr><td class="code"><pre><div class="line">—– BEGIN LICENSE —–</div><div class="line">Michael Barnes</div><div class="line">Single User License</div><div class="line">EA7E<span class="number">-821385</span></div><div class="line"><span class="number">8</span>A353C41 <span class="number">872</span>A0D5C DF9B2950 AFF6F667</div><div class="line">C458EA6D <span class="number">8</span>EA3C286 <span class="number">98</span>D1D650 <span class="number">131</span>A97AB</div><div class="line">AA919AEC EF20E143 B361B1E7 <span class="number">4</span>C8B7F04</div><div class="line">B085E65E <span class="number">2</span>F5F5360 <span class="number">8489</span>D422 FB8FC1AA</div><div class="line"><span class="number">93</span>F6323C FD7F7544 <span class="number">3</span>F39C318 D95E6480</div><div class="line">FCCC7561 <span class="number">8</span>A4A1741 <span class="number">68</span>FA4223 ADCEDE07</div><div class="line"><span class="number">200</span>C25BE DBBC4855 C4CFB774 C5EC138C</div><div class="line"><span class="number">0</span>FEC1CEF D9DCECEC D3A5DAD1 <span class="number">01316</span>C36</div><div class="line">—— END LICENSE ——</div></pre></td></tr></table></figure>
<a id="more"></a>
<figure class="highlight lsl"><table><tr><td class="code"><pre><div class="line">—– BEGIN LICENSE —–</div><div class="line">Free Communities Consultoria em Informática Ltda</div><div class="line">Single User License</div><div class="line">EA7E<span class="number">-801302</span></div><div class="line">C154C122 <span class="number">4</span>EFA4415 F1AAEBCC <span class="number">315</span>F3A7D</div><div class="line"><span class="number">2580735</span>A <span class="number">7955</span>AA57 <span class="number">850</span>ABD88 <span class="number">72</span>A1DDD8</div><div class="line"><span class="number">8</span>D2CE060 CF980C29 <span class="number">890</span>D74F2 <span class="number">53131895</span></div><div class="line"><span class="number">281E324</span>E <span class="number">98</span>EA1FEF <span class="number">7</span>FF69A12 <span class="number">17</span>CA7784</div><div class="line"><span class="number">490862</span>AF <span class="number">833E133</span>D FD22141D D8C89B94</div><div class="line"><span class="number">4</span>C10A4D2 <span class="number">24693</span>D70 AE37C18F <span class="number">72</span>EF0BE5</div><div class="line"><span class="number">1</span>ED60704 <span class="number">651</span>BC71F <span class="number">16</span>CA1B77 <span class="number">496</span>A0B19</div><div class="line"><span class="number">463</span>EDFF9 <span class="number">6</span>BEB1861 CA5BAD96 <span class="number">89</span>D0118E</div><div class="line">—— END LICENSE ——</div></pre></td></tr></table></figure>
<figure class="highlight lsl"><table><tr><td class="code"><pre><div class="line">—– BEGIN LICENSE —–</div><div class="line">Nicolas Hennion</div><div class="line">Single User License</div><div class="line">EA7E<span class="number">-866075</span></div><div class="line"><span class="number">8</span>A01AA83 <span class="number">1</span>D668D24 <span class="number">4484</span>AEBC <span class="number">3</span>B04512C</div><div class="line"><span class="number">827</span>B0DE5 <span class="number">69E9</span>B07A A39ACCC0 F95F5410</div><div class="line"><span class="number">729</span>D5639 <span class="number">4</span>C37CECB B2522FB3 <span class="number">8</span>D37FDC1</div><div class="line"><span class="number">72899363</span> BBA441AC A5F47F08 <span class="number">6</span>CD3B3FE</div><div class="line">CEFB3783 B2E1BA96 <span class="number">71</span>AAF7B4 AFB61B1D</div><div class="line"><span class="number">0</span>CC513E7 <span class="number">52</span>FF2333 <span class="number">9</span>F726D2C CDE53B4A</div><div class="line"><span class="number">810</span>C0D4F E1F419A3 CDA0832B <span class="number">8440565</span>A</div><div class="line"><span class="number">35</span>BF00F6 <span class="number">4</span>CA9F869 ED10E245 <span class="number">469</span>C233E</div><div class="line">—— END LICENSE ——</div></pre></td></tr></table></figure>
<h2 id="Sublime-Text-3-x-Build-309x-之前版本适用"><a href="#Sublime-Text-3-x-Build-309x-之前版本适用" class="headerlink" title="Sublime Text 3.x(Build 309x 之前版本适用)"></a>Sublime Text 3.x(Build 309x 之前版本适用)</h2><blockquote>
<p>适用 3065/3083 低于 309x 的版本</p>
</blockquote>
<figure class="highlight lsl"><table><tr><td class="code"><pre><div class="line">—– BEGIN LICENSE —–</div><div class="line">Andrew Weber</div><div class="line">Single User License</div><div class="line">EA7E<span class="number">-855605</span></div><div class="line"><span class="number">813</span>A03DD <span class="number">5E4</span>AD9E6 <span class="number">6</span>C0EEB94 BC99798F</div><div class="line"><span class="number">942194</span>A6 <span class="number">02396E98</span> E62C9979 <span class="number">4</span>BB979FE</div><div class="line"><span class="number">91424</span>C9D A45400BF F6747D88 <span class="number">2</span>FB88078</div><div class="line"><span class="number">90</span>F5CC94 <span class="number">1</span>CDC92DC <span class="number">8457107</span>A F151657B</div><div class="line"><span class="number">1</span>D22E383 A997F016 <span class="number">42397640</span> <span class="number">33</span>F41CFC</div><div class="line">E1D0AE85 A0BBD039 <span class="number">0E9</span>C8D55 E1B89D5D</div><div class="line"><span class="number">5</span>CDB7036 E56DE1C0 EFCC0840 <span class="number">650</span>CD3A6</div><div class="line">B98FC99C <span class="number">8</span>FAC73EE D2B95564 DF450523</div><div class="line">—— END LICENSE ——</div></pre></td></tr></table></figure>
<h2 id="Sublime-Text-2"><a href="#Sublime-Text-2" class="headerlink" title="Sublime Text 2"></a>Sublime Text 2</h2><figure class="highlight lsl"><table><tr><td class="code"><pre><div class="line">—– BEGIN LICENSE —–</div><div class="line">Andrew Weber</div><div class="line">Single User License</div><div class="line">EA7E<span class="number">-855605</span></div><div class="line"><span class="number">813</span>A03DD <span class="number">5E4</span>AD9E6 <span class="number">6</span>C0EEB94 BC99798F</div><div class="line"><span class="number">942194</span>A6 <span class="number">02396E98</span> E62C9979 <span class="number">4</span>BB979FE</div><div class="line"><span class="number">91424</span>C9D A45400BF F6747D88 <span class="number">2</span>FB88078</div><div class="line"><span class="number">90</span>F5CC94 <span class="number">1</span>CDC92DC <span class="number">8457107</span>A F151657B</div><div class="line"><span class="number">1</span>D22E383 A997F016 <span class="number">42397640</span> <span class="number">33</span>F41CFC</div><div class="line">E1D0AE85 A0BBD039 <span class="number">0E9</span>C8D55 E1B89D5D</div><div class="line"><span class="number">5</span>CDB7036 E56DE1C0 EFCC0840 <span class="number">650</span>CD3A6</div><div class="line">B98FC99C <span class="number">8</span>FAC73EE D2B95564 DF450523</div><div class="line">—— END LICENSE ——</div></pre></td></tr></table></figure>
<h2 id="Axure-rp-8"><a href="#Axure-rp-8" class="headerlink" title="Axure rp 8"></a>Axure rp 8</h2><figure class="highlight gcode"><table><tr><td class="code"><pre><div class="line">用户名：axureuser</div><div class="line">注册码：<span class="number">8</span>wFfIX<span class="number">7</span>a<span class="number">8</span>hHq<span class="number">6</span>yAy<span class="number">6</span>T<span class="number">8</span>zCz<span class="number">5</span>R<span class="number">0</span><span class="symbol">NBKeVxo9</span>IKu+kgKh<span class="number">79</span>FL<span class="number">6</span>IyPD<span class="number">6</span>lK<span class="number">7</span><span class="name">G6</span>+tqEV<span class="number">4</span>LG</div><div class="line"></div><div class="line">用户名：aaa</div><div class="line">注册码：<span class="number">2</span>GQrt<span class="number">5</span>XHYY<span class="number">7</span>SBK/<span class="number">4</span>b<span class="number">22</span>G<span class="name">m4</span>Dh<span class="number">8</span>alaR<span class="number">0</span>/<span class="number">0</span>k<span class="number">3</span><span class="keyword">gE</span><span class="symbol">N5</span>h<span class="number">7</span>FkVPI<span class="symbol">n8</span>o<span class="name">G3</span>uphlOeytIajxGU</div></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      Sublime Text 注册码
    
    </summary>
    
      <category term="备忘" scheme="https://cnt1992.github.io/categories/%E5%A4%87%E5%BF%98/"/>
    
    
      <category term="sublime license" scheme="https://cnt1992.github.io/tags/sublime-license/"/>
    
  </entry>
  
  <entry>
    <title>a note about postcss author share</title>
    <link href="https://cnt1992.github.io/2016/04/15/a-note-about-postcss-author-share/"/>
    <id>https://cnt1992.github.io/2016/04/15/a-note-about-postcss-author-share/</id>
    <published>2016-04-14T16:27:38.000Z</published>
    <updated>2016-07-06T14:37:47.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p><a href="https://github.com/postcss/postcss" target="_blank" rel="external">PostCSS</a> 的作者 <code>Andrey Sitnik</code> 远从俄罗斯来到淘宝城做分享！</p>
</blockquote>
<p>没听过 <code>PostCSS</code> ？先简单看看我之前写的 <a href="http://cnt1992.xyz/2015/12/17/CSS%E5%90%8E%E7%BD%AE%E5%A4%84%E7%90%86%E5%99%A8%E4%B9%8BPostCss/" target="_blank" rel="external">CSS后置处理器之PostCss</a> 做个入门。</p>
<a id="more"></a>
<p>穿着拖鞋略有点腼腆的小伙子从下面几个方面介绍了<code>PostCSS</code>：</p>
<h2 id="Why"><a href="#Why" class="headerlink" title="Why ?"></a>Why ?</h2><h2 id="提问环节的一些干货"><a href="#提问环节的一些干货" class="headerlink" title="提问环节的一些干货"></a>提问环节的一些干货</h2><blockquote>
<p>Q：请问如何提高个人开源作品的影响力？<br>A：第一点也是很重要的一点，放在 <code>GitHub</code> 上的 <code>README.md</code> 请尽量用英文写，其实那些优秀的开源作品的作者不一定都是用英语作为母语，而且别人不会去在意你英文写得好不好，而是在意你开源的作品牛不牛逼；其次，如果觉得自己的作品很不错，可以去 <code>twitter</code> 上 <strong>[At]</strong> 那些致力于开源作品的人，他们其实也特别希望有更多的优秀作品开源出来；</p>
<p>Q：<code>PostCSS</code> 的性能怎样？如何测试出来的？<br>A：<code>PostCSS</code> 性能测试是用来 <a href="https://github.com/gemini-testing/gemini" target="_blank" rel="external">gemini</a> ，这是一个非常棒的回归性能测试工具，由于 <code>PostCSS</code> 自己写了一套解析 <code>STYLE</code> 的算法，所以性能方面对比来看是非常好的。</p>
</blockquote>
<h2 id="个人体会"><a href="#个人体会" class="headerlink" title="个人体会"></a>个人体会</h2><p>如 <code>AI</code> 所言，由于母语的限制，很多国内优秀作品没有在国外流行开来主要还是不用英文的 <code>README</code> ，要养成良好的文档习惯，不要畏惧。同时，发现问题不难，去解决一个问题也不难，难的是一直坚持下去。</p>
<p>最后，附上现场照片一张，难得写过 <code>PostCSS</code> 插件推崇它的我坐在第一排认真听分享（假装听得懂英语ing）。</p>
<p><img src="/img/postcss-author.jpg" alt="PostCSS作者分享照片"></p>
]]></content>
    
    <summary type="html">
    
      PostCSS的作者远从俄罗斯来淘宝城做分享，记录一下此次分享的笔记。
    
    </summary>
    
      <category term="心得体会" scheme="https://cnt1992.github.io/categories/%E5%BF%83%E5%BE%97%E4%BD%93%E4%BC%9A/"/>
    
    
      <category term="PostCSS 分享" scheme="https://cnt1992.github.io/tags/PostCSS-%E5%88%86%E4%BA%AB/"/>
    
  </entry>
  
  <entry>
    <title>前端工程师学习Nginx实践配置HTTP2.0篇</title>
    <link href="https://cnt1992.github.io/2016/04/08/upgrade-nginx-to-http2/"/>
    <id>https://cnt1992.github.io/2016/04/08/upgrade-nginx-to-http2/</id>
    <published>2016-04-08T15:41:54.000Z</published>
    <updated>2016-04-11T09:26:55.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="科普时间"><a href="#科普时间" class="headerlink" title="科普时间"></a>科普时间</h2><p>作为有 <code>全栈工程师</code> 情节并且 <strong>乐于折腾</strong> 的我在上一篇 <a href="http://cnt1992.xyz/2016/03/18/simple-intro-to-nginx/" target="_blank" rel="external">前端工程师学习Nginx入门篇</a> 之后继续捣鼓将 <code>Nginx</code> 配置支持 <code>HTTP2.0</code>，这是为了等以后有钱可以买个服务器的时候直接配置服务器体验一把网页<code>秒开</code>的快感，为啥配置<code>HTTP2.0</code>可以秒开网页？请静心往下看(图片摘自网络，如有侵略版权内容请联系我删除哈)：</p>
<h3 id="HTTP-2-0-带来了哪些新特性-？"><a href="#HTTP-2-0-带来了哪些新特性-？" class="headerlink" title="HTTP 2.0 带来了哪些新特性 ？"></a><code>HTTP 2.0</code> 带来了哪些新特性 ？</h3><blockquote>
<p><code>HTTP 2.0</code> 带来了很多新的特性，这里不会一一详细介绍，只列举了几项，如有兴趣研究的童鞋请移步 <a href="https://tools.ietf.org/html/rfc7540" target="_blank" rel="external">官方RFC文档</a>。</p>
</blockquote>
<p>1.<strong>增加</strong>二进制分帧</p>
<p><code>HTTP</code>协议从<code>0.9</code>版本开始不断增加新的功能特性，但长远来看都是 <code>向前兼容</code> 的，<code>HTTP 2.0</code> 在 <code>应用层</code> 跟 <code>传输层</code> 之间增加了一个 <code>二进制分帧层</code>，从而能够达到 “在不改动HTTP的语义、HTTP方法、状态码、URI及首部字段的情况下，突破HTTP 1.1的性能限制，改进传输性能，实现低延迟和高吞吐量。”</p>
<p><img src="/img/nginx-http/binary.png" alt="HTTP 2.0二进制分帧层"></p>
<p>如上图所示，在 <code>二进制分帧层</code> 上， <code>HTTP 2.0</code> 会将所有传输的信息分割为更小的消息和帧，并对它们采用二进制格式的编码，其中 <code>HTTP 1.1</code> 的首部信息会被封装到 <code>Headers</code> 帧，而 <code>request body</code> 被封装到 <code>Data</code> 帧里面。</p>
<p>2.压缩头部</p>
<p>如下图所示：<code>HTTP 2.0</code> 在 <code>客户端</code> 和 <code>服务端</code> 使用 <strong>首部表</strong> 来跟踪和存储之间发送的 <code>键-值</code> 对，对相同请求而言不需要再次发送请求和相应发送，通信期间几乎不会改变的通用 <code>键-值</code> （如：用户代理、可接受的媒体类型）只需发送一次。</p>
<ul>
<li>如果请求不包含首部（如：对同一资源的轮询请求），那首部开销为零字节</li>
<li>如果首部发生变化，那只需发送变化的数据在 <code>Headers</code> 帧里面，新增或修改的 <code>首部帧</code> 会被追加到 <strong>首部表</strong></li>
</ul>
<p><img src="/img/nginx-http/header.png" alt="HTTP 2.0头部压缩"></p>
<p>3.多路复用</p>
<p>记得大学 <code>计算机网络</code> 跟 <code>计算机组成原理</code> 这两门课都讲到了 <code>多路复用</code> 这个概念，当时一知半解（可能是老师讲得太抽象了o(╯□╰)o）。相比一个入了门的前端开发在谈到 <code>性能优化</code> 的方法时都可以轻轻松松列举如下几点：</p>
<ul>
<li>CSS雪碧图合并 - 减少请求</li>
<li>合并压缩CSS跟JavaScript代码 - 减少请求</li>
<li>CSS代码放在header头部里面，JavaScript代码放到body结束之前 - 因为JavaScript代码执行会阻塞</li>
</ul>
<p>然后我们可以自豪地晒出下面的代码片段：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><div class="line"><span class="meta">&lt;!DOCTYPE HTML&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">head</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"stylesheet"</span> <span class="attr">href</span>=<span class="string">"xxx.cdn.com/??a.css,b.css"</span> /&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">head</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">body</span>&gt;</span></div><div class="line">        ...</div><div class="line">        <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"xxx.cdn.com/??a.js,b.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">body</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></div></pre></td></tr></table></figure>
<p>但 <code>HTTP 2.0</code> 的 <code>多路复用</code> 让我们回到了最原始最自然的写码状态，先看下图：</p>
<p><img src="/img/nginx-http/multiplexing.png" alt="HTTP 2.0 多路复用"></p>
<p>对 <code>HTTP 1.1</code> 而言，浏览器通常有链接的限制，即使开启多个链接，也需要付出相应的代码，而 <code>多路复用</code> 允许同时通过单一的 <code>HTTP 2.0</code> 连接发起多重的 <code>请求-相应</code> 消息。</p>
<p>这意味着 <code>HTTP 2.0</code> 的通信都在一个连接上完成了，这个连接可以承载任意数量的 <strong>双向数据流</strong> ，直观来说，就是我们又可以开开心心写出下面代码：</p>
<figure class="highlight xml"><table><tr><td class="code"><pre><div class="line"><span class="meta">&lt;!DOCTYPE HTML&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">html</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">head</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"stylesheet"</span> <span class="attr">href</span>=<span class="string">"a.css"</span> /&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">link</span> <span class="attr">rel</span>=<span class="string">"stylesheet"</span> <span class="attr">href</span>=<span class="string">"b.css"</span> /&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">head</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">body</span>&gt;</span></div><div class="line">        ...</div><div class="line">        <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"a.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">script</span> <span class="attr">src</span>=<span class="string">"b.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">body</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">html</span>&gt;</span></div></pre></td></tr></table></figure>
<a id="more"></a>
<p>4.请求优先级</p>
<p>既然所有资源都可以并行交错发送，会不会导致下面这种情况呢？</p>
<blockquote>
<p>浏览器： 服务器，请给我需要的CSS文件<br>服务器： Biu，发送图片中…<br>浏览器： 服务器，请给我需要的JS文件<br>服务器： Biu，发送图片中…<br>浏览器： 服务器，你TM的倒是给我发送我需要的CSS文件跟JS文件呀！</p>
</blockquote>
<p>这个时候 <code>HTTP 2.0</code> 的 <code>请求优先级</code> 特性出场了，在每个 <code>HTTP 2.0</code> 的 <code>流</code> 里面有个 <strong>优先值</strong> ，这个 <strong>优先值</strong> 确定着客户端跟服务器处理不同的 <code>流</code> 采取不同的 <strong>优先级策略</strong> ，高优先级的应该优先发送，但这不会绝对的（绝对等待会导致 <code>首队阻塞</code> 问题）。在分配处理资源和客户端与服务器间的宽带，不同优先级的混合都是必须的。</p>
<p>5.服务器提示</p>
<p>一般情况下，客户端需要请求啥东西告诉服务器，然后服务器返回对应资源回到客户端，这也是请求很慢的原因之一。<code>HTTP 2.0</code> 新增加 <code>服务器提示</code> ，可以先于客户端检测到将要请求的资源，提前通知客户端，服务器不发送所有资源的实体，只发送资源的 <code>URL</code>。客户端接到提示后会进行验证缓存，如果发现需要这些资源，则正式发起请求。</p>
<p>这个技术跟我们常用的 <code>预加载</code> 技术实现差不多，但 <code>服务器提示</code> 是通过 <code>HTTP Link Header</code> 和 <code>Link Prefetching</code> 语义重叠的部分来实现的。</p>
<h3 id="Nginx-如何实现-HTTP-2-0"><a href="#Nginx-如何实现-HTTP-2-0" class="headerlink" title="Nginx 如何实现 HTTP 2.0 ?"></a>Nginx 如何实现 HTTP 2.0 ?</h3><p>打开 <a href="http://nginx.org/" target="_blank" rel="external">Nginx官网</a> 可以看到其中有一条新闻如下：</p>
<p><img src="/img/nginx-http/nginx-release-http2.0.png" alt="Nginx的HTTP 2.0模块"></p>
<p>显然，<code>Nginx</code> 是已经有了一个 <code>ngx_http_v2_module</code> 模块用来支持 <code>HTTP 2.0</code> 的，但是还不是稳定版，所以想要尝鲜那就需要 <strong>手动升级Nginx</strong> 。</p>
<p>科普时间到此结束，下面就让我们正式进入今天的主题： <code>一步步实现Nginx配置HTTP 2.0</code>。</p>
<h2 id="Nginx配置HTTP-2-0正式起航"><a href="#Nginx配置HTTP-2-0正式起航" class="headerlink" title="Nginx配置HTTP 2.0正式起航"></a>Nginx配置HTTP 2.0正式起航</h2><blockquote>
<p>下面内容有条件的同学请对比实践操作，体会更深。</p>
</blockquote>
<p>我将从下面几步来介绍：</p>
<ul>
<li>准备工作</li>
<li>源码安装升级 Nginx 到最新版（当前是1.9.14）</li>
<li>生成 <code>SSL/TLS</code> 安全证书</li>
<li>修改Nginx配置</li>
</ul>
<h3 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h3><p>在开始我们的任务之前，请先下载 <a href="https://www.openssl.org/source/openssl-1.0.2g.tar.gz" target="_blank" rel="external">OpenSSL</a>，<a href="http://downloads.sourceforge.net/project/pcre/pcre/8.38/pcre-8.38.tar.gz?r=https%3A%2F%2Fsourceforge.net%2Fprojects%2Fpcre%2Ffiles%2Fpcre%2F8.38%2F&amp;ts=1459928198&amp;use_mirror=liquidtelecom" target="_blank" rel="external">pcre</a>，<a href="http://zlib.net/zlib-1.2.8.tar.gz" target="_blank" rel="external">Zlib</a> 跟 <a href="http://nginx.org/download/nginx-1.9.14.tar.gz" target="_blank" rel="external">Nginx源码</a> ，并且全部解压到同一个目录，假设为 <code>nginx-build</code>，这时候的代码结构如下：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-tag">nginx-build</span></div><div class="line">  <span class="selector-tag">nginx-1</span><span class="selector-class">.9</span><span class="selector-class">.14</span></div><div class="line">  <span class="selector-tag">pcre-8</span><span class="selector-class">.38</span></div><div class="line">  <span class="selector-tag">openssl-1</span><span class="selector-class">.0</span><span class="selector-class">.2g</span></div><div class="line">  <span class="selector-tag">zlib-1</span><span class="selector-class">.2</span><span class="selector-class">.8</span></div></pre></td></tr></table></figure>
<h3 id="源码安装升级-Nginx-到最新版（当前是1-9-14）"><a href="#源码安装升级-Nginx-到最新版（当前是1-9-14）" class="headerlink" title="源码安装升级 Nginx 到最新版（当前是1.9.14）"></a>源码安装升级 Nginx 到最新版（当前是1.9.14）</h3><blockquote>
<p>若之前用 <code>brew</code> 安装过 <code>Nginx</code> ，请先执行 <code>brew uninstall nginx</code> 卸载。 </p>
</blockquote>
<p>准备工作做好之后，打开 <code>Terminal</code> 切换到上面的 <code>nginx-build</code> 目录，然后执行下面命令编译Nginx：</p>
<figure class="highlight clean"><table><tr><td class="code"><pre><div class="line"># 先进入nginx源码目录</div><div class="line">cd nginx<span class="number">-1.9</span><span class="number">.14</span></div><div class="line"></div><div class="line"># 配置Nginx源码</div><div class="line"># --prefix：配置nginx最终安装目录，可以修改为你想要的目录</div><div class="line"># --<span class="keyword">with</span>-http_ssl_module 跟 --<span class="keyword">with</span>-http_v2_module 必带，因为 HTTP <span class="number">2.0</span> 采用 HTTPS ，HTTPS 基于 SSL/TLS</div><div class="line">sudo ./configure --prefix=/usr/local/nginx --<span class="keyword">with</span>-http_ssl_module --<span class="keyword">with</span>-pcre=../pcre<span class="number">-8.38</span> --<span class="keyword">with</span>-openssl=../openssl<span class="number">-1.0</span><span class="number">.2</span>g --<span class="keyword">with</span>-zlib=../zlib<span class="number">-1.2</span><span class="number">.8</span> --<span class="keyword">with</span>-http_v2_module</div><div class="line">&gt; 输入管理员密码然后回车</div></pre></td></tr></table></figure>
<p>配置源码成功之后，为了避免 <code>make</code> 失败，因为我失败了（可能是人品不好…），反正我 <code>make</code> 的时候报了下面的错误：</p>
<p><img src="/img/nginx-http/make-error.png" alt="Nginx make 出错"></p>
<p>所以建议大家跟我一样先修改一个配置文件，如下命令(相对于<code>nginx-build</code>目录)：</p>
<figure class="highlight lsl"><table><tr><td class="code"><pre><div class="line">cd nginx<span class="number">-1.9</span><span class="number">.14</span>/objs</div><div class="line">sudo vi Makefile</div><div class="line">&gt; 输入管理员密码然后回车</div><div class="line"># 在 vi 模式下输入 '/' ，然后输入./config --prefix定位到类似我下面的片段：</div><div class="line"># &amp;&amp; ./config --prefix=/Users/cainengtian/Downloads/software/nginx<span class="number">-1.8</span><span class="number">.0</span>/../openssl<span class="number">-1.0</span><span class="number">.2</span>d/.openssl no-shared  \</div><div class="line"># 将前面的 ./config 改为 ./Congigure darwin64-x86_64-cc ，其他的不要改动， 然后保存即可</div></pre></td></tr></table></figure>
<p>接着回到<code>nginx-build</code>目录，然后执行下面命令编译：</p>
<figure class="highlight vim"><table><tr><td class="code"><pre><div class="line"><span class="keyword">cd</span> nginx-<span class="number">1.9</span>.<span class="number">14</span></div><div class="line">sudo <span class="keyword">make</span> &amp;&amp; <span class="keyword">make</span> install</div></pre></td></tr></table></figure>
<p>如果没有报错那就编译成功，在终端试试 <code>sudo nginx</code> 启动，然后打开 <a href="http://localhost" target="_blank" rel="external">http://localhost</a> 能看到欢迎页，然后在终端输入 <code>nginx -v</code> 看到 <code>nginx version: nginx/1.9.14</code>。</p>
<h3 id="生成-SSL-TLS-安全证书"><a href="#生成-SSL-TLS-安全证书" class="headerlink" title="生成 SSL/TLS 安全证书"></a>生成 <code>SSL/TLS</code> 安全证书</h3><p>要配置 <code>HTTP 2.0</code> 就需要启动 <code>HTTPS</code>，那就需要生成一个 <code>SSL/TLS</code> 证书，而 <code>OpenSSL</code> 正好可以做这件事情，买不起证书但自己造一个来临时用还是可以的，直接执行下面命令：</p>
<figure class="highlight maxima"><table><tr><td class="code"><pre><div class="line"># 切换到nginx的根目录（我这里是/usr/<span class="built_in">local</span>/nginx，请根据上一步编译时候指定的--<span class="built_in">prefix</span>对应修改）</div><div class="line">cd /usr/<span class="built_in">local</span>/nginx</div><div class="line">cd conf</div><div class="line"></div><div class="line"># 利用openssl命令生成公钥跟密钥</div><div class="line">openssl genrsa -des3 -passout pass:x -out cert.pass.<span class="built_in">key</span> <span class="number">2048</span></div><div class="line">openssl rsa -passin pass:x -<span class="keyword">in</span> cert.pass.<span class="built_in">key</span> -out cert.<span class="built_in">key</span></div><div class="line">openssl req -<span class="built_in">new</span> -<span class="built_in">key</span> cert.<span class="built_in">key</span> -out cert.csr</div><div class="line">openssl x509 -req -days <span class="number">365</span> -<span class="keyword">in</span> cert.csr -signkey server.<span class="built_in">key</span> -out cert.crt</div><div class="line"></div><div class="line"># 将crt跟<span class="built_in">key</span>合并生成pem文件</div><div class="line">cat server.crt server.<span class="built_in">key</span> &gt; server.pem</div><div class="line"></div><div class="line"># 删除掉我们不需要用到的文件</div><div class="line">rm -rf cert.crt cert.csr</div></pre></td></tr></table></figure>
<h3 id="修改Nginx配置"><a href="#修改Nginx配置" class="headerlink" title="修改Nginx配置"></a>修改Nginx配置</h3><p>离成功之后一步之遥了，接下来就是最重要的一步，配置Nginx支持HTTP 2.0，修改Nginx配置文件<code>/usr/local/nginx/conf/nginx.conf</code>： </p>
<p>1.将所有HTTP请求重定向到HTTPS请求</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><div class="line"><span class="attribute">location</span> / &#123;</div><div class="line">    <span class="attribute">return</span> <span class="number">301</span> https://<span class="variable">$host</span><span class="variable">$remote_port</span><span class="variable">$request_uri</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>2.配置HTTPS server</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><div class="line"><span class="section">server</span> &#123;</div><div class="line">    <span class="attribute">listen</span> <span class="number">443</span> ssl http2 default_server;</div><div class="line">        <span class="attribute">server_name</span>  localhost;</div><div class="line">        <span class="attribute">ssl_certificate</span>      cert.pem;</div><div class="line">        <span class="attribute">ssl_certificate_key</span>  cert.key;</div><div class="line">        <span class="attribute">ssl_session_cache</span>    shared:SSL:<span class="number">1m</span>;</div><div class="line">        <span class="attribute">ssl_session_timeout</span>  <span class="number">5m</span>;</div><div class="line"></div><div class="line">        <span class="comment"># 我用原始的下面这段启动报错了，所以注释掉改用了后面那段</span></div><div class="line">        <span class="comment">#ssl_ciphers  HIGH:!aNULL:!MD5;</span></div><div class="line">        <span class="attribute">ssl_ciphers</span> <span class="string">'CHACHA20:EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH:ECDHE-RSA-AES128-GCM-SHA384:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA128:DHE-RSA-AES128-GCM-SHA384:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES128-GCM-SHA128:ECDHE-RSA-AES128-SHA384:ECDHE-RSA-AES128-SHA128:ECDHE-RSA-AES128-SHA:ECDHE-RSA-AES128-SHA:DHE-RSA-AES128-SHA128:DHE-RSA-AES128-SHA128:DHE-RSA-AES128-SHA:DHE-RSA-AES128-SHA:ECDHE-RSA-DES-CBC3-SHA:EDH-RSA-DES-CBC3-SHA:AES128-GCM-SHA384:AES128-GCM-SHA128:AES128-SHA128:AES128-SHA128:AES128-SHA:AES128-SHA:DES-CBC3-SHA:HIGH:!aNULL:!eNULL:!EXPORT:!DES:!MD5:!PSK:!RC4;'</span>;</div><div class="line">        <span class="attribute">ssl_prefer_server_ciphers</span>  <span class="literal">on</span>;</div><div class="line"></div><div class="line">        <span class="attribute">location</span> / &#123;</div><div class="line">            <span class="attribute">root</span>   html;</div><div class="line">            <span class="attribute">index</span>  index.html index.htm;</div><div class="line">        &#125;</div><div class="line">    &#125;</div></pre></td></tr></table></figure>
<p>可以注意到上面 <code>HTTPS</code> 的配置比之前多了一个 <code>http2</code> ，这就是 <code>ngx_http_v2_module</code> 模块，若没有安装该模块启动一样会报错。</p>
<p>3.重启服务器</p>
<p>配置完成之后，重新启动服务器即可：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line">sudo nginx <span class="_">-s</span> reload</div></pre></td></tr></table></figure>
<p>4.预览</p>
<p>打开 <a href="http://localhost" target="_blank" rel="external">http://localhost</a> ，自动跳转到 <a href="https://localhost" target="_blank" rel="external">https://localhost</a> ，Done！</p>
<h2 id="一点思考"><a href="#一点思考" class="headerlink" title="一点思考"></a>一点思考</h2><p>虽然 <code>HTTP 2.0</code> 也已经不算是非常新的东西，要普及确实也需要很长一段时间，希望不久将来有钱买个服务器部署到自己网站上。</p>
<p>只有动手实践才知道看起来简单的东西实际上还是会碰到很多坑，anyway，一直在成长的路上~</p>
]]></content>
    
    <summary type="html">
    
      HTTP2.0很火？想知道Nginx如何配置HTTP2.0么？跟着我一步步实际操作体验秒开网页。
    
    </summary>
    
      <category term="教程" scheme="https://cnt1992.github.io/categories/%E6%95%99%E7%A8%8B/"/>
    
    
      <category term="Nginx HTTP2.0 前端" scheme="https://cnt1992.github.io/tags/Nginx-HTTP2-0-%E5%89%8D%E7%AB%AF/"/>
    
  </entry>
  
  <entry>
    <title>迟来的面试总结</title>
    <link href="https://cnt1992.github.io/2016/04/05/%E8%BF%9F%E6%9D%A5%E7%9A%84%E9%9D%A2%E7%BB%8F/"/>
    <id>https://cnt1992.github.io/2016/04/05/迟来的面经/</id>
    <published>2016-04-04T16:12:38.000Z</published>
    <updated>2016-04-04T10:02:50.000Z</updated>
    
    <content type="html"><![CDATA[<p>今天 <code>清明节</code>，已然是第 <em>4</em> 个年头没有回家祭祖，现在坐在工位上，刚将手头上的工作做好，装逼范儿先来诗一首：</p>
<blockquote>
<p>清明时节雨纷纷，路上行人欲断魂；<br>借问酒家何处有，牧童遥指杏花村。</p>
</blockquote>
<p>PIA! 装逼结束，下面开始正题！</p>
<h2 id="谈谈最近"><a href="#谈谈最近" class="headerlink" title="谈谈最近"></a>谈谈最近</h2><p>来<code>天猫</code>已经有一个星期，从 <code>京东</code> 的 <a href="http://aotu.io" target="_blank" rel="external">凹凸实验室</a> 团队的 <strong>老人</strong> 一下子变成了 <code>天猫</code> 的 <strong>新人</strong> ，重新认识周边的同事，申请各种权限再熟悉开发环境，还好脸皮比以前厚了很多，各种不懂的东西到处找人问，总算是进入了开发状态。</p>
<p>从过年回来就不断进行 <code>天猫</code> 的电话面试到交叉面，再到最后的HRG面，以及Leader视频面，感觉身心摧残呀…</p>
<p>每次面试都那么累（说得好像面试了很多次一样o(╯□╰)o），但其实也总算是把国内 <code>JBAT</code> 都面了一下：</p>
<ul>
<li><p>去年9月， <code>腾讯</code> 的一位08级的师兄打电话给我问我有没有意愿去 <code>腾讯应用宝</code> 试试，于是就去试试了，结果聊的全部都是HTTP的东西，没看书华丽挂掉。</p>
</li>
<li><p>去年10月面了 <code>百度</code> 的 <code>凤巢团队</code>，顺利通过了一面、二面、三面，然而却在三面当天收到了 <strong>百度停止社招</strong> 的消息，然后就是漫长的冻结期，等到我最后直接放弃了！</p>
</li>
<li><p>年前 <code>天猫</code> 的同事找到我问有没有意愿加入天猫，抱着试一试的心态，结果一路顺畅就通过了。</p>
</li>
</ul>
<a id="more"></a>
<h2 id="面试总结"><a href="#面试总结" class="headerlink" title="面试总结"></a>面试总结</h2><p>之所以想写面试总结，最大的目的还是想写给自己看，毕竟面试作为你能力的最真实的表现，能否做足准备并将最好的一面表现出来那是一门学问。</p>
<p>我想按照面试时间的顺序来写我的总结，因为<code>前车之鉴后事之师</code>，每次经历过的面试都能让自己清楚地知道自己擅长啥，哪些地方不足需要弥补。</p>
<h3 id="腾讯面试"><a href="#腾讯面试" class="headerlink" title="腾讯面试"></a>腾讯面试</h3><blockquote>
<p>更多地注重理论知识，扎实的专业理论</p>
</blockquote>
<p>首先谈谈 <code>腾讯</code> 的面试，感觉腾讯很注重<code>最基础的东西</code>，当然这最基础的东西大多是理论的，我们出来工作之后大多时间都是在写码，更多侧重于<code>应用实践</code>，而越来越少的时间花在了<code>基础理论</code>上面。</p>
<p>比如：我们已经习惯了用 <code>htmlspecial</code> 方法去处理特殊的 <code>HTML</code> 字符，也知道这是用来防止 <code>XSS</code> 攻击的，然而当被问到 <code>XSS</code> 与 <code>CSRF</code> 的实现原理以及这两者之间的区别是啥？相信很多人一时半会想不起来。</p>
<p>再比如：我们已经习惯于顺手拈来 <code>Ajax</code> 方法来写各种请求，当然也知道 <code>GET</code> 和 <code>POST</code> 等请求的区别，但是你是否知道 <code>Ajax</code> 的实现原理呢？大多数人知道这是基于 <code>Promise</code> 来实现的，但是再深入一点问一下 <code>ES6</code> 的 <code>Promise</code> 的实现原理？ <code>POST</code> 请求如何实现跨域？</p>
<p>保持学习最新的东西是作为一名 <code>合格</code> 程序员的最低要求，那作为合格程序员应该都会去了解一下 <code>HTTP 2.0</code> 的相关知识了吧，什么 <code>首部压缩</code>、<code>优先级处理</code>、<code>双向字节流的请求与相应</code> 都应该了解过了，但当时我被面的时候人家根本不按常理出牌，就问：在一次 <code>POST</code> 请求中一般会带有什么头部过去？ 咳咳，反正在那之后我是乖乖的去买 <code>《HTTP权威指南》</code>恶补了。</p>
<p>当然那次面试还问 <code>缓存的实现机制</code>、<code>闭包的实现</code>等，这些就都是常规的面试题了，总结自己没有通过的原因：在写码的时候会利用各种奇淫技巧去解决问题，但很少去关注背后的一些原理。</p>
<h3 id="百度面试"><a href="#百度面试" class="headerlink" title="百度面试"></a>百度面试</h3><blockquote>
<p>偏向算法以及逻辑思考能力</p>
</blockquote>
<p>这可能跟我面试的团队有关，听面试官介绍他们团队主要负责百度搜索推荐算法的，部门里面基本都是研究生，所以疯狂抛算法的题目给我。</p>
<p>一面的时候还算是比较客气的，让我手写实现 <code>函数继承</code> ，以及考了一下 <code>如何判断一个变量是否为数组</code>，基本大家都能够跟我一样答出下面的答案：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">isArray</span>(<span class="params"> o </span>)</span>&#123;</div><div class="line">    <span class="keyword">return</span> <span class="built_in">Array</span>.isArray( o ) || <span class="built_in">Object</span>.prototype.toString.call( o ) === <span class="string">'[object Array]'</span>; </div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>如果你能答出上面的答案，相比你应该读过 <code>jQuery</code> 或者 <code>Underscore</code> 之类的源码？于是面试官让我打开电脑定位到源码，然后问一些方法是用来干嘛的之类的，所以平常看看一些开源的库或者框架的源码很有必要，一来去看优秀的库是如何实现，培养自己架构的能力，二来方便面试（PIA，赶紧跑~~）</p>
<p>然后二面的时候一上来就让实现 <code>排序算法</code> 的所有我能讲出来的方法，什么 <code>冒泡</code>、<code>二分法</code>等，但我犯了一个错误，那就是我知道有哪些方法可以实现，但我并不知道所有的方法具体是如何实现，这就是 <code>挖坑给自己跳</code> ，然后继续考算法，考 <code>动态规划</code> 的实现与原理等。因为在面试之前特地买了一本书 <a href="http://item.jd.com/11615451.html" target="_blank" rel="external">《算法问题实战策略》</a> 来看，所以这些算法基本都考不到我了，最后面试官很疑惑的问我怎么会知道这些多算法的问题，我就说我刚好看了算法的书，于是面试官笑呵呵让我过了。</p>
<p>前面两面是技术面，第三面好像并没有考特别细的技术细节，就单纯让我拿一个觉得最厉害的项目跟他聊聊，其实就考自己在项目中扮演着什么角色，在项目中具体做了什么，整体的技术能力以及综合能力差不多就出来了，这里想给<a href="http://aotu.io" target="_blank" rel="external">凹凸实验室</a>打个广告 – 我们在大公司基本都扮演着螺丝钉的角色，基本专注到很小的模块然后修修补补，很少有机会让你站在全局上面一路参与一个项目，但我在<code>凹凸实验室</code>团队拿到了很多自己去主导的项目，也正如此才让我离 <code>全栈工程师</code> 更仅一步。也正因为有了自己主导的项目，三面面试官很满意。</p>
<p>虽然很多人在说百度的各种不好，像什么<code>贴吧事件</code>，但百度的技术依然还是不可否认很厉害，虽然社招被冻结，没能如愿进去，但也促使自己学习了更多的东西。</p>
<h3 id="天猫面试"><a href="#天猫面试" class="headerlink" title="天猫面试"></a>天猫面试</h3><blockquote>
<p>偏向项目实战能力</p>
</blockquote>
<p>其实在百度冻结社招之后，自己并没有想换工作的打算，一来是老大对我的重视，把很多福利都争取给了我，二来是女朋友在同一层楼工作，每天一起下班而不是单身狗…</p>
<p>但由于各种原因，当天猫找到我的时候，也并没有拒绝，而是抱着试一试的心态去面，完全没想到就直接过了，搞得我匆匆忙忙离开深圳奔赴杭州，整个平静的生活算是变化特别大了。</p>
<p>来阿里也许是注定的，大三的时候曾经面淘宝UED并且已经拿到了实习的offer，但考虑杭州太远而且学校很多事情要忙放弃掉，辗转几年又回到天猫。</p>
<p>谈谈一面吧，关于 <code>CSS</code> 好像就问了我两道题：</p>
<ul>
<li><p>列举出所有能够让一个元素看不见的CSS属性</p>
</li>
<li><p>列举出所有能够让不定宽元素垂直居中的方法</p>
</li>
</ul>
<p>考 <code>重构</code> 的题目我很拿手，毕竟在京东的前大半年还是专注于重构能力的，面试官很满意我的回答，于是考了 <code>性能优化</code> 的一些东西，以及 <code>Gulp</code>跟 <code>Grunt</code> 两个工具的区别等，感觉都答得很不错，但面试官觉得我工作经验不够（因为毕业才1年多），然后我反驳了，我笑了笑说我虽然工作一年多但我实际做的项目已经比很多工作一年多的人要多得多，可能没有我这句话就直接因为经验不足被干掉了…</p>
<p>后面的面试全部围绕 <code>你觉得你最牛逼的项目</code> 来展开，考察了 <code>Angular</code>、<code>Vue</code>这些框架在我项目中的使用，考察了<code>ES7</code>新增加的东西（感觉ES6才出来就要去学习ES7的节奏，伤不起），还是那句话，其实还是想知道你在整个项目中的综合能力。</p>
<p>就酱紫入坑 <code>天猫</code> ，不过杭州阿里巴巴西溪园区真心不错，😄有种重回大学的感觉！</p>
<h2 id="继续碎碎念"><a href="#继续碎碎念" class="headerlink" title="继续碎碎念"></a>继续碎碎念</h2><p><code>Node</code>的崛起让<code>JS</code>大展拳脚，前端各种库跟框架层出不穷，社区各种争论喋喋不休，像前几天看到的，现在的学生 <code>JS</code> 能力是越来越强了，但最基本的 <code>HTML</code> 跟 <code>CSS</code> 能力却很弱，基本上都知道了 <code>ECMA 2016</code>,然而却没几个能关注 <code>HTTP 2.0</code> 的东西。前端圈子还是有点浮躁，过来天猫给自己的定位还是全栈工程师，做一个重构能力很强的开发，如果可以给自己一句话总结，那便是：</p>
<blockquote>
<p>懂设计（PS/Sketch），会视频制作（AE/PR），能写后端脚本（PHP/SHELL），喜欢看书，追求 <code>全栈工程师</code> 的程序猿。</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      记录自己的面试经历与总结
    
    </summary>
    
      <category term="心得体会" scheme="https://cnt1992.github.io/categories/%E5%BF%83%E5%BE%97%E4%BD%93%E4%BC%9A/"/>
    
    
      <category term="面试 总结" scheme="https://cnt1992.github.io/tags/%E9%9D%A2%E8%AF%95-%E6%80%BB%E7%BB%93/"/>
    
  </entry>
  
  <entry>
    <title>前端工程师学习Nginx入门篇</title>
    <link href="https://cnt1992.github.io/2016/03/18/simple-intro-to-nginx/"/>
    <id>https://cnt1992.github.io/2016/03/18/simple-intro-to-nginx/</id>
    <published>2016-03-18T14:10:49.000Z</published>
    <updated>2016-04-04T11:10:55.000Z</updated>
    
    <content type="html"><![CDATA[<p><img src="/img/nginx/nginx.png" alt="Nginx Logo"></p>
<h2 id="What-is-Nginx"><a href="#What-is-Nginx" class="headerlink" title="What is Nginx"></a>What is Nginx</h2><p><code>Nginx</code>(发音：engine X)是一款轻量级的<code>HTTP</code>服务器（相比于Apache、Lighttpd而言），同时是一个高性能的<code>HTTP</code>和<a href="#tips_1">反向代理</a>服务器，如今国内主流网站基本搭建于<code>Nginx</code>之上，诸如新浪、腾讯、网易、豆瓣。</p>
<p><code>Nginx</code>主要以<strong>事件驱动</strong>的方式编写，有兴趣可以移步<a href="https://github.com/nginx/nginx" target="_blank" rel="external">这里</a>看他们的源码，这让它拥有非常好的性能，同时也是一个非常高效的反向代理、负载均衡（不知道反向代理跟负载均衡的童鞋请自觉移步文章结尾恶补一下）。</p>
<p><a href="http://nginx.org/en/" target="_blank" rel="external">官方站点</a>也指出了<code>Nginx</code>作为HTTP服务器的几项基本特性：</p>
<ul>
<li>处理静态文件，索引文件以及自动索引；打开文件描述符缓冲</li>
<li>无缓存的反向代理加速，简单的负载均衡和容错</li>
<li>FastCGI，简单的负载均衡和容错</li>
<li>模块化的结构，包括gzipping,byte ranges,chunked responses,以及SSI-filter等filter。</li>
<li>支持SSL和TLSSNI.</li>
</ul>
<p>对于前端童鞋而言，可能基本不会碰到服务器的东西，但如果像我这样子有『全栈工程师』心结的话倒是可以研究一下，自己成功配置Nginx启动自己的服务，以后再碰到这些关于Nginx的问题自己能够解决，丰衣足食~</p>
<p>接下来我将手把手教大家从安装到配置，搭建起Nginx环境，走起~</p>
<a id="more"></a>
<h2 id="安装并启动Nginx"><a href="#安装并启动Nginx" class="headerlink" title="安装并启动Nginx"></a>安装并启动Nginx</h2><p>由于我是用Mac办公的，所以安装<code>Nginx</code>是采用<code>brew</code>进行的，在<code>终端</code>输入下面命令安装好<code>Nginx</code>：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line"><span class="comment"># 强烈建议每次brew安装软件的时候先执行brew update保持软件依赖包都是最新的</span></div><div class="line">brew update &amp;&amp; brew install nginx</div></pre></td></tr></table></figure>
<p>紧接着就可以用浏览器打开<a href="http://localhost:8080" target="_blank" rel="external">http://localhost:8080</a>看到<code>Nginx</code>的欢迎信息。</p>
<p>跟<code>Linux</code>系统有些不同，在<code>Mac</code>下面<code>Nginx</code>默认监听了<code>8080</code>端口号，若强迫症（比如我）不希望每次打开网页都要输入端口号的话，那么请在<code>终端</code>执行下面命令：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line"><span class="comment"># 下面的1.8.0请根据最新安装版本号对应修改</span></div><div class="line">sudo chown root:wheel /usr/<span class="built_in">local</span>/Cellar/nginx/1.8.0/bin/nginx</div><div class="line">sudo chmod u+s /usr/<span class="built_in">local</span>/Cellar/nginx/1.8.0/bin/nginx</div><div class="line"></div><div class="line"><span class="comment"># 用vi编辑器打开nginx配置文件，找到server字段的listen字段并将其值修改为80</span></div><div class="line">vi /usr/<span class="built_in">local</span>/etc/nginx/nginx.conf</div></pre></td></tr></table></figure>
<p>修改完上面<code>配置信息</code>之后，执行下面命令检查配置文件语法是否有误并且重新加载配置：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line">nginx -t &amp;&amp; nginx <span class="_">-s</span> reload</div></pre></td></tr></table></figure>
<p>更多关于<code>Nginx</code>命令的帮助可以输入<code>nginx -h</code>查看，若想每次开机自动开启<code>Nginx</code>，在<code>终端</code>执行下面命令即可：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line">ln -sfv /usr/<span class="built_in">local</span>/opt/nginx/*.plist ~/Library/LaunchAgents</div><div class="line">launchctl load ~/Library/LaunchAgents/homebrew.mxcl.nginx.plist</div></pre></td></tr></table></figure>
<h2 id="Nginx配置不完全详解"><a href="#Nginx配置不完全详解" class="headerlink" title="Nginx配置不完全详解"></a>Nginx配置不完全详解</h2><p><code>Nginx</code>能否发挥淋漓尽致，就看配置文件了，由于<code>Nginx</code>配置实在太多，不能一一解释，有兴趣移步<a href="http://nginx.org/en/docs/" target="_blank" rel="external">官方文档</a>。接下来我会给大家解释比较重要的配置，下面是我机器上的<code>Nginx</code>的配置文件（我会带上注释）：</p>
<p>| 强烈建议大家先打开自己的默认Nginx配置跟我的对比来看<br>| 可以在<code>终端</code>执行 <code>cat /usr/local/etc/nginx/nginx.conf.default</code> 查看默认配置文件</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><div class="line"><span class="comment"># user字段表明了Nginx服务是由哪个用户哪个群组来负责维护进程的，默认是nobody</span></div><div class="line"><span class="comment"># 我这里用了cainengtian用户，staff组来启动并维护进程</span></div><div class="line"><span class="comment"># 查看当前用户命令： whoami</span></div><div class="line"><span class="comment"># 查看当前用户所属组命令： groups ，当前用户可能有多个所属组，选第一个即可</span></div><div class="line"><span class="attribute">user</span> cainengtian staff;</div><div class="line"></div><div class="line"><span class="comment"># worker_processes字段表示Nginx服务占用的内核数量</span></div><div class="line"><span class="comment"># 为了充分利用服务器性能你可以直接写你本机最高内核</span></div><div class="line"><span class="comment"># 查看本机最高内核数量命令： sysctl -n hw.ncpu</span></div><div class="line"><span class="attribute">worker_processes</span> <span class="number">4</span>;</div><div class="line"></div><div class="line"><span class="comment"># error_log字段表示Nginx错误日志记录的位置</span></div><div class="line"><span class="comment"># 模式选择：debug/info/notice/warn/error/crit</span></div><div class="line"><span class="comment"># 上面模式从左到右记录的信息从最详细到最少</span></div><div class="line"><span class="attribute">error_log</span>  /usr/local/var/logs/nginx/error.log <span class="literal">debug</span>;</div><div class="line"></div><div class="line"><span class="comment"># Nginx执行的进程id,默认配置文件是注释了</span></div><div class="line"><span class="comment"># 如果上面worker_processes的数量大于1那Nginx就会启动多个进程</span></div><div class="line"><span class="comment"># 而发信号的时候需要知道要向哪个进程发信息，不同进程有不同的pid，所以写进文件发信号比较简单</span></div><div class="line"><span class="comment"># 你只需要手动创建，比如我下面的位置： touch /usr/local/var/run/nginx.pid</span></div><div class="line"><span class="attribute">pid</span>  /usr/local/var/run/nginx.pid;</div><div class="line"></div><div class="line"><span class="section">events</span> &#123;</div><div class="line">    <span class="comment"># 每一个worker进程能并发处理的最大连接数</span></div><div class="line">    <span class="comment"># 当作为反向代理服务器，计算公式为： `worker_processes * worker_connections / 4`</span></div><div class="line">    <span class="comment"># 当作为HTTP服务器时，公式是除以2</span></div><div class="line">    <span class="attribute">worker_connections</span>  <span class="number">2048</span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="section">http</span> &#123;</div><div class="line">    <span class="comment"># 关闭错误页面的nginx版本数字，提高安全性</span></div><div class="line">    <span class="attribute">server_tokens</span> <span class="literal">off</span>;</div><div class="line">    <span class="attribute">include</span>       mime.types;</div><div class="line">    <span class="attribute">default_type</span>  application/octet-stream;</div><div class="line"></div><div class="line">    <span class="comment"># 日志记录格式，如果关闭了access_log可以注释掉这段</span></div><div class="line">    <span class="comment">#log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '</span></div><div class="line">    <span class="comment">#                 '$status $body_bytes_sent "$http_referer" '</span></div><div class="line">    <span class="comment">#                '"$http_user_agent" "$http_x_forwarded_for"';</span></div><div class="line"></div><div class="line">    <span class="comment"># 关闭access_log可以让读取磁盘IO操作更快</span></div><div class="line">    <span class="comment"># 当然如果你在学习的过程中可以打开方便查看Nginx的访问日志</span></div><div class="line">    <span class="attribute">access_log</span> <span class="literal">off</span>;</div><div class="line"></div><div class="line">    <span class="attribute">sendfile</span>        <span class="literal">on</span>;</div><div class="line"></div><div class="line">    <span class="comment"># 在一个数据包里发送所有头文件，而不是一个接一个的发送</span></div><div class="line">    <span class="attribute">tcp_nopush</span>     <span class="literal">on</span>;</div><div class="line"></div><div class="line">    <span class="comment"># 不要缓存</span></div><div class="line">    <span class="attribute">tcp_nodelay</span> <span class="literal">on</span>;</div><div class="line"></div><div class="line">    <span class="attribute">keepalive_timeout</span>  <span class="number">65</span>;</div><div class="line"></div><div class="line">    <span class="attribute">gzip</span>  <span class="literal">on</span>;</div><div class="line">    <span class="attribute">client_max_body_size</span> <span class="number">10m</span>;</div><div class="line">    <span class="attribute">client_body_buffer_size</span> <span class="number">128k</span>;</div><div class="line"></div><div class="line">    <span class="comment"># 关于下面这段在后面紧接着来谈！</span></div><div class="line">    <span class="attribute">include</span> /usr/local/etc/nginx/sites-enabled/*;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h2 id="Nginx配置最佳实践"><a href="#Nginx配置最佳实践" class="headerlink" title="Nginx配置最佳实践"></a>Nginx配置最佳实践</h2><p>上面的<code>配置文件</code>最后一行<code>include</code>关键词会将<code>/usr/local/etc/nginx/sites-enabled/</code>文件夹下面的所有文件都加载进当前的配置文件，这样子就可以将配置文件分离，<code>nginx.conf</code>这个<code>配置文件</code>修改之后以后基本不会修改，配置不同站点的时候只需要在<code>/usr/local/etc/nginx/sites-enabled/</code>不断增加新的文件即可，这是比较好的配置方式。</p>
<p>比如我在<code>/usr/local/etc/nginx/sites-enabled/</code>下面增加了两个文件，用来配置普通的<code>HTTP</code>服务还有<code>HTTPS</code>服务：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line">touch /usr/<span class="built_in">local</span>/etc/nginx/sites-enabled/default</div><div class="line">touch /usr/<span class="built_in">local</span>/etc/nginx/sites-enabled/default-ssl</div></pre></td></tr></table></figure>
<h3 id="default配置解析"><a href="#default配置解析" class="headerlink" title="default配置解析"></a>default配置解析</h3><p><code>Nginx</code>整个配置的结构大致如下：</p>
<figure class="highlight clean"><table><tr><td class="code"><pre><div class="line">...</div><div class="line">events &#123;</div><div class="line">    ...</div><div class="line">&#125;</div><div class="line">http &#123;</div><div class="line">    ...</div><div class="line">    server &#123;</div><div class="line">        ...</div><div class="line">        location xxx &#123;</div><div class="line">            ...</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>对比上面我的<code>nginx.conf</code>文件可以知道<code>default</code>文件的内容就是配置<code>server</code>部分的，下面先弄一份最基本的配置（带有详细说明）：</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><div class="line"><span class="section">server</span> &#123;</div><div class="line">    <span class="comment"># Nginx监听端口号</span></div><div class="line">    <span class="attribute">listen</span>       <span class="number">80</span>;</div><div class="line">    <span class="comment"># 服务器的名字，默认为localhost，你也可以写成aotu.jd.com，这样子就可以通过aotu.jd.com来访问</span></div><div class="line">    <span class="attribute">server_name</span>  localhost;</div><div class="line">    <span class="comment"># 代码放置的根目录</span></div><div class="line">    <span class="attribute">root</span> /var/www/;</div><div class="line">    <span class="comment"># 编码</span></div><div class="line">    <span class="attribute">charset</span> utf-<span class="number">8</span>;    </div><div class="line"></div><div class="line">    <span class="attribute">location</span> / &#123;</div><div class="line">        <span class="comment"># index字段声明了解析的后缀名的先后顺序</span></div><div class="line">        <span class="comment"># 下面匹配到/的时候默认找后缀名为php的文件，找不到再找html，再找不到就找htm</span></div><div class="line">        <span class="attribute">index</span> index.php index.html index.htm;</div><div class="line">        <span class="comment"># 自动索引</span></div><div class="line">        <span class="attribute">autoindex</span> <span class="literal">on</span>;</div><div class="line">        <span class="comment"># 这里引入了解析PHP的东西</span></div><div class="line">        <span class="attribute">include</span> /usr/local/etc/nginx/conf.d/php-fpm;</div><div class="line">    &#125;    </div><div class="line"></div><div class="line">    <span class="comment"># 404页面跳转到404.html，相对于上面的root目录</span></div><div class="line">    <span class="attribute">error_page</span>  <span class="number">404</span>              /<span class="number">404</span>.html;</div><div class="line">    <span class="comment"># 403页面跳转到403.html，相对于上面的root目录</span></div><div class="line">    <span class="attribute">error_page</span>  <span class="number">403</span>              /<span class="number">403</span>.html;</div><div class="line">    <span class="comment"># 50x页面跳转到50x.html</span></div><div class="line">    <span class="attribute">error_page</span>   <span class="number">500</span> <span class="number">502</span> <span class="number">503</span> <span class="number">504</span>  /50x.html;</div><div class="line">    <span class="attribute">location</span> = /50x.html &#123;</div><div class="line">        <span class="attribute">root</span>   html;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>上面的配置的意思就是：访问<a href="http://localhost" target="_blank" rel="external">http://localhost</a>『80端口号可以直接省略』的时候会在<code>/var/www/</code>下面找<code>index.php</code>文件，如果没有找到就找<code>index.html</code>，如果再没有找到那就找<code>index.htm</code>，如果还是没有找到的话就<code>404</code>跳转到<code>404.html</code>，如果你刚好将<code>/var/www/</code>设置为<code>root</code>用户访问的话，那么就会直接无访问权限<code>403</code>跳转到<code>403.html</code>。</p>
<p>值得注意的是<code>server</code>字段里面的<code>root</code>字段，这个字段需要跟<code>alias</code>字段区分开来，通过下面两段配置解释一下：</p>
<figure class="highlight dts"><table><tr><td class="code"><pre><div class="line"><span class="meta"># 当用root配置的时候，root后面指定的目录是上级目录</span></div><div class="line"><span class="meta"># 并且该上级目录必须含有和location后指定的名称的同名目录，否则404</span></div><div class="line"><span class="meta"># root末尾的<span class="string">"/"</span>加不加无所谓</span></div><div class="line"><span class="meta"># 下面的配置如果访问站点http:<span class="comment">//localhost/test1访问的就是/var/www/test1目录下的站点信息</span></span></div><div class="line">location <span class="meta-keyword">/test1/</span> &#123;</div><div class="line">    root <span class="meta-keyword">/var/</span>www/;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="meta"># 如果用alias配置，其后面跟的指定目录是准确的，并且末尾必须加<span class="string">"/"</span>，否则404</span></div><div class="line"><span class="meta"># 下面的配置如果访问站点http:<span class="comment">//localhost/test2访问的就是/var/www/目录下的站点信息</span></span></div><div class="line">location <span class="meta-keyword">/test2/</span> &#123;</div><div class="line">    alias <span class="meta-keyword">/var/</span>www/;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>大家在实践过程中注意区分即可，配置之后要是碰到<code>404</code>可以先考虑是否是这个原因。</p>
<h3 id="配置反向代理"><a href="#配置反向代理" class="headerlink" title="配置反向代理"></a>配置反向代理</h3><p>对于前端工程师而言，可能最容易成为<code>全栈</code>的技能就是<code>NodeJS</code>了，当我们用<code>express</code>框架写好了一个<code>Node</code>应用之后，比如启动的时候的访问地址是：<code>http://localhost:3000/</code>，但是在部署到服务器上去之后，我们当然不希望别人这样子访问，最好的情况肯定是隐藏掉端口号。</p>
<p>例如我有一个<code>Node</code>服务的名字是<code>o2blog_wx</code>，在启动<code>Node</code>的时候访问的地址是：<code>http://localhost:3000/</code>，但是对外网我们希望是：<code>http://aotu.jd.com/o2blog_wx</code>，接下来我们将通过<code>Nginx</code>进行配置（带有详细注释）。</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><div class="line"><span class="section">server</span> &#123;</div><div class="line">    <span class="attribute">listen</span> <span class="number">80</span>;</div><div class="line">    <span class="attribute">server_name</span> aotu.jd.com;</div><div class="line">    <span class="attribute">root</span> /var/www/;</div><div class="line">    <span class="attribute">location</span> /o2blog_wx/ &#123;</div><div class="line">        <span class="comment"># 反向代理我们通过proxy_pass字段来设置</span></div><div class="line">        <span class="comment"># 也就是当访问http://aotu.jd.com/o2blog_wx的时候经过Nginx反向代理到服务器上的http://127.0.0.1:3000</span></div><div class="line">        <span class="comment"># 同时由于解析到服务器上的时候o2blog_wx这个字段都要处理</span></div><div class="line">        <span class="comment"># 所以通过rewrite字段来进行正则匹配替换</span></div><div class="line">        <span class="comment"># 也就是http://aotu.jd.com/o2blog_wx/hello经过Nginx解析到服务器变成http://127.0.0.1:3000/hello</span></div><div class="line">        <span class="attribute">proxy_pass</span> http://127.0.0.1:3000;</div><div class="line">        <span class="attribute">rewrite</span><span class="regexp"> ^/o2blog_wx/(.*)</span> /<span class="variable">$1</span> <span class="literal">break</span>;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="配置临时跳转"><a href="#配置临时跳转" class="headerlink" title="配置临时跳转"></a>配置临时跳转</h3><p>有时候我们觉得一开始配置的URL不好想换掉，但又不想原先的链接失效，比如一开始对外网的链接是：<a href="http://aotu.jd.com/o2blog_wx/，后来想改成http://aotu.jd.com/wxblog，又不想原先的失效。" target="_blank" rel="external">http://aotu.jd.com/o2blog_wx/，后来想改成http://aotu.jd.com/wxblog，又不想原先的失效。</a></p>
<p>这个时候可以在<code>Nginx</code>上配置一个<code>302</code>临时跳转，如下（<code>server</code>部分跟前面的一样）：</p>
<figure class="highlight crmsh"><table><tr><td class="code"><pre><div class="line"><span class="keyword">location</span> <span class="title">/o2blog_wx</span>/ &#123;</div><div class="line">    <span class="comment"># 当匹配到http://aotu.jd.com/o2blog_wx/的时候会跳转到http://aotu.jd.com/wxblog</span></div><div class="line">    return <span class="number">302</span> http://aotu.jd.com/wxblog</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="配置限制访问"><a href="#配置限制访问" class="headerlink" title="配置限制访问"></a>配置限制访问</h3><p>在一台服务器上的资源不全部都是对外开放的，这个时候就需要通过<code>Nginx</code>配置一个限制访问，比如查看本服务器的<code>PHP</code>信息，我们就可以通过下面配置来实现限制访问：</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><div class="line"><span class="comment"># 当匹配到/info的时候只允许10.7.101.224访问，其它的全部限制</span></div><div class="line"><span class="comment"># 同时改写为/info.php</span></div><div class="line"><span class="attribute">location</span> = /info &#123;</div><div class="line">    <span class="attribute">allow</span> <span class="number">10.7.101.224</span>;</div><div class="line">    <span class="attribute">deny</span> all;</div><div class="line">    <span class="attribute">rewrite</span> (.*) /info.php</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这个时候只有<code>IP</code>为<code>10.7.101.224</code>的机器才可以访问：<a href="http://aotu.jd.com/info，其它机器都会`403`拒绝访问！" target="_blank" rel="external">http://aotu.jd.com/info，其它机器都会`403`拒绝访问！</a></p>
<p>当然最佳的实践是将<code>IP</code>抽取出来变成白名单，这样子就可以实现部分<code>IP</code>可以访问，其它的不能访问。</p>
<h2 id="default-ssl-配置解析"><a href="#default-ssl-配置解析" class="headerlink" title="default-ssl 配置解析"></a>default-ssl 配置解析</h2><p>我们都知道<code>HTTP</code>在传输的过程中都是明文的，这直接导致了在传输的任何一个过程中都容易被窃取信息，所以才有了<code>SSL</code>（安全套接层）以及升级版<code>TLS</code>（传输层安全协议）的出现，其实就是在<code>HTTP</code>应用层给<code>TCP/IP</code>传输层的中间增加了<code>TLS/SSL</code>层，统称为<code>HTTPS</code>。</p>
<p>那如何通过<code>Nginx</code>配置<code>HTTPS</code>站点呢，下面就是<code>default-ssl</code>配置文件的内容（详细解析）：</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><div class="line"><span class="section">server</span> &#123;</div><div class="line">    <span class="comment"># 默认情况下HTTPS监听443端口</span></div><div class="line">    <span class="attribute">listen</span>  <span class="number">443</span> ssl;</div><div class="line">    <span class="attribute">server_name</span>  localhost;</div><div class="line">    <span class="attribute">root</span>  /var/www/;</div><div class="line">    <span class="comment"># 下面这些都是配置SSL需要的</span></div><div class="line">    <span class="attribute">ssl</span> <span class="literal">on</span>;</div><div class="line">    <span class="comment"># 下面两个字段需要的crt利用openssl生成，具体可以看[这里](http://nginx.org/en/docs/http/configuring_https_servers.html)</span></div><div class="line">    <span class="attribute">ssl_certificate</span> ssl/localhost.crt;</div><div class="line">    <span class="attribute">ssl_certificate_key</span> ssl/localhost.key;</div><div class="line"></div><div class="line">    <span class="attribute">ssl_session_timeout</span> <span class="number">10m</span>;</div><div class="line"></div><div class="line">    <span class="attribute">ssl_protocols</span> SSLv2 SSLv3 TLSv1;</div><div class="line">    <span class="attribute">ssl_ciphers</span> HIGH:!aNULL:!MD5;</div><div class="line">    <span class="attribute">ssl_prefer_server_ciphers</span> <span class="literal">on</span>;</div><div class="line"></div><div class="line"></div><div class="line">    <span class="attribute">location</span> = /info &#123;</div><div class="line">        <span class="attribute">allow</span> <span class="number">127.0.0.1</span>;</div><div class="line">        <span class="attribute">deny</span> all;</div><div class="line">        <span class="attribute">rewrite</span> (.*) /info.php;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="attribute">location</span> /phpmyadmin/ &#123;</div><div class="line">        <span class="attribute">root</span> /usr/local/share/phpmyadmin;</div><div class="line">        <span class="attribute">index</span> index.php index.html index.htm;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="attribute">location</span> / &#123;</div><div class="line">        <span class="attribute">include</span> /usr/local/etc/nginx/conf.d/php-fpm;</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="attribute">error_page</span> <span class="number">403</span> /<span class="number">403</span>.html;</div><div class="line">    <span class="attribute">error_page</span> <span class="number">404</span> /<span class="number">404</span>.html;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>上面配置之后，就可以通过<a href="https://localhost" target="_blank" rel="external">https://localhost</a>访问我们的<code>Nginx</code>首页了。</p>
<p>当然若要在对外网使用，必须购买第三方信任证书才行，有兴趣的童鞋可以谷歌了解，这里不细谈。</p>
<h2 id="小结"><a href="#小结" class="headerlink" title="小结"></a>小结</h2><p>写到这里，最基本的<code>Nginx</code>配置就基本介绍完了，若按照我上面的配置一步步跟着改，基本上都可以跑起来<code>Nginx</code>服务了吧，若想更加深入学习<code>Nginx</code>的配置，强烈建议看<a href="http://nginx.org/en/docs/" target="_blank" rel="external">官方文档</a>，写得很清晰明了，还是那句老话：<code>授之以鱼不如授之以渔</code>。</p>
<h2 id="反向代理"><a href="#反向代理" class="headerlink" title="反向代理"></a>反向代理</h2><p>提到<code>反向代理</code>，必然先提到<code>正向代理</code>，正向代理(forward)是一个位于客户端【用户A】和原始服务器(origin server)【服务器B】之间的服务器【代理服务器Z】，为了从原始服务器取得内容，用户A向代理服务器Z发送一个请求并指定目标(服务器B),然后代理服务器Z向服务器B转交请求并将获得的内容返回给客户端。客户端必须要进行一些特别的设置才能使用正向代理。如下图（图来自网络，如有侵权请联系我删除~）：</p>
<p><img src="/img/nginx/forwards_proxy.jpg" alt="正向代理示意图"></p>
<p>从上图可以看出，所谓的<code>正向代理</code>就是<code>代理服务器替代访问方【用户A】去访问目标服务器【服务器B】</code>，在现实中的例子就是『翻墙』！但如果代理服务器Z被完全控制（或不完全控制），就变成了『肉鸡』了。</p>
<p>而<code>反向代理</code>与正向代理相反，对客户端而言代理服务器就像是原始服务器，并且客户端不需要进行任何特别的设置。客户端向反向代理的命名空间（name-space）中的内容发送普通请求，接着反向代理将判断向何处（原始服务器）转交请求，并将获得的内容返回给客户端。</p>
<p>使用反向代理服务器主要核心作用如下：</p>
<ol>
<li>保护和隐藏原始资源服务器</li>
</ol>
<p><img src="/img/nginx/backward_proxy_1.jpg" alt="反向代理原理图"></p>
<p>从上图可以看出，用户A始终认为它访问的是代理服务器Z而不是原始服务器B，但实际上反向代理服务器接受用户A的应答，从原始资源服务器B中取得用户A的需求资源，然后发送给用户A。由于防火墙的作用，只允许代理服务器Z访问原始资源服务器B。尽管在这个虚拟的环境下，防火墙和反向代理的共同作用保护了原始资源服务器B，但用户A并不知情。</p>
<ol>
<li>负载均衡</li>
</ol>
<p><img src="/img/nginx/backward_balance.jpg" alt="反向代理负载均衡示例图"></p>
<p>当反向代理服务器不止一个的时候，我们甚至可以把它们做成集群，当更多的用户访问资源服务器B的时候，让不同的代理服务器Z（x）去应答不同的用户，然后发送不同用户需要的资源。</p>
<p>当然<code>反向代理服务器</code>像<code>正向代理服务器</code>一样拥有CACHE的作用，它可以缓存原始资源服务器B的资源，而不是每次都要向原始资源服务器B请求数据，特别是一些静态的数据，比如图片和文件，如果这些反向代理服务器能够做到和用户X来自同一个网络，那么用户X访问反向代理服务器X，就会得到很高质量的速度。这正是<code>CDN技术</code>的核心。如下图：</p>
<p><img src="/img/nginx/cdn.jpg" alt="CDN原理图"></p>
]]></content>
    
    <summary type="html">
    
      本文手把手带领前端工程师走进Nginx配置
    
    </summary>
    
      <category term="教程" scheme="https://cnt1992.github.io/categories/%E6%95%99%E7%A8%8B/"/>
    
    
      <category term="Nginx" scheme="https://cnt1992.github.io/tags/Nginx/"/>
    
  </entry>
  
  <entry>
    <title>从程序猿角度来看2016年为啥那么6</title>
    <link href="https://cnt1992.github.io/2016/01/04/happy-new-year-to-2016/"/>
    <id>https://cnt1992.github.io/2016/01/04/happy-new-year-to-2016/</id>
    <published>2016-01-04T10:01:36.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<p>最近网上流传一张图来说明<code>2016年很6</code>，大概就是下面的意思：</p>
<figure class="highlight lsl"><table><tr><td class="code"><pre><div class="line"><span class="number">2016</span>=<span class="number">666</span>+<span class="number">666</span>+<span class="number">666</span>+<span class="number">6</span>+<span class="number">6</span>+<span class="number">6</span></div><div class="line"><span class="number">2016</span>=<span class="number">777</span>+<span class="number">777</span>+<span class="number">77</span>+<span class="number">77</span>+<span class="number">77</span>+<span class="number">77</span>+<span class="number">77</span>+<span class="number">77</span></div><div class="line"><span class="number">2016</span>=<span class="number">888</span>+<span class="number">888</span>+<span class="number">88</span>+<span class="number">88</span>+<span class="number">8</span>+<span class="number">8</span>+<span class="number">8</span>+<span class="number">8</span>+<span class="number">8</span>+<span class="number">8</span>+<span class="number">8</span>+<span class="number">8</span></div><div class="line"><span class="number">2016</span>=<span class="number">999</span>+<span class="number">999</span>+<span class="number">9</span>+<span class="number">9</span></div></pre></td></tr></table></figure>
<p>看起来确实很6吧，不过作为<code>程序猿</code>的我看到上面的数字第一想法就是：咦，这个的算法是咋实现的？</p>
<p>于是，带着上面的疑问，简单去实现了一个算法来计算出上面这样的结果。</p>
<h2 id="先瞎掰一下"><a href="#先瞎掰一下" class="headerlink" title="先瞎掰一下"></a>先瞎掰一下</h2><p>咳咳，在开始算法之前，请允许我先扯一下 <code>6</code> 这个词，网络用语意思是 <code>溜溜溜</code>，形容很牛逼；<br>但是，在伟大的数学当中，<code>6</code> 可是一个 <code>完美数</code>，不知道啥是完美数？移步 <a href="https://zh.wikipedia.org/wiki/%E5%AE%8C%E5%85%A8%E6%95%B0" target="_blank" rel="external">维基百科</a>。作为第一个完美数，<code>6</code> 当然拥有至高无上的地位！纯属个人瞎掰，下面正式开始。</p>
<h2 id="抽象化"><a href="#抽象化" class="headerlink" title="抽象化"></a>抽象化</h2><p>算法最有趣的地方应该就是将现实复杂的事物简单抽象化，以此例子抽象化之后的意思大概就是：</p>
<blockquote>
<p>给定一个数字（假设是2016），求将其分解成 <strong>N</strong> 个 <code>各位数都相等</code> 的数的和（假设这里的位数分别为6、7、8、9）</p>
</blockquote>
<h2 id="步骤分解"><a href="#步骤分解" class="headerlink" title="步骤分解"></a>步骤分解</h2><p>知道了算法的意思之后，就可以分解步骤了，大概如下：</p>
<p>1.将位数分别为6、7、8、9存进一个数组，用来遍历求出不同结果；<br>2.求出<code>各位数都相等</code>的数，这里基于第一步的基数；<br>3.将2016进行分解；<br>4.将结果串联起来</p>
<h2 id="具体实现"><a href="#具体实现" class="headerlink" title="具体实现"></a>具体实现</h2><p>我也不知道该怎么扯了，非常扯淡的算法如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">spiltYear</span>(<span class="params"> year </span>)</span>&#123;</div><div class="line">    <span class="comment">// 将year显式转为数字，避免传入字符串报错</span></div><div class="line">    year = <span class="built_in">parseInt</span>( year, <span class="number">10</span> );    </div><div class="line">    <span class="keyword">if</span>( year &lt; <span class="number">1000</span> )&#123;</div><div class="line">        <span class="keyword">return</span>;</div><div class="line">    &#125;                </div><div class="line">    <span class="keyword">var</span> baseArr = [<span class="number">6</span>,<span class="number">7</span>,<span class="number">8</span>,<span class="number">9</span>];</div><div class="line">    baseArr.forEach(<span class="function"><span class="keyword">function</span>(<span class="params"> base, i </span>)</span>&#123;</div><div class="line">        <span class="keyword">var</span> result = [];</div><div class="line">        <span class="keyword">var</span> val1 = base + <span class="number">10</span> * base,                <span class="comment">// 十位数</span></div><div class="line">            val2 = base + <span class="number">10</span> * base + <span class="number">100</span> * base;   <span class="comment">// 百位数</span></div><div class="line">        <span class="keyword">var</span> num2 = <span class="built_in">Math</span>.floor( year / val2 ),</div><div class="line">            num1 = <span class="built_in">Math</span>.floor( ( year - num2 * val2 ) / val1 ),</div><div class="line">            numBase = ( year - num2 * val2 - num1 * val1 ) / base,</div><div class="line">            flag = numBase == <span class="built_in">Math</span>.floor( numBase ) ? <span class="literal">true</span> : <span class="literal">false</span>;</div><div class="line">        <span class="keyword">if</span>( !flag )&#123;</div><div class="line">            <span class="built_in">console</span>.error(<span class="string">'当前年份不符合条件'</span>);</div><div class="line">            <span class="keyword">return</span> ;</div><div class="line">        &#125;</div><div class="line">        <span class="keyword">while</span>( num2 )&#123;</div><div class="line">            result.push(val2);</div><div class="line">            num2--;</div><div class="line">        &#125;</div><div class="line">        <span class="keyword">while</span>( num1 )&#123;</div><div class="line">            result.push(val1);</div><div class="line">            num1--;</div><div class="line">        &#125;</div><div class="line">        <span class="keyword">while</span>( numBase )&#123;</div><div class="line">            result.push(base);</div><div class="line">            numBase--;</div><div class="line">        &#125;</div><div class="line">        <span class="built_in">console</span>.log(<span class="string">'2016=%s'</span>,result.join(<span class="string">'+'</span>));</div><div class="line">    &#125;);</div><div class="line">&#125;</div><div class="line">spiltYear(<span class="number">2016</span>);</div></pre></td></tr></table></figure>
<h2 id="写在后面"><a href="#写在后面" class="headerlink" title="写在后面"></a>写在后面</h2><p>2016年本想写一篇长长的总结2015年，然而下笔却发现写不了啥东西，2016年希望所有人都666！</p>
]]></content>
    
    <summary type="html">
    
      2016年是一个非常6的一年，为啥这样子说？请往下看。
    
    </summary>
    
      <category term="技术研究" scheme="https://cnt1992.github.io/categories/%E6%8A%80%E6%9C%AF%E7%A0%94%E7%A9%B6/"/>
    
    
      <category term="算法" scheme="https://cnt1992.github.io/tags/%E7%AE%97%E6%B3%95/"/>
    
  </entry>
  
  <entry>
    <title>CSS后置处理器之PostCss</title>
    <link href="https://cnt1992.github.io/2015/12/17/CSS%E5%90%8E%E7%BD%AE%E5%A4%84%E7%90%86%E5%99%A8%E4%B9%8BPostCss/"/>
    <id>https://cnt1992.github.io/2015/12/17/CSS后置处理器之PostCss/</id>
    <published>2015-12-16T19:44:29.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<h2 id="初识PostCSS"><a href="#初识PostCSS" class="headerlink" title="初识PostCSS"></a>初识PostCSS</h2><p>如果你第一次听说<code>PostCSS</code>这个东西，那么请看下面摘自官方<code>Github</code>的介绍：</p>
<blockquote>
<p>PostCSS is a tool for transforming CSS with JS Plugins. These plugins can support variables and mixins, transpile future CSS syntax, inline images, and more</p>
</blockquote>
<p>翻译成中文的意思如下：</p>
<blockquote>
<p>PostCSS 是一套利用JS插件实现的用来改变CSS的工具.这些插件能够支持变量和混合语法，转换未来CSS语法，内联图片，还有更多</p>
</blockquote>
<p>我们用过<code>Less</code>、<code>SASS</code>等工具来对CSS做<code>预处理</code>操作，按照它们约定的语法来书写并且最终转换成可用的样式，这付出的代价是<strong>必须先熟悉这个工具的书写语法</strong>。 </p>
<p>随着近几年 <a href="http://gruntjs.cn/" target="_blank" rel="external">Grunt</a>、<a href="http://gulpjs.com/" target="_blank" rel="external">Gulp</a>、<a href="http://webpack.github.io/docs/" target="_blank" rel="external">Webpack</a> 等自动化工具的兴起，<code>组合式应用</code>变得非常的热门，那<code>PostCSS</code>存在的意义是什么呢？答案是：<strong>CSS生态系统</strong></p>
<p><code>PostCSS</code>拥有非常多的插件，诸如自动为CSS添加浏览器前缀的插件<code>autoprefixer</code>、当前移动端最常用的<code>px</code>转<code>rem</code>插件<code>px2rem</code>，还有支持尚未成为CSS标准但特定可用的插件<code>cssnext</code>，还有很多很多。就连著名的<code>Bootstrap</code>在下一个版本<code>Bootstrap 5</code>也将使用<code>PostCSS</code>作为样式的基础。</p>
<p>一句话来概括PostCSS：<strong>CSS编译器能够做到的事情，它也可以做到，而且能够做得更好</strong></p>
<a id="more"></a>
<h2 id="快速使用PostCSS"><a href="#快速使用PostCSS" class="headerlink" title="快速使用PostCSS"></a>快速使用PostCSS</h2><p>上面大致介绍了<code>PostCSS</code>，也许我们并没有在头脑里形成对它的认知，那下面我们就通过一个简单地实例来看看如何使用<code>PostCSS</code>。</p>
<p><code>PostCSS</code>得益于插件，支持Grunt,Gulp,webpack,Broccoli,Brunch还有ENB，这里我们将以<code>Gulp</code>作为实例来讲。</p>
<h3 id="环境准备"><a href="#环境准备" class="headerlink" title="环境准备"></a>环境准备</h3><p>创建并进入我们的实例目录</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line">mkdir postcss-demo &amp;&amp; <span class="built_in">cd</span> postcss-demo</div></pre></td></tr></table></figure>
<p>然后快速生成<code>package.json</code>文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line"><span class="comment"># --yes 参数能够帮助我们快速生成默认的package.json</span></div><div class="line">npm init --yes</div></pre></td></tr></table></figure>
<p>将上面创建的<code>package.json</code>文件的<code>main</code>参数改为<code>gulpfile.js</code>，然后安装我们所需的依赖</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line"><span class="comment"># gulp跟gulp-postcss是必须的，后面两个插件为了演示用途</span></div><div class="line">npm i gulp gulp-postcss autoprefixer autoprefixer-core cssnext --save-dev <span class="_">-d</span></div></pre></td></tr></table></figure>
<p>创建<code>gulpfile.js</code></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line"><span class="comment"># 这里用命令行进行创建，你也可以手动新建</span></div><div class="line">touch gulpfile.js</div></pre></td></tr></table></figure>
<h3 id="修改gulpfile-js"><a href="#修改gulpfile-js" class="headerlink" title="修改gulpfile.js"></a>修改gulpfile.js</h3><p>将下面代码贴进<code>gulpfile.js</code></p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> gulp = <span class="built_in">require</span>(<span class="string">'gulp'</span>);</div><div class="line"><span class="keyword">var</span> postcss = <span class="built_in">require</span>(<span class="string">'gulp-postcss'</span>);</div><div class="line"><span class="keyword">var</span> autoprefixer = <span class="built_in">require</span>(<span class="string">'autoprefixer'</span>);</div><div class="line"><span class="keyword">var</span> cssnext = <span class="built_in">require</span>(<span class="string">'cssnext'</span>);</div><div class="line"></div><div class="line"><span class="comment">//定义css任务</span></div><div class="line">gulp.task(<span class="string">'css'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">  <span class="comment">//定义postcss任务流数组</span></div><div class="line">  <span class="keyword">var</span> processors = [</div><div class="line">    autoprefixer(&#123;</div><div class="line">      <span class="attr">browsers</span>:[<span class="string">'last 3 version'</span>],</div><div class="line">      <span class="attr">cascade</span>: <span class="literal">false</span>,</div><div class="line">      <span class="attr">remove</span>: <span class="literal">false</span></div><div class="line">    &#125;),</div><div class="line">    cssnext()</div><div class="line">  ];</div><div class="line">  <span class="keyword">return</span> gulp.src(<span class="string">'./src/css/*.css'</span>)</div><div class="line">             .pipe(postcss(processors))</div><div class="line">             .pipe(gulp.dest(<span class="string">'./dist'</span>));</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<h3 id="创建示例样式"><a href="#创建示例样式" class="headerlink" title="创建示例样式"></a>创建示例样式</h3><p>在项目根目录下创建src目录，再在src目录下面创建css目录，然后创建style.css文件</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line"><span class="comment"># 这里用命令创建，你也可以手动创建</span></div><div class="line">mkdir -p src/css &amp;&amp; touch style.css</div></pre></td></tr></table></figure>
<p>编辑样式如下：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-tag">h1</span>&#123;</div><div class="line">  <span class="attribute">display</span>:flex;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="selector-pseudo">:root</span> &#123;</div><div class="line"><span class="attribute">--fontSize</span>: <span class="number">1rem</span>;</div><div class="line"><span class="attribute">--mainColor</span>: <span class="number">#12345678</span>;</div><div class="line"><span class="attribute">--highlightColor</span>: <span class="built_in">hwb</span>(190, 35%, 20%);</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="selector-tag">body</span> &#123;</div><div class="line"><span class="attribute">color</span>: <span class="built_in">var</span>(--mainColor);</div><div class="line"></div><div class="line"><span class="attribute">font-size</span>: <span class="built_in">var</span>(--fontSize);</div><div class="line"><span class="attribute">line-height</span>: <span class="built_in">calc</span>(var(--fontSize) * <span class="number">1.5</span>);</div><div class="line"><span class="attribute">padding</span>: <span class="built_in">calc</span>((var(--fontSize) / <span class="number">2</span>) + <span class="number">1px</span>);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="运行实例"><a href="#运行实例" class="headerlink" title="运行实例"></a>运行实例</h3><p>一切准备就绪之后可以在项目根目录下执行刚才我们定义的任务</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line">gulp css</div></pre></td></tr></table></figure>
<p>如果不出什么意外的话就会在根目录下面生成一个<code>dist</code>文件夹，里面有一个样式文件，内容如下：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-tag">body</span>&#123;</div><div class="line">  <span class="attribute">display</span>:-webkit-flex;</div><div class="line">  <span class="attribute">display</span>:-ms-flexbox;</div><div class="line">  <span class="attribute">display</span>:-webkit-box;</div><div class="line">  <span class="attribute">display</span>:flex;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="selector-tag">body</span> &#123;</div><div class="line"><span class="attribute">color</span>:<span class="number">#123456</span>;</div><div class="line"><span class="attribute">color</span>:<span class="built_in">rgba</span>(18, 52, 86, 0.47059);</div><div class="line"></div><div class="line"><span class="attribute">font-size</span>:<span class="number">16px</span>;</div><div class="line"><span class="attribute">font-size</span>:<span class="number">1rem</span>;</div><div class="line"><span class="attribute">line-height</span>:<span class="number">24px</span>;</div><div class="line"><span class="attribute">line-height</span>:<span class="number">1.5rem</span>;</div><div class="line"><span class="attribute">padding</span>:<span class="built_in">calc</span>(0.5rem + 1px);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>我们可以看到我们写的样式自动添加了浏览器前缀，并且一些未来CSS语法也被转换了。</p>
<h2 id="了解PostCSS"><a href="#了解PostCSS" class="headerlink" title="了解PostCSS"></a>了解PostCSS</h2><p>通过上面的实例我们应该知道<code>PostCSS</code>的使用方法，此时让我们先回想一下<code>CSS预处理器</code>的使用历程：</p>
<ol>
<li>学习该CSS预处理器的语法特性，诸如：变量定义、嵌套、继承</li>
<li>在特定后缀名(.less/.scss等)的文件按照上面的语法进行编写</li>
<li>通过Gulp/Grunt/Webpack等自动化工具或者手动编译成CSS样式</li>
</ol>
<p>而<code>PostCSS</code>的使用历程：</p>
<ol>
<li>直接按照CSS标准语法来书写CSS样式文件</li>
<li>通过Gulp/Grunt/Webpack等自动化工具加载PostCSS插件转换输出</li>
</ol>
<p>通过对比我们类比一个结论：<em>CSS预处理器好比给你一个工具箱，工具箱里面有什么东西该怎么拿已经跟你约定好，你必须按照这个规则来拿；而PostCSS好比给你一个盒子，你可以从旁边选择自己需要的工具放进盒子打包拿走，如果还不够用你可以自己造一个工具</em></p>
<h2 id="深入PostCSS"><a href="#深入PostCSS" class="headerlink" title="深入PostCSS"></a>深入PostCSS</h2><p><code>PostCSS</code>自身只包括了<code>CSS分析器</code>，<code>CSS节点树API</code>，<code>source map生成器</code>，<code>CSS节点拼接器</code>，而基于<code>PostCSS</code>的插件都是使用了<code>CSS节点树API</code>来实现的。</p>
<p>我们都知道CSS的组成如下：</p>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">element &#123;</div><div class="line">  prop1 : rule1 rule2 ...;</div><div class="line">  prop2 : rule1 rule2 ...;</div><div class="line">  prop2 : rule1 rule2 ...;</div><div class="line">  ...</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>也就是一条一条的样式规则组成，每一条样式规则包含一个或多个属性跟值。所以<code>PostCSS</code>的执行过程大致如下：</p>
<ol>
<li><p>Parser<br> 利用<code>CSS分析器</code>读取CSS字符内容，得到一个完整的<code>节点树</code></p>
</li>
<li><p>Plugin<br> 对上面拿到的<code>节点树</code>利用<code>CSS节点树API</code>进行一系列的转换操作</p>
</li>
<li><p>Plugin<br> 利用<code>CSS节点拼接器</code>将上面转换之后的节点树重新组成CSS字符</p>
</li>
<li><p>Stringifier<br> 在上面转换期间可利用<code>source map生成器</code>表明转换前后字符的对应关系</p>
</li>
</ol>
<h3 id="PostCSS性能"><a href="#PostCSS性能" class="headerlink" title="PostCSS性能"></a>PostCSS性能</h3><p>在PostCSS官方推特上看到，由JavaScript编写的PostCSS比C++编写的libsass还要快3倍.</p>
<p>如果你对上面的性能截图有疑问，可以亲自来<a href="https://github.com/postcss/benchmark" target="_blank" rel="external">这里</a>测试看看。</p>
<h3 id="开始编写自己的PostCSS插件"><a href="#开始编写自己的PostCSS插件" class="headerlink" title="开始编写自己的PostCSS插件"></a>开始编写自己的PostCSS插件</h3><p><code>PostCSS</code>在自己的<a href="https://github.com/postcss/postcss#plugins" target="_blank" rel="external">Github</a>上放了一些常用的插件，更多的插件可以在<a href="http://postcss.parts/" target="_blank" rel="external">postcss.parts</a>进行搜索。</p>
<p>但有时候已有的插件不满足我们的需求，这个时候需要编写自己的PostCSS插件，下面我们将一步步创建一个简单的插件，这个插件功能非常简单，如下：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="comment">/* </span></div><div class="line">  文件位置：src/css/style.css</div><div class="line"> */</div><div class="line"><span class="selector-tag">h1</span> &#123;</div><div class="line">  <span class="attribute">font-family</span>: <span class="string">"\5FAE\8F6F\96C5\9ED1"</span>,<span class="built_in">fontstack</span>(<span class="string">'Arial'</span>);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>当输入上面的样式时，会生成下面的样式：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="comment">/*</span></div><div class="line">  文件位置：dist/style.css</div><div class="line"> */</div><div class="line"><span class="selector-tag">h1</span> &#123;</div><div class="line">  <span class="attribute">font-family</span>: <span class="string">"\5FAE\8F6F\96C5\9ED1"</span>,tahoma,arial;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="环境准备-1"><a href="#环境准备-1" class="headerlink" title="环境准备"></a>环境准备</h3><p>我们将以<code>Gulp</code>作为基础来实现我们的插件，首先创建项目文件夹</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line">mkdir postcss-plugin &amp;&amp; <span class="built_in">cd</span> postcss-plugin</div></pre></td></tr></table></figure>
<p>然后快速创建<code>package.json</code>文件：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line">npm init --yes</div></pre></td></tr></table></figure>
<p>紧接着先安装必备的包</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line">npm i gulp postcss gulp-postcss --save-dev <span class="_">-d</span></div></pre></td></tr></table></figure>
<p>再创建<code>gulpfile.js</code>并且输入下面内容:</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> gulp = <span class="built_in">require</span>(<span class="string">'gulp'</span>);</div><div class="line"><span class="keyword">var</span> postcss = <span class="built_in">require</span>(<span class="string">'gulp-postcss'</span>);</div><div class="line"></div><div class="line">gulp.task(<span class="string">'css'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">  <span class="keyword">var</span> processors = [</div><div class="line">  ];</div><div class="line">  <span class="keyword">return</span> gulp.src(<span class="string">'./src/css/*.css'</span>)</div><div class="line">             .pipe(postcss(processors))</div><div class="line">             .pipe(gulp.dest(<span class="string">'./dist'</span>));</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<h3 id="创建插件文件夹"><a href="#创建插件文件夹" class="headerlink" title="创建插件文件夹"></a>创建插件文件夹</h3><p>我们在执行<code>npm install</code>安装的包都放置在<code>node_modules</code>文件夹下面，这里我们创建PostCSS的插件文件夹，注意：<strong>PostCSS的插件命名格式为：postcss-插件名字</strong></p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line"><span class="comment"># 这里采用命令新建文件夹，你也可以手动创建</span></div><div class="line">mkdir node_modules/postcss-fontstack</div></pre></td></tr></table></figure>
<h3 id="创建插件入口文件"><a href="#创建插件入口文件" class="headerlink" title="创建插件入口文件"></a>创建插件入口文件</h3><p>现在我们可以在<code>postcss-fontstack</code>文件夹创建入口文件<code>index.js</code>，<code>PostCSS</code>创建插件的方式如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> postcss = <span class="built_in">require</span>(<span class="string">'postcss'</span>);</div><div class="line"><span class="built_in">module</span>.exports = postcss.plugin(<span class="string">'插件名字'</span>, <span class="function"><span class="keyword">function</span> 插件名字(<span class="params">选项</span>)</span>&#123;</div><div class="line">  <span class="comment">//这里写插件代码</span></div><div class="line">&#125;)</div></pre></td></tr></table></figure>
<p>那我们可以在<code>index.js</code>里面贴入下面代码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> postcss = <span class="built_in">require</span>(<span class="string">'postcss'</span>);</div><div class="line"></div><div class="line">modules.exports = postcss.plugin(<span class="string">'fontstack'</span>, <span class="function"><span class="keyword">function</span> <span class="title">fontstack</span>(<span class="params"> options </span>)</span>&#123;</div><div class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params"> css </span>)</span>&#123;</div><div class="line">    options = options || &#123;&#125;;</div><div class="line"></div><div class="line">    <span class="keyword">var</span> fontstack_config = &#123;</div><div class="line">      <span class="string">'Arial'</span>: <span class="string">'tahoma,arial'</span>,</div><div class="line">      <span class="string">'Times New Roman'</span>: <span class="string">'TimesNewRoman, "Times New Roman", Times, Baskerville, Georgia, serif'</span></div><div class="line">    &#125;;</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">function</span> <span class="title">toTitleCase</span>(<span class="params"> str </span>)</span>&#123;</div><div class="line">      <span class="keyword">return</span> str.replace(<span class="regexp">/\w\S*/g</span>,<span class="function"><span class="keyword">function</span>(<span class="params"> txt </span>)</span>&#123;</div><div class="line">          <span class="keyword">return</span> txt.charAt(<span class="number">0</span>).toUpperCase() + txt.substr(<span class="number">1</span>).toLowerCase();</div><div class="line">      &#125;);</div><div class="line">    &#125;</div><div class="line"></div><div class="line">    <span class="comment">// css.walkRules方法用来遍历每一条css规则</span></div><div class="line">    css.walkRules( <span class="function"><span class="keyword">function</span>(<span class="params"> rule </span>)</span>&#123;</div><div class="line">        <span class="comment">// walkDecls方法用来解析属性跟值</span></div><div class="line">        rule.walkDecls( <span class="function"><span class="keyword">function</span>(<span class="params"> decl, i </span>)</span>&#123;</div><div class="line">            <span class="keyword">var</span> value = decl.value;</div><div class="line">            <span class="keyword">if</span>( value.indexOf( <span class="string">'fontstack('</span> ) !== <span class="number">-1</span> )&#123;</div><div class="line">                <span class="keyword">var</span> fontstack_requested = value.match(<span class="regexp">/\(([^)]+)\)/</span>)[<span class="number">1</span>].replace(<span class="regexp">/["']/g</span>,<span class="string">""</span>);</div><div class="line">                fontstack_requested = toTitleCase( fontstack_requested );</div><div class="line"></div><div class="line">                <span class="keyword">var</span> fontstack = fontstack_config[ fontstack_requested ];</div><div class="line"></div><div class="line">                <span class="keyword">var</span> firstFont = value.substr( <span class="number">0</span>, value.indexOf(<span class="string">'fontstack('</span>) );</div><div class="line"></div><div class="line">                <span class="keyword">var</span> newValue = firstFont + fontstack;</div><div class="line"></div><div class="line">                decl.value = newValue;</div><div class="line">            &#125;</div><div class="line">        &#125;);</div><div class="line"></div><div class="line">    &#125;);</div><div class="line">  &#125;</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<p>在<code>gulpfile.js</code>引入上面的插件，代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> gulp = <span class="built_in">require</span>(<span class="string">'gulp'</span>);</div><div class="line"><span class="keyword">var</span> postcss = <span class="built_in">require</span>(<span class="string">'gulp-postcss'</span>);</div><div class="line"></div><div class="line">gulp.task(<span class="string">'css'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">  <span class="keyword">var</span> processors = [</div><div class="line">  ];</div><div class="line">  <span class="keyword">return</span> gulp.src(<span class="string">'./src/css/*.css'</span>)</div><div class="line">             .pipe(postcss(processors))</div><div class="line">             .pipe(gulp.dest(<span class="string">'./dist'</span>));</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<h3 id="运行实例-1"><a href="#运行实例-1" class="headerlink" title="运行实例"></a>运行实例</h3><p>在项目根目录下运行实例，最终实现我们的效果</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line">gulp css</div></pre></td></tr></table></figure>
<h2 id="再谈PostCSS"><a href="#再谈PostCSS" class="headerlink" title="再谈PostCSS"></a>再谈PostCSS</h2><p>基于<code>PostCSS</code>能够做到很多<code>CSS预处理器</code>做不到的事情，未来发展前景还是挺不错的，而且最新的<code>Atom</code>编辑器也可以下载插件来支持<code>PostCSS</code>这种语法。</p>
<p>但这就意味着<code>CSS预处理器</code>过时了么？不会。<code>PostCSS</code>的出现并不是为了替换掉之前的技术，只是提供多一种思路让我们去考虑，就比如Sass编译后再加<code>autoprefixer</code>自动补齐浏览器前缀这种做法当前还是比较流行的。</p>
<p>再回到文章最开始说的，<code>PostCSS</code>其实是在打造一个改变CSS开发方式的生态系统。也许暂时我们还是保持传统的开发方式，但未来对于<code>PostCSS</code>我还是保持关注，它是值得期待的。</p>
]]></content>
    
    <summary type="html">
    
      PostCSS 是一套利用JS插件实现的用来改变CSS的工具.这些插件能够支持变量和混合语法，转换未来CSS语法，内联图片，还有更多
    
    </summary>
    
      <category term="个人学习" scheme="https://cnt1992.github.io/categories/%E4%B8%AA%E4%BA%BA%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="PostCss 后置处理器" scheme="https://cnt1992.github.io/tags/PostCss-%E5%90%8E%E7%BD%AE%E5%A4%84%E7%90%86%E5%99%A8/"/>
    
  </entry>
  
  <entry>
    <title>介绍Sass Maps：用法跟例子</title>
    <link href="https://cnt1992.github.io/2015/12/09/an-introduction-to-sass-maps/"/>
    <id>https://cnt1992.github.io/2015/12/09/an-introduction-to-sass-maps/</id>
    <published>2015-12-09T15:03:16.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>本文翻译自<a href="http://webdesign.tutsplus.com/tutorials/an-introduction-to-sass-maps-usage-and-examples--cms-22184" target="_blank" rel="external">An Introduction to Sass Maps:Usage and Examples</a></p>
</blockquote>
<a id="more"></a>
<p>对于前端可伸缩页面的编写来说Sass Maps可以说是一个福音，从逻辑模块中抽取出配置是一种非常好的方法。现在就让我跟你解释为什么我认为在Sass 3.3中Sass Maps是最好的特性。</p>
<h2 id="Sass-3-3"><a href="#Sass-3-3" class="headerlink" title="Sass 3.3"></a>Sass 3.3</h2><p>Sass 3.3[<em>注1</em>]即将更新给所有人使用，但是对于很多开发者来说还有很多实用性的特性他们依然不熟悉。新版本的Sass 3.3带给我们新的数据类型称为<code>map</code>。<code>Maps</code>是<code>key/value</code>键值对的集合，能够帮助我们通过简单的代码创建一块配置区域。</p>
<h2 id="如何使用-Sass-Maps"><a href="#如何使用-Sass-Maps" class="headerlink" title="如何使用 Sass Maps"></a>如何使用 Sass Maps</h2><p>首先我们会覆盖Sass Maps的基本用法，后面会看几个实例。</p>
<h3 id="创建一个Map"><a href="#创建一个Map" class="headerlink" title="创建一个Map"></a>创建一个Map</h3><p>下面是一个快速创建Sass Maps的语句，创建一个变量（这里用了<code>$map</code>）然后输入一些<code>keys</code>跟<code>values</code>，通过<code>,</code>来分割，这些键值对通过括号包围起来：</p>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">$map: (</div><div class="line">  key: value,</div><div class="line">  nextkey: nextvalue</div><div class="line">);</div></pre></td></tr></table></figure>
<h3 id="如何获取Map的值"><a href="#如何获取Map的值" class="headerlink" title="如何获取Map的值"></a>如何获取Map的值</h3><p>当你定义了很多对<code>key/value</code>键值对之后，有时候你需要取出一些值。你可以通过<code>map-get()</code>方法来找出某个<code>key</code>的<code>value</code>。该方法需要传入两个参数：<code>map</code>的名称还有<code>key</code>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">.element:before &#123;</div><div class="line">  <span class="attr">content</span>: map-get($map, key);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>上面编译输出后的结果如下：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-class">.element</span><span class="selector-pseudo">:before</span> &#123;</div><div class="line">  <span class="attribute">content</span>: value</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="如何检查某一个key是否存在"><a href="#如何检查某一个key是否存在" class="headerlink" title="如何检查某一个key是否存在"></a>如何检查某一个key是否存在</h3><p>在用<code>Sass</code>进行编码的时候强烈建议使用<a href="https://webdesign.tutsplus.com/tutorials/an-introduction-to-error-handling-in-sass--cms-19996" target="_blank" rel="external">可靠的错误处理</a>。在这里<code>Sass</code>给了我们一个方法<code>map-has-key()</code>。这个方法能够检测出某个<code>key</code>是否存在，如果不存在能够输出其他信息给开发者。</p>
<p>可以移步 <code>Hugo Giraudel</code> 写的这篇如何处理错误的文章 <a href="http://webdesign.tutsplus.com/tutorials/an-introduction-to-error-handling-in-sass--cms-19996" target="_blank" rel="external">An Introduction to Error Handing in Sass</a>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$map: (</div><div class="line">  key: value,</div><div class="line">  <span class="attr">nextkey</span>: nextvalue</div><div class="line">);</div><div class="line"></div><div class="line">.element &#123;</div><div class="line">  @<span class="keyword">if</span> map-has-key($map, key)&#123;</div><div class="line">    <span class="attr">content</span>: <span class="string">'Map has this key.'</span>;</div><div class="line">  &#125; @<span class="keyword">else</span> &#123;</div><div class="line">    <span class="attr">content</span>: <span class="string">'Map has not this key.'</span></div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>编译之后结果如下：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-class">.element</span> &#123;</div><div class="line">  <span class="attribute">content</span>: <span class="string">'Map has this key.'</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="如何合并Maps"><a href="#如何合并Maps" class="headerlink" title="如何合并Maps"></a>如何合并Maps</h3><p>这个一个福利：<code>Sass</code>允许我们合并两个甚至更多个<code>maps</code>成一个，这是一个非常实用的功能，通过下面这个例子我们将知道如何使用<code>map-merge()</code>方法：</p>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">$colors: (</div><div class="line">  light: #ccc,</div><div class="line">  dark: #000</div><div class="line">);</div><div class="line"></div><div class="line">$brand-colors: (</div><div class="line">  main: red,</div><div class="line">  alternative: blue</div><div class="line">);</div><div class="line"></div><div class="line">// 合并maps</div><div class="line">$merged: map-merge($colors, $brand-colors);</div><div class="line"></div><div class="line">.element &#123;</div><div class="line">  content: map-get($merged, alternative);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>编译之后结果如下：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-class">.element</span> &#123;</div><div class="line">  <span class="attribute">content</span>: blue;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h2 id="利用Sass-Maps进行实战"><a href="#利用Sass-Maps进行实战" class="headerlink" title="利用Sass Maps进行实战"></a>利用Sass Maps进行实战</h2><p>上面我们介绍了如何实用<code>Sass Maps</code>，现在我们将通过一些实战训练看看在哪些地方适合使用该特性。</p>
<p>1.如何循环Map生成类</p>
<p>你可以遍历<code>map</code>通过里面的<code>values</code>去定义你需要的变量然后加到<code>map</code>里面的<code>name</code>去，这样子你可以创建出很多种<code>values</code>。</p>
<p>在下面的例子中我将输出<code>classes</code>来展示<code>icons</code>。我将<code>icon</code>的<code>name</code>作为<code>key</code>，让<code>value</code>去替代实际的<code>content</code>(通过伪元素加进去)。</p>
<blockquote>
<p>注意：在项目实战中我们通过会先声明一些基础的样式，这不在本教程的范围内。</p>
</blockquote>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">/* 定义一个sass map名称为$icons */</div><div class="line">$icons: (</div><div class="line">  checkmark: a,</div><div class="line">  plus: b,</div><div class="line">  minus: c</div><div class="line">);</div><div class="line"></div><div class="line">/* 遍历map的所有key，创建各自的类 */</div><div class="line">@each $name, $value in $icons &#123;</div><div class="line">  .icon--#&#123;$name&#125; &#123;</div><div class="line">    content: $value;</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>编译之后结果如下：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="comment">/* 遍历map的所有key，创建各自的类 */</span></div><div class="line"><span class="selector-class">.icon--checkmark</span> &#123;</div><div class="line">  <span class="attribute">content</span>: <span class="string">"a"</span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="selector-class">.icon--plus</span> &#123;</div><div class="line">  <span class="attribute">content</span>: <span class="string">"b"</span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="selector-class">.icon--minus</span> &#123;</div><div class="line">  <span class="attribute">content</span>: <span class="string">"c"</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这是一种非常高效的方法来输出icons的所有类，还有大量情况也会使用这种方法。</p>
<p>2.如何拿出Maps的多个值</p>
<p>让我们继续，给一个<code>key</code>赋予多个<code>value</code>也是可以的。多个<code>value</code>之间通过<code>,</code>来分割。下面的例子能够非常好的输出不同模块的样式。</p>
<p>这里我将定义一系列<code>buttons</code>，每一个<code>key</code>的第一个<code>value</code>是<code>background-color</code>，第二个<code>value</code>是<code>font-color</code>。</p>
<p>然后我将通遍历<code>keys</code>赋值给<code>$colors</code>对象。通过<code>nth($colors,1)</code>（第一个参数是对象的名称，第二个参数是值得位置）拿到第一个<code>key</code>。如果你需要拿第二个<code>value</code>，那将第二个参数改为<code>2</code>。</p>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">// _m-buttons.scss</div><div class="line">$buttons: (</div><div class="line">  error: (#d82d2d, #666),</div><div class="line">  success: (#52bf4a, #fff),</div><div class="line">  warning: (#c23435, #fff)</div><div class="line">);</div><div class="line"></div><div class="line">.m-button &#123;</div><div class="line">  display: inling-block;</div><div class="line">  padding: .5em;</div><div class="line">  background: #ccc;</div><div class="line">  color: #666;</div><div class="line"></div><div class="line">  @each $name, $colors in $buttons &#123;</div><div class="line">    $bgcolor: nth($colors, 1);</div><div class="line">    $fontcolor: nth($colors, 2);</div><div class="line"></div><div class="line">    &amp;--#&#123;$name&#125; &#123;</div><div class="line">      background-color: $bgcolor;</div><div class="line">      color: $fontcolor;</div><div class="line">    &#125;</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>编译之后结果如下：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-class">.m-button</span> &#123;</div><div class="line">  <span class="attribute">display</span>: inline-block;</div><div class="line">  <span class="attribute">padding</span>: .<span class="number">5em</span>;</div><div class="line">  <span class="attribute">background</span>: <span class="number">#ccc</span>;</div><div class="line">  <span class="attribute">color</span>: <span class="number">#666</span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="selector-class">.m-button--error</span> &#123;</div><div class="line">  <span class="attribute">background-color</span>: <span class="number">#d82d2d</span>;</div><div class="line">  <span class="attribute">color</span>: <span class="number">#666</span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="selector-class">.m-button--success</span> &#123;</div><div class="line">  <span class="attribute">background-color</span>: <span class="number">#52bf4a</span>;</div><div class="line">  <span class="attribute">color</span>: <span class="number">#fff</span>;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="selector-class">.m-button--warning</span> &#123;</div><div class="line">  <span class="attribute">background-color</span>: <span class="number">#c23435</span>;</div><div class="line">  <span class="attribute">color</span>: <span class="number">#fff</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>3.处理层(z-index)</p>
<p>在某种程度上来说，我还没有见过不跟<a href="https://webdesign.tutsplus.com/articles/what-you-may-not-know-about-the-z-index-property--webdesign-16892" target="_blank" rel="external">z-index</a>打交道的前端开发。当你在项目中多个地方需要使用到<code>z-index</code>的时候问题通常随之而来，<code>Sass maps</code>能够帮我们解决这些问题。</p>
<p>首先我们定义了一个map名称为<code>$layer</code>，所有的<code>key</code>都应该合理命名以便我们能够知道哪个value是对应哪个element的-比如：<code>offcanvas</code>,<code>lightbox</code>,<code>dropdown</code>等。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// _config.scss</span></div><div class="line">$layer: (</div><div class="line">  offcanvas: <span class="number">1</span>,</div><div class="line">  <span class="attr">lightbox</span>: <span class="number">500</span>,</div><div class="line">  <span class="attr">dropdown</span>: <span class="number">10</span>,</div><div class="line">  <span class="attr">tooltip</span>: <span class="number">15</span></div><div class="line">);</div><div class="line"></div><div class="line"><span class="comment">// _m-lightboxes.scss</span></div><div class="line">@<span class="function"><span class="keyword">function</span> <span class="title">layer</span>(<span class="params">$name</span>) </span>&#123;</div><div class="line">  @<span class="keyword">if</span> map-has-key($layer, $name) &#123;</div><div class="line">    @<span class="keyword">return</span> map-get($layer, $name);</div><div class="line">  &#125;</div><div class="line"></div><div class="line">  @warn <span class="string">"The key #&#123;$name&#125; is not in the map '$layer'"</span>;</div><div class="line">  @<span class="keyword">return</span> <span class="literal">null</span>;</div><div class="line">&#125;;</div><div class="line"></div><div class="line">.m-lightbox &#123;</div><div class="line">  z-index: layer(lightbox);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>上面我定义了一个方法用来获取特定<code>key</code>的<code>value</code>，但为什么我要这样做？理由很简单：这样子比每次都写<code>map-get()</code>方法要方便快捷。另外一个方面就是你可以创建<code>错误处理</code>给开发者一些错误信息当没有输出期望的信息的时候。</p>
<p>编译结果如下：</p>
<figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-class">.m-lightbox</span> &#123;</div><div class="line">  <span class="attribute">z-index</span>: <span class="number">500</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>4.在项目中为字体创建基本样式</p>
<p>每一个项目都拥有自己的配置文件，用来给全局使用。例如在我的项目中我会定义一些字体属性：字体颜色，可选的字体颜色，字体集或者字体大小。我通常都会为每个属性创建一个变量，但是<code>map</code>能够做得更好。</p>
<p>下面是一个简单的例子，先从<em>旧</em>的解决方法开始：</p>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">$base-font-color: #666;</div><div class="line">$base-font-family: Arial, Heletica, Sans-Serif;</div><div class="line">$base-font-size: 16px;</div><div class="line">$base-line-height: 1.4;</div></pre></td></tr></table></figure>
<p>接下来看通过<code>Sass Map</code>写的<code>新</code>的解决方法：</p>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">// _config.scss</div><div class="line">$font: (</div><div class="line">  color: #666;</div><div class="line">  family: (Arial, Helvetica),</div><div class="line">  size: 16px,</div><div class="line">  line-height: 1.4</div><div class="line">);</div><div class="line"></div><div class="line">// _presets.scss</div><div class="line">body &#123;</div><div class="line">  color: map-get($font, color);</div><div class="line">  font-family: map-get($font, family);</div><div class="line">  font-size: map-get($font, size);</div><div class="line">  line-height: map-get($font, line-height);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>5.Breakpoints &lt; 3</p>
<p>我很喜欢这个使用案例。在你的整个项目中拥有一块专门用来处理断点是非常好的。所以，像这一节中关于处理<code>z-index</code>，你就已经用到了断点。当你改变值得时候，整个项目的行为也随之改变，这多么令人惊讶。</p>
<p>那就先让我们通过一个<code>map</code>名称为<code>$breakpoints</code>开始吧。</p>
<p>我们的目标就是在一个元素中使用断点通过易懂的名字替代那些<code>pixel</code>值，所以需要一个<code>mixin</code>方法来实现占位<code>name</code>，我把mixin命名为<code>respond-to</code>以及传入<code>$brakpoint</code>参数。通过<code>$value</code>我就能得到期望的断点然后后面在<code>媒体查询</code>中使用。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// Map with much breakpoints</span></div><div class="line">$breakpoints: (</div><div class="line">  small: <span class="number">320</span>px,</div><div class="line">  <span class="attr">medium</span>: <span class="number">600</span>px,</div><div class="line">  <span class="attr">large</span>: <span class="number">768</span>px</div><div class="line">);</div><div class="line"></div><div class="line"><span class="comment">// Respond-To Mixin</span></div><div class="line">@mixin respond-to($breakpoint) &#123;</div><div class="line">  @<span class="keyword">if</span> map-has-key($breakpoints, $breakpoint) &#123;</div><div class="line">    <span class="attr">$value</span>: map-get($breakpoints, $breakpoint);</div><div class="line"></div><div class="line">    @media screen and (min-width: $value) &#123;</div><div class="line">      @content;</div><div class="line">    &#125;</div><div class="line">  &#125;</div><div class="line"></div><div class="line">  @warning <span class="string">"Unknown `#&#123;$breakpoint&#125;` in $breakpoints"</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>示例：</p>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">// Sass</div><div class="line">.m-tabs &#123;</div><div class="line">  background-color: #f2f2f2;</div><div class="line"></div><div class="line">  @include reponse-to(medium) &#123;</div><div class="line">    background-color: #666;</div><div class="line">  &#125;</div><div class="line">&#125;</div><div class="line"></div><div class="line">// Output</div><div class="line">.m-tabs &#123;</div><div class="line">  background-color: #f2f2f2;</div><div class="line">&#125;</div><div class="line">@media screen and (min-width: 600px) &#123;</div><div class="line">  background-color: #666;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>这种使用案例是我最喜欢之一！</p>
<p>6.颜色的高级使用</p>
<p>现在事情变得有一点点困难了，让我们看看通过不同色调定义的颜色计划。</p>
<p>我们的<code>Sass map</code>在这个例子中定义了一个<code>$colorscheme</code>同时里面定义了很多对象拥有<code>keys</code>跟<code>values</code>。项目中拥有不同的灰色调，但我们不想为每一个都声明一个变量。所以我们增加了一个对象<code>gray</code>，然后通过键值对分割。</p>
<p>下面开始这个Map:</p>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">// Scheme of colors</div><div class="line">$colorscheme: (</div><div class="line">  gray: (</div><div class="line">    base: #ccc,</div><div class="line">    light: #f2f2f2,</div><div class="line">    dark: #666</div><div class="line">  ),</div><div class="line">  brown: (</div><div class="line">    base: #ab906b,</div><div class="line">    light: #ecdac3,</div><div class="line">    dark: #5e421c</div><div class="line">  )</div><div class="line">);</div></pre></td></tr></table></figure>
<p>现在让我们加入<code>setcolor</code>方法来获取颜色的不同选择。第一个参数是Sass map的对象(<code>$scheme</code>)-在这个例子中可能是<code>gray</code>或者<code>brown</code>，第二个参数就是你想要的颜色(<code>$tone</code>)，默认值是<code>base</code>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">@<span class="function"><span class="keyword">function</span> <span class="title">setcolor</span>(<span class="params">$scheme, $tone: base</span>) </span>&#123;</div><div class="line">  @<span class="keyword">return</span> map-get(map-get($colorscheme, $scheme), $tone);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>最后，这里给出了一个例子能够让你从这个map中获取不同颜色，比你想象中的还要简单（也许）！</p>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">// Sass</div><div class="line">.element &#123;</div><div class="line">  color: setcolor(brown);</div><div class="line">&#125;</div><div class="line">.element--light &#123;</div><div class="line">  color: setcolor(brown, light);</div><div class="line">&#125;</div><div class="line"></div><div class="line">// Output</div><div class="line">.element &#123;</div><div class="line">  color: #ab906b;</div><div class="line">&#125;</div><div class="line">.element--light &#123;</div><div class="line">  color: #ecdac3;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>你会完成上面的挑战的，现在你能够创建一组色调而不需要通过为每一种颜色创建一个变量。</p>
<p>这个方法我是受到了<a href="http://erskinedesign.com/blog/friendlier-colour-names-sass-maps/" target="_blank" rel="external">Tom Davies</a>的启发，同时我也建议你看看他这篇文章。</p>
<h2 id="通过Classes定制主题"><a href="#通过Classes定制主题" class="headerlink" title="通过Classes定制主题"></a>通过Classes定制主题</h2><p>这是为高级Sass用户准备的。在项目中会经常需要通过一些基础代码创建多套主题，所以这里给出一个建议：在文档的最开始就定义一个 <em>主题类</em> 来满足特定的工作。我们需要一个对象以便能够处理不同名字的主题，同时给出不同的样式模块。</p>
<h3 id="定义themes"><a href="#定义themes" class="headerlink" title="定义themes"></a>定义themes</h3><p>最开始，在你的项目中通过Sass map全局定义themes，<code>value</code>就是主题名字，同时这个类必须附在<code>body</code>元素。在这个例子中我创建了一个map<code>$themes</code>，里面有两个主题：<code>theme-light</code>和<code>theme-dark</code>。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// _config.scss</span></div><div class="line">$themes: (</div><div class="line">  theme1: theme-light;</div><div class="line">  theme2: theme-dark;</div><div class="line">);</div></pre></td></tr></table></figure>
<h3 id="获取值（捷径）"><a href="#获取值（捷径）" class="headerlink" title="获取值（捷径）"></a>获取值（捷径）</h3><p>现在我们需要一个方法来快速获取模块的值，这是一个简单的方法包括三个变量如下：</p>
<ul>
<li><code>$map</code>: 定义map的名字确定values是从哪里来的</li>
<li><code>$object</code>: 在这里例子中就是theme的key</li>
<li><code>$style</code>: 需要的样式属性值</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// _functions.scss</span></div><div class="line">@<span class="function"><span class="keyword">function</span> <span class="title">setStyle</span>(<span class="params">$map, $object, $style</span>) </span>&#123;</div><div class="line">  @<span class="keyword">if</span> map-has-key($map, $object) &#123;</div><div class="line">    @<span class="keyword">return</span> map-get(map-get($map, $object), $style);</div><div class="line">  &#125;</div><div class="line">  @warn <span class="string">"The key `#&#123;$object&#125;` is not available in the map."</span>;</div><div class="line">  @<span class="keyword">return</span> <span class="literal">null</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="生成模块"><a href="#生成模块" class="headerlink" title="生成模块"></a>生成模块</h3><p>现在我们创建新的<code>Sass Map</code>名称为<code>$config</code>，每一个主题都是一个对象同时名字必须是<code>$themes</code>中的<code>key</code>：不然将会报错。</p>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">// _m-buttons.scss</div><div class="line">// 1.Config</div><div class="line">$config: (</div><div class="line">  theme1: (</div><div class="line">    background: #f2f2f2,</div><div class="line">    color: #000</div><div class="line">  ),</div><div class="line">  theme2: (</div><div class="line">    background: #666,</div><div class="line">    color: #fff</div><div class="line">  )</div><div class="line">);</div></pre></td></tr></table></figure>
<h3 id="遍历主题"><a href="#遍历主题" class="headerlink" title="遍历主题"></a>遍历主题</h3><p>最后一部分会使用一点点技巧。开始的地方我们定义了一个模块<code>.m-button</code>然后我们期望在每个主题下面外观是不一样的。所以我们使用<code>@each</code>方法遍历Map<code>$themes</code>拿到<code>$key</code>跟<code>$value</code>。遍历之后就能够为不同主题创建map。</p>
<p>在这一节的开始我提到了<code>keys</code>在每个map里面必须是一样的(<code>$themes</code>跟<code>$config</code>)。因此我们必须检查map<code>$config</code>的key是否都来自map<code>$themes</code>，这里使用到了<code>map-has-key()</code>方法。如果包含了key那就继续往下执行，否则抛出错误给开发者。</p>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">// _m-buttons.scss</div><div class="line">// 2.Base</div><div class="line">.m-button &#123;</div><div class="line">  @each $key, $value in $themes &#123;</div><div class="line">    @if map-has-key($config, $key) &#123;</div><div class="line">      .#&#123;$value&#125; &amp; &#123;</div><div class="line">        background: setStyle($config, $key, background);</div><div class="line">        color: setStyle($config, $key, color);</div><div class="line">      &#125;</div><div class="line">    &#125; @else &#123;</div><div class="line">      @warn "The key `#&#123;$key&#125; isn't defined in the map $config`"</div><div class="line">    &#125;</div><div class="line">  &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>通过上面的代码，让我们看看输出的结果。这是非常好的，保证了配置区域跟模块解耦。</p>
<figure class="highlight css"><table><tr><td class="code"><pre><div class="line"><span class="selector-class">.theme-light</span> <span class="selector-class">.m-button</span> &#123;</div><div class="line">  <span class="attribute">background</span>: <span class="number">#f2f2f2</span>;</div><div class="line">  <span class="attribute">color</span>: <span class="number">#000</span>;</div><div class="line">&#125;</div><div class="line"><span class="selector-class">.theme-dark</span> <span class="selector-class">.m-button</span> &#123;</div><div class="line">  <span class="attribute">background</span>: <span class="number">#666</span>;</div><div class="line">  <span class="attribute">color</span>: <span class="number">#fff</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>最后你可以自己试试上面的代码看能不能运行出结果。也许对于你来说这个方案并不是最好的，你会寻求另外的方案，但是我依然希望这能够帮助你维持代码。</p>
<h2 id="最后思考"><a href="#最后思考" class="headerlink" title="最后思考"></a>最后思考</h2><p>在我看来 <code>Sass Maps</code>是Sass 3.3最值得介绍的特性。理由是我认为它给出了非常好的方式来创建健壮的结构，这只需要少量的配置。Sass maps使得我们可以在不影响整个项目的基础逻辑代码基础上改变值。开始使用它吧，你的伙伴将会感激你！</p>
<p>如果你已经开始使用<code>Sass maps</code>了，让我知道你如何在你的项目中使用它！</p>
<ul>
<li>注1: 截至译文的Sass最新版本是3.4.19</li>
</ul>
]]></content>
    
    <summary type="html">
    
      本文介绍如何利用Sass Maps简化我们的代码，翻译水平有限，敬请指正。
    
    </summary>
    
      <category term="翻译" scheme="https://cnt1992.github.io/categories/%E7%BF%BB%E8%AF%91/"/>
    
    
      <category term="sass maps" scheme="https://cnt1992.github.io/tags/sass-maps/"/>
    
  </entry>
  
  <entry>
    <title>mac装机指南</title>
    <link href="https://cnt1992.github.io/2015/11/12/mac%E8%A3%85%E6%9C%BA%E6%8C%87%E5%8D%97/"/>
    <id>https://cnt1992.github.io/2015/11/12/mac装机指南/</id>
    <published>2015-11-11T22:50:00.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>本文已整合进 [<a href="http://aotu.io/mac" target="_blank" rel="external">http://aotu.io/mac</a>] ，欢迎指正~</p>
</blockquote>
<a id="more"></a>
<p>作为折腾了Mac一年多的菜鸟，虽然谈不上是Mac高手，但是有些使用的技巧个人总结分享一下，权当给新入手Mac的童鞋一个参考。</p>
<p><strong>目前更新了软件篇及系统应用篇</strong></p>
<h3 id="Xcode-Command-Line-Tools"><a href="#Xcode-Command-Line-Tools" class="headerlink" title="Xcode Command Line Tools"></a>Xcode Command Line Tools</h3><p>在Mac下面的很多软件安装都会涉及到<code>编译</code>，依赖于<code>Xcode Command Line Tools</code>这个工具，Mac下面的Xcode自带了这个工具，我们可以在<code>Terminal</code>输入<code>gcc</code>或者<code>make</code>,然后弹出一个选项会让你安装。为了避免其它关于编译的错误，建议去<code>AppStore</code>下载一个<code>Xcode</code></p>
<h3 id="HomeBrew"><a href="#HomeBrew" class="headerlink" title="HomeBrew"></a>HomeBrew</h3><p>HomeBrew是Mac下面的一个包管理器，方便我们安装一些Mac OS没有的UNIX工具。</p>
<ul>
<li><p>安装HomeBrew</p>
  <figure class="highlight bash"><table><tr><td class="code"><pre><div class="line">ruby <span class="_">-e</span> <span class="string">"<span class="variable">$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)</span>"</span></div></pre></td></tr></table></figure>
</li>
<li><p>确保HomeBrew是否正常安装</p>
<p>安装好HomeBrew之后，在<code>Terminal</code>执行下面命令确保正常安装：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><div class="line"><span class="keyword">brew </span>doctor</div></pre></td></tr></table></figure>
<p>若上面命令执行过程出现 <em>hombrew libevent not link</em> 的错误，执行下面命令即可：</p>
<figure class="highlight crmsh"><table><tr><td class="code"><pre><div class="line">sudo chown -R $<span class="keyword">USER</span> <span class="title">$</span>(brew --prefix)</div></pre></td></tr></table></figure>
<p>确保正常安装之后，可以输入<code>brew -h</code>查看帮助：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><div class="line"> Example usage:</div><div class="line"> brew [info | home | options ] [FORMULA...]</div><div class="line"> brew <span class="keyword">install</span> FORMULA...</div><div class="line"> brew <span class="keyword">uninstall</span> FORMULA...</div><div class="line"> brew <span class="keyword">search</span> [foo]</div><div class="line"> brew <span class="keyword">list</span> [FORMULA...]</div><div class="line"> brew <span class="keyword">update</span></div><div class="line"> brew <span class="keyword">upgrade</span> [FORMULA...]</div><div class="line"> brew pin/unpin [FORMULA...]</div><div class="line"></div><div class="line">Troubleshooting:</div><div class="line">  brew doctor</div><div class="line">  brew <span class="keyword">install</span> -vd FORMULA</div><div class="line">  brew [<span class="comment">--env | config]</span></div><div class="line"></div><div class="line">Brewing:</div><div class="line">  brew <span class="keyword">create</span> [<span class="keyword">URL</span> [<span class="comment">--no-fetch]]</span></div><div class="line">  brew edit [FORMULA...]</div><div class="line">  https://github.com/Homebrew/homebrew/<span class="built_in">blob</span>/<span class="keyword">master</span>/<span class="keyword">share</span>/doc/homebrew/Formula-Cookbook.md</div><div class="line"></div><div class="line">Further <span class="keyword">help</span>:</div><div class="line">  man brew</div><div class="line">  brew home</div></pre></td></tr></table></figure>
</li>
<li><p>使用HomeBrew</p>
<p>搜索一个包，在不确定想要安装的包是否有之前，建议先搜索一下，执行下面命令：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><div class="line"><span class="keyword">brew </span>search mongod</div></pre></td></tr></table></figure>
<p>安装包，在搜索到想要安装的包之后，执行下面命令即可安装：</p>
<figure class="highlight mipsasm"><table><tr><td class="code"><pre><div class="line"><span class="keyword">brew </span><span class="keyword">install </span>mongod</div></pre></td></tr></table></figure>
<p>更新HomeBrew在服务端的包结构：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><div class="line"><span class="keyword">brew </span>update</div></pre></td></tr></table></figure>
<p>查看安装过的包列表：</p>
<figure class="highlight applescript"><table><tr><td class="code"><pre><div class="line">brew <span class="built_in">list</span></div></pre></td></tr></table></figure>
</li>
</ul>
<h3 id="HomeBrew-Cask"><a href="#HomeBrew-Cask" class="headerlink" title="HomeBrew Cask"></a>HomeBrew Cask</h3><p>由于国内墙的问题，AppStore下载应用速度总是比较慢，而且应用更新速度也是比较慢，而HomeBrew Cask可以第一时间获取到官方最新的应用，并且下载速度杠杠的。</p>
<p>另外，Homebrew Cask可以优雅、简单、快速的安装和管理 OS X 图形界面程序。</p>
<ul>
<li><p>安装HomeBrew Cask</p>
<p>HomeBrew Cask依赖于Xcode以及HomeBrew，建议在装这个之前装好前面两个软件，在<code>Terminal</code>执行下面命令进行安装：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><div class="line"><span class="keyword">brew </span>tap caskroom/cask &amp;&amp; <span class="keyword">brew </span>install <span class="keyword">brew-cask</span></div></pre></td></tr></table></figure>
</li>
<li><p>使用HomeBrew Cask</p>
<p>搜索想要安装的软件，在<code>Terminal</code>输入下面命令即可：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><div class="line"><span class="keyword">brew </span>cask search qq</div></pre></td></tr></table></figure>
<p>一些推荐安装的程序，一键搞定：</p>
<figure class="highlight sql"><table><tr><td class="code"><pre><div class="line">rew cask <span class="keyword">install</span> alfred cleanmymac cheatsheet dropbox google-chrome sublime-<span class="built_in">text</span></div></pre></td></tr></table></figure>
<p>如果你是一个热衷于<code>Alfred</code>的爱好者（不熟悉这个软件的下面会讲到），默认情况下<code>Alfred</code>是不会搜索到<code>HomeBrew Cask</code>安装的软件的，你需要额外执行下面命令关联起来才行：</p>
<figure class="highlight armasm"><table><tr><td class="code"><pre><div class="line"><span class="keyword">brew </span>cask alfred link</div></pre></td></tr></table></figure>
</li>
</ul>
<h3 id="Git"><a href="#Git" class="headerlink" title="Git"></a>Git</h3><p>Mac 自带的 Git 版本相对比较旧，执行下面命令安装最新版本的 Git：</p>
<figure class="highlight mipsasm"><table><tr><td class="code"><pre><div class="line"><span class="keyword">brew </span><span class="keyword">install </span>git</div></pre></td></tr></table></figure>
<p>由于<code>brew</code>安装的软件软路径在<code>/usr/local/bin</code>下面，而系统自带的软件在<code>/usr/bin</code>下面，为了让<code>brew</code>安装的软件覆盖系统自带的软件，需要在<code>.bashrc/.zshrc</code>的<code>PATH</code>添加下面变量：</p>
<figure class="highlight applescript"><table><tr><td class="code"><pre><div class="line">/usr/<span class="keyword">local</span>/bin</div></pre></td></tr></table></figure>
<p>改完之后执行下面命令让配置文件生效：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line"><span class="comment"># bash shell</span></div><div class="line"><span class="built_in">source</span> ~/.bashrc</div><div class="line"></div><div class="line"><span class="comment"># zsh shell</span></div><div class="line"><span class="built_in">source</span> ~/.zshrc</div></pre></td></tr></table></figure>
<h3 id="SVN"><a href="#SVN" class="headerlink" title="SVN"></a>SVN</h3><p>Mac 自带的 SVN 版本也是相对较旧，执行下面命令安装最新版本的 SVN：</p>
<figure class="highlight mipsasm"><table><tr><td class="code"><pre><div class="line"><span class="keyword">brew </span><span class="keyword">install </span>svn</div></pre></td></tr></table></figure>
<h3 id="iTerm2-zsh-tmux"><a href="#iTerm2-zsh-tmux" class="headerlink" title="iTerm2 + zsh + tmux"></a>iTerm2 + zsh + tmux</h3><p>Mac下面我们大概花费70%的时间在于终端打交道，在<code>OS X El Capitan‎</code>之前，恐怕系统默认的<code>Terminal+bash+screen</code>是无法跟<code>iTerms+zsh+tmux</code>进行匹敌的，当然热爱苹果原生App的童鞋大可不必安装这三个软件，但是作为一名<code>前端爱好者</code>，装上这三个软件会让你爱不释手的。</p>
<h4 id="iTerm2-替代terminal"><a href="#iTerm2-替代terminal" class="headerlink" title="iTerm2 - 替代terminal"></a>iTerm2 - 替代terminal</h4><ul>
<li><p>快捷键非常丰富（不一一详细列举，只列了几个目前常用的）</p>
<ul>
<li><code>⌘ + 数字</code>: 切换标签页，<code>⌘ + 方向键</code> 按方向切换标签页</li>
<li><code>⌘ + enter</code>: 切换全屏</li>
<li><code>⌘ + d</code>: 垂直分屏，<code>⌘ + shift + d</code>: 水平分屏。使用<code>⌘ + ]</code>和<code>⌘ + [</code>在最近使用的分屏直接切换.而<code>⌘ + opt + 方向键</code>切换到指定位置的分屏。</li>
<li><code>⌘ + t</code> :新的标签页</li>
<li><code>⌘ + r</code>:清屏</li>
</ul>
</li>
<li><p>兼容性好</p>
<p>这一点在远程连接服务器的时候才可以体验到，默认的系统终端有时候会出现乱码问题。</p>
</li>
</ul>
<ul>
<li><p>安装iTerm2</p>
<figure class="highlight mipsasm"><table><tr><td class="code"><pre><div class="line"><span class="keyword">brew </span>cask <span class="keyword">install </span>iTerm2</div></pre></td></tr></table></figure>
</li>
<li><p>颜色和字体设置</p>
<p>下载<a href="https://github.com/altercation/solarized/tree/master/iterm2-colors-solarized" target="_blank" rel="external">Solarized dark iterm colors</a>，在Preferences -&gt; Profiles -&gt; Default -&gt; Colors -&gt; Load Presets 将其导入，作为默认颜色。</p>
</li>
</ul>
<h4 id="zsh-号称终极终端神器"><a href="#zsh-号称终极终端神器" class="headerlink" title="zsh - 号称终极终端神器"></a>zsh - 号称终极终端神器</h4><p><a href="https://github.com/robbyrussell/oh-my-zsh" target="_blank" rel="external">zsh</a>，也叫oh-my-zsh，Mac是基于UNIX的一套系统，而Shell可以说是UNIX的一个外壳，在Mac里面我们可以在<code>Terminal</code>执行下面命令看看系统有几种shell：</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><div class="line">cat <span class="regexp">/etc/</span>shells</div></pre></td></tr></table></figure>
<p>不出意外的话显示如下：</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><div class="line"><span class="regexp">/bin/</span>bash</div><div class="line"><span class="regexp">/bin/</span>csh</div><div class="line"><span class="regexp">/bin/</span>ksh</div><div class="line"><span class="regexp">/bin/</span>sh</div><div class="line"><span class="regexp">/bin/</span>tcsh</div><div class="line"><span class="regexp">/bin/</span>zsh</div></pre></td></tr></table></figure>
<p>Mac默认使用的是<code>bash</code>这个shell，英文26个字母当中<code>z</code>排在最后，这足以说明<code>zsh</code>是有多强大的（当然了这是我瞎掰的），不过<code>zsh</code>说是<code>终极Shell</code>应该是一点都不过分的，列举几个优势让你看看有多强大 <strong>下面优势摘自MacTalk</strong>：</p>
<ul>
<li>强大的历史纪录功能，输入 grep 然后用上下箭头可以翻阅你执行的所有 grep 命令</li>
<li>智能拼写纠正，输入gtep mactalk * -R，系统会提示：zsh: correct ‘gtep’ to ‘grep’ [nyae]? 比妹纸贴心吧，她们向来都是让你猜的……</li>
<li>各种补全：路径补全、命令补全，命令参数补全，插件内容补全等等。触发补全只需要按一下或两下 tab 键，补全项可以使用 ctrl+n/p/f/b上下左右切换。比如你想杀掉 java 的进程，只需要输入 kill java + tab键，如果只有一个 java 进程，zsh 会自动替换为进程的 pid，如果有多个则会出现选择项供你选择。ssh + 空格 + 两个tab键，zsh会列出所有访问过的主机和用户名进行补全</li>
<li>智能跳转，安装了autojump之后，zsh 会自动记录你访问过的目录，通过 j + 目录名 可以直接进行目录跳转，而且目录名支持模糊匹配和自动补全，例如你访问过hadoop-1.0.0目录，输入j hado 即可正确跳转。j –stat 可以看你的历史路径库</li>
<li>目录浏览和跳转：输入 d，即可列出你在这个会话里访问的目录列表，输入列表前的序号，即可直接跳转。</li>
</ul>
<p>下面介绍<code>zsh</code>的安装跟使用：</p>
<ul>
<li><p>安装</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><div class="line">sh -c <span class="string">"<span class="variable">$(curl -fsSL https://raw.github.com/robbyrussell/oh-my-zsh/master/tools/install.sh)</span>"</span></div></pre></td></tr></table></figure>
</li>
<li><p>设置zsh为你的默认shell</p>
<figure class="highlight awk"><table><tr><td class="code"><pre><div class="line">chsh -s <span class="regexp">/bin/</span>zsh</div></pre></td></tr></table></figure>
</li>
<li><p>更多关于<code>zsh</code>的使用请移步<a href="https://github.com/robbyrussell/oh-my-zsh" target="_blank" rel="external">这里</a></p>
</li>
</ul>
<h4 id="tmux-优秀的终端复用软件"><a href="#tmux-优秀的终端复用软件" class="headerlink" title="tmux - 优秀的终端复用软件"></a>tmux - 优秀的终端复用软件</h4><p>你是否<code>ssh</code>远程到服务器之后因为一不小心网络中断而得重新<code>ssh</code>上去？</p>
<p>你是否在你的编译还没结束的时候想要暂停下来，然后关掉电脑回家之后再继续编译但最后结果却是需要重新进行编译？</p>
<p>又或者你想过自己在终端开了一大堆的窗口但是关闭终端之后还想你的服务继续运行？</p>
<p>如果你上面的问题都没想过，那么你可以跳过这个软件了，等你哪一天有需要的时候再回来看看。</p>
<p>如果你跟我一样想过上面的其中某个问题，那么<code>tmux</code>强烈建议你安装。</p>
<ul>
<li>window，pane的概念可以很好地进行多窗口切换，窗口分割。</li>
<li>状态行配置很容易。</li>
<li>vi 模式</li>
<li>复制粘贴缓冲区</li>
<li>脚本化.通过脚本可以自动化窗口布局。</li>
</ul>
<p>下面介绍<code>tmux</code>的安装跟简单使用：</p>
<ul>
<li><p>安装</p>
<figure class="highlight mipsasm"><table><tr><td class="code"><pre><div class="line"><span class="keyword">brew </span><span class="keyword">install </span>tmux</div></pre></td></tr></table></figure>
</li>
<li><p>使用</p>
<p>Prefix-Command前置操作：所有下面介绍的快捷键，都必须以前置操作开始。tmux默认的前置操作是CTRL+b。例如，我们想要新建一个窗体，就需要先在键盘上摁下CTRL+b，松开后再摁下n键。</p>
</li>
</ul>
<p>Session相关操作</p>
<table>
<thead>
<tr>
<th style="text-align:left">操作</th>
<th style="text-align:left">快捷键</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">查看/切换session</td>
<td style="text-align:left">prefix s</td>
</tr>
<tr>
<td style="text-align:left">离开Session</td>
<td style="text-align:left">prefix d</td>
</tr>
<tr>
<td style="text-align:left">重命名当前Session</td>
<td style="text-align:left">prefix $</td>
</tr>
</tbody>
</table>
<p>Window相关操作</p>
<table>
<thead>
<tr>
<th style="text-align:left">操作</th>
<th style="text-align:left">快捷键</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">新建窗口</td>
<td style="text-align:left">prefix c</td>
</tr>
<tr>
<td style="text-align:left">切换到上一个活动的窗口</td>
<td style="text-align:left">prefix space</td>
</tr>
<tr>
<td style="text-align:left">关闭一个窗口</td>
<td style="text-align:left">prefix &amp;</td>
</tr>
<tr>
<td style="text-align:left">使用窗口号切换</td>
<td style="text-align:left">prefix 窗口号</td>
</tr>
</tbody>
</table>
<p>Pane相关操作</p>
<table>
<thead>
<tr>
<th style="text-align:left">操作</th>
<th style="text-align:left">快捷键</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:left">切换到下一个窗格</td>
<td style="text-align:left">prefix o</td>
</tr>
<tr>
<td style="text-align:left">查看所有窗格的编号</td>
<td style="text-align:left">prefix q</td>
</tr>
<tr>
<td style="text-align:left">垂直拆分出一个新窗格</td>
<td style="text-align:left">prefix “</td>
</tr>
<tr>
<td style="text-align:left">水平拆分出一个新窗格</td>
<td style="text-align:left">prefix %</td>
</tr>
<tr>
<td style="text-align:left">暂时把一个窗体放到最大</td>
<td style="text-align:left">prefix z</td>
</tr>
</tbody>
</table>
<ul>
<li><p>个性化定制tmux</p>
<p>默认情况下的<code>tmux</code>风格比较简陋，推荐使用<a href="https://github.com/gpakosz/.tmux" target="_blank" rel="external">gpakoz的tmux配置</a>，实现了如下功能：</p>
<ul>
<li>基于powerline的美化</li>
<li>显示笔记本电池电量</li>
<li>和Mac互通的剪切板</li>
<li>和vim更相近的快捷键</li>
</ul>
<p>安装方式如下：</p>
<figure class="highlight stylus"><table><tr><td class="code"><pre><div class="line">cd</div><div class="line">rm -rf <span class="selector-class">.tmux</span></div><div class="line">git clone https:<span class="comment">//github.com/gpakosz/.tmux.git</span></div><div class="line">ln -s .tmux/<span class="selector-class">.tmux</span><span class="selector-class">.conf</span></div><div class="line">cp .tmux/<span class="selector-class">.tmux</span><span class="selector-class">.conf</span><span class="selector-class">.local</span> .</div></pre></td></tr></table></figure>
</li>
<li><p>了解更多<code>tmux</code>可以点击<a href="http://foocoder.com/blog/zhong-duan-huan-jing-zhi-tmux.html/" target="_blank" rel="external">这里</a></p>
</li>
</ul>
<h3 id="终端里临时设置代理"><a href="#终端里临时设置代理" class="headerlink" title="终端里临时设置代理"></a>终端里临时设置代理</h3><p>有时候使用终端命令例如 brew 需要设置代理以便FQ。</p>
<p>举个例子，利用brew-cask安装macdown:</p>
<figure class="highlight mipsasm"><table><tr><td class="code"><pre><div class="line"><span class="keyword">brew </span>cask <span class="keyword">install </span>macdown</div></pre></td></tr></table></figure>
<p>因为macdown的安装源托管在amazonaws，不幸的是aws被Q，</p>
<p>这时候我们可以设置终端代理为蓝灯代理，让brew-cask的相关命令顺利FQ:</p>
<figure class="highlight groovy"><table><tr><td class="code"><pre><div class="line"></div><div class="line">ALL_PROXY=<span class="string">http:</span><span class="comment">//127.0.0.1:8787 brew cask install macdown</span></div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;本文已整合进 [&lt;a href=&quot;http://aotu.io/mac&quot;&gt;http://aotu.io/mac&lt;/a&gt;] ，欢迎指正~&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="个人学习" scheme="https://cnt1992.github.io/categories/%E4%B8%AA%E4%BA%BA%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="mac 前端开发" scheme="https://cnt1992.github.io/tags/mac-%E5%89%8D%E7%AB%AF%E5%BC%80%E5%8F%91/"/>
    
  </entry>
  
  <entry>
    <title>微信公众号开发记录</title>
    <link href="https://cnt1992.github.io/2015/09/04/%E5%BE%AE%E4%BF%A1%E5%85%AC%E4%BC%97%E5%8F%B7%E5%BC%80%E5%8F%91%E8%AE%B0%E5%BD%95/"/>
    <id>https://cnt1992.github.io/2015/09/04/微信公众号开发记录/</id>
    <published>2015-09-04T15:06:00.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<p>最近接到公司校招面试结果查询的一个需求，功能点其实很简单：那就是能让HR导入excel数据，然后学生可以通过微信公众账号查询到自己的面试结果。</p>
<p>后端技术选型用了PHP+Mysql，前端用Angular+Bootstrap，所有功能自己一个人来完成，算是重新复习了一遍Mysql的操作，以及PHP的一些语法，顺便把Angular真正用于项目中，总体收获蛮大。</p>
<p>微信公众号开发这一块算是卡了我比较久的，毕竟官方文档太久没更新，而且官方文档真心写得不咋地，然后就直接拿了公司一个同事的PHP代码过来改，顺利完成功能。</p>
<a id="more"></a>
<h3 id="公众号开发配置"><a href="#公众号开发配置" class="headerlink" title="公众号开发配置"></a>公众号开发配置</h3><ol>
<li>要进行公众号的服务开发，首先你得有自己的一个服务器用于接收公众号转发过来的信息进行处理返回结果，国内免费的如<code>sinaapp</code>或者<code>duapp</code>都可以用于接收。</li>
<li>既然要通过公众号来转发服务，那就得有一个公众号，可以到<a href="https://mp.weixin.qq.com/" target="_blank" rel="external">微信公众平台</a>注册一个个人账号。</li>
<li>公众号注册成功之后，进入到开发者中心，也就是登录进去之后左边导航最下面的一个，发现有个<code>服务器配置</code>，然后填写一下服务器配置信息，说明如下：<ul>
<li>URL（服务器地址）：这个可以填写你需要接收公众号转发信息的地址，当然这个地址需要配置一下能够让微信认识你的东西，稍后说。</li>
<li>Token（令牌）：一个md5戳，可以随便填，建议以你自己服务器地址域名的md5戳，这样子方便记忆。</li>
<li>EncodingAESKey(消息加解密密钥)：这个随机生成一个即可。</li>
<li>消息加解密方式：这个明文就行了。</li>
</ul>
</li>
<li>上面的配置写好之后，不要那么快保存，因为你一点击保存微信就会请求你填写的URL看能否返回它需要的信息，所以我们要先配置一下自己的服务器，下面代码是用<code>PHP</code>写的：</li>
</ol>
<figure class="highlight php"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="comment">//下面的TOKEN写自己在公众号填写的那个</span></div><div class="line">define(<span class="string">"TOKEN"</span>, <span class="string">"XXXXXXXXXXXXXXXXXX"</span>);</div><div class="line">define(<span class="string">"YTAG"</span>, <span class="string">"0.1220200001300000"</span>);</div><div class="line"></div><div class="line"><span class="comment">//验证来源</span></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">checkSignature</span><span class="params">()</span></span>&#123;</div><div class="line">    $signature = $_GET[<span class="string">"signature"</span>]; <span class="comment">//微信加密签名</span></div><div class="line">    $timestamp = $_GET[<span class="string">"timestamp"</span>]; <span class="comment">//时间戳</span></div><div class="line">    $nonce = $_GET[<span class="string">"nonce"</span>];  <span class="comment">//随机数	                </span></div><div class="line">    $token = TOKEN; <span class="comment">//自定义的token参数</span></div><div class="line">    $tmpArr = <span class="keyword">array</span>($token, $timestamp, $nonce); </div><div class="line">    sort($tmpArr);  <span class="comment">//三个参数进行字典序排序</span></div><div class="line">    $tmpStr = implode( $tmpArr );  <span class="comment">//将三个参数字符串拼接成一个字符串</span></div><div class="line">    $tmpStr = sha1( $tmpStr );  <span class="comment">//进行SHA1加密，获得最终加密后的字符串</span></div><div class="line">        </div><div class="line">    <span class="keyword">if</span>( $tmpStr == $signature )&#123;  <span class="comment">//加密字符串与微信加密签名对比</span></div><div class="line">        <span class="keyword">return</span> <span class="keyword">true</span>;</div><div class="line">    &#125;<span class="keyword">else</span>&#123;</div><div class="line">        <span class="keyword">return</span> <span class="keyword">false</span>;</div><div class="line">    &#125;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="keyword">if</span>( checkSignture() )&#123;</div><div class="line">  <span class="comment">// 接口校验时返回</span></div><div class="line">  <span class="keyword">echo</span> $_GET[<span class="string">'echostr'</span>];</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>上面服务器代码配置好了之后，就可以在微信公众号保存服务器配置信息，提示成功就绑定成功了。当然即使你的配置完全正确，它还是有时会提醒你失败，重复保存几次就好了。</p>
<h3 id="正式进入开发"><a href="#正式进入开发" class="headerlink" title="正式进入开发"></a>正式进入开发</h3><p>微信公众号绑定你的服务器成功之后就可以正式进入开发了，当然在正式开发过程中<code>模块化</code>的思维还是首选的，我们可以考虑主要分几个模块：</p>
<ol>
<li>接收从公众号转发过来的关键词，判断进行何种处理，姑且为<code>config.php</code>。</li>
<li>主入口文件，这里包括了一开始进行验证的代码，然后验证成功之后进行处理的主函数，就为<code>index.php</code>吧。</li>
<li>函数集合，将所有需要处理的函数都写在这里面，那就叫<code>function.php</code>吧。</li>
</ol>
<p>下面给出这三个文件的一个非常简单的demo，实现你在公众号发送<code>hello world</code>的时候回复你<code>hello world</code>。</p>
<ul>
<li>index.php （主入口文件）</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line">define(<span class="string">"TOKEN"</span>, <span class="string">"xxxxxxxxxxxxxx"</span>);</div><div class="line">define(<span class="string">"YTAG"</span>, <span class="string">"0.1220200001300000"</span>);</div><div class="line"></div><div class="line"><span class="comment">//验证来源</span></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">checkSignature</span>(<span class="params"></span>)</span>&#123;</div><div class="line">    $signature = $_GET[<span class="string">"signature"</span>]; <span class="comment">//微信加密签名</span></div><div class="line">    $timestamp = $_GET[<span class="string">"timestamp"</span>]; <span class="comment">//时间戳</span></div><div class="line">    $nonce = $_GET[<span class="string">"nonce"</span>];  <span class="comment">//随机数	              </span></div><div class="line">    $token = TOKEN; <span class="comment">//自定义的token参数</span></div><div class="line">    $tmpArr = array($token, $timestamp, $nonce); </div><div class="line">    sort($tmpArr);  <span class="comment">//三个参数进行字典序排序</span></div><div class="line">    $tmpStr = implode( $tmpArr );  <span class="comment">//将三个参数字符串拼接成一个字符串</span></div><div class="line">    $tmpStr = sha1( $tmpStr );  <span class="comment">//进行SHA1加密，获得最终加密后的字符串</span></div><div class="line">        </div><div class="line">    <span class="keyword">if</span>( $tmpStr == $signature )&#123;  <span class="comment">//加密字符串与微信加密签名对比</span></div><div class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</div><div class="line">    &#125;<span class="keyword">else</span>&#123;</div><div class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</div><div class="line">    &#125;</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="comment">//回复消息</span></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">responseMsg</span>(<span class="params"></span>)</span>&#123;</div><div class="line">	$beginTime = microtime(<span class="literal">true</span>);</div><div class="line">	<span class="comment">//载入配置文件及函数</span></div><div class="line">	include_once(<span class="string">"function.php"</span>) ;</div><div class="line">	include_once(<span class="string">"config.php"</span>) ;</div><div class="line">	<span class="comment">//获取微信传参</span></div><div class="line">	$postStr = $GLOBALS[<span class="string">"HTTP_RAW_POST_DATA"</span>];</div><div class="line"></div><div class="line">	<span class="keyword">if</span> (!empty($postStr))&#123;</div><div class="line">		$postObj = simplexml_load_string($postStr, <span class="string">'SimpleXMLElement'</span>, LIBXML_NOCDATA);</div><div class="line">		$postInfo[<span class="string">'FromUserName'</span>] = $postObj-&gt;FromUserName;</div><div class="line">		$postInfo[<span class="string">'ToUserName'</span>] = $postObj-&gt;ToUserName;</div><div class="line">		$postInfo[<span class="string">'CreateTime'</span>] = $postObj-&gt;CreateTime;</div><div class="line">		$postInfo[<span class="string">'MsgType'</span>] = $postObj-&gt;MsgType;</div><div class="line">		$postInfo[<span class="string">'MsgId'</span>] = $postObj-&gt;MsgId;</div><div class="line">		$postInfo[<span class="string">'Content'</span>] = trim($postObj-&gt;Content);</div><div class="line">		$postInfo[<span class="string">'Event'</span>] = trim($postObj-&gt;Event);</div><div class="line">		$postInfo[<span class="string">'EventKey'</span>] = trim($postObj-&gt;EventKey);</div><div class="line">		$postInfo[<span class="string">'PicUrl'</span>] = trim($postObj-&gt;PicUrl);</div><div class="line">		$postInfo[<span class="string">'Recognition'</span>] = trim($postObj-&gt;Recognition);</div><div class="line">		$postInfo[<span class="string">'Ytag'</span>] = <span class="string">"?YTAG="</span>.YTAG;</div><div class="line">	&#125;</div><div class="line">	$msgType = $postInfo[<span class="string">'MsgType'</span>];</div><div class="line">	$keyword = $postInfo[<span class="string">'Content'</span>];</div><div class="line">	$event = $postInfo[<span class="string">'Event'</span>];</div><div class="line">	$eventKey = $postInfo[<span class="string">'EventKey'</span>];</div><div class="line"></div><div class="line">	<span class="keyword">switch</span> ($msgType) &#123;</div><div class="line">	   <span class="comment">// 下面只处理了text的情况，实际还可以包括：voice、event、image等</span></div><div class="line">		<span class="keyword">case</span> <span class="string">'text'</span>:</div><div class="line">			&#123;</div><div class="line">				<span class="keyword">if</span>(isset($keyword))&#123;</div><div class="line">					<span class="comment">//config.php中存在时，执行特定操作</span></div><div class="line">					<span class="keyword">if</span> (function_exists($config[$keyword][<span class="string">'fName'</span>])) &#123;</div><div class="line">						$postInfo[<span class="string">'Parameter'</span>] = $config[$keyword][<span class="string">'fPara'</span>];						</div><div class="line">						$output = $config[$keyword][<span class="string">'fName'</span>]($postInfo);</div><div class="line">					&#125;</div><div class="line">					<span class="comment">//不存在时</span></div><div class="line">					<span class="keyword">else</span>&#123;</div><div class="line">						$postInfo[<span class="string">'Parameter'</span>] = $config[<span class="string">"default"</span>][<span class="string">'fPara'</span>];</div><div class="line">						$output = $config[<span class="string">"default"</span>][<span class="string">'fName'</span>]($postInfo);</div><div class="line">					&#125;</div><div class="line">				&#125;</div><div class="line">			&#125;</div><div class="line">			<span class="keyword">break</span>;		</div><div class="line">	&#125;</div><div class="line">	$endTime = microtime(<span class="literal">true</span>);	</div><div class="line">&#125;</div><div class="line"></div><div class="line"><span class="keyword">if</span>(checkSignature())&#123;  </div><div class="line">	responseMsg();</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<ul>
<li>config.php（关键词匹配）</li>
</ul>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="comment">//下面的fName表示执行的函数名称，fPara表示回复的信息</span></div><div class="line">  $config = array(</div><div class="line">  	<span class="comment">// 当传入hello world的时候执行showTxt函数，传入hello world参数</span></div><div class="line"><span class="string">"hello world"</span>=&gt;array(</div><div class="line">	<span class="string">"fName"</span>=&gt;<span class="string">"showTxt"</span>,</div><div class="line">	<span class="string">"fPara"</span>=&gt;<span class="string">"hello world"</span></div><div class="line">	),</div><div class="line"><span class="string">"default"</span>=&gt;array(</div><div class="line">	<span class="string">"fName"</span>=&gt;<span class="string">"showTxt"</span>,</div><div class="line">	<span class="string">"fPara"</span>=&gt;<span class="string">"还在开发中……"</span></div><div class="line">	),</div><div class="line">);</div></pre></td></tr></table></figure>
<ul>
<li>function.php（具体执行函数）</li>
</ul>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line"> </div><div class="line"> function showTxt($postInfo) &#123;</div><div class="line">	$fromUsername = $postInfo ['FromUserName'];</div><div class="line">	$toUsername = $postInfo ['ToUserName'];</div><div class="line">	$txtContent = $postInfo ['Parameter'];</div><div class="line">	</div><div class="line">	$time = time ();</div><div class="line">	$msgType = "text";</div><div class="line">	// 下面的xml格式是微信要求返回的，必须严格按照这个格式返回，不能私自添加属性</div><div class="line">	$textTpl = "&lt;xml&gt;</div><div class="line">                &lt;ToUserName&gt;&lt;![CDATA[$fromUsername]]&gt;&lt;/ToUserName&gt;</div><div class="line">                &lt;FromUserName&gt;&lt;![CDATA[$toUsername]]&gt;&lt;/FromUserName&gt;</div><div class="line">                &lt;CreateTime&gt;$time&lt;/CreateTime&gt;</div><div class="line">                &lt;MsgType&gt;&lt;![CDATA[$msgType]]&gt;&lt;/MsgType&gt;</div><div class="line">                &lt;Content&gt;&lt;![CDATA[$txtContent]]&gt;&lt;/Content&gt;</div><div class="line">                &lt;FuncFlag&gt;0&lt;/FuncFlag&gt;</div><div class="line">              &lt;/xml&gt;";</div><div class="line">	return $textTpl;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<h3 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h3><p>上面代码写好丢到服务器，就可以进行测试了，当然了，更常规的测试应该是直接用<a href="http://mp.weixin.qq.com/debug/cgi-bin/sandboxinfo?action=showinfo&amp;t=sandbox/index" target="_blank" rel="external">微信测试号</a>来进行测试，我懒得搞了，就可以丢服务器测试了，这个时候发送<code>hello world</code>给公众号，然后就可以看到回复了<code>hello world</code>，说明我们的开发就成功了。</p>
<p>至于我的需求，那就是PHP的逻辑代码问题了，有了上面的骨架，需要啥功能基本上可以完成了。</p>
<h3 id="注意点"><a href="#注意点" class="headerlink" title="注意点"></a>注意点</h3><ol>
<li>在发送消息给公众号若回复：该公众号暂时无法提供服务。这个是微信的机制导致的，我们开发过程经常采用被动回复机制，在5秒内微信若收不到回复就会提示这种信息，所以我们可以在还没做好回复之前先回复空的消息，微信会自动不理。若还有问题就要检查一下我们自己的代码，比如回复的<code>XML</code>格式问题，必须严格按照微信的要求，另外检查一下代码是否有echo、var_dump等代码干扰。</li>
</ol>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;最近接到公司校招面试结果查询的一个需求，功能点其实很简单：那就是能让HR导入excel数据，然后学生可以通过微信公众账号查询到自己的面试结果。&lt;/p&gt;
&lt;p&gt;后端技术选型用了PHP+Mysql，前端用Angular+Bootstrap，所有功能自己一个人来完成，算是重新复习了一遍Mysql的操作，以及PHP的一些语法，顺便把Angular真正用于项目中，总体收获蛮大。&lt;/p&gt;
&lt;p&gt;微信公众号开发这一块算是卡了我比较久的，毕竟官方文档太久没更新，而且官方文档真心写得不咋地，然后就直接拿了公司一个同事的PHP代码过来改，顺利完成功能。&lt;/p&gt;
    
    </summary>
    
      <category term="个人学习" scheme="https://cnt1992.github.io/categories/%E4%B8%AA%E4%BA%BA%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="微信 公众号" scheme="https://cnt1992.github.io/tags/%E5%BE%AE%E4%BF%A1-%E5%85%AC%E4%BC%97%E5%8F%B7/"/>
    
  </entry>
  
  <entry>
    <title>手机移动端的知识点</title>
    <link href="https://cnt1992.github.io/2015/08/13/%E6%94%B6%E9%9B%86%E7%A7%BB%E5%8A%A8%E7%AB%AF%E7%9A%84%E7%9F%A5%E8%AF%86%E7%82%B9/"/>
    <id>https://cnt1992.github.io/2015/08/13/收集移动端的知识点/</id>
    <published>2015-08-13T10:59:00.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<p>从github上拿来的，只是为了方便查阅~~<br><a id="more"></a></p>
<p>##工具类网站</p>
<p> <a href="http://html5please.com/" title="html5与css3技术应用评估" target="_blank" rel="external">HTML5 与 CSS3 技术应用评估</a></p>
<p> <a href="http://browserhacks.com/" title="各种奇妙的hack" target="_blank" rel="external">各种奇妙的hack</a></p>
<p> <a href="http://en.wikipedia.org/wiki/List_of_displays_by_pixel_density" title="几乎所有设备的屏幕尺寸与像素密度表" target="_blank" rel="external">几乎所有设备的屏幕尺寸与像素密度表</a></p>
<p> <a href="http://screensiz.es/phone" title="移动设备参数表" target="_blank" rel="external">移动设备参数表</a></p>
<p> <a href="http://ivomynttinen.com/blog/the-ios-design-cheat-sheet-volume-2/" title="ios端移动设备参数速查" target="_blank" rel="external">ios端移动设备参数速查</a></p>
<p> <a href="http://www.quirksmode.org/compatibility.html" title="浏览器兼容表" target="_blank" rel="external">浏览器兼容表</a></p>
<p> <a href="https://deviceatlas.com/device-data/devices" title="移动设备查询器" target="_blank" rel="external">移动设备查询器</a></p>
<p> <a href="http://51degrees.codeplex.com/" title="移动设备适配库" target="_blank" rel="external">移动设备适配库</a></p>
<p> <a href="http://detectmobilebrowsers.com/" title="移动设备适配库2" target="_blank" rel="external">移动设备适配库2</a></p>
<p> <a href="https://deviceatlas.com/device-data/devices" title="viewport与设备尺寸在线检测器" target="_blank" rel="external">viewport与设备尺寸在线检测器</a></p>
<p> <a href="http://mobilehtml5.org/" title="html5移动端兼容性速查" target="_blank" rel="external">html5 移动端兼容性速查</a></p>
<p> <a href="http://www.fontsquirrel.com/tools/webfont-generator" title="在线转换字体" target="_blank" rel="external">在线转换字体</a></p>
<p> <a href="http://tools.css3.info/selectors-test/test.html" title="css3选择器测试" target="_blank" rel="external">css3 选择器测试</a></p>
<p> <a href="http://caniuse.com/" title="兼容性速查表" target="_blank" rel="external">兼容性速查表</a></p>
<p> <a href="http://www.browserscope.org/" title="浏览器的一些独特参数" target="_blank" rel="external">浏览器的一些独特参数</a></p>
<p> <a href="http://nmsdvid.com/snippets/" title="各种各样的媒体查询收集" target="_blank" rel="external">各种各样的媒体查询收集</a></p>
<p> <a href="http://css3lib.alloyteam.com/#animation/AnimatedButtons" title="css3动画在线制作器" target="_blank" rel="external">css3 动画在线制作器</a></p>
<p> <a href="http://www.colorzilla.com/gradient-editor/" title="css3渐变在线制作器" target="_blank" rel="external">css3 渐变在线制作器</a></p>
<p><a href="http://ww1.sinaimg.cn/bmiddle/c2c57f68jw1e4fh7dmw12j20fi2w6qe1.jpg" title="移动端手势表" target="_blank" rel="external">移动端手势表</a></p>
<p><a href="http://ued.ctrip.com/blog/wp-content/webkitcss/" title="webkit独有的样式分析" target="_blank" rel="external">webkit独有的样式分析</a></p>
<p><a href="https://github.com/Modernizr/Modernizr/wiki/HTML5-Cross-Browser-Polyfills" target="_blank" rel="external">HTML5 Cross Browser Polyfills</a></p>
<p><a href="http://html5polyfill.com/" title="HTML5 POLYFILLS" target="_blank" rel="external">HTML5 POLYFILLS</a></p>
<p>##iphone6的那些事</p>
<p><a href="http://wileam.com/iphone-6-screen-cn/" target="_blank" rel="external">iPhone 6 屏幕揭秘</a></p>
<p>##响应式测试工具</p>
<p>Firefox 浏览器内置了 <code>自定义设计视图</code> 的功能，可以通过 <code>Firefox-&gt;Web 开发者-&gt;自定义设计视图</code>（或者摁下 <code>Shift + Ctrl + m</code> ）。相比网络工具，运行更加流畅，无需联网。</p>
<p><a href="http://yujiangshui.com/document/css-media-queries-for-ipads-and-iphones-chinese-version/" target="_blank" rel="external">判断 iPad 和 iPhone 的版本和状态的 CSS 媒体查询代码</a></p>
<p><a href="http://lab.maltewassermann.com/viewport-resizer/" title="resize" target="_blank" rel="external">Viewport Resizer</a></p>
<p><a href="http://beta.screenqueri.es/" title="resize" target="_blank" rel="external">http://beta.screenqueri.es/</a></p>
<p><a href="http://responsivepx.com" title="resize" target="_blank" rel="external">http://responsivepx.com</a></p>
<p><a href="http://www.responsinator.com/" title="resize" target="_blank" rel="external">http://www.responsinator.com/</a></p>
<p><a href="http://resizemybrowser.com/" title="resize" target="_blank" rel="external">http://resizemybrowser.com/</a></p>
<p><a href="https://quirktools.com/screenfly/" title="resize" target="_blank" rel="external">https://quirktools.com/screenfly/</a></p>
<p>媒体查询常用样式表：</p>
<pre><code>&lt;link rel=&quot;stylesheet&quot; media=&quot;all and (orientation:portrait)&quot; href=&quot;portrait.css&quot;&gt;    // 竖放加载
&lt;link rel=&quot;stylesheet&quot; media=&quot;all and (orientation:landscape)&quot;href=&quot;landscape.css&quot;&gt;   // 横放加载

//竖屏时使用的样式
&lt;style media=&quot;all and (orientation:portrait)&quot; type=&quot;text/css&quot;&gt;
    #landscape { display: none; }
&lt;/style&gt;

//横屏时使用的样式
&lt;style media=&quot;all and (orientation:landscape)&quot; type=&quot;text/css&quot;&gt;
    #portrait { display: none; }
&lt;/style&gt;
</code></pre><p>##Web app 开发的最佳实践与中文总结</p>
<p><a href="http://blog.forecast.io/its-not-a-web-app-its-an-app-you-install-from-the-web/" target="_blank" rel="external">It’s not a web app. It’s an app you install from the web.</a></p>
<p><a href="http://lyric.im/best-practice-for-web-app-development/" target="_blank" rel="external">当前 WEB APP 开发的最佳实践</a></p>
<p><a href="https://www.icloud.com/keynote/AwBWCAESEJd5uucfBPGt6KPotb3tNfsaKm-Q7fqs2-4ojmPoPJuWZCvjYgKl5jEf1URdRgdgNHe38BTzeF3DK7q1ewMCUCAQEEIJ85mw21ii_AwybOqxoF-V02v51Vdg855ED4qVA_8bXr#Mobile_Webpage_如何自适应屏幕_2" target="_blank" rel="external">如何自适应网页屏幕</a><br>以及<a href="https://github.com/unbug/generator-webappstarter/blob/master/app/templates/app/src/util/MetaHandler.js" target="_blank" rel="external">配套的解决方案</a></p>
<p>##来自<a href="https://github.com/maxzhang" title="ava" target="_blank" rel="external">maxzhang</a>的一些移动端经验总结干货</p>
<p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/8" title="ava" target="_blank" rel="external">移动Web单页应用开发实践——页面结构化</a></p>
<p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/1" title="ava" target="_blank" rel="external">移动Web产品前端开发口诀——“快”</a></p>
<p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/7" title="ava" target="_blank" rel="external">移动Web开发，4行代码检测浏览器是否支持position:fixed</a></p>
<p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/4" title="ava" target="_blank" rel="external">使用border-image实现类似iOS7的1px底边</a></p>
<p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/2" title="ava" target="_blank" rel="external">移动端web页面使用position:fixed问题总结</a></p>
<p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/11" title="ava" target="_blank" rel="external">移动Web开发实践——解决position:fixed自适应BUG</a></p>
<p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/19" title="ava" target="_blank" rel="external">移动手机浏览器m3u8格式视频流播放支持程度测试</a></p>
<p>##本资料很多引用了指尖上的js系列</p>
<p> <a href="http://www.cnblogs.com/pifoo/archive/2011/05/23/webkit-touch-event-1.html" title="article1" target="_blank" rel="external">指尖下的js ——多触式web前端开发之一：对于Touch的处理</a></p>
<p> <a href="http://www.cnblogs.com/pifoo/archive/2011/05/22/webkit-touch-event-2.html" title="article2" target="_blank" rel="external">指尖下的js ——多触式web前端开发之二：处理简单手势</a></p>
<p> <a href="http://www.cnblogs.com/pifoo/archive/2011/05/22/webkit-touch-event-3.html" title="article3" target="_blank" rel="external">指尖下的js —— 多触式web前端开发之三：处理复杂手势</a></p>
<p>##基础知识</p>
<p>###meta标签</p>
<p>meta标签大全 <a href="http://segmentfault.com/blog/ciaocc/1190000002407912" target="_blank" rel="external">http://segmentfault.com/blog/ciaocc/1190000002407912</a></p>
<p>meta标签，这些meta标签在开发webapp时起到非常重要的作用</p>
<pre><code>&lt;meta content=&quot;width=device-width; initial-scale=1.0; maximum-scale=1.0; user-scalable=0&quot; name=&quot;viewport&quot; /&gt;
&lt;meta content=&quot;yes&quot; name=&quot;apple-mobile-web-app-capable&quot; /&gt;
&lt;meta content=&quot;black&quot; name=&quot;apple-mobile-web-app-status-bar-style&quot; /&gt;
&lt;meta content=&quot;telephone=no&quot; name=&quot;format-detection&quot; /&gt;
</code></pre><p>第一个meta标签表示：强制让文档的宽度与设备的宽度保持1:1，并且文档最大的宽度比例是1.0，且不允许用户点击屏幕放大浏览；<br>尤其要注意的是content里多个属性的设置一定要用分号+空格来隔开，如果不规范将不会起作用。</p>
<p>注意根据 <a href="http://www.weibo.com/avajayam" title="ava" target="_blank" rel="external">public_00</a> 提供的资料补充，content 使用分号作为分隔，在老的浏览器是支持的，但不是规范写法。</p>
<p>规范的写法应该是使用逗号分隔，参考 <a href="http://developer.apple.com/library/safari/#documentation/appleapplications/reference/SafariHTMLRef/Articles/MetaTags.html" target="_blank" rel="external">Safari HTML Reference - Supported Meta Tags</a> 和 <a href="http://developer.android.com/guide/webapps/targeting.html" target="_blank" rel="external">Android - Supporting Different Screens in Web Apps</a></p>
<p>其中：</p>
<ul>
<li>width - viewport的宽度</li>
<li>height - viewport的高度</li>
<li>initial-scale - 初始的缩放比例</li>
<li>minimum-scale - 允许用户缩放到的最小比例</li>
<li>maximum-scale - 允许用户缩放到的最大比例</li>
<li>user-scalable - 用户是否可以手动缩放</li>
</ul>
<p>第二个meta标签是iphone设备中的safari私有meta标签，它表示：允许全屏模式浏览；<br>第三个meta标签也是iphone的私有标签，它指定的iphone中safari顶端的状态条的样式；<br>第四个meta标签表示：告诉设备忽略将页面中的数字识别为电话号码</p>
<p>在设置了initial-scale=1 之后，我们终于可以以1:1 的比例进行页面设计了。<br>关于viewport，还有一个很重要的概念是：iphone 的safari 浏览器完全没有滚动条，而且不是简单的“隐藏滚动条”，<br>是根本没有这个功能。iphone 的safari 浏览器实际上从一开始就完整显示了这个网页，然后用viewport 查看其中的一部分。<br>当你用手指拖动时，其实拖的不是页面，而是viewport。浏览器行为的改变不止是滚动条，交互事件也跟普通桌面不一样。<br>(请参考：指尖的下JS 系列文章)</p>
<p>更详细的 viewport 相关的知识也可以参考</p>
<p><a href="http://www.w3cplus.com/css/A-pixel-is-not-a-pixel-is-not-a-pixel.html" title="pixel" target="_blank" rel="external">此像素非彼像素</a></p>
<p>##移动开发事件</p>
<p><a href="http://wo.poco.cn/manson/post/id/268780" target="_blank" rel="external">手机浏览器常用手势动作监听封装</a></p>
<p>###手势事件</p>
<ul>
<li>touchstart            //当手指接触屏幕时触发</li>
<li>touchmove           //当已经接触屏幕的手指开始移动后触发</li>
<li>touchend             //当手指离开屏幕时触发</li>
<li>touchcancel</li>
</ul>
<p>###触摸事件</p>
<ul>
<li>gesturestart          //当两个手指接触屏幕时触发</li>
<li>gesturechange      //当两个手指接触屏幕后开始移动时触发</li>
<li>gestureend</li>
</ul>
<p>###屏幕旋转事件</p>
<ul>
<li>onorientationchange    </li>
</ul>
<p>###检测触摸屏幕的手指何时改变方向</p>
<ul>
<li>orientationchange      </li>
</ul>
<p>###touch事件支持的相关属性</p>
<ul>
<li>touches</li>
<li>targetTouches</li>
<li>changedTouches</li>
<li>clientX　　　　// X coordinate of touch relative to the viewport (excludes scroll offset)</li>
<li>clientY　　　　// Y coordinate of touch relative to the viewport (excludes scroll offset)</li>
<li>screenX　　　 // Relative to the screen</li>
<li>screenY 　　  // Relative to the screen</li>
<li>pageX　　 　　// Relative to the full page (includes scrolling)</li>
<li>pageY　　　　 // Relative to the full page (includes scrolling)</li>
<li>target　　　　 // Node the touch event originated from</li>
<li>identifier　　   // An identifying number, unique to each touch event</li>
<li>屏幕旋转事件：onorientationchange</li>
</ul>
<p>###判断屏幕是否旋转</p>
<pre><code>function orientationChange() {
    switch(window.orientation) {
    　　case 0:
            alert(&quot;肖像模式 0,screen-width: &quot; + screen.width + &quot;; screen-height:&quot; + screen.height);
            break;
    　　case -90:
            alert(&quot;左旋 -90,screen-width: &quot; + screen.width + &quot;; screen-height:&quot; + screen.height);
            break;
    　　case 90:
            alert(&quot;右旋 90,screen-width: &quot; + screen.width + &quot;; screen-height:&quot; + screen.height);
            break;
    　　case 180:
        　　alert(&quot;风景模式 180,screen-width: &quot; + screen.width + &quot;; screen-height:&quot; + screen.height);
        　　break;
    };};
</code></pre><p>###添加事件监听<br>    addEventListener(‘load’, function(){<br>        orientationChange();<br>        window.onorientationchange = orientationChange;<br>    });</p>
<p>###JS 单击延迟<br>click 事件因为要等待单击确认，会有 300ms 的延迟，体验并不是很好。</p>
<p>开发者大多数会使用封装的 tap 事件来代替click 事件，所谓的 tap 事件由 touchstart 事件 + touchmove 判断 + touchend 事件封装组成。</p>
<p> <a href="https://developers.google.com/mobile/articles/fast_buttons?hl=de-DE" title="article5" target="_blank" rel="external">Creating Fast Buttons for Mobile Web Applications</a></p>
<p> <a href="http://stackoverflow.com/questions/12238587/eliminate-300ms-delay-on-click-events-in-mobile-safari" title="article5" target="_blank" rel="external">Eliminate 300ms delay on click events in mobile Safari</a></p>
<p>##WebKit CSS:<br><a href="http://ued.ctrip.com/blog/wp-content/webkitcss/index.html" target="_blank" rel="external">携程 UED 整理的 Webkit CSS 文档</a> ，全面、方便查询，下面为常用属性。</p>
<p>①“盒模型”的具体描述性质的包围盒块内容，包括边界，填充等等。</p>
<pre><code>-webkit-border-bottom-left-radius: radius;
-webkit-border-top-left-radius: horizontal_radius vertical_radius;
-webkit-border-radius: radius;      //容器圆角
-webkit-box-sizing: sizing_model; 边框常量值：border-box/content-box
-webkit-box-shadow: hoff voff blur color; //容器阴影（参数分别为：水平X 方向偏移量；垂直Y 方向偏移量；高斯模糊半径值；阴影颜色值）
-webkit-margin-bottom-collapse: collapse_behavior; 常量值：collapse/discard/separate
-webkit-margin-start: width;
-webkit-padding-start: width;
-webkit-border-image: url(borderimg.gif) 25 25 25 25 round/stretch round/stretch;
-webkit-appearance: push-button;   //内置的CSS 表现，暂时只支持push-button
</code></pre><p>②“视觉格式化模型”描述性质，确定了位置和大小的块元素。</p>
<pre><code>direction: rtl
unicode-bidi: bidi-override; 常量：bidi-override/embed/normal
</code></pre><p>③“视觉效果”描述属性，调整的视觉效果块内容，包括溢出行为，调整行为，能见度，动画，变换，和过渡。</p>
<pre><code>clip: rect(10px, 5px, 10px, 5px)
resize: auto; 常量：auto/both/horizontal/none/vertical
visibility: visible; 常量: collapse/hidden/visible
-webkit-transition: opacity 1s linear; 动画效果 ease/linear/ease-in/ease-out/ease-in-out
-webkit-backface-visibility: visibler; 常量：visible(默认值)/hidden
-webkit-box-reflect: right 1px; 镜向反转
-webkit-box-reflect: below 4px -webkit-gradient(linear, left top, left bottom,
from(transparent), color-stop(0.5, transparent), to(white));
-webkit-mask-image: -webkit-gradient(linear, left top, left bottom, from(rgba(0,0,0,1)), to(rgba(0,0,0,0)));;   //CSS 遮罩/蒙板效果
-webkit-mask-attachment: fixed; 常量：fixed/scroll
-webkit-perspective: value; 常量：none(默认)
-webkit-perspective-origin: left top;
-webkit-transform: rotate(5deg);
-webkit-transform-style: preserve-3d; 常量：flat/preserve-3d; (2D 与3D)
</code></pre><p>④“生成的内容，自动编号，并列出”描述属性，允许您更改内容的一个组成部分，创建自动编号的章节和标题，和操纵的风格清单的内容。</p>
<pre><code>content: “Item” counter(section) ” “;
This resets the counter.
First section
&gt;two section
three section
counter-increment: section 1;
counter-reset: section;
</code></pre><p>⑤“分页媒体”描述性能与外观的属性，控制印刷版本的网页，如分页符的行为。</p>
<pre><code>page-break-after: auto; 常量：always/auto/avoid/left/right
page-break-before: auto; 常量：always/auto/avoid/left/right
page-break-inside: auto; 常量：auto/avoid
</code></pre><p>⑥“颜色和背景”描述属性控制背景下的块级元素和颜色的文本内容的组成部分。</p>
<pre><code>-webkit-background-clip: content; 常量：border/content/padding/text
-webkit-background-origin: padding; 常量：border/content/padding/text
-webkit-background-size: 55px; 常量：length/length_x/length_y
</code></pre><p>⑦ “字型”的具体描述性质的文字字体的选择范围内的一个因素。报告还描述属性用于下载字体定义。</p>
<pre><code>unicode-range: U+00-FF, U+980-9FF;
</code></pre><p>⑧“文本”描述属性的特定文字样式，间距和自动滚屏。</p>
<pre><code>text-shadow: #00FFFC 10px 10px 5px;
text-transform: capitalize; 常量：capitalize/lowercase/none/uppercase
word-wrap: break-word; 常量：break-word/normal
-webkit-marquee: right large infinite normal 10s; 常量：direction(方向) increment(迭代次数) repetition(重复) style(样式) speed(速度);
-webkit-marquee-direction: ahead/auto/backwards/down/forwards/left/reverse/right/up
-webkit-marquee-incrementt: 1-n/infinite(无穷次)
-webkit-marquee-speed: fast/normal/slow
-webkit-marquee-style: alternate/none/scroll/slide
-webkit-text-fill-color: #ff6600; 常量：capitalize, lowercase, none, uppercase
-webkit-text-security: circle; 常量：circle/disc/none/square
-webkit-text-size-adjust: none; 常量:auto/none;
-webkit-text-stroke: 15px #fff;
-webkit-line-break: after-white-space; 常量：normal/after-white-space
-webkit-appearance: caps-lock-indicator;
-webkit-nbsp-mode: space; 常量： normal/space
-webkit-rtl-ordering: logical; 常量：visual/logical
-webkit-user-drag: element; 常量：element/auto/none
-webkit-user-modify: read- only; 常量：read-write-plaintext-only/read-write/read-only
-webkit-user-select: text; 常量：text/auto/none
</code></pre><p>⑨“表格”描述的布局和设计性能表的具体内容。</p>
<pre><code>-webkit-border-horizontal-spacing: 2px;
-webkit-border-vertical-spacing: 2px;
-webkit-column-break-after: right; 常量：always/auto/avoid/left/right
-webkit-column-break-before: right; 常量：always/auto/avoid/left/right
–webkit-column-break-inside: logical; 常量：avoid/auto
-webkit-column-count: 3; //分栏
-webkit-column-rule: 1px solid #fff;
style:dashed,dotted,double,groove,hidden,inset,none,outset,ridge,solid
</code></pre><p>⑩“用户界面”描述属性，涉及到用户界面元素在浏览器中，如滚动文字区，滚动条，等等。报告还描述属性，范围以外的网页内容，如光标的标注样式和显示当您按住触摸触摸<br>目标，如在iPhone上的链接。</p>
<pre><code>-webkit-box-align: baseline,center,end,start,stretch 常量：baseline/center/end/start/stretch
-webkit-box-direction: normal;常量：normal/reverse
-webkit-box-flex: flex_valuet
-webkit-box-flex-group: group_number
-webkit-box-lines: multiple; 常量：multiple/single
-webkit-box-ordinal-group: group_number
-webkit-box-orient: block-axis; 常量：block-axis/horizontal/inline-axis/vertical/orientation
–webkit-box-pack: alignment; 常量：center/end/justify/start
</code></pre><p>动画过渡<br>这是 Webkit 中最具创新力的特性：使用过渡函数定义动画。</p>
<pre><code>-webkit-animation: title infinite ease-in-out 3s;
animation 有这几个属性：
-webkit-animation-name： //属性名，就是我们定义的keyframes
-webkit-animation-duration：3s //持续时间
-webkit-animation-timing-function： //过渡类型：ease/ linear(线性) /ease-in(慢到快)/ease-out(快到慢) /ease-in-out(慢到快再到慢) /cubic-bezier
-webkit-animation-delay：10ms //动画延迟(默认0)
-webkit-animation-iteration-count： //循环次数(默认1)，infinite 为无限
-webkit-animation-direction： //动画方式：normal(默认 正向播放)； alternate(交替方向，第偶数次正向播放，第奇数次反向播放)
</code></pre><p>这些同样是可以简写的。但真正让我觉的很爽的是keyframes，它能定义一个动画的转变过程供调用，过程为0%到100%或from(0%)到to(100%)。简单点说，只要你有想法，你想让元素在这个过程中以什么样的方式改变都是很简单的。</p>
<pre><code>-webkit-transform: 类型（缩放scale/旋转rotate/倾斜skew/位移translate）
scale(num,num) 放大倍率。scaleX 和 scaleY(3)，可以简写为：scale(* , *)
rotate(*deg) 转动角度。rotateX 和 rotateY，可以简写为：rotate(* , *)
Skew(*deg) 倾斜角度。skewX 和skewY，可简写为：skew(* , *)
translate(*,*) 坐标移动。translateX 和translateY，可简写为：translate(* , *)。
</code></pre><p>###页面描述</p>
<pre><code>&lt;link rel=&quot;apple-touch-icon-precomposed&quot; href=&quot;http://www.xxx.com/App_icon_114.png&quot; /&gt;
&lt;link rel=&quot;apple-touch-icon-precomposed&quot; sizes=&quot;72x72&quot; href=&quot;http://www.xxx.com/App_icon_72.png&quot; /&gt;
&lt;link rel=&quot;apple-touch-icon-precomposed&quot; sizes=&quot;114x114&quot; href=&quot;http://www.xxx.com/App_icon_114.png&quot; /&gt;
</code></pre><p>这个属性是当用户把连接保存到手机桌面时使用的图标，如果不设置，则会用网页的截图。有了这，就可以让你的网页像APP一样存在手机里了</p>
<pre><code>&lt;link rel=&quot;apple-touch-startup-image&quot; href=&quot;/img/startup.png&quot; /&gt;
</code></pre><p>这个是APP启动画面图片，用途和上面的类似，如果不设置，启动画面就是白屏，图片像素就是手机全屏的像素</p>
<pre><code>&lt;meta name=&quot;apple-mobile-web-app-status-bar-style&quot; content=&quot;black-translucent&quot; /&gt;
</code></pre><p>这个描述是表示打开的web app的最上面的时间、信号栏是黑色的，当然也可以设置其它参数，详细参数说明请参照：<a href="https://developer.apple.com/library/safari/documentation/appleapplications/reference/SafariHTMLRef/Articles/MetaTags.html" target="_blank" rel="external">Safari HTML Reference - Supported Meta Tags</a></p>
<pre><code>&lt;meta name=&quot;apple-touch-fullscreen&quot; content=&quot;yes&quot; /&gt;
&lt;meta name=&quot;apple-mobile-web-app-capable&quot; content=&quot;yes&quot; /&gt;
</code></pre><p>##常见的 iPhone 和 Android 屏幕参数。</p>
<ul>
<li>设备         分辨率        设备像素比率</li>
<li>Android LDPI  320×240 0.75</li>
<li>Iphone 3 &amp; Android MDPI   320×480 1</li>
<li>Android HDPI  480×800 1.5</li>
<li>Iphone 4  960×640 2.0</li>
</ul>
<p>iPhone 4的一个 CSS 像素实际上表现为一块 2×2 的像素。所以图片像是被放大2倍一样，模糊不清晰。</p>
<p>解决办法：</p>
<p>1、页面引用</p>
<pre><code>&lt;link rel=&quot;stylesheet&quot; media=&quot;screen and (-webkit-device-pixel-ratio: 0.75)&quot; href=&quot;ldpi.css&quot; /&gt;
&lt;link rel=&quot;stylesheet&quot; media=&quot;screen and (-webkit-device-pixel-ratio: 1.0)&quot; href=&quot;mdpi.css&quot; /&gt;
&lt;link rel=&quot;stylesheet&quot; media=&quot;screen and (-webkit-device-pixel-ratio: 1.5)&quot; href=&quot;hdpi.css&quot; /&gt;
&lt;link rel=&quot;stylesheet&quot; media=&quot;screen and (-webkit-device-pixel-ratio: 2.0)&quot; href=&quot;retina.css&quot; /&gt;
</code></pre><p>2、CSS文件里</p>
<pre><code>#header {
    background:url(mdpi/bg.png);
}

@media screen and (-webkit-device-pixel-ratio: 1.5) {
    /*CSS for high-density screens*/
    #header {
        background:url(hdpi/bg.png);
    }
}
</code></pre><p>##移动 Web 开发经验技巧</p>
<p>###点击与click事件</p>
<p>对于a标记的点击导航，默认是在onclick事件中处理的。而移动客户端对onclick的响应相比PC浏览器有着明显的几百毫秒延迟。</p>
<p>在移动浏览器中对触摸事件的响应顺序应当是： </p>
<pre><code>ontouchstart -&gt; ontouchmove -&gt; ontouchend -&gt; onclick
</code></pre><p>因此，如果确实要加快对点击事件的响应，就应当绑定ontouchend事件。</p>
<p>使用click会出现绑定点击区域闪一下的情况，解决：给该元素一个样式如下</p>
<pre><code>-webkit-tap-highlight-color: rgba(0,0,0,0);
</code></pre><p>如果不使用click，也不能简单的用touchstart或touchend替代，需要用touchstart的模拟一个click事件，并且不能发生touchmove事件，或者用zepto中的tap（轻击）事件。</p>
<pre><code>body
{
    -webkit-overflow-scrolling: touch;
}
</code></pre><p>用iphone或ipad浏览很长的网页滚动时的滑动效果很不错吧？不过如果是一个div，然后设置 <code>height:200px;overflow:auto;</code>的话，可以滚动但是完全没有那滑动效果，很郁闷吧？</p>
<p>我看到很多网站为了实现这一效果，用了第三方类库，最常用的是iscroll（包括新浪手机页，百度等）<br>我一开始也使用，不过自从用了<code>-webkit-overflow-scrolling: touch;</code>样式后，就完全可以抛弃第三方类库了，把它加在body{}区域，所有的overflow需要滚动的都可以生效了。</p>
<p>另外有一篇比较全的移动端点击解决方案 <a href="http://www.zhihu.com/question/28979857" target="_blank" rel="external">http://www.zhihu.com/question/28979857</a></p>
<p>###锁定 viewport</p>
<pre><code>ontouchmove=&quot;event.preventDefault()&quot; //锁定viewport，任何屏幕操作不移动用户界面（弹出键盘除外）。
</code></pre><p>###利用 Media Query监听</p>
<p>Media Query 相信大部分人已经使用过了。其实 JavaScript可以配合 Media Query这么用：</p>
<pre><code>var mql = window.matchMedia(&quot;(orientation: portrait)&quot;);
mql.addListener(handleOrientationChange);
handleOrientationChange(mql); 
function handleOrientationChange(mql) {
  if (mql.matches) {
    alert(&apos;The device is currently in portrait orientation &apos;)
  } else {
    alert(&apos;The device is currently in landscape orientation&apos;)
  }}
</code></pre><p>借助了 Media Query 接口做的事件监听，所以很强大！</p>
<p>也可以通过获取 CSS 值来使用 Media Query 判断设备情况，详情请看：<a href="http://yujiangshui.com/use-javascript-css-media-queries-detect-device-state/" target="_blank" rel="external">JavaScript 依据 CSS Media Queries 判断设备的方法</a>。</p>
<p>###rem最佳实践</p>
<p>rem是非常好用的一个属性，可以根据html来设定基准值，而且兼容性也很不错。不过有的时候还是需要对一些莫名其妙的浏览器优雅降级。以下是两个实践</p>
<ol>
<li><a href="http://jsbin.com/vaqexuge/4/edit" target="_blank" rel="external">http://jsbin.com/vaqexuge/4/edit</a>  这有个demo，发现chrome当font-size小于12时，rem会按照12来计算。因此设置基准值要考虑这一点</li>
<li>可以用以下的代码片段保证在低端浏览器下也不会出问题</li>
</ol>
<pre><code>html { font-size: 62.5%; } 
body { font-size: 14px; font-size: 1.4rem; } /* =14px */
h1   { font-size: 24px; font-size: 2.4rem; } /* =24px */
</code></pre><p>###被点击元素的外观变化，可以使用样式来设定：</p>
<pre><code>-webkit-tap-highlight-color: 颜色
</code></pre><p>###检测判断 iPhone/iPod<br>开发特定设备的移动网站，首先要做的就是设备侦测了。下面是使用Javascript侦测iPhone/iPod的UA，然后转向到专属的URL。</p>
<pre><code>if((navigator.userAgent.match(/iPhone/i)) || (navigator.userAgent.match(/iPod/i))) {
　　if (document.cookie.indexOf(&quot;iphone_redirect=false&quot;) == -1) {
　　　　window.location = &quot;http://m.example.com&quot;;
　　}
}
</code></pre><p>虽然Javascript是可以在水果设备上运行的，但是用户还是可以禁用。它也会造成客户端刷新和额外的数据传输，所以下面是服务器端侦测和转向：</p>
<pre><code>if(strstr($_SERVER[&apos;HTTP_USER_AGENT&apos;],&apos;iPhone&apos;) || strstr($_SERVER[&apos;HTTP_USER_AGENT&apos;],&apos;iPod&apos;)) {
　　header(&apos;Location: http://yoursite.com/iphone&apos;);
　　exit();
}
</code></pre><p>###阻止旋转屏幕时自动调整字体大小</p>
<pre><code>html, body, form, fieldset, p, div, h1, h2, h3, h4, h5, h6 {-webkit-text-size-adjust:none;}
</code></pre><p>###模拟:hover伪类<br>因为iPhone并没有鼠标指针，所以没有hover事件。那么CSS :hover伪类就没用了。但是iPhone有Touch事件，onTouchStart 类似 onMouseOver，onTouchEnd 类似 onMouseOut。所以我们可以用它来模拟hover。使用Javascript：</p>
<pre><code>var myLinks = document.getElementsByTagName(&apos;a&apos;);
for(var i = 0; i &lt; myLinks.length; i++){
　　myLinks[i].addEventListener(’touchstart’, function(){this.className = “hover”;}, false);
　　myLinks[i].addEventListener(’touchend’, function(){this.className = “”;}, false);
}
</code></pre><p>然后用CSS增加hover效果：</p>
<pre><code>a:hover, a.hover { /* 你的hover效果 */ }
</code></pre><p>这样设计一个链接，感觉可以更像按钮。并且，这个模拟可以用在任何元素上。</p>
<p>###Flexbox 布局</p>
<p> <a href="http://jsbin.com/ibuwol/2/edit" title="article5" target="_blank" rel="external">Flex 模板和实例</a></p>
<p> <a href="http://www.w3cplus.com/blog/666.html" title="article6" target="_blank" rel="external">深入了解 Flexbox 伸缩盒模型</a></p>
<p> <a href="http://yehao.diandian.com/post/2013-09-15/40052216426" target="_blank" rel="external">CSS Flexbox Intro</a></p>
<p><a href="http://www.w3.org/TR/css3-flexbox/" target="_blank" rel="external">http://www.w3.org/TR/css3-flexbox/</a></p>
<p>###居中问题</p>
<p>居中是移动端跟pc端共同的噩梦。这里有两种兼容性比较好的新方案。</p>
<ul>
<li><p>table布局法</p>
<p>  .box{</p>
<pre><code>text-align:center; 
display:table-cell;
vertical-align:middle;
</code></pre><p>  }</p>
</li>
<li><p>老版本flex布局法</p>
<p>  .box{</p>
<pre><code>display:-webkit-box;
-webkit-box-pack: center;
-webkit-box-align: center; 
text-align:center;
</code></pre><p>  }</p>
</li>
</ul>
<p>以上两种其实分别是retchat跟ionic的布局基石。</p>
<p>这里有更详细的更多的选择<a href="http://www.zhouwenbin.com/%E5%9E%82%E7%9B%B4%E5%B1%85%E4%B8%AD%E7%9A%84%E5%87%A0%E7%A7%8D%E6%96%B9%E6%B3%95/" target="_blank" rel="external">http://www.zhouwenbin.com/%E5%9E%82%E7%9B%B4%E5%B1%85%E4%B8%AD%E7%9A%84%E5%87%A0%E7%A7%8D%E6%96%B9%E6%B3%95/</a> 来自周文彬的博客</p>
<p>###移动端实现标题文字截断<br><a href="http://www.75team.com/archives/611" target="_blank" rel="external">http://www.75team.com/archives/611</a></p>
<p>###处理 Retina 双倍屏幕</p>
<p> <a href="http://miekd.com/articles/using-css-sprites-to-optimize-your-website-for-retina-displays/" title="article5" target="_blank" rel="external">（经典）Using CSS Sprites to optimize your website for Retina Displays</a></p>
<p> <a href="http://www.w3cplus.com/css/css-background-size-graphics.html" title="article5" target="_blank" rel="external">使用CSS3的background-size优化苹果的Retina屏幕的图像显示</a> </p>
<p> <a href="http://www.w3cplus.com/css/using-css-sprites-to-optimize-your-website-for-retina-displays.html" title="article5" target="_blank" rel="external">使用 CSS sprites 来优化你的网站在 Retina 屏幕下显示</a> </p>
<p> <a href="http://alexthorpe.com/uncategorized/css-sprites-for-retina-display-devices/683/" title="article5" target="_blank" rel="external">（案例）CSS IMAGE SPRITES FOR RETINA (HIRES) DEVICES</a> </p>
<p>###input类型为date情况下不支持placeholder（来自于江水）</p>
<p>这其实是浏览器自己的处理。因为浏览器会针对此类型 input 增加 datepicker 模块。</p>
<p>对 input type date 使用 placeholder 的目的是为了让用户更准确的输入日期格式，iOS 上会有 datepicker 不会显示 placeholder 文字，但是为了统一表单外观，往往需要显示。Android 部分机型没有 datepicker 也不会显示 placeholder 文字。</p>
<p>桌面端（Mac）</p>
<ul>
<li>Safari 不支持 datepicker，placeholder 正常显示。</li>
<li>Firefox 不支持 datepicker，placeholder 正常显示。</li>
<li>Chrome 支持 datepicker，显示 年、月、日 格式，忽略 placeholder。</li>
</ul>
<p>移动端</p>
<ul>
<li>iPhone5 iOS7 有 datepicker 功能，但是不显示 placeholder。</li>
<li>Andorid 4.0.4 无 datepicker 功能，不显示 placeholder</li>
</ul>
<p>解决方法：</p>
<pre><code>&lt;input placeholder=&quot;Date&quot; class=&quot;textbox-n&quot; type=&quot;text&quot; onfocus=&quot;(this.type=&apos;date&apos;)&quot;  id=&quot;date&quot;&gt; 
</code></pre><p>因为text是支持placeholder的。因此当用户focus的时候自动把type类型改变为date，这样既有placeholder也有datepicker了</p>
<p>###判断照片的横竖排列</p>
<p>有这样一种需求，需要判断用户照片是横着拍出来的还是竖着拍出来的，这里需要使用照片得exif信息：</p>
<pre><code>$(&quot;input&quot;).change(function() {
    var file = this.files[0];
    fr   = new FileReader;

    fr.onloadend = function() {
        var exif = EXIF.readFromBinaryFile(new BinaryFile(this.result));
        alert(exif.Orientation);
    };

    fr.readAsBinaryString(file);
});
</code></pre><p>可以使用这两个库 来取exif信息<a href="http://www.nihilogic.dk/labs/binaryajax/binaryajax.js" target="_blank" rel="external">http://www.nihilogic.dk/labs/binaryajax/binaryajax.js</a> <a href="http://www.nihilogic.dk/labs/exif/exif.js" target="_blank" rel="external">http://www.nihilogic.dk/labs/exif/exif.js</a></p>
<p>###Android上当viewport的width大于device-width时出现文字无故折行的解决办法</p>
<p><a href="http://www.iunbug.com/archives/2013/04/23/798.html" target="_blank" rel="external">http://www.iunbug.com/archives/2013/04/23/798.html</a></p>
<p>###白屏解决与优化方案</p>
<p>当前很多无线页面都使用前端模板进行数据渲染，那么在糟糕的网速情况下，一进去页面，看到的不是白屏就是 loading，这成为白屏问题。</p>
<p>此问题发生的原因基本可以归结为网速跟静态资源 </p>
<p>1、css文件加载需要一些时间，在加载的过程中页面是空白的。 解决：可以考虑将css代码前置和内联。<br>2、首屏无实际的数据内容，等待异步加载数据再渲染页面导致白屏。 解决：在首屏直接同步渲染html，后续的滚屏等再采用异步请求数据和渲染html。<br>3、首屏内联js的执行会阻塞页面的渲染。 解决：尽量不在首屏html代码中放置内联脚本。（来自翔歌）</p>
<p>解决方案</p>
<p>根本原因是客户端渲染的无力，因此最简单的方法是在服务器端，使用模板引擎渲染所有页面。同时</p>
<p> 1减少文件加载体积，如html压缩，js压缩<br> 2加快js执行速度 比如常见的无限滚动的页面，可以使用js先渲染一个屏幕范围内的东西<br> 3提供一些友好的交互，比如提供一些假的滚动条<br> 4使用本地存储处理静态文件。</p>
<p>###如何实现打开已安装的app，若未安装则引导用户安装?</p>
<p>来自 <a href="http://gallery.kissyui.com/redirectToNative/1.2/guide/index.html" target="_blank" rel="external">http://gallery.kissyui.com/redirectToNative/1.2/guide/index.html</a> kissy mobile<br>通过iframe src发送请求打开app自定义url scheme，如taobao://home（淘宝首页） 、etao://scan（一淘扫描）);<br>如果安装了客户端则会直接唤起，直接唤起后，之前浏览器窗口（或者扫码工具的webview）推入后台；<br>如果在指定的时间内客户端没有被唤起，则js重定向到app下载地址。<br>大概实现代码如下    </p>
<pre><code>goToNative:function(){

    if(!body) {
            setTimeout(function(){
                doc.body.appendChild(iframe);
            }, 0);
        } else {
            body.appendChild(iframe);
        }

setTimeout(function() {
            doc.body.removeChild(iframe);
            gotoDownload(startTime);//去下载，下载链接一般是itunes app store或者apk文件链接
            /**
             * 测试时间设置小于800ms时，在android下的UC浏览器会打开native app时并下载apk，
             * 测试android+UC下打开native的时间最好大于800ms;
             */
        }, 800);
}
</code></pre><p>需要注意的是 如果是android chrome 25版本以后，在iframe src不会发送请求，<br>原因如下<a href="https://developers.google.com/chrome/mobile/docs/intents" target="_blank" rel="external">https://developers.google.com/chrome/mobile/docs/intents</a> ，通过location href使用intent机制拉起客户端可行并且当前页面不跳转。</p>
<pre><code>window.location = &apos;intent://&apos; + schemeUrl + &apos;#Intent;scheme=&apos; + scheme + &apos;;package=&apos; + self.package + &apos;;end&apos;;
</code></pre><p>补充一个来自三水清的详细讲解 <a href="http://js8.in/2013/12/16/ios%E4%BD%BF%E7%94%A8schema%E5%8D%8F%E8%AE%AE%E8%B0%83%E8%B5%B7app/" target="_blank" rel="external">http://js8.in/2013/12/16/ios%E4%BD%BF%E7%94%A8schema%E5%8D%8F%E8%AE%AE%E8%B0%83%E8%B5%B7app/</a></p>
<p>###active的兼容(来自薛端阳)</p>
<p>今天发现，要让a链接的CSS active伪类生效，只需要给这个a链接的touch系列的任意事件touchstart/touchend绑定一个空的匿名方法即可hack成功</p>
<pre><code>&lt;style&gt;
a {
color: #000;
}
a:active {
color: #fff;
}
&lt;/style&gt;
&lt;a herf=”asdasd”&gt;asdasd&lt;/a&gt;
&lt;script&gt;
var a=document.getElementsByTagName(‘a’);
for(var i=0;i&lt;a.length;i++){
a[i].addEventListener(‘touchstart’,function(){},false);
}
&lt;/script&gt;
</code></pre><p>###消除transition闪屏</p>
<p>两个方法：使用css3动画的时尽量利用3D加速，从而使得动画变得流畅。动画过程中的动画闪白可以通过 backface-visibility 隐藏。</p>
<pre><code>-webkit-transform-style: preserve-3d;
/*设置内嵌的元素在 3D 空间如何呈现：保留 3D*/
-webkit-backface-visibility: hidden;
/*（设置进行转换的元素的背面在面对用户时是否可见：隐藏）*/
</code></pre><p>###测试是否支持svg图片</p>
<pre><code>document.implementation.hasFeature(&quot;http:// www.w3.org/TR/SVG11/feature#Image&quot;, &quot;1.1&quot;)
</code></pre><p>##考虑兼容“隐私模式”(from <a href="http://blog.youyo.name/archives/smarty-phones-webapp-deverlop-advance.html" target="_blank" rel="external">http://blog.youyo.name/archives/smarty-phones-webapp-deverlop-advance.html</a>)<br>ios的safari提供一种“隐私模式”，如果你的webapp考虑兼容这个模式，那么在使用html5的本地存储的一种————localStorage时，可能因为“隐私模式”下没有权限读写localstorge而使代码抛出错误，导致后续的js代码都无法运行了。</p>
<p>既然在safari的“隐私模式”下，没有调用localStorage的权限，首先想到的是先判断是否支持localStorage，代码如下：</p>
<pre><code>if(&apos;localStorage&apos; in window){
    //需要使用localStorage的代码写在这
}else{
    //不支持的提示和向下兼容代码
}
</code></pre><p>测试发现，即使在safari的“隐私模式”下，’localStorage’ in window的返回值依然为true，也就是说，if代码块内部的代码依然会运行，问题没有得到解决。<br>接下来只能相当使用try catch了，虽然这是一个不太推荐被使用的方法，使用try catch捕获错误，使后续的js代码可以继续运行，代码如下：</p>
<pre><code>try{
    if(&apos;localStorage&apos; in window){
         //需要使用localStorage的代码写在这
    }else{
         //不支持的提示和向下兼容代码
    }
}catch(e){
    // 隐私模式相关提示代码和不支持的提示和向下兼容代码
}
</code></pre><p>所以，提醒大家注意，在需要兼容ios的safari的“隐私模式”的情况下，本地存储相关的代码需要使用try catch包裹并降级兼容。</p>
<p>###安卓手机点击锁定页面效果问题</p>
<p>有些安卓手机，页面点击时会停止页面的javascript，css3动画等的执行，这个比较蛋疼。不过可以用阻止默认事件解决。详细见<br><a href="http://stackoverflow.com/questions/10246305/android-browser-touch-events-stop-display-being-updated-inc-canvas-elements-h" target="_blank" rel="external">http://stackoverflow.com/questions/10246305/android-browser-touch-events-stop-display-being-updated-inc-canvas-elements-h</a></p>
<pre><code>function touchHandlerDummy(e)
{
    e.preventDefault();
    return false;
}
document.addEventListener(&quot;touchstart&quot;, touchHandlerDummy, false);
document.addEventListener(&quot;touchmove&quot;, touchHandlerDummy, false);
document.addEventListener(&quot;touchend&quot;, touchHandlerDummy, false);
</code></pre><p>###消除ie10里面的那个叉号<br><a href="http://msdn.microsoft.com/en-us/library/windows/apps/hh767361.aspx" title="article4" target="_blank" rel="external">IE Pseudo-elements</a></p>
<pre><code>input:-ms-clear{display:none;}
</code></pre><p>###关于ios与os端字体的优化(横竖屏会出现字体加粗不一致等)<br><a href="http://blog.sina.com.cn/s/blog_6da647a601011u4v.html" title="article5" target="_blank" rel="external">mac下网页中文字体优化</a></p>
<p><a href="http://stackoverflow.com/questions/3220662/uiwebview-font-is-thinner-in-portrait-than-landscape" title="article5" target="_blank" rel="external">UIWebView font is thinner in portrait than landscape</a></p>
<p>##判断用户是否是“将网页添加到主屏后，再从主屏幕打开这个网页”的</p>
<pre><code>navigator.standalone
</code></pre><p>###隐藏地址栏 &amp; 处理事件的时候，防止滚动条出现：</p>
<pre><code>// 隐藏地址栏  &amp; 处理事件的时候 ，防止滚动条出现
addEventListener(&apos;load&apos;, function(){
        setTimeout(function(){ window.scrollTo(0, 1); }, 100);
});
</code></pre><p>###ios7 可以通过meta标签的minimal来隐藏地址栏了<br><a href="http://darkblue.sdf.org/weblog/ios-7-dot-1-mobile-safari-minimal-ui.html" target="_blank" rel="external">http://darkblue.sdf.org/weblog/ios-7-dot-1-mobile-safari-minimal-ui.html</a></p>
<p>###判断是否为iPhone：</p>
<pre><code>// 判断是否为 iPhone ：
function isAppleMobile() {
    return (navigator.platform.indexOf(&apos;iPhone&apos;) != -1);
};
</code></pre><p>###localStorage:</p>
<pre><code>var v = localStorage.getItem(&apos;n&apos;) ? localStorage.getItem(&apos;n&apos;) : &quot;&quot;;   // 如果名称是  n 的数据存在 ，则将其读出 ，赋予变量  v  。
localStorage.setItem(&apos;n&apos;, v);                                           // 写入名称为 n、值为  v  的数据
localStorage.removeItem(&apos;n&apos;);        // 删除名称为  n  的数据
</code></pre><p>###使用特殊链接：<br>如果你关闭自动识别后 ，又希望某些电话号码能够链接到 iPhone 的拨号功能 ，那么可以通过这样来声明电话链接 ,</p>
<pre><code>&lt;a href=&quot;tel:12345654321&quot;&gt;打电话给我&lt;/a&gt;
&lt;a href=&quot;sms:12345654321&quot;&gt;发短信&lt;/a&gt;
</code></pre><p>或用于单元格：</p>
<pre><code>&lt;td onclick=&quot;location.href=&apos;tel:122&apos;&quot;&gt;
</code></pre><p>###自动大写与自动修正<br>要关闭这两项功能，可以通过autocapitalize 与autocorrect 这两个选项：</p>
<pre><code>&lt;input type=&quot;text&quot; autocapitalize=&quot;off&quot; autocorrect=&quot;off&quot; /&gt;
</code></pre><p>###不让 Android 识别邮箱</p>
<pre><code>&lt;meta content=&quot;email=no&quot; name=&quot;format-detection&quot; /&gt;
</code></pre><p>###禁止 iOS 弹出各种操作窗口</p>
<pre><code>-webkit-touch-callout:none
</code></pre><p>###禁止用户选中文字</p>
<pre><code>-webkit-user-select:none
</code></pre><p>###动画效果中，使用 translate 比使用定位性能高</p>
<p><a href="http://paulirish.com/2012/why-moving-elements-with-translate-is-better-than-posabs-topleft/" target="_blank" rel="external">Why Moving Elements With Translate() Is Better Than Pos:abs Top/left</a></p>
<p>###拿到滚动条</p>
<pre><code>window.scrollY
window.scrollX
</code></pre><p> 比如要绑定一个touchmove的事件，正常的情况下类似这样(来自呼吸二氧化碳)</p>
<pre><code>$(&apos;div&apos;).on(&apos;touchmove&apos;, function(){
//.….code
{});
</code></pre><p>而如果中间的code需要处理的东西多的话，fps就会下降影响程序顺滑度，而如果改成这样</p>
<pre><code>$(&apos;div&apos;).on(&apos;touchmove&apos;, function(){
setTimeout(function(){
//.….code
},0);
{});
</code></pre><p>把代码放在setTimeout中，会发现程序变快.</p>
<p>###关于 iOS 系统中，Web APP 启动图片在不同设备上的适应性设置</p>
<p><a href="http://stackoverflow.com/questions/4687698/mulitple-apple-touch-startup-image-resolutions-for-ios-web-app-esp-for-ipad/10011893#10011893" target="_blank" rel="external">http://stackoverflow.com/questions/4687698/mulitple-apple-touch-startup-image-resolutions-for-ios-web-app-esp-for-ipad/10011893#10011893</a></p>
<p>###position:sticky与position:fixed布局<br><a href="http://www.zhouwenbin.com/positionsticky-%E7%B2%98%E6%80%A7%E5%B8%83%E5%B1%80/" target="_blank" rel="external">http://www.zhouwenbin.com/positionsticky-%E7%B2%98%E6%80%A7%E5%B8%83%E5%B1%80/</a><br><a href="http://www.zhouwenbin.com/sticky%E6%A8%A1%E6%8B%9F%E9%97%AE%E9%A2%98/" target="_blank" rel="external">http://www.zhouwenbin.com/sticky%E6%A8%A1%E6%8B%9F%E9%97%AE%E9%A2%98/</a></p>
<p>###关于 iOS 系统中，中文输入法输入英文时，字母之间可能会出现一个六分之一空格<br>可以通过正则去掉 </p>
<pre><code>this.value = this.value.replace(/\u2006/g, &apos;&apos;);
</code></pre><p>###关于android webview中，input元素输入时出现的怪异情况<br>见下图</p>
<p><img src="http://cdn.bielousov.com/wp-content/uploads/2012/08/android-input-label-text-issue.png" alt="怪异图"></p>
<p>Android Web 视图,至少在 HTC EVO 和三星的 Galaxy Nexus 中，文本输入框在输入时表现的就像占位符。情况为一个类似水印的东西在用户输入区域，一旦用户开始输入便会消失(见图片)。</p>
<p>在 Android 的默认样式下当输入框获得焦点后，若存在一个绝对定位或者 fixed 的元素，布局会被破坏，其他元素与系统输入字段会发生重叠(如搜索图标将消失为搜索字段)，可以观察到布局与原始输入字段有偏差(见截图)。</p>
<p>这是一个相当复杂的问题，以下简单布局可以重现这个问题:</p>
<pre><code>&lt;label for=&quot;phone&quot;&gt;Phone: *&lt;/label&gt;
&lt;input type=&quot;tel&quot; name=&quot;phone&quot; id=&quot;phone&quot; minlength=&quot;10&quot; maxlength=&quot;10&quot; inputmode=&quot;latin digits&quot; required=&quot;required&quot; /&gt;
</code></pre><p>解决方法</p>
<pre><code>-webkit-user-modify: read-write-plaintext-only
</code></pre><p>详细参考<a href="http://www.bielousov.com/2012/android-label-text-appears-in-input-field-as-a-placeholder/" target="_blank" rel="external">http://www.bielousov.com/2012/android-label-text-appears-in-input-field-as-a-placeholder/</a><br>注意，该属性会导致中文不能输入词组，只能单个字。感谢鬼哥与飞（游勇飞）贡献此问题与解决方案</p>
<p>另外，在position:fixed后的元素里，尽量不要使用输入框。更多的bug可参考<br><a href="http://www.cosdiv.com/page/M0/S882/882353.html" target="_blank" rel="external">http://www.cosdiv.com/page/M0/S882/882353.html</a></p>
<p>依旧无法解决（摩托罗拉ME863手机），则使用input:text类型而非password类型，并设置其设置  -webkit-text-security: disc; 隐藏输入密码从而解决。</p>
<p>###JS动态生成的select下拉菜单在Android2.x版本的默认浏览器里不起作用</p>
<p>解决方法删除了overflow-x:hidden; 然后在JS生成下来菜单之后focus聚焦，这两步操作之后解决了问题。(来自岛都-小Qi)</p>
<p>参考<a href="http://stackoverflow.com/questions/4697908/html-select-control-disabled-in-android-webview-in-emulator" target="_blank" rel="external">http://stackoverflow.com/questions/4697908/html-select-control-disabled-in-android-webview-in-emulator</a></p>
<p>###Andriod 上去掉语音输入按钮</p>
<pre><code>input::-webkit-input-speech-button {display: none}
</code></pre><p>##IE10 的特殊鼠标事件</p>
<p><a href="http://www.mansonchor.com/blog/blog_detail_73.html" title="article5" target="_blank" rel="external">IE10 事件监听</a></p>
<p>##iOS 输入框最佳实践</p>
<p><a href="http://stackoverflow.com/questions/11219242/mobile-friendly-input-of-a-digits-spaces-string-a-credit-card-number" target="_blank" rel="external">Mobile-friendly input of a digits + spaces string (a credit card number)</a></p>
<p><a href="http://stackoverflow.com/questions/8216278/html5-input-type-number-vs-tel" target="_blank" rel="external">HTML5 input type number vs tel</a></p>
<p><a href="http://stackoverflow.com/questions/6178556/iphone-numeric-keyboard-for-text-input" target="_blank" rel="external">iPhone: numeric keyboard for text input</a></p>
<p><a href="https://developer.apple.com/library/ios/documentation/StringsTextFonts/Conceptual/TextAndWebiPhoneOS/KeyboardManagement/KeyboardManagement.html" target="_blank" rel="external">Text Programming Guide for iOS - Managing the Keyboard</a></p>
<p><a href="http://www.miketaylr.com/code/input-type-attr.html" target="_blank" rel="external">HTML5 inputs and attribute support</a></p>
<p>##往返缓存问题</p>
<p>点击浏览器的回退，有时候不会自动执行js，特别是在mobilesafari中。这与<strong>往返缓存(bfcache)</strong>有关系。有很多hack的处理方法，可以参考</p>
<p><a href="http://stackoverflow.com/questions/24046/the-safari-back-button-problem" target="_blank" rel="external">http://stackoverflow.com/questions/24046/the-safari-back-button-problem</a></p>
<p><a href="http://stackoverflow.com/questions/11979156/mobile-safari-back-button" target="_blank" rel="external">http://stackoverflow.com/questions/11979156/mobile-safari-back-button</a></p>
<p>##不暂停的计时器（safari的进程冻结）</p>
<p><a href="https://www.imququ.com/post/ios-none-freeze-timer.html" target="_blank" rel="external">https://www.imququ.com/post/ios-none-freeze-timer.html</a><br>或者可以用postmessage方式:<br>主页面:</p>
<pre><code>// 解决ios safari tab在后台会遭遇进程冻结问题
// http://www.apple.com/safari/#gallery-icloud-tabs
// Safari takes advantage of power-saving technologies such as App Nap, which puts background Safari tabs into a low-power state until you start using them again. In addition, Safari Power Saver conserves battery life by intelligently pausing web videos and other plug‑in content when they’re not front and center on the web pages you visit. All told, Safari on OS X Mavericks lets you browse up to an hour longer than with Chrome or Firefox.1
var work;
function startWorker() {
    if (typeof(Worker) !== &quot;undefined&quot;) {
        if (typeof(work) == &quot;undefined&quot;) {
            work = new Worker(&quot;/workers.js&quot;);
        }
        work.onmessage = function(event) {
            // document.getElementById(&quot;result-count&quot;).innerHTML = event.data.count;
            // document.getElementById(&quot;result-url&quot;).innerHTML = event.data.targetURL;
            if (target &amp;&amp; event.data.targetURL != &quot;&quot;) target.location.href = event.data.targetURL;
        };
    } else {
        console.log(&apos;does not support Web Workers...&apos;);
    }
}

function stopWorker() {
    work.terminate();
}

startWorker();
</code></pre><p>worker:</p>
<pre><code>// 解决ios safari tab在后台会遭遇进程冻结问题
// http://www.apple.com/safari/#gallery-icloud-tabs
// Safari takes advantage of power-saving technologies such as App Nap, which puts background Safari tabs into a low-power state until you start using them again. In addition, Safari Power Saver conserves battery life by intelligently pausing web videos and other plug‑in content when they’re not front and center on the web pages you visit. All told, Safari on OS X Mavericks lets you browse up to an hour longer than with Chrome or Firefox.1

importScripts(&apos;/socket.io/socket.io.js&apos;);

var count = 0,
    targetURL = &apos;&apos;
    ; 

var socket = io.connect(&apos;/&apos;);
socket.on(&apos;navigate&apos;, function (data) {
  count = count++;
  postMessage({targetURL:data.url,count:count});
});
</code></pre><p>##Web移动端Fixed布局的解决方案<br><a href="http://efe.baidu.com/blog/mobile-fixed-layout/" target="_blank" rel="external">http://efe.baidu.com/blog/mobile-fixed-layout/</a></p>
<p>##ios上background-attachment:fixed不能正常工作</p>
<p>参考 <a href="http://stackoverflow.com/questions/20443574/fixed-background-image-with-ios7" target="_blank" rel="external">http://stackoverflow.com/questions/20443574/fixed-background-image-with-ios7</a></p>
<p>##如何让音频跟视频在ios跟android上自动播放</p>
<pre><code>&lt;audio autoplay &gt;&lt;source  src=&quot;audio/alarm1.mp3&quot; type=&quot;audio/mpeg&quot;&gt;&lt;/audio&gt;
</code></pre><p>系统默认情况下 audio的autoplay属性是无法生效的，这也是手机为节省用户流量做的考虑。<br>如果必须要自动播放，有两种方式可以解决。</p>
<p>1.捕捉一次用户输入后，让音频加载，下次即可播放。</p>
<pre><code>//play and pause it once
document.addEventListener(&apos;touchstart&apos;, function () {
    document.getElementsByTagName(&apos;audio&apos;)[0].play();
    document.getElementsByTagName(&apos;audio&apos;)[0].pause();
});
</code></pre><p>这种方法需要捕获一次用户的点击事件来促使音频跟视频加载。当加载后，你就可以用javascript控制音频的播放了，如调用audio.play()</p>
<p>2.利用iframe加载资源</p>
<pre><code>var ifr=document.createElement(&quot;iframe&quot;);
ifr.setAttribute(&apos;src&apos;, &quot;http://mysite.com/myvideo.mp4&quot;);
ifr.setAttribute(&apos;width&apos;, &apos;1px&apos;);
ifr.setAttribute(&apos;height&apos;, &apos;1px&apos;);
ifr.setAttribute(&apos;scrolling&apos;, &apos;no&apos;);
ifr.style.border=&quot;0px&quot;;
document.body.appendChild(ifr);
</code></pre><p>这种方式其实跟第一种原理是一样的。当资源加载了你就可以控制播放了，但是这里使用iframe来加载，相当于直接触发资源加载。<br>注意，使用创建audio标签并让其加载的方式是不可行的。<br>慎用这种方法，会对用户造成很糟糕的影响。。</p>
<p>##iOS 6 跟 iPhone 5 的那些事</p>
<p>###IP5 的媒体查询</p>
<pre><code>@media (device-height: 568px) and (-webkit-min-device-pixel-ratio: 2) {

/* iPhone 5 or iPod Touch 5th generation */

}
</code></pre><p>###使用媒体查询，提供不同的启动图片：</p>
<pre><code>&lt;link href=&quot;startup-568h.png&quot; rel=&quot;apple-touch-startup-image&quot; media=&quot;(device-height: 568px)&quot;&gt;
&lt;link href=&quot;startup.png&quot; rel=&quot;apple-touch-startup-image&quot; sizes=&quot;640x920&quot; media=&quot;(device-height: 480px)&quot;&gt;
</code></pre><p>###拍照上传</p>
<pre><code>&lt;input type=file accept=&quot;video/*&quot;&gt;
&lt;input type=file accept=&quot;image/*&quot;&gt;
</code></pre><p>不支持其他类型的文件 ，如音频，Pages文档或PDF文件。 也没有getUserMedia摄像头的实时流媒体支持。</p>
<p>###可以使用的 HTML5 高级 api</p>
<ul>
<li>multipart POST 表单提交上传</li>
<li>XMLHttpRequest 2 AJAX 上传（甚至进度支持）</li>
<li>文件 API ，在 iOS 6 允许 JavaScript 直接读取的字节数和客户端操作文件。</li>
</ul>
<p>###智能应用程序横幅</p>
<p>有了智能应用程序横幅，当网站上有一个相关联的本机应用程序时，Safari浏览器可以显示一个横幅。 如果用户没有安装这个应用程序将显示“安装”按钮，或已经安装的显示“查看”按钮可打开它。</p>
<p>在 iTunes Link Maker 搜索我们的应用程序和应用程序ID。</p>
<pre><code>&lt;meta name=&quot;apple-itunes-app&quot; content=&quot;app-id=9999999&quot;&gt;
</code></pre><p>可以使用 app-argument 提供字符串值，如果参加iTunes联盟计划，可以添加元标记数据 </p>
<pre><code>&lt;meta name=&quot;apple-itunes-app&quot; content=&quot;app-id=9999999, app-argument=xxxxxx&quot;&gt;

&lt;meta name=&quot;apple-itunes-app&quot; content=&quot;app-id=9999999, app-argument=xxxxxx, affiliate-data=partnerId=99&amp;siteID=XXXX&quot;&gt;
</code></pre><p>横幅需要156像素（设备是312 hi-dpi）在顶部，直到用户在下方点击内容或关闭按钮，你的网站才会展现全部的高度。 它就像HTML的DOM对象，但它不是一个真正的DOM。 </p>
<p>CSS3 滤镜</p>
<pre><code>-webkit-filter: blur(5px) grayscale (.5) opacity(0.66) hue-rotate(100deg);
</code></pre><p>交叉淡变</p>
<pre><code>background-image: -webkit-cross-fade(url(&quot;logo1.png&quot;), url(&quot;logo2.png&quot;), 50%);
</code></pre><p>Safari中的全屏幕</p>
<p>除了chrome-less 主屏幕meta标签，现在的iPhone和iPod Touch（而不是在iPad）支持全屏幕模式的窗口。 没有办法强制全屏模式，它需要由用户启动（工具栏上的最后一个图标）。需要引导用户按下屏幕上的全屏图标来激活全屏效果。 可以使用onresize事件检测是否用户切换到全屏幕。</p>
<p>支持requestAnimationFrameAPI</p>
<p>支持image-set,retina屏幕的利器</p>
<pre><code>-webkit-image-set(url(low.png) 1x, url(hi.jpg) 2x)
</code></pre><p>应用程序缓存限制增加至25MB。</p>
<p>Web View（pseudobrowsers，PhoneGap/Cordova应用程序，嵌入式浏览器） 上Javascript运行比Safari慢3.3倍（或者说，Nitro引擎在Safari浏览器是Web应用程序是3.3倍速度）。</p>
<p>autocomplete属性的输入遵循DOM规范</p>
<p>来自DOM4的Mutation Observers已经实现。 您可以使用WebKitMutationObserver构造器捕获DOM的变化</p>
<p>Safari不再总是对用 -webkit-transform:preserve-3d 的元素创建硬件加速</p>
<p>支持window.selection 的Selection API</p>
<p>Canvas更新 ：createImageData有一个参数，现在有两个新的功能做好准备，用webkitGetImageDataHD和webkitPutImageDataHD提供高分辨率图像 。</p>
<p>更新SVG处理器和事件构造函数</p>
<p>##IOS7的大更新</p>
<p><a href="http://jinlong.github.io/blog/2013/09/23/safari-ios7-html5-problems-apis-review/#jtss-tsina" target="_blank" rel="external">iOS 7 的 Safari 和 HTML5：问题，变化和新 API</a>(张金龙翻译)</p>
<p><a href="http://www.sencha.com/blog/the-html5-scorecard-the-good-the-bad-and-the-ugly-in-ios7" title="ios7的一些bug" target="_blank" rel="external">iOS 7 的一些坑(英文)</a></p>
<p><a href="http://www.mobilexweb.com/blog/safari-ios7-html5-problems-apis-review" title="ios7的一些bug" target="_blank" rel="external">ios7的一些坑2(英文)</a></p>
<p>##webview相关</p>
<p>#Cache开启和设置</p>
<pre><code>browser.getSettings().setAppCacheEnabled(true);
browser.getSettings().setAppCachePath(&quot;/data/data/[com.packagename]/cache&quot;);
browser.getSettings().setAppCacheMaxSize(5*1024*1024); // 5MB
</code></pre><p>#LocalStorage相关设置</p>
<pre><code>browser.getSettings().setDatabaseEnabled(true);
browser.getSettings().setDomStorageEnabled(true);
String databasePath = browser.getContext().getDir(&quot;databases&quot;, Context.MODE_PRIVATE).getPath();
browser.getSettings().setDatabasePath(databasePath);//Android　webview的LocalStorage有个问题，关闭APP或者重启后，就清楚了，所以需要browser.getSettings().setDatabase相关的操作，把LocalStoarge存到DB中

myWebView.setWebChromeClient(new WebChromeClient(){
　　　 @Override
　　　 public void onExceededDatabaseQuota(String url, String databaseIdentifier, long currentQuota, long estimatedSize, long totalUsedQuota, WebStorage.QuotaUpdater quotaUpdater)
　　　 {
　　　　　　　 quotaUpdater.updateQuota(estimatedSize * 2);
　　　 }
}
</code></pre><p>#浏览器自带缩放按钮取消显示</p>
<pre><code>browser.getSettings().setBuiltInZoomControls(false);
</code></pre><p>#几个比较好的实践</p>
<p>使用localstorage缓存html</p>
<p>使用lazyload，还要记得lazyload占位图虽然小，但是最好能提前加载到缓存</p>
<p>延时加载执行js</p>
<p>主要原因就在于Android Webview的onPageFinished事件，Android端一般是用这个事件来标识页面加载完成并显示的，也就是说在此之前，会一直loading，但是Android的OnPageFinished事件会在Javascript脚本执行完成之后才会触发。如果在页面中使用JQuery，会在处理完DOM对象，执行完$(document).ready(function() {});事件自会后才会渲染并显示页面。</p>
<p>###manifest与缓存相关:<br><a href="http://www.alloyteam.com/2013/12/web-cache-6-hybrid-app-tailored-cache/" target="_blank" rel="external">http://www.alloyteam.com/2013/12/web-cache-6-hybrid-app-tailored-cache/</a><br>相关解决方案<br><a href="http://mt.tencent.com/" target="_blank" rel="external">http://mt.tencent.com/</a></p>
<p>##移动端调适篇</p>
<p>###手机抓包与配host</p>
<p>在PC上，我们可以很方便地配host，但是手机上如何配host，这是一个问题。</p>
<p>这里主要使用fiddler和远程代理，实现手机配host的操作，具体操作如下：</p>
<p>首先，保证PC和移动设备在同一个局域网下；</p>
<p>PC上开启fiddler，并在设置中勾选“allow remote computers to connect”</p>
<ol>
<li><p>首先，保证PC和移动设备在同一个局域网下；</p>
</li>
<li><p>PC上开启fiddler，并在设置中勾选“allow remote computers to connect”<br><img src="https://github.com/hoosin/mobile-web-favorites/raw/master/img/01.png" alt="fiddler"></p>
</li>
<li><p>手机上设置代理，代理IP为PC的IP地址，端口为8888（这是fiddler的默认端口）。通常手机上可以直接设置代理，如果没有，可以去下载一个叫ProxyDroid的APP来实现代理的设置。</p>
</li>
<li><p>此时你会发现，用手机上网，走的其实是PC上的fiddler，所有的请求包都会在fiddler中列出来，配合willow使用，即可实现配host，甚至是反向代理的操作。</p>
</li>
</ol>
<p>也可以用CCProxy之类软件，还有一种方法就是买一个随身wifi，然后手机连接就可以了！</p>
<p>###高级抓包</p>
<p><a href="http://danqingdani.blog.163.com/blog/static/1860941952012112353515306/?suggestedreading&amp;wumii" title="iPhone上使用Burp Suite捕捉HTTPS通信包方法" target="_blank" rel="external">iPhone上使用Burp Suite捕捉HTTPS通信包方法</a></p>
<p><a href="http://danqingdani.blog.163.com/blog/static/1860941952012101331848980/" title="mobile app 通信分析方法小议（iOS/Android)" target="_blank" rel="external">mobile app 通信分析方法小议（iOS/Android)</a></p>
<p><a href="http://danqingdani.blog.163.com/blog/static/1860941952012111954741585/" title="实时抓取移动设备上的通信包(ADVsock2pipe+Wireshark+nc+tcpdump)" target="_blank" rel="external">实时抓取移动设备上的通信包(ADVsock2pipe+Wireshark+nc+tcpdump)</a></p>
<p>###静态资源缓存问题</p>
<p>一般用代理软件代理过来的静态资源可以设置nocache避免缓存，但是有的手机比较诡异，会一直缓存住css等资源文件。由于静态资源一般都是用版本号管理的，我们以charles为例子来处理这个问题</p>
<p>charles 选择静态的html页面文件-saveResponse。之后把这个文件保存一下，修改一下版本号。之后继续发请求，<br>刚才的html页面文件 右键选择 –map local 选择我们修改过版本号的html文件即ok。这其实也是fiddler远程映射并修改文件的一个应用场景。</p>
<p>###安卓模拟器和真机区别</p>
<p><a href="http://www.farsight.com.cn/news/emb105.htm" target="_blank" rel="external">http://www.farsight.com.cn/news/emb105.htm</a></p>
<p><a href="http://testerhome.com/topics/388" target="_blank" rel="external">http://testerhome.com/topics/388</a></p>
<p><a href="http://www.cnblogs.com/zdz8207/archive/2012/01/30/2332436.html" target="_blank" rel="external">http://www.cnblogs.com/zdz8207/archive/2012/01/30/2332436.html</a></p>
<p>##移动浏览器篇</p>
<p>###微信浏览器</p>
<p>微信浏览器的各种bug汇总 （x5内核） <a href="http://www.qianduan.net/qqliu-lan-qi-x5nei-he-wen-ti-hui-zong/" target="_blank" rel="external">http://www.qianduan.net/qqliu-lan-qi-x5nei-he-wen-ti-hui-zong/</a></p>
<p>因为微信浏览器屏蔽了一部分链接图片，所以需要引导用户去打开新页面，可以用以下方式判断微信浏览器的ua</p>
<pre><code>function is_weixn(){
    var ua = navigator.userAgent.toLowerCase();
    if(ua.match(/MicroMessenger/i)==&quot;micromessenger&quot;) {
        return true;
    } else {
        return false;
    }
}
</code></pre><p>后端判断也很简单，比如php</p>
<pre><code>function is_weixin(){
    if ( strpos($_SERVER[&apos;HTTP_USER_AGENT&apos;], &apos;MicroMessenger&apos;) !== false ) {
            return true;
    }  
    return false;
}
</code></pre><p><a href="https://github.com/maxzhang/maxzhang.github.com/issues/31" target="_blank" rel="external">https://github.com/maxzhang/maxzhang.github.com/issues/31</a> 微信浏览器踩坑，来自maxZhang <a href="https://github.com/maxzhang" target="_blank" rel="external">https://github.com/maxzhang</a></p>
<p>###【UC浏览器】video标签脱离文档流</p>
<p>场景：<video>标签的父元素(祖辈元素)设置transform样式后，<video>标签会脱离文档流。</video></video></p>
<p>测试环境：UC浏览器 8.7/8.6 + Android 2.3/4.0 。</p>
<p>Demo：<a href="http://t.cn/zj3xiyu" target="_blank" rel="external">http://t.cn/zj3xiyu</a></p>
<p>解决方案：不使用transform属性。translate用top、margin等属性替代。</p>
<p>###【UC浏览器】video标签总在最前</p>
<p>场景：<video>标签总是在最前（可以理解为video标签的z-index属性是Max）。</video></p>
<p>测试环境：UC浏览器 8.7/8.6 + Android 2.3/4.0 。</p>
<p>###【UC浏览器】position:fixed 属性在UC浏览器的奇葩现象</p>
<p>场景：设置了position: fixed 的元素会遮挡z-index值更高的同辈元素。</p>
<p>　　　在8.6的版本,这个情况直接出现。</p>
<p>　　　在8.7之后的版本,当同辈元素的height大于713这个「神奇」的数值时,才会被遮挡。</p>
<p>测试环境：UC浏览器 8.8_beta/8.7/8.6 + Android 2.3/4.0 。</p>
<p>Demo：<a href="http://t.cn/zYLTSg6" target="_blank" rel="external">http://t.cn/zYLTSg6</a></p>
<p>###【QQ手机浏览器】不支持HttpOnly</p>
<p>场景：带有HttpOnly属性的Cookie，在QQ手机浏览器版本从4.0开始失效。JavaScript可以直接读取设置了HttpOnly的Cookie值。</p>
<p>测试环境：QQ手机浏览器 4.0/4.1/4.2 + Android 4.0 。</p>
<p>###【MIUI原生浏览器】浏览器地址栏hash不改变</p>
<p>场景：location.hash 被赋值后，地址栏的地址不会改变。</p>
<p>　　　但实际上 location.href 已经更新了，通过JavaScript可以顺利获取到更新后的地址。</p>
<p>　　　虽然不影响正常访问，但用户无法将访问过程中改变hash后的地址存为书签。</p>
<p>测试环境：MIUI 4.0</p>
<p>###【Chrome Mobile】fixed元素无法点击</p>
<p> 场景：父元素设置position: fixed;</p>
<p>　　　子元素设置position: absolute;</p>
<p>　　　此时，如果父元素/子元素还设置了overflow: hidden 则出现“父元素遮挡该子元素“的bug。</p>
<p>　　　视觉(view)层并没有出现遮挡，只是无法触发绑定在该子元素上的事件。可理解为：「看到点不到」。</p>
<p>补充： 页面往下滚动，触发position: fixed;的特性时，才会出现这个bug，在最顶不会出现。</p>
<p>测试平台： 小米1S，Android4.0的Chrome18</p>
<p>demo： <a href="http://maplejan.sinaapp.com/demo/fixed_chromemobile.html" target="_blank" rel="external">http://maplejan.sinaapp.com/demo/fixed_chromemobile.html</a></p>
<p>解决办法： 把父元素和子元素的overflow: hidden去掉。</p>
<p>以上来源于  <a href="http://www.cnblogs.com/maplejan/archive/2013/04/26/3045928.html" target="_blank" rel="external">http://www.cnblogs.com/maplejan/archive/2013/04/26/3045928.html</a></p>
<p>##库的使用实践</p>
<p>###zepto.js</p>
<p> <a href="http://chaoskeh.com/blog/some-experience-of-using-zepto.html" title="zepto" target="_blank" rel="external">zepto的一篇使用注意点讲解</a></p>
<p> <a href="http://blog.youyo.name/archives/zepto-tap-click-through-research.html" title="zepto" target="_blank" rel="external">zepto的著名的tap“点透”bug</a></p>
<p> <a href="http://www.cnblogs.com/sky000/archive/2013/03/29/2988952.html" title="zepto" target="_blank" rel="external">zepto源码注释</a></p>
<p>###使用zeptojs内嵌到android webview影响正常滚动时<br><a href="https://github.com/madrobby/zepto/blob/master/src/touch.js" target="_blank" rel="external">https://github.com/madrobby/zepto/blob/master/src/touch.js</a> 去掉61行,其实就是使用原生的滚动</p>
<p>###iscroll4</p>
<p> iscroll4 的几个bug(来自 <a href="http://www.mansonchor.com/blog/blog_detail_64.html" target="_blank" rel="external">http://www.mansonchor.com/blog/blog_detail_64.html</a> 内有详细讲解)</p>
<p>1.滚动容器点击input框、select等表单元素时没有响应】</p>
<pre><code>onBeforeScrollStart: function (e) { e.preventDefault(); }
</code></pre><p>改为</p>
<pre><code>onBeforeScrollStart: function (e) { var nodeType = e.explicitOriginalTarget © e.explicitOriginalTarget.nodeName.toLowerCase():(e.target © e.target.nodeName.toLowerCase():&apos;&apos;);if(nodeType !=&apos;select&apos;&amp;&amp; nodeType !=&apos;option&apos;&amp;&amp; nodeType !=&apos;input&apos;&amp;&amp; nodeType!=&apos;textarea&apos;) e.preventDefault(); }
</code></pre><p>2.往iscroll容器内添加内容时，容器闪动的bug</p>
<p>源代码的</p>
<pre><code>has3d = &apos;WebKitCSSMatrix&apos; in window &amp;&amp; &apos;m11&apos; in new WebKitCSSMatrix()
</code></pre><p>改成</p>
<pre><code>has3d = false
</code></pre><p>在配置iscroll时，useTransition设置成false</p>
<p>3.过长的滚动内容，导致卡顿和app直接闪退</p>
<ol>
<li>不要使用checkDOMChanges。虽然checkDOMChanges很方便，定时检测容器长度是否变化来refresh，但这也意味着你要消耗一个Interval的内存空间</li>
<li>隐藏iscroll滚动条，配置时设置hScrollbar和vScrollbar为false。</li>
<li>不得已的情况下，去掉各种效果，momentum、useTransform、useTransition都设置为false</li>
</ol>
<p>4.左右滚动时，不能正确响应正文上下拉动</p>
<p>iscroll的闪动问题也与渲染有关系，可以参考<br> <a href="http://www.iunbug.com/archives/2012/09/19/411.html" title="iscroll4" target="_blank" rel="external">运用webkit绘制渲染页面原理解决iscroll4闪动的问题</a><br><a href="http://blog.csdn.net/gcz564539969/article/details/9156141" title="iscroll5" target="_blank" rel="external">iscroll4升级到5要注意的问题</a></p>
<p>###iscroll或者滚动类框架滚动时不点击的方法</p>
<p>可以使用以下的解决方案(利用data-setapi)</p>
<pre><code>&lt;a ontouchmove=&quot;this.s=1&quot; ontouchend=&quot;this.s || window.open(this.dataset.href),this.s=0&quot; target=&quot;_blank&quot; data-href=&quot;http://www.hao123.com/topic/pig&quot;&gt;黄浦江死猪之谜&lt;/a&gt;
</code></pre><p>也可以用这种方法</p>
<pre><code>$(document).delegate(&apos;[data-target]&apos;, &apos;touchmove&apos;, function () {
    $(this).attr(&apos;moving&apos;,&apos;moving&apos;);

})


$(document).delegate(&apos;[data-target]&apos;, &apos;touchend&apos;, function () {
    if ($(this).attr(&apos;moving&apos;) !== &apos;moving&apos;) {
     //做你想做的。。
        $(this).attr(&apos;moving&apos;, &apos;notMoving&apos;);
    } else {
        $(this).attr(&apos;moving&apos;, &apos;notMoving&apos;);
    }

})
</code></pre><p>##移动端字体问题</p>
<p><a href="http://zhuanlan.zhihu.com/zhezhexiong/19565895" target="_blank" rel="external">知乎专栏 - [无线手册-4] dp、sp、px傻傻分不清楚[完整]</a></p>
<p><a href="http://www.sencha.com/blog/resolution-independent-mobile-ui" target="_blank" rel="external">Resolution Independent Mobile UI</a></p>
<p><a href="http://stackoverflow.com/questions/12058574/pixel-density-retina-display-and-font-size-in-css" target="_blank" rel="external">Pixel density, retina display and font-size in CSS</a></p>
<p><a href="http://bjango.com/articles/min-device-pixel-ratio/" target="_blank" rel="external">Device pixel density tests</a></p>
<p>##跨域问题 </p>
<p>手机浏览器也是浏览器，在ajax调用外部api的时候也存在跨域问题。当然利用 PhoneGap 打包后，由于协议不一样就不存在跨域问题了。<br>但页面通常是需要跟后端进行调试的。一般会报类似</p>
<pre><code>XMLHttpRequest cannot load XXX
Origin null is not allowed by Access-Control-Allow-Origin.
</code></pre><p>以及</p>
<pre><code>XMLHttpRequest cannot load http://. Request header field Content-Type is not allowed by Access-Control-Allow-Headers.&quot;
</code></pre><p>这时候可以让后端加上两个http头</p>
<pre><code>Access-Control-Allow-Origin &quot;*&quot;
Access-Control-Allow-Headers &quot;Origin, X-Requested-With, Content-Type, Accept&quot;
</code></pre><p>第一个头可以避免跨域问题，第二个头可以方便ajax请求设置content-type等配置项</p>
<p>这个会存在一些安全问题，可以参考这个问题的讨论 <a href="http://www.zhihu.com/question/22992229" target="_blank" rel="external">http://www.zhihu.com/question/22992229</a></p>
<p>##PhoneGap 部分</p>
<p><a href="http://snoopyxdy.blog.163.com/blog/static/60117440201432491123551" target="_blank" rel="external">http://snoopyxdy.blog.163.com/blog/static/60117440201432491123551</a> 这里有一大堆snoopy总结的phonggap开发坑</p>
<p>###Should not happen: no rect-based-test nodes found<br>在 Android 项目中的 assets 中的 HTML 页面中加入以下代码，便可解决问题</p>
<pre><code>window,html,body{
    overflow-x:hidden !important;
    -webkit-overflow-scrolling: touch !important;
    overflow: scroll !important;
}
</code></pre><p>参考：</p>
<p><a href="http://stackoverflow.com/questions/12090899/android-webview-jellybean-should-not-happen-no-rect-based-test-nodes-found" target="_blank" rel="external">http://stackoverflow.com/questions/12090899/android-webview-jellybean-should-not-happen-no-rect-based-test-nodes-found</a></p>
<p>###拿联系人的时候报 ContactFindOptions is not defined</p>
<p>出现这个问题可能是因为 Navigator 取 contacts 时绑定的 window.onload</p>
<p>注意使用 PhoneGap 的 API 时，一定要在 devicereay 事件的处理函数中使用 API</p>
<pre><code>document.addEventListener(&quot;deviceready&quot;, onDeviceReady, false);

    function onDeviceReady() {    
        callFetchContacts();
    }

function callFetchContacts(){
    var options = new ContactFindOptions();
    options.multiple = true;
    var fields       = [&quot;displayName&quot;, &quot;name&quot;,&quot;phoneNumbers&quot;];
    navigator.contacts.find(fields, onSuccess, onError,options);  
    }
</code></pre>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;从github上拿来的，只是为了方便查阅~~&lt;br&gt;
    
    </summary>
    
      <category term="个人学习" scheme="https://cnt1992.github.io/categories/%E4%B8%AA%E4%BA%BA%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="mobile" scheme="https://cnt1992.github.io/tags/mobile/"/>
    
  </entry>
  
  <entry>
    <title>浅谈webpack</title>
    <link href="https://cnt1992.github.io/2015/07/29/%E6%B5%85%E8%B0%88webpack/"/>
    <id>https://cnt1992.github.io/2015/07/29/浅谈webpack/</id>
    <published>2015-07-28T22:08:00.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<h3 id="关于Webpack"><a href="#关于Webpack" class="headerlink" title="关于Webpack"></a>关于Webpack</h3><p>或许你对<a href="http://webpack.github.io/docs/" target="_blank" rel="external">Webpack</a>并没那么熟悉，那你应该听过<a href="http://www.html-js.com/article/browserify.org" target="_blank" rel="external">browserify</a>吧，如果没有听过browserify，那应该知道<a href="http://gulpjs.com/" target="_blank" rel="external">gulp</a>或者<a href="http://gruntjs.com/getting-started" target="_blank" rel="external">grunt</a>这两个东西吧。</p>
<p>其实Webpack就是一个模块加载器，官方的原话如下：</p>
<p><code>webpack takes modules with dependencies and generates static assets representing those modules.</code></p>
<a id="more"></a>
<p>在谈Webpack之前，我们先谈谈两个规范：<a href="https://github.com/seajs/seajs/issues/242" target="_blank" rel="external">CMD</a>和<a href="https://github.com/amdjs/amdjs-api/wiki/AMD" target="_blank" rel="external">AMD</a>。</p>
<p><code>CMD</code>，也称<code>通用模块定义</code>，是一种同步的模块机制，相信玩过Node的童鞋对下面这种写法一点也不陌生：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="keyword">var</span> demo = <span class="built_in">require</span>(<span class="string">'otherModule'</span>);</div><div class="line"><span class="comment">// some code...</span></div><div class="line"><span class="built_in">module</span>.exports = demo;</div></pre></td></tr></table></figure>
<p><code>AMD</code>，也称<code>异步模块定义</code>，是一种异步的模块机制，比较典型的是<a href="http://www.requirejs.cn/home.html" target="_blank" rel="external">requireJS</a>的写法：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line">define( [<span class="string">'module1'</span>,<span class="string">'module2'</span>], <span class="function"><span class="keyword">function</span>(<span class="params">module1,module2</span>)</span>&#123;</div><div class="line">    <span class="comment">// some code...</span></div><div class="line">    <span class="keyword">return</span> <span class="built_in">module</span>;</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<p>对比上面两种规范的写法，显然我们更加愿意接受<code>CMD</code>，相信写过一大堆callback的童鞋深有体会。<strong>随着npm成为主流的JavaScript组件发布平台，越来越多的前端项目也依赖于npm上的项目</strong>，这时候browserify就出现了，它支持<code>require()</code>的同步语法去加载npm模块。</p>
<p>当然在最近公布的ES2015(ES6)里也定义了一套新的模块机制，写法大概如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="keyword">import</span> &#123;module1&#125; <span class="keyword">from</span> <span class="string">'module1'</span>;</div><div class="line"><span class="comment">// some code...</span></div><div class="line"><span class="keyword">export</span> module2;</div></pre></td></tr></table></figure>
<h3 id="为何用webpack"><a href="#为何用webpack" class="headerlink" title="为何用webpack"></a>为何用webpack</h3><p>webpack具有非常好的功能特性：</p>
<ol>
<li>同时支持CMD和AMD模块；</li>
<li>支持模块加载器和插件机制，可对模块进行定制；</li>
<li>可通过配置，打包成多个文件，有效利用浏览器的缓存机制提升性能；</li>
<li>对样式文件和图片等静态资源视为模块进行打包，配合loader加载器可以支持sass，less等css预处理器；</li>
<li>内置source map，即使打包在一起依旧方便调试。</li>
</ol>
<h3 id="webpack使用入门"><a href="#webpack使用入门" class="headerlink" title="webpack使用入门"></a>webpack使用入门</h3><h4 id="全局安装webpack"><a href="#全局安装webpack" class="headerlink" title="全局安装webpack"></a>全局安装webpack</h4><p>在终端执行下面代码将webpack安装到全局：<code>npm install -g webpack</code>，若是mac或者linux系统提示权限不足，请用<code>sudo npm install -g webpack</code>然后输入管理员密码执行。</p>
<h4 id="初始化项目"><a href="#初始化项目" class="headerlink" title="初始化项目"></a>初始化项目</h4><ul>
<li><p>新建一个名为xxx的项目，在该项目根目录下打开终端，执行<code>npm init</code>，接下来填写一些项目的基本信息，最后确认ok之后会在根目录下生成package.json文件。</p>
</li>
<li><p>新建一些示例文件，大概示意图如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">xxx</div><div class="line">    assets</div><div class="line">        css</div><div class="line">            demo.less</div><div class="line">        js</div><div class="line">            entry.js</div><div class="line">            index.js</div><div class="line">        html</div><div class="line">            index.html</div><div class="line">    build</div><div class="line">    package.json(上一步生成的文件)</div><div class="line">    webpack.config.js(webpack核心配置文件)</div></pre></td></tr></table></figure>
</li>
</ul>
<h4 id="配置webpack-config-js"><a href="#配置webpack-config-js" class="headerlink" title="配置webpack.config.js"></a>配置webpack.config.js</h4><p>我们在使用webpack的时候默认配置文件是<code>webpack.config.js</code>，下面配置一下这个js文件（稍微复杂了一点，已经有详细的说明）：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="keyword">var</span> webpack = <span class="built_in">require</span>(<span class="string">'webpack'</span>);</div><div class="line"><span class="built_in">module</span>.exports = &#123;</div><div class="line">    <span class="comment">// webpack入口文件</span></div><div class="line">    entry : <span class="string">'./assets/js/entry.js'</span>, </div><div class="line">    <span class="comment">// webpack输出文件，</span></div><div class="line">    output : &#123;</div><div class="line">        <span class="attr">path</span> : <span class="string">'./build'</span>,</div><div class="line">        <span class="attr">publicPath</span> : <span class="string">'/assets/'</span>,</div><div class="line">        <span class="attr">filename</span> : <span class="string">'bundle.js'</span></div><div class="line">    &#125;,</div><div class="line">    <span class="comment">// loader加载器，包括了less编译，图片压缩为base64，es6转为es5三个</span></div><div class="line">    <span class="built_in">module</span> : &#123;</div><div class="line">        <span class="attr">loaders</span> : [</div><div class="line">            &#123; <span class="attr">test</span> : <span class="regexp">/\.less/</span>, <span class="attr">loader</span> : <span class="string">'style-loader!css-loader!less-loader'</span> &#125;,</div><div class="line">            &#123; <span class="attr">test</span> : <span class="regexp">/\.(png|jpg)$/</span>, <span class="attr">loader</span> : <span class="string">'url-loader?limit=8192'</span> &#125;,</div><div class="line">            &#123; <span class="attr">test</span> : <span class="regexp">/\.js$/</span>, <span class="attr">exclude</span> : <span class="regexp">/node_modules/</span>, <span class="attr">loader</span> : <span class="string">'babel-loader'</span> &#125;</div><div class="line">        ]</div><div class="line">    &#125;,</div><div class="line">    <span class="comment">// 文件不写扩展名时使用的默认扩展名</span></div><div class="line">    resolve : &#123;</div><div class="line">        <span class="attr">extensions</span> : [<span class="string">''</span>, <span class="string">'.js'</span>, <span class="string">'.json'</span>]</div><div class="line">    &#125;,</div><div class="line">    <span class="comment">// 插件，提取公用的JS出来，重复利用</span></div><div class="line">    plugins : [</div><div class="line">        <span class="keyword">new</span> webpack.optimize.CommonsChunkPlugin(<span class="string">'common.js'</span>)</div><div class="line">    ]</div><div class="line">&#125;;</div></pre></td></tr></table></figure>
<h4 id="完善其他demo文件"><a href="#完善其他demo文件" class="headerlink" title="完善其他demo文件"></a>完善其他demo文件</h4><ul>
<li>index.html文件</li>
</ul>
<p>index.html文件只需要引入bundle.js即可，代码如下：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><div class="line">%lt;script src="/assets/bundle.js"&gt;&amp;lt;/script&amp;gt;</div></pre></td></tr></table></figure>
<ul>
<li>index.js文件</li>
</ul>
<p>index.js文件写了一个es6的demo，代码如下：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="class"><span class="keyword">class</span> <span class="title">People</span> </span>&#123;</div><div class="line">    <span class="keyword">constructor</span>( name ) &#123;</div><div class="line">        <span class="keyword">this</span>.name = name;</div><div class="line">    &#125;</div><div class="line">    sayHi() &#123;</div><div class="line">        <span class="keyword">let</span> name = <span class="string">`hi <span class="subst">$&#123;<span class="keyword">this</span>.name&#125;</span> !`</span>;</div><div class="line">        <span class="built_in">console</span>.log(name);</div><div class="line">    &#125;</div><div class="line">&#125;</div><div class="line"><span class="built_in">module</span>.exports = People;</div></pre></td></tr></table></figure></p>
<ul>
<li>entry.js文件</li>
</ul>
<p>entry.js文件则引入index.js文件以及less样式文件写一些demo代码：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="keyword">let</span> People = <span class="built_in">require</span>(<span class="string">'./index'</span>);</div><div class="line"><span class="keyword">let</span> p = <span class="keyword">new</span> People(<span class="string">'sky'</span>);</div><div class="line">p.sayHi();</div><div class="line"><span class="built_in">require</span>(<span class="string">'../css/demo.less'</span>);</div></pre></td></tr></table></figure></p>
<ul>
<li>demo.less</li>
</ul>
<p>demo.less文件主要写一些演示的样式代码：<br><figure class="highlight css"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="selector-tag">h1</span> &#123;</div><div class="line">    <span class="attribute">color</span>:<span class="number">#f00</span>;</div><div class="line">    <span class="attribute">font-size</span>:<span class="number">20px</span>;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<h4 id="启动webpack"><a href="#启动webpack" class="headerlink" title="启动webpack"></a>启动webpack</h4><p>这时候上面的整个项目已经准备好，只需要在项目根目录下面，用webpack的命令执行即可：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="comment">// webpack 命令行的几种基本命令,更多命令可以在终端输入webpack --help</span></div><div class="line">$ webpack <span class="comment">// 最基本的启动webpack方法</span></div><div class="line">$ webpack -w <span class="comment">// 提供watch方法，实时进行打包更新</span></div><div class="line">$ webpack -p <span class="comment">// 对打包后的文件进行压缩，提供production</span></div><div class="line">$ webpack -d <span class="comment">// 提供source map，方便调试。</span></div></pre></td></tr></table></figure></p>
<h4 id="验证"><a href="#验证" class="headerlink" title="验证"></a>验证</h4><p>经过上一步的webpack执行命令之后，build文件夹下面应该多了一个<code>bundle.js</code>文件，这时候打开<code>index.html</code>文件会发现less样式也已经生效。</p>
<h3 id="实战项目注意点"><a href="#实战项目注意点" class="headerlink" title="实战项目注意点"></a>实战项目注意点</h3><ul>
<li>项目中如果需要引入Zepto这类前端包是不兼容webpack的，需要加入包引出的声明：<br><code>module.exports=Zepto</code></li>
</ul>
<h3 id="最后总结"><a href="#最后总结" class="headerlink" title="最后总结"></a>最后总结</h3><p>前端组件化是一条很漫长的路，也只有前端这个圈子在不断的造轮子却还依旧欣欣向荣的景象，在新技术层出不穷的情况下，保持对新技术的热情追求，但追根到底，背后的<code>模块化</code>思想才是支持这些东西源源不断的动力。</p>
]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;关于Webpack&quot;&gt;&lt;a href=&quot;#关于Webpack&quot; class=&quot;headerlink&quot; title=&quot;关于Webpack&quot;&gt;&lt;/a&gt;关于Webpack&lt;/h3&gt;&lt;p&gt;或许你对&lt;a href=&quot;http://webpack.github.io/docs/&quot;&gt;Webpack&lt;/a&gt;并没那么熟悉，那你应该听过&lt;a href=&quot;http://www.html-js.com/article/browserify.org&quot;&gt;browserify&lt;/a&gt;吧，如果没有听过browserify，那应该知道&lt;a href=&quot;http://gulpjs.com/&quot;&gt;gulp&lt;/a&gt;或者&lt;a href=&quot;http://gruntjs.com/getting-started&quot;&gt;grunt&lt;/a&gt;这两个东西吧。&lt;/p&gt;
&lt;p&gt;其实Webpack就是一个模块加载器，官方的原话如下：&lt;/p&gt;
&lt;p&gt;&lt;code&gt;webpack takes modules with dependencies and generates static assets representing those modules.&lt;/code&gt;&lt;/p&gt;
    
    </summary>
    
      <category term="个人学习" scheme="https://cnt1992.github.io/categories/%E4%B8%AA%E4%BA%BA%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="webpack" scheme="https://cnt1992.github.io/tags/webpack/"/>
    
  </entry>
  
  <entry>
    <title>Javascript版排序算法</title>
    <link href="https://cnt1992.github.io/2015/06/11/Javascript%E7%89%88%E6%8E%92%E5%BA%8F%E7%AE%97%E6%B3%95/"/>
    <id>https://cnt1992.github.io/2015/06/11/Javascript版排序算法/</id>
    <published>2015-06-11T09:28:00.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<h3 id="必备知识点"><a href="#必备知识点" class="headerlink" title="必备知识点"></a>必备知识点</h3><ul>
<li><a href="http://bonsaiden.github.io/JavaScript-Garden/zh/#object.prototype" target="_blank" rel="external">JS原型</a></li>
<li>排序中的有序区和无序区<br>简单来说，有序区就是在一组数据中已经排好序的部分，无序区就是该组数据中还没排好序的部分。</li>
<li><a href="https://zh.wikipedia.org/zh/%E4%BA%8C%E5%8F%89%E6%A0%91" target="_blank" rel="external">二叉树的基本知识</a></li>
</ul>
<h3 id="封装原型方法"><a href="#封装原型方法" class="headerlink" title="封装原型方法"></a>封装原型方法</h3><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">			</div><div class="line"><span class="built_in">Function</span>.prototype.method = <span class="function"><span class="keyword">function</span> (<span class="params"> name, func </span>) </span>&#123;</div><div class="line">       <span class="keyword">this</span>.prototype[ name ] = func;</div><div class="line">       <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">   &#125;</div></pre></td></tr></table></figure>
<a id="more"></a>
<h3 id="算法思路及复杂度对比"><a href="#算法思路及复杂度对比" class="headerlink" title="算法思路及复杂度对比"></a>算法思路及复杂度对比</h3><table>
<thead>
<tr>
<th style="text-align:center">名称</th>
<th style="text-align:center">数据对象</th>
<th style="text-align:center">时间复杂度</th>
<th style="text-align:center">空间复杂度</th>
<th style="text-align:center">描述</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">冒泡排序</td>
<td style="text-align:center">数组</td>
<td style="text-align:center">O(n²)</td>
<td style="text-align:center">O(1)</td>
<td style="text-align:center">(无序区，有序区) 通过在无序区的相邻元素的比较和替换，使较小的元素浮到最上面</td>
</tr>
<tr>
<td style="text-align:center">选择排序</td>
<td style="text-align:center">数组、链表</td>
<td style="text-align:center">O(n²)</td>
<td style="text-align:center">O(1)</td>
<td style="text-align:center">(有序区，无序区) </td>
</tr>
</tbody>
</table>
<p>在无序区里找一个最小的元素跟在有序区的后面。对数组：比较得多，换得少 |<br>| 插入排序 | 数组、链表 | O(n²) | O(1) | (有序区，无序区) 把无序区的第一个元素插入到有序区的合适的位置。对数组：比较得少，换得多 |<br>| 堆排序 | 数组 | O(nlogn) | O(1) | (有序区，无序区) 在无序区里找一个最小的元素跟在有序区的后面。对数组：比较得多，换得少 |<br>| 归并排序 | 数组、链表 | O(nlogn) |  | (最大堆，有序区) 从堆顶把根卸下来放在有序区之前，再恢复堆 |<br>| 快速排序 | 数组 | O(nlogn)~O(n²) | O(logn)，O(n) | （小数，枢纽元，大数） |<br>| 希尔排序 | 数组 | O(nlog²n)~O(n²) | O(1) | 每一轮按照事先决定的间隔进行插入排序，间隔会依次缩小，最后依次一定要是1 |</p>
<h3 id="算法实现"><a href="#算法实现" class="headerlink" title="算法实现"></a>算法实现</h3><p>1.冒泡排序</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="built_in">Array</span>.method(<span class="string">'bubbleSort'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> len = <span class="keyword">this</span>.length,</div><div class="line">        i, j, tmp;</div><div class="line">    <span class="keyword">for</span> ( i = <span class="number">0</span>; i &lt; len; i++ ) &#123;</div><div class="line">        <span class="keyword">for</span> ( j = len - <span class="number">1</span>; j &gt; i; j-- ) &#123;</div><div class="line">            <span class="keyword">if</span> ( <span class="keyword">this</span>[j] &lt; <span class="keyword">this</span>[j<span class="number">-1</span>] ) &#123;</div><div class="line">                tmp = <span class="keyword">this</span>[j<span class="number">-1</span>];</div><div class="line">                <span class="keyword">this</span>[j<span class="number">-1</span>] = <span class="keyword">this</span>[j];</div><div class="line">                <span class="keyword">this</span>[j] = tmp;</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<p><code>改进冒泡排序</code>:如果在某次排序中没有出现交换的情况，那么说明在无序的元素现在已经是有序了，可以直接返回。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="built_in">Array</span>.method(<span class="string">'rBubbleSort'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> len = <span class="keyword">this</span>.length,</div><div class="line">        i, j, tmp, exchange;</div><div class="line">    <span class="keyword">for</span> ( i = <span class="number">0</span>; i &lt; len; i++ ) &#123;</div><div class="line">        exchange = <span class="number">0</span>;</div><div class="line">        <span class="keyword">for</span> ( j = len - <span class="number">1</span>; j &gt; i; j-- ) &#123;</div><div class="line">            <span class="keyword">if</span> ( <span class="keyword">this</span>[j] &lt; <span class="keyword">this</span>[j<span class="number">-1</span>] ) &#123;</div><div class="line">                tmp = <span class="keyword">this</span>[j<span class="number">-1</span>];</div><div class="line">                <span class="keyword">this</span>[j<span class="number">-1</span>] = <span class="keyword">this</span>[j];</div><div class="line">                <span class="keyword">this</span>[j] = tmp;</div><div class="line">                exchange = <span class="number">1</span>;</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">        <span class="keyword">if</span> ( !exchange ) &#123;</div><div class="line">            <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<p>2.选择排序</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="built_in">Array</span>.method(<span class="string">'selectSort'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> len = <span class="keyword">this</span>.length,</div><div class="line">        i, j, k, tmp;</div><div class="line">    <span class="keyword">for</span> ( i = <span class="number">0</span>; i &lt; len; i++ ) &#123;</div><div class="line">        k = i;</div><div class="line">        <span class="keyword">for</span> ( j = i + <span class="number">1</span>; j &lt; len; j++ ) &#123;</div><div class="line">            <span class="keyword">if</span> ( <span class="keyword">this</span>[j] &lt; <span class="keyword">this</span>[k] ) &#123;</div><div class="line">                k = j;</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">        <span class="keyword">if</span> ( k!=i ) &#123;</div><div class="line">            tmp = <span class="keyword">this</span>[k];</div><div class="line">            <span class="keyword">this</span>[k] = <span class="keyword">this</span>[i];</div><div class="line">            <span class="keyword">this</span>[i] = tmp;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<p>3.插入排序</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="built_in">Array</span>.method(<span class="string">'insertSort'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> len = <span class="keyword">this</span>.length,</div><div class="line">        i, j, k, tmp;</div><div class="line">    <span class="keyword">for</span> ( i = <span class="number">1</span>; i &lt; len; i++ ) &#123;</div><div class="line">        tmp = <span class="keyword">this</span>[i];</div><div class="line">        j = i - <span class="number">1</span>;</div><div class="line">        <span class="keyword">while</span> ( j&gt;=<span class="number">0</span> &amp;&amp; tmp &lt; <span class="keyword">this</span>[j] ) &#123;</div><div class="line">            <span class="keyword">this</span>[j+<span class="number">1</span>] = <span class="keyword">this</span>[j];</div><div class="line">            j--;</div><div class="line">        &#125;</div><div class="line">        <span class="keyword">this</span>[j+<span class="number">1</span>] = tmp;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<p>4.堆排序</p>
<pre><code>堆排序是一种树形选择排序方法(注意下标是从1开始的，也就是R[1...n])。
堆排序思路：
1) 初始堆：
将原始数组调整成大根堆的方法——筛选算法：
比较R[2i]、R[2i+1]和R[i]，将最大者放在R[i]的位置上(递归调用此方法到结束)
2) 堆排序：
每次将堆顶元素与数组最后面的且没有被置换的元素互换。
</code></pre><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="built_in">Array</span>.method(<span class="string">'createHeap'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">low, high</span>)</span>&#123;</div><div class="line">    <span class="keyword">var</span> i=low, j=<span class="number">2</span>*i, tmp=<span class="keyword">this</span>[i];</div><div class="line">    <span class="keyword">while</span>(j&lt;=high)&#123;</div><div class="line">        <span class="comment">// 从左右子节点中选出较大的节点</span></div><div class="line">        <span class="keyword">if</span>(j&lt; high &amp;&amp; <span class="keyword">this</span>[j]&lt;<span class="keyword">this</span>[j+<span class="number">1</span>]) j++;</div><div class="line">        <span class="keyword">if</span>(tmp &lt; <span class="keyword">this</span>[j])&#123;                    </div><div class="line">            <span class="comment">//根节点(tmp)&lt;较大的节点</span></div><div class="line">            <span class="keyword">this</span>[i] = <span class="keyword">this</span>[j];</div><div class="line">            i = j;</div><div class="line">            j = <span class="number">2</span>*i;</div><div class="line">        &#125;<span class="keyword">else</span> <span class="keyword">break</span>;</div><div class="line">    &#125;</div><div class="line">    <span class="comment">//被筛选的元素放在最终的位置上</span></div><div class="line">    <span class="keyword">this</span>[i] = tmp;                            </div><div class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">&#125;);</div><div class="line"><span class="built_in">Array</span>.method(<span class="string">'heapSort'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">    <span class="keyword">var</span> i, tmp, len=<span class="keyword">this</span>.length<span class="number">-1</span>;</div><div class="line">    <span class="keyword">for</span>(i=<span class="built_in">parseInt</span>(len/<span class="number">2</span>); i&gt;=<span class="number">1</span>; i--) <span class="keyword">this</span>.createHeap(i, len);</div><div class="line">    <span class="keyword">for</span>(i=len; i&gt;=<span class="number">2</span>; i--)&#123;</div><div class="line">        tmp = <span class="keyword">this</span>[<span class="number">1</span>];</div><div class="line">        <span class="keyword">this</span>[<span class="number">1</span>] = <span class="keyword">this</span>[i];</div><div class="line">        <span class="keyword">this</span>[i] = tmp;</div><div class="line">        <span class="keyword">this</span>.createHeap(<span class="number">1</span>, i<span class="number">-1</span>);</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<p>5.归并排序<br>    归并排序思路：<br>    1) 归并<br>    从两个有序表R[low…mid]和R[mid+1…high]，每次从左边依次取出一个数进行比较，将较小者放入tmp数组中，最后将两段中剩下的部分直接复制到tmp中。<br>    这样tmp是一个有序表，再将它复制加R中。(其中要考虑最后一个子表的长度不足length的情况)<br>    2) 排序<br>    自底向上的归并，第一回：length=1；第二回：length=2*length …</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="built_in">Array</span>.method(<span class="string">'createHeap'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">low, high</span>)</span>&#123;</div><div class="line">    <span class="keyword">var</span> i=low, j=<span class="number">2</span>*i, tmp=<span class="keyword">this</span>[i];</div><div class="line">    <span class="keyword">while</span>(j&lt;=high)&#123;</div><div class="line">        <span class="comment">// 从左右子节点中选出较大的节点</span></div><div class="line">        <span class="keyword">if</span>(j&lt; high &amp;&amp; <span class="keyword">this</span>[j]&lt;<span class="keyword">this</span>[j+<span class="number">1</span>]) j++;</div><div class="line">        <span class="keyword">if</span>(tmp &lt; <span class="keyword">this</span>[j])&#123;                    </div><div class="line">            <span class="comment">//根节点(tmp)&lt;较大的节点</span></div><div class="line">            <span class="keyword">this</span>[i] = <span class="keyword">this</span>[j];</div><div class="line">            i = j;</div><div class="line">            j = <span class="number">2</span>*i;</div><div class="line">        &#125;<span class="keyword">else</span> <span class="keyword">break</span>;</div><div class="line">    &#125;</div><div class="line">    <span class="comment">//被筛选的元素放在最终的位置上</span></div><div class="line">    <span class="keyword">this</span>[i] = tmp;                            </div><div class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">&#125;);</div><div class="line"><span class="built_in">Array</span>.method(<span class="string">'heapSort'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">    <span class="keyword">var</span> i, tmp, len=<span class="keyword">this</span>.length<span class="number">-1</span>;</div><div class="line">    <span class="keyword">for</span>(i=<span class="built_in">parseInt</span>(len/<span class="number">2</span>); i&gt;=<span class="number">1</span>; i--) <span class="keyword">this</span>.createHeap(i, len);</div><div class="line">    <span class="keyword">for</span>(i=len; i&gt;=<span class="number">2</span>; i--)&#123;</div><div class="line">        tmp = <span class="keyword">this</span>[<span class="number">1</span>];</div><div class="line">        <span class="keyword">this</span>[<span class="number">1</span>] = <span class="keyword">this</span>[i];</div><div class="line">        <span class="keyword">this</span>[i] = tmp;</div><div class="line">        <span class="keyword">this</span>.createHeap(<span class="number">1</span>, i<span class="number">-1</span>);</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<p>6.快速排序<br>    快速排序思路：<br>    1) 假设第一个元素为基准元素<br>    2) 把所有比基准元素小的记录放置在前一部分，把所有比基准元素大的记录放置在后一部分，并把基准元素放在这两部分的中间(i=j的位置)</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="built_in">Array</span>.method(<span class="string">'quickSort'</span>, <span class="function"><span class="keyword">function</span>(<span class="params">s, t</span>)</span>&#123;</div><div class="line">    <span class="keyword">var</span> i=s, j=t,</div><div class="line">        tmp;</div><div class="line">    <span class="keyword">if</span>(s &lt; t)&#123;</div><div class="line">        tmp = <span class="keyword">this</span>[s];</div><div class="line">        <span class="keyword">while</span>(i!=j)&#123;</div><div class="line">            <span class="keyword">while</span>(j&gt;i &amp;&amp; <span class="keyword">this</span>[j]&gt;tmp) j--;<span class="comment">//右—&gt;左</span></div><div class="line">            R[i] = R[j];</div><div class="line">            <span class="keyword">while</span>(i&lt;j &amp;&amp; <span class="keyword">this</span>[j]&lt;tmp) i++;<span class="comment">//左—&gt;右</span></div><div class="line">            R[j] = R[i]; </div><div class="line">        &#125;</div><div class="line">        R[i] = tmp;</div><div class="line">        <span class="keyword">this</span>.quickSort(s, i<span class="number">-1</span>);</div><div class="line">        <span class="keyword">this</span>.quickSort(i+<span class="number">1</span>, t);</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<p>7.希尔排序</p>
<pre><code>希尔排序思路:
我们在第 i 次时取gap = n/(2的i次方)，然后将数组分为gap组(从下标0开始，每相邻的gap个元素为一组)，接下来我们对每一组进行直接插入排序。
</code></pre><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="built_in">Array</span>.method(<span class="string">'shellSort'</span>, <span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">    <span class="keyword">var</span> len = <span class="keyword">this</span>.length, gap = <span class="built_in">parseInt</span>(len/<span class="number">2</span>), </div><div class="line">        i, j, tmp;</div><div class="line">    <span class="keyword">while</span>(gap &gt; <span class="number">0</span>)&#123;</div><div class="line">        <span class="keyword">for</span>(i=gap; i&lt;len; i++)&#123;</div><div class="line">            tmp = <span class="keyword">this</span>[i];</div><div class="line">            j = i - gap;</div><div class="line">            <span class="keyword">while</span>(j&gt;=<span class="number">0</span> &amp;&amp; tmp &lt; <span class="keyword">this</span>[j])&#123;</div><div class="line">                <span class="keyword">this</span>[j+gap] = <span class="keyword">this</span>[j];</div><div class="line">                j = j - gap;</div><div class="line">            &#125;</div><div class="line">            <span class="keyword">this</span>[j + gap] = tmp;</div><div class="line">        &#125;</div><div class="line">        gap = <span class="built_in">parseInt</span>(gap/<span class="number">2</span>);</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> <span class="keyword">this</span>;</div><div class="line">&#125;);</div></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;h3 id=&quot;必备知识点&quot;&gt;&lt;a href=&quot;#必备知识点&quot; class=&quot;headerlink&quot; title=&quot;必备知识点&quot;&gt;&lt;/a&gt;必备知识点&lt;/h3&gt;&lt;ul&gt;
&lt;li&gt;&lt;a href=&quot;http://bonsaiden.github.io/JavaScript-Garden/zh/#object.prototype&quot;&gt;JS原型&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;排序中的有序区和无序区&lt;br&gt;简单来说，有序区就是在一组数据中已经排好序的部分，无序区就是该组数据中还没排好序的部分。&lt;/li&gt;
&lt;li&gt;&lt;a href=&quot;https://zh.wikipedia.org/zh/%E4%BA%8C%E5%8F%89%E6%A0%91&quot;&gt;二叉树的基本知识&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id=&quot;封装原型方法&quot;&gt;&lt;a href=&quot;#封装原型方法&quot; class=&quot;headerlink&quot; title=&quot;封装原型方法&quot;&gt;&lt;/a&gt;封装原型方法&lt;/h3&gt;&lt;figure class=&quot;highlight javascript&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;			&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;built_in&quot;&gt;Function&lt;/span&gt;.prototype.method = &lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;function&lt;/span&gt; (&lt;span class=&quot;params&quot;&gt; name, func &lt;/span&gt;) &lt;/span&gt;&amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;       &lt;span class=&quot;keyword&quot;&gt;this&lt;/span&gt;.prototype[ name ] = func;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;       &lt;span class=&quot;keyword&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;keyword&quot;&gt;this&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;   &amp;#125;&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
    
    </summary>
    
      <category term="个人学习" scheme="https://cnt1992.github.io/categories/%E4%B8%AA%E4%BA%BA%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="javascript 算法" scheme="https://cnt1992.github.io/tags/javascript-%E7%AE%97%E6%B3%95/"/>
    
  </entry>
  
  <entry>
    <title>数组洗牌算法</title>
    <link href="https://cnt1992.github.io/2015/04/21/%E6%95%B0%E7%BB%84%E6%B4%97%E7%89%8C%E7%AE%97%E6%B3%95%E5%AE%9E%E7%8E%B0/"/>
    <id>https://cnt1992.github.io/2015/04/21/数组洗牌算法实现/</id>
    <published>2015-04-20T16:28:00.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<p>在做<a href="http://www.paipai.com" target="_blank" rel="external">拍拍首页</a>改版过程碰到一个需求，抽象出来的意思就是：从一个数组当中随机抽出几个组成一个新的数组，然后思考了一下，代码如下()：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">// 判断数组是否包含某个元素</span></div><div class="line">  <span class="built_in">Array</span>.prototype.contains = <span class="function"><span class="keyword">function</span>(<span class="params">obj</span>) </span>&#123;</div><div class="line">      <span class="keyword">var</span> i = <span class="keyword">this</span>.length;</div><div class="line">      <span class="keyword">while</span> (i--) &#123;</div><div class="line">          <span class="keyword">if</span> (<span class="keyword">this</span>[i] === obj) &#123;</div><div class="line">              <span class="keyword">return</span> <span class="literal">true</span>;</div><div class="line">          &#125;</div><div class="line">      &#125;</div><div class="line">      <span class="keyword">return</span> <span class="literal">false</span>;</div><div class="line">  &#125;;</div><div class="line"></div><div class="line">  <span class="comment">// 在min和max之间随机生成一个数字</span></div><div class="line">  <span class="keyword">var</span> randomNum = <span class="function"><span class="keyword">function</span> (<span class="params">min, max</span>) </span>&#123;</div><div class="line">      <span class="keyword">if</span> (max == <span class="literal">undefined</span>) &#123;</div><div class="line">          max = min;</div><div class="line">          min = <span class="number">0</span>;</div><div class="line">      &#125;</div><div class="line">      <span class="keyword">return</span> <span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * (max - min) + min);</div><div class="line">  &#125;;</div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">shuffle</span> (<span class="params">obj, arrLen</span>) </span>&#123;</div><div class="line">	<span class="keyword">var</span> randomArr = [],</div><div class="line">            $obj = obj,</div><div class="line">            len = $obj.length + <span class="number">1</span>;</div><div class="line"></div><div class="line">        <span class="keyword">while</span> ( randomArr.length &lt; arrLen ) &#123;</div><div class="line">            <span class="keyword">var</span> num = randomNum (len);</div><div class="line">            <span class="keyword">if</span> ( randomArr.contains(num) ) &#123;</div><div class="line">                <span class="keyword">continue</span>;</div><div class="line">            &#125;</div><div class="line">            randomArr.push(num);</div><div class="line">        &#125;	</div><div class="line">        <span class="keyword">return</span> randomArr;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>写完之后，虽然功能实现了，但是发现代码有点多而且逻辑有点复杂，我考虑的是从数组中随机抽出一个元素放入一个新的数组，只要新的数组的个数未到达指定个数，就不断循环从原数组中取元素，同时不能取出跟新数组重复元素，这样一来时间复杂度相对较高。</p>
<a id="more"></a>
<p>回到正题，doodle了一下，发现原来这个可以用传说中的<em>洗牌算法</em>来实现，基本原理是：从所有元素中随机选取一个与第一个元素进行交换，然后在第二个之后选择一个元素与第二个交换，直到最后一个元素。这样能确保每个元素在每个位置的概率都是1/n。</p>
<p>于是改进算法，代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"></div><div class="line"><span class="comment">//生成从1到length之间的随机数组</span></div><div class="line"></div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">shuffleArray</span> (<span class="params"> len </span>) </span>&#123;</div><div class="line">	<span class="keyword">var</span> arr = [], temp, index, i ;</div><div class="line">	<span class="keyword">if</span> ( !len ) &#123;</div><div class="line">		<span class="keyword">return</span> <span class="literal">false</span>;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">for</span> ( i = <span class="number">1</span>; i &lt;= len; i++) &#123;</div><div class="line">		arr.push(i<span class="number">-1</span>);</div><div class="line">	&#125;</div><div class="line"></div><div class="line">	<span class="comment">//利用洗牌算法打乱数组</span></div><div class="line">	<span class="keyword">for</span> ( i = <span class="number">1</span>; i &lt;= len; i++ ) &#123;</div><div class="line">		<span class="comment">//随机生成i到len之间的随机数</span></div><div class="line">		index = <span class="built_in">parseInt</span>(<span class="built_in">Math</span>.random() * ( len - i ), <span class="number">10</span>) + i;</div><div class="line">		<span class="keyword">if</span> ( index != i ) &#123;</div><div class="line">			temp = arr[i];</div><div class="line">			arr[i] = arr[index];</div><div class="line">			arr[index] = temp;</div><div class="line">		&#125;</div><div class="line">	&#125;</div><div class="line"></div><div class="line">	<span class="keyword">return</span> arr;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>思考继续改进中…</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;在做&lt;a href=&quot;http://www.paipai.com&quot;&gt;拍拍首页&lt;/a&gt;改版过程碰到一个需求，抽象出来的意思就是：从一个数组当中随机抽出几个组成一个新的数组，然后思考了一下，代码如下()：&lt;/p&gt;
&lt;figure class=&quot;highlight javascript&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;comment&quot;&gt;// 判断数组是否包含某个元素&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;  &lt;span class=&quot;built_in&quot;&gt;Array&lt;/span&gt;.prototype.contains = &lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;function&lt;/span&gt;(&lt;span class=&quot;params&quot;&gt;obj&lt;/span&gt;) &lt;/span&gt;&amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;      &lt;span class=&quot;keyword&quot;&gt;var&lt;/span&gt; i = &lt;span class=&quot;keyword&quot;&gt;this&lt;/span&gt;.length;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;      &lt;span class=&quot;keyword&quot;&gt;while&lt;/span&gt; (i--) &amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;          &lt;span class=&quot;keyword&quot;&gt;if&lt;/span&gt; (&lt;span class=&quot;keyword&quot;&gt;this&lt;/span&gt;[i] === obj) &amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;              &lt;span class=&quot;keyword&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;literal&quot;&gt;true&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;          &amp;#125;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;      &amp;#125;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;      &lt;span class=&quot;keyword&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;literal&quot;&gt;false&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;  &amp;#125;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;  &lt;span class=&quot;comment&quot;&gt;// 在min和max之间随机生成一个数字&lt;/span&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;  &lt;span class=&quot;keyword&quot;&gt;var&lt;/span&gt; randomNum = &lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;function&lt;/span&gt; (&lt;span class=&quot;params&quot;&gt;min, max&lt;/span&gt;) &lt;/span&gt;&amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;      &lt;span class=&quot;keyword&quot;&gt;if&lt;/span&gt; (max == &lt;span class=&quot;literal&quot;&gt;undefined&lt;/span&gt;) &amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;          max = min;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;          min = &lt;span class=&quot;number&quot;&gt;0&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;      &amp;#125;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;      &lt;span class=&quot;keyword&quot;&gt;return&lt;/span&gt; &lt;span class=&quot;built_in&quot;&gt;Math&lt;/span&gt;.floor(&lt;span class=&quot;built_in&quot;&gt;Math&lt;/span&gt;.random() * (max - min) + min);&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;  &amp;#125;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;function&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;function&lt;/span&gt; &lt;span class=&quot;title&quot;&gt;shuffle&lt;/span&gt; (&lt;span class=&quot;params&quot;&gt;obj, arrLen&lt;/span&gt;) &lt;/span&gt;&amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;	&lt;span class=&quot;keyword&quot;&gt;var&lt;/span&gt; randomArr = [],&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;            $obj = obj,&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;            len = $obj.length + &lt;span class=&quot;number&quot;&gt;1&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;        &lt;span class=&quot;keyword&quot;&gt;while&lt;/span&gt; ( randomArr.length &amp;lt; arrLen ) &amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;            &lt;span class=&quot;keyword&quot;&gt;var&lt;/span&gt; num = randomNum (len);&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;            &lt;span class=&quot;keyword&quot;&gt;if&lt;/span&gt; ( randomArr.contains(num) ) &amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;                &lt;span class=&quot;keyword&quot;&gt;continue&lt;/span&gt;;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;            &amp;#125;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;            randomArr.push(num);&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;        &amp;#125;	&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;        &lt;span class=&quot;keyword&quot;&gt;return&lt;/span&gt; randomArr;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;&amp;#125;&lt;/div&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;写完之后，虽然功能实现了，但是发现代码有点多而且逻辑有点复杂，我考虑的是从数组中随机抽出一个元素放入一个新的数组，只要新的数组的个数未到达指定个数，就不断循环从原数组中取元素，同时不能取出跟新数组重复元素，这样一来时间复杂度相对较高。&lt;/p&gt;
    
    </summary>
    
      <category term="个人学习" scheme="https://cnt1992.github.io/categories/%E4%B8%AA%E4%BA%BA%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="javascript 算法" scheme="https://cnt1992.github.io/tags/javascript-%E7%AE%97%E6%B3%95/"/>
    
  </entry>
  
  <entry>
    <title>整理自己常用的网站资源</title>
    <link href="https://cnt1992.github.io/2015/04/04/%E6%95%B4%E7%90%86%E8%87%AA%E5%B7%B1%E5%B8%B8%E7%94%A8%E7%9A%84%E7%BD%91%E7%AB%99%E8%B5%84%E6%BA%90/"/>
    <id>https://cnt1992.github.io/2015/04/04/整理自己常用的网站资源/</id>
    <published>2015-04-03T16:43:29.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>整理一下自己常用的网站资源，主要web前端为主。</p>
</blockquote>
<a id="more"></a>
<h3 id="web标准文档"><a href="#web标准文档" class="headerlink" title="web标准文档"></a>web标准文档</h3><ol>
<li><a href="http://dev.w3.org/csswg/css2/" target="_blank" rel="external">css 2.1官方文档</a></li>
<li><a href="http://www.ecma-international.org/ecma-262/5.1/" target="_blank" rel="external">ECMA 5.1官方文档</a></li>
</ol>
<h3 id="资源"><a href="#资源" class="headerlink" title="资源"></a>资源</h3><ol>
<li><a href="http://web.memect.com/" target="_blank" rel="external">web技术日报</a> <em>每天更新</em></li>
<li><a href="http://newsletter.csdn.net/module/news/newsletter/index/1" target="_blank" rel="external">CSDN 移动周刊</a> <em>每周更新</em></li>
<li><a href="http://www.zaoduke.net/" target="_blank" rel="external">前端早读课</a> <em>不定时更新</em></li>
</ol>
<h3 id="技术博客"><a href="#技术博客" class="headerlink" title="技术博客"></a>技术博客</h3><ol>
<li><a href="http://blog.cssforest.org/" target="_blank" rel="external">CSS森林</a> <em>鬼哥的博客</em></li>
<li><a href="http://hikejun.com/blog/" target="_blank" rel="external">张克军博客</a> <em>无更新状态，不过文章挺经典</em></li>
<li><a href="http://ued.taobao.org/blog/" target="_blank" rel="external">淘宝UED博客</a></li>
<li><a href="http://www.alloyteam.com/" target="_blank" rel="external">AlloyTeam博客</a> <em>腾讯全端AlloyTeam团队Blog</em></li>
</ol>
<h3 id="在线学习"><a href="#在线学习" class="headerlink" title="在线学习"></a>在线学习</h3><ol>
<li><a href="http://www.codecademy.com/" target="_blank" rel="external">codecademy</a>  <em>问答式的学习平台</em></li>
<li><a href="http://www.w3cplus.com/" target="_blank" rel="external">w3c plus</a></li>
<li><a href="http://www.w3school.com.cn/" target="_blank" rel="external">w3c school</a> <em>入门级别的教程</em></li>
</ol>
<h3 id="流程自动化"><a href="#流程自动化" class="headerlink" title="流程自动化"></a>流程自动化</h3><ol>
<li><a href="http://gulpjs.com/" target="_blank" rel="external">gulpjs</a> <em>基于流的自动化</em></li>
<li><a href="http://gruntjs.com/getting-started" target="_blank" rel="external">grunt</a> </li>
</ol>
<h3 id="工具类"><a href="#工具类" class="headerlink" title="工具类"></a>工具类</h3><ol>
<li><a href="http://kindeditor.net/demo.php" target="_blank" rel="external">在线HTML编辑器</a> <em>针对一大篇幅word文档内容直接转换成html比较有效</em></li>
<li><a href="http://www.bejson.com/index.php" target="_blank" rel="external">在线JSON校验格式化工具</a></li>
<li><a href="http://validator.w3.org/" target="_blank" rel="external">w3c HTML验证服务</a> <em>刚入门HTML的建议写好代码放这上面测试看通过不通过</em></li>
<li><a href="http://jigsaw.w3.org/css-validator/#validate_by_upload" target="_blank" rel="external">w3c CSS验证服务</a> <em>检查自己写的CSS有没有错误</em></li>
<li><a href="http://www.img2ico.net/" target="_blank" rel="external">图标转换工具</a> <em>将jpg、png、gif的图片转换成ico格式</em></li>
<li><a href="http://www.pjhome.net/web/html5/encodeDataUrl.htm" target="_blank" rel="external">在线文件转换成base64编码</a></li>
</ol>
<h3 id="代码试验场"><a href="#代码试验场" class="headerlink" title="代码试验场"></a>代码试验场</h3><ol>
<li><a href="http://jsfiddle.net/" target="_blank" rel="external">JSFiddle</a> <em>可以在这里写代码看效果，也可以分享给他人</em></li>
<li><a href="http://codepen.io/" target="_blank" rel="external">codepen</a> <em>可以看别人学的一些好的代码demo</em></li>
</ol>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;整理一下自己常用的网站资源，主要web前端为主。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="个人学习" scheme="https://cnt1992.github.io/categories/%E4%B8%AA%E4%BA%BA%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="web 学习资源" scheme="https://cnt1992.github.io/tags/web-%E5%AD%A6%E4%B9%A0%E8%B5%84%E6%BA%90/"/>
    
  </entry>
  
  <entry>
    <title>gulp自动化脚本</title>
    <link href="https://cnt1992.github.io/2014/12/01/gulp%E8%87%AA%E5%8A%A8%E5%8C%96%E8%84%9A%E6%9C%AC/"/>
    <id>https://cnt1992.github.io/2014/12/01/gulp自动化脚本/</id>
    <published>2014-12-01T09:54:45.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<p>上周腾讯<a href="http://tgideas.qq.com/" target="_blank" rel="external">TGideas</a>团队来公司分享了一个gulp前端构建利器，自个儿玩了一下，于是有了此文。</p>
<p>号称新一代前端构建利器的<a href="https://github.com/gulpjs/gulp/blob/master/docs/getting-started.md" target="_blank" rel="external">gulp</a>是基于流的，代码优于配置的构建工具。虽然已经熟悉用<a href="http://gruntjs.cn/getting-started/" target="_blank" rel="external">grunt</a>来配置构建，但用了gulp之后感觉这个gulp似乎更胜一筹。</p>
<a id="more"></a>
<p>关于如何全局安装<a href="https://github.com/gulpjs/gulp/blob/master/docs/getting-started.md" target="_blank" rel="external">gulp</a>，该项目<a href="https://github.com/gulpjs/gulp/blob/master/docs/getting-started.md" target="_blank" rel="external">官网</a>已经有详细的介绍，这里不多赘述，直接进入主题。</p>
<h3 id="利用npm创建package-json文件"><a href="#利用npm创建package-json文件" class="headerlink" title="利用npm创建package.json文件"></a>利用npm创建package.json文件</h3><p>如果对package.json文件已经比较熟悉了，那就没有必要利用这个”引导式”的方式创建package.json，这里是针对”懒人”(比如我…)比较好的方式。切换到项目根目录下面，在终端敲入下面代码:<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">npm init</div></pre></td></tr></table></figure></p>
<p>接下来只需按照提示一步步填写，一分钟不到就搞定了。</p>
<h3 id="安装所需的插件并且自动写入package-json文件"><a href="#安装所需的插件并且自动写入package-json文件" class="headerlink" title="安装所需的插件并且自动写入package.json文件"></a>安装所需的插件并且自动写入package.json文件</h3><p>在<a href="https://www.npmjs.org/" target="_blank" rel="external">npm官网</a>或者到gulp的<a href="http://gulpjs.com/plugins/" target="_blank" rel="external">插件官网</a>可以查询你所需要的插件关键词，假设输入imagemin，可以看到下载量最多并且属于gulp的一般就是你想要的那个了，然后在终端敲入以下代码下载插件并且自动保存到package.json文件的devDependencies里面去，避免自己手动输入。<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">npm install --save-dev gulp-imagemin</div></pre></td></tr></table></figure></p>
<p>如果mac或者linux系统的可能会提示权限不足，暴力一点sudo解决。</p>
<h3 id="创建gulpfile-js文件"><a href="#创建gulpfile-js文件" class="headerlink" title="创建gulpfile.js文件"></a>创建gulpfile.js文件</h3><p>这个是gulp的核心配置文件，配置模式也很简单，基本是下面这种模式<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">gulp.task(<span class="string">'任务名称'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> gulp.src(<span class="string">'任务路径'</span>)</div><div class="line">        .pipe()</div><div class="line">        .pipe()</div><div class="line">        ...</div><div class="line">        .dest()</div><div class="line">&#125;);</div></pre></td></tr></table></figure></p>
<h3 id="简单的gulpfile-js例子"><a href="#简单的gulpfile-js例子" class="headerlink" title="简单的gulpfile.js例子"></a>简单的gulpfile.js例子</h3><p>这里给了自己写的一个简单gulpfile.js的配置，项目结构如下：<br><figure class="highlight stylus"><table><tr><td class="code"><pre><div class="line">root</div><div class="line">    css</div><div class="line">        images</div><div class="line">            xxx<span class="selector-class">.png</span></div><div class="line">            yyy<span class="selector-class">.png</span></div><div class="line">            ...</div><div class="line">        xxx<span class="selector-class">.css</span></div><div class="line">        yyy<span class="selector-class">.css</span></div><div class="line">        ...</div><div class="line">    <span class="selector-tag">html</span></div><div class="line">        xxx<span class="selector-class">.html</span></div><div class="line">        yyy<span class="selector-class">.html</span></div><div class="line">        ...</div><div class="line">    images</div><div class="line">        xxx<span class="selector-class">.png</span></div><div class="line">        yyy<span class="selector-class">.jpg</span></div><div class="line">        ...</div><div class="line">    js</div><div class="line">        xxx<span class="selector-class">.js</span></div><div class="line">        yyy<span class="selector-class">.js</span></div><div class="line">        lib</div><div class="line">            jquery<span class="selector-class">.js</span></div><div class="line">            jquery<span class="selector-class">.xxx</span><span class="selector-class">.js</span></div></pre></td></tr></table></figure></p>
<p>gulpfile.js内容如下(项目结构如果不一样，只需修改配置路径以及输出路径即可)：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> gulp = <span class="built_in">require</span>(<span class="string">'gulp'</span>);</div><div class="line"><span class="keyword">var</span> jshint = <span class="built_in">require</span>(<span class="string">'gulp-jshint'</span>);</div><div class="line"><span class="keyword">var</span> uglify = <span class="built_in">require</span>(<span class="string">'gulp-uglify'</span>);</div><div class="line"><span class="keyword">var</span> stylish = <span class="built_in">require</span>(<span class="string">'jshint-stylish'</span>);</div><div class="line"><span class="keyword">var</span> imagemin = <span class="built_in">require</span>(<span class="string">'gulp-imagemin'</span>);</div><div class="line"><span class="keyword">var</span> pngquant = <span class="built_in">require</span>(<span class="string">'imagemin-pngquant'</span>);</div><div class="line"><span class="keyword">var</span> minifyCSS = <span class="built_in">require</span>(<span class="string">'gulp-minify-css'</span>);</div><div class="line"><span class="keyword">var</span> concat = <span class="built_in">require</span>(<span class="string">'gulp-concat'</span>);</div><div class="line"><span class="keyword">var</span> rename = <span class="built_in">require</span>(<span class="string">'gulp-rename'</span>);</div><div class="line"></div><div class="line"><span class="comment">//统一配置路径</span></div><div class="line"><span class="keyword">var</span> path = &#123;</div><div class="line">    <span class="attr">scriptLint</span> : <span class="string">'./js/*.js'</span>,</div><div class="line">    <span class="attr">scriptUglify</span> : <span class="string">'./js/**/*.js'</span>,</div><div class="line">    <span class="attr">styles</span> : <span class="string">'./css/*.css'</span>,</div><div class="line">    <span class="attr">cssImage</span> : <span class="string">'./css/images/*'</span>,</div><div class="line">    <span class="attr">images</span> : <span class="string">'./images/*'</span></div><div class="line">&#125;;</div><div class="line"></div><div class="line"><span class="comment">//统一配置输出路径</span></div><div class="line"><span class="keyword">var</span> dest = &#123;</div><div class="line">    <span class="attr">js</span> : <span class="string">'./dist/js'</span>,</div><div class="line">    <span class="attr">css</span> : <span class="string">'./dist/css'</span>,</div><div class="line">    <span class="attr">cssImage</span> : <span class="string">'./dist/css/images'</span>,</div><div class="line">    <span class="attr">images</span> : <span class="string">'./dist/images'</span>,</div><div class="line">    <span class="attr">html</span> : <span class="string">'./dist/html'</span></div><div class="line">&#125;;</div><div class="line"></div><div class="line"><span class="comment">//全自动执行任务</span></div><div class="line">gulp.task(<span class="string">'build'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    gulp.run(<span class="string">'lint'</span>, <span class="string">'uglify'</span>, <span class="string">'imagemin'</span>, <span class="string">'css'</span>, <span class="string">'css-imagemin'</span>);</div><div class="line">&#125;);</div><div class="line"></div><div class="line"><span class="comment">//js语法检查</span></div><div class="line">gulp.task(<span class="string">'lint'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> gulp.src(path.scriptLint)</div><div class="line">        .pipe(jshint())</div><div class="line">        .pipe(jshint.reporter(stylish))</div><div class="line">        .pipe(jshint.reporter(<span class="string">'fail'</span>))      <span class="comment">//若语法错误过多，自动抛错处理</span></div><div class="line">&#125;);</div><div class="line"></div><div class="line"><span class="comment">//js压缩输出到指定文件夹,若需合并再压缩参考下面css合并压缩任务</span></div><div class="line">gulp.task(<span class="string">'uglify'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    gulp.src(path.scriptUglify)</div><div class="line">        .pipe(uglify())</div><div class="line">        .pipe(gulp.dest(dest.js))</div><div class="line">&#125;);</div><div class="line"></div><div class="line"><span class="comment">//image压缩输出到指定文件夹</span></div><div class="line">gulp.task(<span class="string">'imagemin'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> gulp.src(path.cssImage)</div><div class="line">        .pipe(imagemin(&#123;</div><div class="line">            <span class="attr">progessive</span> : <span class="literal">true</span>,</div><div class="line">            <span class="attr">svgoPlugins</span> : [&#123;<span class="attr">removeViewBox</span> : <span class="literal">false</span>&#125;],</div><div class="line">            <span class="attr">use</span> : [pngquant()]</div><div class="line">        &#125;))</div><div class="line">        .pipe(gulp.dest(dest.images))</div><div class="line">&#125;);</div><div class="line"></div><div class="line"><span class="comment">//合并压缩css到指定文件夹</span></div><div class="line">gulp.task(<span class="string">'css'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    gulp.src(path.styles)</div><div class="line">        .pipe(concat(<span class="string">'style.css'</span>))</div><div class="line">        .pipe(gulp.dest(dest.css))</div><div class="line">        .pipe(rename(<span class="string">'style.min.css'</span>))</div><div class="line">        .pipe(minifyCSS(&#123;</div><div class="line">            <span class="attr">keepBreaks</span> : <span class="literal">true</span>       <span class="comment">//是否保持换行,默认为false</span></div><div class="line">        &#125;))</div><div class="line">        .pipe(gulp.dest(dest.css))</div><div class="line">&#125;);</div><div class="line"></div><div class="line"><span class="comment">//css目录下面的images压缩输出到指定文件夹</span></div><div class="line">gulp.task(<span class="string">'css-imagemin'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">return</span> gulp.src(path.images)</div><div class="line">        .pipe(imagemin(&#123;</div><div class="line">            <span class="attr">progessive</span> : <span class="literal">true</span>,</div><div class="line">            <span class="attr">svgoPlugins</span> : [&#123;<span class="attr">removeViewBox</span> : <span class="literal">false</span>&#125;],</div><div class="line">            <span class="attr">use</span> : [pngquant()]</div><div class="line">        &#125;))</div><div class="line">        .pipe(gulp.dest(dest.cssImage))</div><div class="line">&#125;);</div></pre></td></tr></table></figure></p>
<h3 id="运行"><a href="#运行" class="headerlink" title="运行"></a>运行</h3><p>在根目录下面敲入下面代码，一次执行全自动化流程:<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">gulp build</div></pre></td></tr></table></figure></p>
<p>当然你也可以单独执行某个任务：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">gulp imagemin</div></pre></td></tr></table></figure></p>
<h3 id="写在最后"><a href="#写在最后" class="headerlink" title="写在最后"></a>写在最后</h3><p>例子中的gulpfile.js只是一个最简单的demo，没有加入watch文件监听功能，其它额外的功能也没有，具体所需什么功能可以到<a href="https://www.npmjs.org/" target="_blank" rel="external">npm官网</a>进行查询，总之“授之以鱼，不如授之予渔”。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;上周腾讯&lt;a href=&quot;http://tgideas.qq.com/&quot;&gt;TGideas&lt;/a&gt;团队来公司分享了一个gulp前端构建利器，自个儿玩了一下，于是有了此文。&lt;/p&gt;
&lt;p&gt;号称新一代前端构建利器的&lt;a href=&quot;https://github.com/gulpjs/gulp/blob/master/docs/getting-started.md&quot;&gt;gulp&lt;/a&gt;是基于流的，代码优于配置的构建工具。虽然已经熟悉用&lt;a href=&quot;http://gruntjs.cn/getting-started/&quot;&gt;grunt&lt;/a&gt;来配置构建，但用了gulp之后感觉这个gulp似乎更胜一筹。&lt;/p&gt;
    
    </summary>
    
      <category term="技术研究" scheme="https://cnt1992.github.io/categories/%E6%8A%80%E6%9C%AF%E7%A0%94%E7%A9%B6/"/>
    
    
      <category term="gulp,javascript" scheme="https://cnt1992.github.io/tags/gulp-javascript/"/>
    
  </entry>
  
  <entry>
    <title>koa下一代nodejs框架</title>
    <link href="https://cnt1992.github.io/2014/11/10/koa%E4%B8%8B%E4%B8%80%E4%BB%A3nodejs%E6%A1%86%E6%9E%B6/"/>
    <id>https://cnt1992.github.io/2014/11/10/koa下一代nodejs框架/</id>
    <published>2014-11-10T11:21:50.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<p><a href="http://koajs.com" target="_blank" rel="external">koa</a>是下一代的NodeJS框架，由原先<a href="http://expressjs.com/" target="_blank" rel="external">Express</a>团队开发，致力于更小，更有表现力的web应用程序。</p>
<p>若想在自己的机器上运行koa应用程序，必须是node 0.11版本以上，建议下载node版本控制器<a href="https://github.com/visionmedia/n" target="_blank" rel="external">n</a>或者<a href="https://github.com/creationix/nvm" target="_blank" rel="external">nvm</a>,自己试用了一下，感觉比较喜欢nvm，下面是koa入门的步骤。</p>
<blockquote>
<p>注：本实验平台基于mac osx ，若是windows平台，有一些对应步骤可能不同。</p>
</blockquote>
<a id="more"></a>
<ol>
<li><p>利用命令行直接安装nvm</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">curl https:<span class="comment">//raw.githubusercontent.com/creationix/nvm/v0.18.0/install.sh | bash</span></div></pre></td></tr></table></figure>
</li>
<li><p>利用nvm安装指定版本（必须是0.11.*以上版本）</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">nvm install <span class="number">0.11</span><span class="number">.12</span></div></pre></td></tr></table></figure>
</li>
</ol>
<p>或者安装node最新版本<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">nvm install latest</div></pre></td></tr></table></figure></p>
<ol>
<li><p>指定使用安装的node 版本</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">nvm use <span class="number">0.11</span><span class="number">.12</span></div></pre></td></tr></table></figure>
</li>
<li><p>新建app.js文件，录入下面经典的hello world代码</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> koa = <span class="built_in">require</span>(<span class="string">'koa'</span>);</div><div class="line"><span class="keyword">var</span> app = koa();</div><div class="line"></div><div class="line">app.use(<span class="function"><span class="keyword">function</span> *(<span class="params"></span>)</span>&#123;</div><div class="line">  <span class="keyword">this</span>.body = <span class="string">'Hello World'</span>;</div><div class="line">&#125;);</div><div class="line"></div><div class="line">app.listen(<span class="number">3000</span>);</div></pre></td></tr></table></figure>
</li>
<li><p>进入应用根目录，下载依赖module</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">npm install koa</div></pre></td></tr></table></figure>
</li>
<li><p>用node的harmony模式启动服务器</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">node --harmony koa</div></pre></td></tr></table></figure>
</li>
</ol>
<p>为了以后方便使用，可以将node设置为默认启动harmony模式的别名：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">alias node=<span class="string">'node --harmony'</span></div></pre></td></tr></table></figure></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;http://koajs.com&quot;&gt;koa&lt;/a&gt;是下一代的NodeJS框架，由原先&lt;a href=&quot;http://expressjs.com/&quot;&gt;Express&lt;/a&gt;团队开发，致力于更小，更有表现力的web应用程序。&lt;/p&gt;
&lt;p&gt;若想在自己的机器上运行koa应用程序，必须是node 0.11版本以上，建议下载node版本控制器&lt;a href=&quot;https://github.com/visionmedia/n&quot;&gt;n&lt;/a&gt;或者&lt;a href=&quot;https://github.com/creationix/nvm&quot;&gt;nvm&lt;/a&gt;,自己试用了一下，感觉比较喜欢nvm，下面是koa入门的步骤。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;注：本实验平台基于mac osx ，若是windows平台，有一些对应步骤可能不同。&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="技术研究" scheme="https://cnt1992.github.io/categories/%E6%8A%80%E6%9C%AF%E7%A0%94%E7%A9%B6/"/>
    
    
      <category term="koa,node.js,nvm,javascript" scheme="https://cnt1992.github.io/tags/koa-node-js-nvm-javascript/"/>
    
  </entry>
  
  <entry>
    <title>grunt自动化脚本</title>
    <link href="https://cnt1992.github.io/2014/11/05/grunt%E8%87%AA%E5%8A%A8%E5%8C%96%E8%84%9A%E6%9C%AC/"/>
    <id>https://cnt1992.github.io/2014/11/05/grunt自动化脚本/</id>
    <published>2014-11-05T10:49:29.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<p>前端自动化工具<a href="http://gruntjs.cn/getting-started/" target="_blank" rel="external">Grunt</a>自动化脚本，直接上例子。</p>
<a id="more"></a>
<h3 id="项目根目录下建立三个文件"><a href="#项目根目录下建立三个文件" class="headerlink" title="项目根目录下建立三个文件"></a>项目根目录下建立三个文件</h3><ol>
<li><p>package.json</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">&#123;</div><div class="line">    <span class="string">"name"</span>: <span class="string">"grunt demo"</span>,</div><div class="line">    <span class="string">"version"</span>: <span class="string">"0.1.0"</span>,</div><div class="line">    <span class="string">"author"</span>: <span class="string">"sky"</span>,</div><div class="line">    <span class="string">"devDependencies"</span>: &#123;</div><div class="line">        <span class="string">"matchdep"</span>: <span class="string">"~0.3.0"</span>,   <span class="comment">//一次执行多次load任务</span></div><div class="line">        <span class="string">"grunt"</span>: <span class="string">"~0.4.1"</span>,</div><div class="line">        <span class="string">"grunt-contrib-clean"</span>:<span class="string">"*"</span>,</div><div class="line">        <span class="string">"grunt-contrib-concat"</span>: <span class="string">"~0.3.0"</span>,</div><div class="line">        <span class="string">"grunt-contrib-uglify"</span>: <span class="string">"~0.2.1"</span>,</div><div class="line">        <span class="string">"grunt-contrib-jshint"</span>: <span class="string">"~0.10.0"</span>,</div><div class="line">        <span class="string">"grunt-contrib-copy"</span>: <span class="string">"*"</span>,</div><div class="line">        <span class="string">"grunt-contrib-watch"</span>: <span class="string">"~0.4.4"</span>,</div><div class="line">        <span class="string">"grunt-contrib-htmlmin"</span> : <span class="string">"*"</span>,</div><div class="line">        <span class="string">"grunt-contrib-cssmin"</span>:<span class="string">"*"</span>,</div><div class="line">        <span class="string">"grunt-contrib-imagemin"</span>:<span class="string">"*"</span>,</div><div class="line">        <span class="string">"grunt-contrib-connect"</span>:<span class="string">"*"</span></div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
</li>
<li><p>Gruntfile.js (注释代码看具体项目需要自行修改)</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">/**</span></div><div class="line"> * Created by sky on 2014/11/05</div><div class="line"> * 自动化脚本</div><div class="line"> */</div><div class="line"><span class="built_in">module</span>.exports = <span class="function"><span class="keyword">function</span> (<span class="params">grunt</span>) </span>&#123;</div><div class="line"><span class="meta">    'use strict'</span>;</div><div class="line"></div><div class="line">    <span class="comment">//加载所有grunt任务</span></div><div class="line">    <span class="built_in">require</span>(<span class="string">'matchdep'</span>).filterDev(<span class="string">'grunt-*'</span>).forEach(grunt.loadNpmTasks);</div><div class="line"></div><div class="line">    <span class="comment">//注册任务</span></div><div class="line">    grunt.registerTask(<span class="string">'build'</span>,<span class="string">'全自动CSS、JS 压缩 生成文件到build文件夹'</span>,[<span class="string">'clean'</span>,<span class="string">'uglify:build'</span>, <span class="string">'cssmin'</span>,<span class="string">'imagemin'</span>,<span class="string">'copy'</span>]);</div><div class="line">    grunt.registerTask(<span class="string">'server'</span>,<span class="string">'本地创建服务器 及 文件监听'</span>,[<span class="string">'connect'</span>,<span class="string">'watch'</span>]);</div><div class="line"></div><div class="line">    <span class="comment">//参数配置</span></div><div class="line">    <span class="keyword">var</span> open = grunt.option(<span class="string">'open'</span>) || <span class="literal">false</span>;</div><div class="line"></div><div class="line">    <span class="comment">//配置文件信息</span></div><div class="line">    <span class="keyword">var</span> cfg = grunt.file.readJSON(<span class="string">'config.json'</span>),</div><div class="line">        serverConf = cfg.server[cfg.server.selected];   <span class="comment">//本地web server信息</span></div><div class="line"></div><div class="line">    <span class="keyword">var</span> pkg = grunt.file.readJSON(<span class="string">'package.json'</span>);</div><div class="line"></div><div class="line">    <span class="comment">//写入文件头的信息</span></div><div class="line">    <span class="keyword">var</span> banner = <span class="string">'/*\n'</span> +</div><div class="line">        <span class="string">' * Copyright &lt;%= grunt.template.today("yyyy") %&gt;, &lt;%= pkg.author %&gt;\n'</span> +</div><div class="line">        <span class="string">' *\n'</span> +</div><div class="line">        <span class="string">' * Released on: &lt;%= grunt.template.today("yyyy-mm-dd h:MM:ss TT")  %&gt;  '</span>+ <span class="string">'&lt;%= pkg.author %&gt;'</span> +<span class="string">'\n'</span> +</div><div class="line">        <span class="string">'*/\n'</span>;</div><div class="line"></div><div class="line">    grunt.initConfig(&#123;</div><div class="line">        <span class="attr">pkg</span> : pkg,</div><div class="line">        <span class="comment">//先清除上一次操作遗留的记录</span></div><div class="line">        clean:&#123;</div><div class="line">            <span class="attr">build</span>:[<span class="string">'./build/'</span>]</div><div class="line">        &#125;,</div><div class="line">        <span class="comment">//JS代码校验</span></div><div class="line">        <span class="comment">// jshint : &#123;</span></div><div class="line">        <span class="comment">//  //更多选项: http://www.jshint.com/docs/options/</span></div><div class="line">        <span class="comment">//  options : &#123;</span></div><div class="line">        <span class="comment">//      eqeqeq : true,</span></div><div class="line">        <span class="comment">//      devel : true,</span></div><div class="line">        <span class="comment">//      asi : true, //忽略分号</span></div><div class="line">        <span class="comment">//      expr : true,  //支持赋值语句</span></div><div class="line">        <span class="comment">//      globals : &#123;</span></div><div class="line">        <span class="comment">//          jQuery : true,</span></div><div class="line">        <span class="comment">//          console : true</span></div><div class="line">        <span class="comment">//      &#125;</span></div><div class="line">        <span class="comment">//  &#125;,</span></div><div class="line">        <span class="comment">//  build : &#123;</span></div><div class="line">        <span class="comment">//      files : &#123;</span></div><div class="line">        <span class="comment">//          src : [ './js/*']</span></div><div class="line">        <span class="comment">//      &#125;</span></div><div class="line">        <span class="comment">//  &#125;</span></div><div class="line">        <span class="comment">// &#125;,</span></div><div class="line">        <span class="comment">//JS合并文件</span></div><div class="line">        <span class="comment">// concat: &#123;</span></div><div class="line">        <span class="comment">//     options: &#123;</span></div><div class="line">        <span class="comment">//         //文件内容的分隔符</span></div><div class="line">        <span class="comment">//         separator: '\n',</span></div><div class="line">        <span class="comment">//         stripBanners: true,</span></div><div class="line">        <span class="comment">//         banner: banner</span></div><div class="line">        <span class="comment">//     &#125;,</span></div><div class="line">        <span class="comment">//     build: &#123;</span></div><div class="line">        <span class="comment">//         src: ['./js/*.js'],</span></div><div class="line">        <span class="comment">//         dest: './build/js/script.full.js'</span></div><div class="line">        <span class="comment">//     &#125;</span></div><div class="line">        <span class="comment">// &#125;,</span></div><div class="line">        <span class="comment">//HTML压缩</span></div><div class="line">      <span class="comment">//   htmlmin : &#123;</span></div><div class="line">      <span class="comment">//    //更多选项：https://github.com/gruntjs/grunt-contrib-htmlmin</span></div><div class="line">      <span class="comment">//    options : &#123;</span></div><div class="line">      <span class="comment">//        removeComments : true,</span></div><div class="line">      <span class="comment">//        collapseWhitespace : true</span></div><div class="line">      <span class="comment">//    &#125;,</span></div><div class="line">      <span class="comment">//    minify : &#123;</span></div><div class="line">            <span class="comment">//  expand : true,</span></div><div class="line">            <span class="comment">//  cwd : './html/',</span></div><div class="line">            <span class="comment">//  src : ['*.html'],</span></div><div class="line">            <span class="comment">//  dest : './build/html/',</span></div><div class="line">            <span class="comment">//  ext : '.html'</span></div><div class="line">            <span class="comment">// &#125;</span></div><div class="line">      <span class="comment">//   &#125;,</span></div><div class="line">        <span class="comment">//JS压缩</span></div><div class="line">        uglify : &#123;</div><div class="line">            <span class="comment">//文件头部输出信息</span></div><div class="line">            options: &#123;</div><div class="line">                <span class="attr">banner</span>: banner,</div><div class="line">                <span class="attr">report</span>: <span class="string">"min"</span><span class="comment">//输出压缩率，可选的值有 false(不输出信息)，gzip</span></div><div class="line">            &#125;,</div><div class="line">            <span class="comment">//具体任务配置</span></div><div class="line">            build:&#123;</div><div class="line">                <span class="comment">//用该方法表示只压缩js分别输出到对应文件夹,不合并js</span></div><div class="line">                expand : <span class="literal">true</span>,</div><div class="line">                <span class="attr">cwd</span> : <span class="string">'./js/'</span>,</div><div class="line">                <span class="attr">src</span> : [<span class="string">'**/*.js'</span>],</div><div class="line">                <span class="attr">dest</span> : <span class="string">'./build/js/'</span></div><div class="line"></div><div class="line">                <span class="comment">//用该方法表示先启动上面js合并再压缩</span></div><div class="line">                <span class="comment">// src: ['&lt;%= concat.build.dest %&gt;'],</span></div><div class="line">                <span class="comment">// dest: './build/js/script.js'</span></div><div class="line">            &#125;</div><div class="line">        &#125;,</div><div class="line">        <span class="comment">//CSS压缩</span></div><div class="line">        cssmin : &#123;</div><div class="line">            <span class="comment">//文件头部输出信息</span></div><div class="line">            options : &#123;</div><div class="line">                <span class="attr">banner</span> : banner</div><div class="line">            &#125;,</div><div class="line">            <span class="attr">minify</span> : &#123;</div><div class="line">                <span class="comment">//用该方法会将所有css合并压缩成一个文件</span></div><div class="line">                <span class="comment">// files : &#123;</span></div><div class="line">                <span class="comment">//  './build/css/style.css' : ['./css/*.css']</span></div><div class="line">                <span class="comment">// &#125;</span></div><div class="line"></div><div class="line">                <span class="comment">//用该方法会压缩所有css并且保持每个css独立</span></div><div class="line">                expand : <span class="literal">true</span>,</div><div class="line">                <span class="attr">cwd</span> : <span class="string">'./css/'</span>,</div><div class="line">                <span class="attr">src</span> : [<span class="string">'*.css'</span>],</div><div class="line">                <span class="attr">dest</span> : <span class="string">'./build/css/'</span>,</div><div class="line">                <span class="attr">ext</span> : <span class="string">'.css'</span></div><div class="line">            &#125;</div><div class="line">        &#125;,</div><div class="line">        <span class="comment">//图片压缩</span></div><div class="line">        imagemin : &#123;</div><div class="line">            <span class="attr">build</span> : &#123;</div><div class="line">                <span class="attr">files</span> : [&#123;</div><div class="line">                    <span class="attr">expand</span> : <span class="literal">true</span>,          <span class="comment">//启动下面选项</span></div><div class="line">                    cwd : <span class="string">'./css/images/'</span>,</div><div class="line">                    <span class="attr">src</span> : [<span class="string">'**/*.&#123;png,jpg,gif&#125;'</span>],</div><div class="line">                    <span class="attr">dest</span> : <span class="string">'./build/css/images/'</span></div><div class="line">                &#125;]</div><div class="line">            &#125;</div><div class="line">        &#125;,</div><div class="line">        <span class="comment">//将images文件夹copy到build目录下</span></div><div class="line">        copy : &#123;</div><div class="line">            <span class="attr">build</span> : &#123;</div><div class="line">                <span class="attr">expand</span> : <span class="literal">true</span>,</div><div class="line">                <span class="attr">cwd</span> : <span class="string">'./images/'</span>,</div><div class="line">                <span class="attr">src</span> : <span class="string">'**'</span>,</div><div class="line">                <span class="attr">dest</span> : <span class="string">'./build/images/'</span>,</div><div class="line">                <span class="attr">filter</span> : <span class="string">'isFile'</span></div><div class="line">            &#125;</div><div class="line">        &#125;,</div><div class="line">        <span class="comment">//本机运行一个web server</span></div><div class="line">        connect: &#123;</div><div class="line">            <span class="attr">server</span>: &#123;</div><div class="line">                <span class="attr">options</span>: &#123;</div><div class="line">                    <span class="attr">port</span>: serverConf.port,</div><div class="line">                    <span class="attr">hostname</span>: serverConf.host,</div><div class="line">                    <span class="attr">base</span>: serverConf.src,</div><div class="line">                    <span class="attr">open</span>: open,</div><div class="line">                    <span class="attr">livereload</span>:serverConf.livereload</div><div class="line">                &#125;</div><div class="line">            &#125;</div><div class="line">        &#125;,</div><div class="line">        <span class="comment">//监听文件变动</span></div><div class="line">        <span class="comment">//改动哪个文件类型 就执行哪个命令 减少多余命令的执行</span></div><div class="line">        watch : &#123;</div><div class="line">            <span class="comment">//更多选项： http://github.com/gruntjs/grunt-contrib-watch</span></div><div class="line">            options : &#123;</div><div class="line">                <span class="attr">livereload</span> : <span class="literal">true</span></div><div class="line">            &#125;,</div><div class="line">            <span class="comment">// html : &#123;</span></div><div class="line">            <span class="comment">//  files : './html/*',</span></div><div class="line">            <span class="comment">//  tasks : ['htmlmin']</span></div><div class="line">            <span class="comment">// &#125;,</span></div><div class="line">            <span class="comment">//JS变动</span></div><div class="line">            scripts : &#123;</div><div class="line">                <span class="attr">files</span> : <span class="string">'./js/*'</span>,</div><div class="line">                <span class="attr">tasks</span> : [<span class="string">'uglify:build'</span>]</div><div class="line">            &#125;,</div><div class="line">            <span class="comment">//CSS变动</span></div><div class="line">            css : &#123;</div><div class="line">                <span class="attr">files</span> : <span class="string">'./css/*'</span>,</div><div class="line">                <span class="attr">tasks</span> : [<span class="string">'cssmin'</span>]</div><div class="line">            &#125;,</div><div class="line">            <span class="comment">//图片变动</span></div><div class="line">            img : &#123;</div><div class="line">                <span class="attr">files</span> : <span class="string">'./css/images/*'</span>,</div><div class="line">                <span class="attr">tasks</span> : [<span class="string">'imagemin'</span>]</div><div class="line">            &#125;,</div><div class="line">            <span class="comment">//内容图变动</span></div><div class="line">            images : &#123;</div><div class="line">                <span class="attr">files</span> : <span class="string">'./images/*'</span>,</div><div class="line">                <span class="attr">tasks</span> : [<span class="string">'copy'</span>]</div><div class="line">            &#125;   </div><div class="line">        &#125;</div><div class="line">        </div><div class="line">    &#125;);</div><div class="line">&#125;</div></pre></td></tr></table></figure>
</li>
<li><p>config.json (该文件看是否需要启动服务器来监听，若不需要不需配置)</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">&#123;</div><div class="line">    <span class="string">"server"</span>:&#123;</div><div class="line">        <span class="string">"selected"</span>: <span class="string">"localhost"</span>,</div><div class="line">        <span class="string">"localhost"</span>:&#123;</div><div class="line">            <span class="string">"host"</span>: <span class="string">"localhost"</span>,</div><div class="line">            <span class="string">"port"</span>: <span class="string">"8081"</span>,</div><div class="line">            <span class="string">"livereload"</span>: <span class="string">"35729"</span>,</div><div class="line">            <span class="string">"src"</span>: <span class="string">"./"</span></div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
</li>
</ol>
<h3 id="运行命令"><a href="#运行命令" class="headerlink" title="运行命令"></a>运行命令</h3><blockquote>
<p>npm install (加载安装相对应的包)</p>
<p>grunt build (执行grunt build全自动化命令，在项目根目录下生成build文件夹)</p>
<p>grunt server (启动服务器并且监听文件改动)</p>
</blockquote>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;前端自动化工具&lt;a href=&quot;http://gruntjs.cn/getting-started/&quot;&gt;Grunt&lt;/a&gt;自动化脚本，直接上例子。&lt;/p&gt;
    
    </summary>
    
      <category term="技术研究" scheme="https://cnt1992.github.io/categories/%E6%8A%80%E6%9C%AF%E7%A0%94%E7%A9%B6/"/>
    
    
      <category term="grunt,javascript" scheme="https://cnt1992.github.io/tags/grunt-javascript/"/>
    
  </entry>
  
  <entry>
    <title>利用exif获取图片信息</title>
    <link href="https://cnt1992.github.io/2014/10/08/%E5%88%A9%E7%94%A8exif%E8%8E%B7%E5%8F%96%E5%9B%BE%E7%89%87%E4%BF%A1%E6%81%AF/"/>
    <id>https://cnt1992.github.io/2014/10/08/利用exif获取图片信息/</id>
    <published>2014-10-08T09:38:00.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<p>生活中拍摄了很多图片，有时候想用照片流的形式来展示，很多手机都已经做了这些处理，你可以选择按照地点来排列，或者按照拍摄日期来排列，那这些都是通过什么技术来实现的呢？</p>
<p>其实图片存储了大量的信息，可以通过exif来获取到这些信息，获取这些信息可以通过java或者php等后台语言来实现，在这里我用jquery的一个插件来实现。</p>
<a id="more"></a>
<p>HTML代码:</p>
<figure class="highlight html"><table><tr><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">"exif.jpg"</span> <span class="attr">id</span>=<span class="string">"img"</span> <span class="attr">exif</span>=<span class="string">"true"</span> <span class="attr">alt</span>=<span class="string">"点击图片查看GPS信息"</span> <span class="attr">title</span>=<span class="string">"tiger"</span> <span class="attr">width</span>=<span class="string">"500"</span>/&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span> <span class="attr">src</span>=<span class="string">"jquery.min.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span> <span class="attr">src</span>=<span class="string">"jquery.exif.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></div><div class="line"><span class="tag">&lt;<span class="name">script</span> <span class="attr">type</span>=<span class="string">"text/javascript"</span> <span class="attr">src</span>=<span class="string">"demo.js"</span>&gt;</span><span class="undefined"></span><span class="tag">&lt;/<span class="name">script</span>&gt;</span></div></pre></td></tr></table></figure>
<p>JS代码：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">$(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">    </div><div class="line">    $(<span class="string">"#img"</span>).on( <span class="string">"click"</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line"></div><div class="line">        <span class="keyword">var</span> lat_ref = $(<span class="keyword">this</span>).exif(<span class="string">"GPSLatitudeRef"</span>);</div><div class="line"></div><div class="line">        <span class="keyword">if</span>(<span class="keyword">typeof</span> lat_ref[<span class="number">0</span>] == <span class="string">"undefined"</span>)&#123;</div><div class="line">            <span class="built_in">console</span>.log(<span class="string">"图片没有GPS信息"</span>);</div><div class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        <span class="keyword">var</span> $<span class="keyword">this</span> = $(<span class="keyword">this</span>);</div><div class="line"></div><div class="line">        <span class="keyword">var</span> lat = $<span class="keyword">this</span>.exif(<span class="string">"GPSLatitude"</span>);        <span class="comment">//纬度</span></div><div class="line">        <span class="keyword">var</span> lng = $<span class="keyword">this</span>.exif(<span class="string">"GPSLongitude"</span>);       <span class="comment">//经度</span></div><div class="line">        <span class="keyword">var</span> lng_ref = $<span class="keyword">this</span>.exif(<span class="string">"GPSLongitudeRef"</span>);</div><div class="line">        <span class="keyword">var</span> make = $<span class="keyword">this</span>.exif(<span class="string">"Make"</span>);              <span class="comment">//相机</span></div><div class="line">        <span class="keyword">var</span> model = $<span class="keyword">this</span>.exif(<span class="string">"Model"</span>);            <span class="comment">//型号</span></div><div class="line">        <span class="keyword">var</span> t = $<span class="keyword">this</span>.exif(<span class="string">"ExposureTime"</span>);         <span class="comment">//快门</span></div><div class="line">        <span class="keyword">var</span> f = $<span class="keyword">this</span>.exif(<span class="string">"FNumber"</span>);              <span class="comment">//光圈</span></div><div class="line">        <span class="keyword">var</span> length = $<span class="keyword">this</span>.exif(<span class="string">"FocalLength"</span>);</div><div class="line">        <span class="keyword">var</span> iso = $<span class="keyword">this</span>.exif(<span class="string">"ISOSpeedRatings"</span>);    <span class="comment">//ISO</span></div><div class="line">        <span class="keyword">var</span> ev = $<span class="keyword">this</span>.exif(<span class="string">"ExposureBias"</span>);</div><div class="line">        <span class="keyword">var</span> soft = $<span class="keyword">this</span>.exif(<span class="string">"Software"</span>);          <span class="comment">//软体</span></div><div class="line">        <span class="keyword">var</span> date = $<span class="keyword">this</span>.exif(<span class="string">"DateTime"</span>);          <span class="comment">//拍摄时间</span></div><div class="line">        <span class="keyword">var</span> dpi = $<span class="keyword">this</span>.exif(<span class="string">"XResolution"</span>);</div><div class="line">        <span class="keyword">var</span> sa = $<span class="keyword">this</span>.exif(<span class="string">"Saturation"</span>);</div><div class="line">        <span class="keyword">var</span> sha = $<span class="keyword">this</span>.exif(<span class="string">"Sharpness"</span>);</div><div class="line">        <span class="keyword">var</span> wb = $<span class="keyword">this</span>.exif(<span class="string">"WhiteBalance"</span>);</div><div class="line">        <span class="keyword">var</span> lat_ref = $<span class="keyword">this</span>.exif(<span class="string">"GPSLatitudeRef"</span>);<span class="comment">//North or South</span></div><div class="line">        <span class="keyword">var</span> lat = $<span class="keyword">this</span>.exif(<span class="string">"GPSLatitude"</span>);       <span class="comment">//纬度值</span></div><div class="line">        <span class="keyword">var</span> lng_ref = $<span class="keyword">this</span>.exif(<span class="string">"GPSLongitudeRef"</span>);<span class="comment">//West or East</span></div><div class="line">        <span class="keyword">var</span> lng = $<span class="keyword">this</span>.exif(<span class="string">"GPSLongitude"</span>);</div><div class="line"></div><div class="line">        <span class="keyword">var</span> lng_s = lng_ref == <span class="string">"E"</span> ? <span class="string">"东经："</span> : <span class="string">"西经："</span>;</div><div class="line">        <span class="keyword">var</span> lat_s = lat_ref == <span class="string">"N"</span> ? <span class="string">"北纬："</span> : <span class="string">"南纬："</span>;</div><div class="line">        </div><div class="line">        </div><div class="line">        <span class="built_in">console</span>.log(lat_s+change_latlng(lat)+<span class="string">"\r\n"</span>+lng_s+change_latlng(lng));</div><div class="line"></div><div class="line">        <span class="built_in">console</span>.log(<span class="string">"相机："</span> + make + <span class="string">"，型号："</span> + model + <span class="string">",拍摄时间："</span> + date);</div><div class="line">    &#125;);</div><div class="line"></div><div class="line">    <span class="function"><span class="keyword">function</span> <span class="title">change_latlng</span>(<span class="params">latlng</span>)</span>&#123; <span class="comment">//经纬度转换为十进制     </span></div><div class="line">        <span class="keyword">var</span> arr=latlng;</div><div class="line">        <span class="keyword">return</span> arr[<span class="number">0</span>][<span class="number">0</span>]+arr[<span class="number">0</span>][<span class="number">1</span>]/<span class="number">60</span>+arr[<span class="number">0</span>][<span class="number">2</span>]/<span class="number">3600</span>;</div><div class="line">    &#125;</div><div class="line">&#125;);</div></pre></td></tr></table></figure>
<p>通过上面demo获取到拍摄的相机，型号，拍摄时间，以及经纬度，足够我们去制作个性化的照片墙了。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;生活中拍摄了很多图片，有时候想用照片流的形式来展示，很多手机都已经做了这些处理，你可以选择按照地点来排列，或者按照拍摄日期来排列，那这些都是通过什么技术来实现的呢？&lt;/p&gt;
&lt;p&gt;其实图片存储了大量的信息，可以通过exif来获取到这些信息，获取这些信息可以通过java或者php等后台语言来实现，在这里我用jquery的一个插件来实现。&lt;/p&gt;
    
    </summary>
    
      <category term="技术研究" scheme="https://cnt1992.github.io/categories/%E6%8A%80%E6%9C%AF%E7%A0%94%E7%A9%B6/"/>
    
    
      <category term="javascript exif" scheme="https://cnt1992.github.io/tags/javascript-exif/"/>
    
  </entry>
  
  <entry>
    <title>JS放大镜效果</title>
    <link href="https://cnt1992.github.io/2014/10/01/JS%E6%94%BE%E5%A4%A7%E9%95%9C%E6%95%88%E6%9E%9C/"/>
    <id>https://cnt1992.github.io/2014/10/01/JS放大镜效果/</id>
    <published>2014-09-30T16:14:00.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>本文将利用原生JS打造一个放大镜效果</p>
</blockquote>
<a id="more"></a>
<p>HTML部分：</p>
<figure class="highlight html"><table><tr><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"demo"</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"small-box"</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"mark"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"float-box"</span>&gt;</span><span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">"small.jpg"</span>/&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">div</span> <span class="attr">id</span>=<span class="string">"big-box"</span>&gt;</span></div><div class="line">        <span class="tag">&lt;<span class="name">img</span> <span class="attr">src</span>=<span class="string">"big.jpg"</span>/&gt;</span></div><div class="line">    <span class="tag">&lt;/<span class="name">div</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">div</span>&gt;</span></div></pre></td></tr></table></figure>
<p>CSS部分：</p>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">* &#123;</div><div class="line">    margin: 0;</div><div class="line">    padding: 0</div><div class="line">&#125;</div><div class="line"></div><div class="line">#demo &#123;</div><div class="line">    display: block;</div><div class="line">    width: 400px;</div><div class="line">    height: 255px;</div><div class="line">    margin: 50px;</div><div class="line">    position: relative;</div><div class="line">    border: 1px solid #ccc;</div><div class="line">&#125;</div><div class="line"></div><div class="line">#small-box &#123;</div><div class="line">    position: relative;</div><div class="line">    z-index: 1;</div><div class="line">&#125;</div><div class="line"></div><div class="line">#float-box &#123;</div><div class="line">    display: none;</div><div class="line">    width: 160px;</div><div class="line">    height: 120px;</div><div class="line">    position: absolute;</div><div class="line">    background: #ffffcc;</div><div class="line">    border: 1px solid #ccc;</div><div class="line">    filter: alpha(opacity=50);</div><div class="line">    opacity: 0.5;</div><div class="line">&#125;</div><div class="line"></div><div class="line">#mark &#123;</div><div class="line">    position: absolute;</div><div class="line">    display: block;</div><div class="line">    width: 400px;</div><div class="line">    height: 255px;</div><div class="line">    background-color: #fff;</div><div class="line">    filter: alpha(opacity=0);</div><div class="line">    opacity: 0;</div><div class="line">    z-index: 10;</div><div class="line">&#125;</div><div class="line"></div><div class="line">#big-box &#123;</div><div class="line">    display: none;</div><div class="line">    position: absolute;</div><div class="line">    top: 0;</div><div class="line">    left: 460px;</div><div class="line">    width: 400px;</div><div class="line">    height: 300px;</div><div class="line">    overflow: hidden;</div><div class="line">    border: 1px solid #ccc;</div><div class="line">    z-index: 1;;</div><div class="line">&#125;</div><div class="line"></div><div class="line">#big-box img &#123;</div><div class="line">    position: absolute;</div><div class="line">    z-index: 5</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>JS部分：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">//页面加载完毕后执行</span></div><div class="line"><span class="built_in">window</span>.onload = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line"></div><div class="line">    <span class="keyword">var</span> objDemo = <span class="built_in">document</span>.getElementById(<span class="string">"demo"</span>);</div><div class="line">    <span class="keyword">var</span> objSmallBox = <span class="built_in">document</span>.getElementById(<span class="string">"small-box"</span>);</div><div class="line">    <span class="keyword">var</span> objMark = <span class="built_in">document</span>.getElementById(<span class="string">"mark"</span>);</div><div class="line">    <span class="keyword">var</span> objFloatBox = <span class="built_in">document</span>.getElementById(<span class="string">"float-box"</span>);</div><div class="line">    <span class="keyword">var</span> objBigBox = <span class="built_in">document</span>.getElementById(<span class="string">"big-box"</span>);</div><div class="line">    <span class="keyword">var</span> objBigBoxImage = objBigBox.getElementsByTagName(<span class="string">"img"</span>)[<span class="number">0</span>];</div><div class="line"></div><div class="line">    objMark.onmouseover = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">        objFloatBox.style.display = <span class="string">"block"</span></div><div class="line">        objBigBox.style.display = <span class="string">"block"</span></div><div class="line">    &#125;</div><div class="line"></div><div class="line">    objMark.onmouseout = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">        objFloatBox.style.display = <span class="string">"none"</span></div><div class="line">        objBigBox.style.display = <span class="string">"none"</span></div><div class="line">    &#125;</div><div class="line"></div><div class="line">    objMark.onmousemove = <span class="function"><span class="keyword">function</span> (<span class="params">ev</span>) </span>&#123;</div><div class="line"></div><div class="line">        <span class="keyword">var</span> _event = ev || <span class="built_in">window</span>.event;</div><div class="line"></div><div class="line">        <span class="keyword">var</span> left = _event.clientX - objDemo.offsetLeft - objSmallBox.offsetLeft - objFloatBox.offsetWidth / <span class="number">2</span>;</div><div class="line">        <span class="keyword">var</span> top = _event.clientY - objDemo.offsetTop - objSmallBox.offsetTop - objFloatBox.offsetHeight / <span class="number">2</span>;</div><div class="line"></div><div class="line">        <span class="keyword">if</span> (left &lt; <span class="number">0</span>) &#123;</div><div class="line">            left = <span class="number">0</span>;</div><div class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (left &gt; (objMark.offsetWidth - objFloatBox.offsetWidth)) &#123;</div><div class="line">            left = objMark.offsetWidth - objFloatBox.offsetWidth;</div><div class="line">        &#125;</div><div class="line"></div><div class="line">        <span class="keyword">if</span> (top &lt; <span class="number">0</span>) &#123;</div><div class="line">            top = <span class="number">0</span>;</div><div class="line">        &#125; <span class="keyword">else</span> <span class="keyword">if</span> (top &gt; (objMark.offsetHeight - objFloatBox.offsetHeight)) &#123;</div><div class="line">            top = objMark.offsetHeight - objFloatBox.offsetHeight;</div><div class="line"></div><div class="line">        &#125;</div><div class="line"></div><div class="line">        objFloatBox.style.left = left + <span class="string">"px"</span>;</div><div class="line">        objFloatBox.style.top = top + <span class="string">"px"</span>;</div><div class="line"></div><div class="line">        <span class="keyword">var</span> percentX = left / (objMark.offsetWidth - objFloatBox.offsetWidth);</div><div class="line">        <span class="keyword">var</span> percentY = top / (objMark.offsetHeight - objFloatBox.offsetHeight);</div><div class="line"></div><div class="line">        objBigBoxImage.style.left = -percentX * (objBigBoxImage.offsetWidth - objBigBox.offsetWidth) + <span class="string">"px"</span>;</div><div class="line">        objBigBoxImage.style.top = -percentY * (objBigBoxImage.offsetHeight - objBigBox.offsetHeight) + <span class="string">"px"</span>;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;本文将利用原生JS打造一个放大镜效果&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="个人学习" scheme="https://cnt1992.github.io/categories/%E4%B8%AA%E4%BA%BA%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="javascript" scheme="https://cnt1992.github.io/tags/javascript/"/>
    
  </entry>
  
  <entry>
    <title>节流函数实现</title>
    <link href="https://cnt1992.github.io/2014/10/01/%E8%8A%82%E6%B5%81%E5%87%BD%E6%95%B0%E5%AE%9E%E7%8E%B0/"/>
    <id>https://cnt1992.github.io/2014/10/01/节流函数实现/</id>
    <published>2014-09-30T16:04:00.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="keyword">var</span> timer = <span class="literal">null</span>;</div><div class="line">$(<span class="built_in">window</span>).on(<span class="string">'事件名称'</span>,<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">       <span class="keyword">if</span>(timer)&#123;</div><div class="line">           clearTimeout(timer)</div><div class="line">       &#125;</div><div class="line">       timer = setTimeout(<span class="function"><span class="keyword">function</span>(<span class="params"></span>)</span>&#123;</div><div class="line">           <span class="comment">//执行相应函数</span></div><div class="line">       &#125;,<span class="number">400</span>);</div><div class="line">   &#125;</div><div class="line">);</div></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;figure class=&quot;highlight javascript&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;&lt;span class=&quot;keyword&quot;&gt;var&lt;/span&gt; timer = &lt;span class
    
    </summary>
    
      <category term="个人学习" scheme="https://cnt1992.github.io/categories/%E4%B8%AA%E4%BA%BA%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="javascript" scheme="https://cnt1992.github.io/tags/javascript/"/>
    
  </entry>
  
  <entry>
    <title>NodeJS NPM 镜像使用方法</title>
    <link href="https://cnt1992.github.io/2014/08/22/NodeJS%20NPM%E9%95%9C%E5%83%8F%E4%BD%BF%E7%94%A8%E6%96%B9%E6%B3%95/"/>
    <id>https://cnt1992.github.io/2014/08/22/NodeJS NPM镜像使用方法/</id>
    <published>2014-08-21T19:22:00.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<p>通过改变默认npm镜像代理服务，以下三种办法任意一种都能解决问题，建议使用第三种，将配置写死，下次用的时候不用重新配置。</p>
<ol>
<li><p>通过config命令</p>
<figure class="highlight gams"><table><tr><td class="code"><pre><div class="line">npm config <span class="keyword">set</span> registry <span class="comment">https:</span>//<span class="comment">registry.npm.taobao.org</span></div><div class="line">npm <span class="comment">info underscore (</span>如果上面配置正确这个命令会有字符串<span class="comment">response)</span></div></pre></td></tr></table></figure>
</li>
<li><p>命令行指定</p>
<figure class="highlight nginx"><table><tr><td class="code"><pre><div class="line"><span class="attribute">npm</span> --registry https://registry.npm.taobao.org <span class="literal">info</span> underscore</div></pre></td></tr></table></figure>
</li>
<li><p>编辑 ~/.npmrc 加入下面内容</p>
<figure class="highlight groovy"><table><tr><td class="code"><pre><div class="line">registry = <span class="string">https:</span><span class="comment">//registry.npm.taobao.org</span></div></pre></td></tr></table></figure>
</li>
</ol>
<p>搜索镜像：<a href="https://npm.taobao.org" target="_blank" rel="external">https://npm.taobao.org</a></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;通过改变默认npm镜像代理服务，以下三种办法任意一种都能解决问题，建议使用第三种，将配置写死，下次用的时候不用重新配置。&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;p&gt;通过config命令&lt;/p&gt;
&lt;figure class=&quot;highlight gams&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td
    
    </summary>
    
      <category term="个人学习" scheme="https://cnt1992.github.io/categories/%E4%B8%AA%E4%BA%BA%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="node.js npm 淘宝镜像" scheme="https://cnt1992.github.io/tags/node-js-npm-%E6%B7%98%E5%AE%9D%E9%95%9C%E5%83%8F/"/>
    
  </entry>
  
  <entry>
    <title>数组递增算法</title>
    <link href="https://cnt1992.github.io/2014/07/01/%E6%95%B0%E7%BB%84%E9%80%92%E5%A2%9E%E7%AE%97%E6%B3%95/"/>
    <id>https://cnt1992.github.io/2014/07/01/数组递增算法/</id>
    <published>2014-07-01T14:40:00.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>Question：已经一个由数字构成的数组，用Javascript求出数组里面递增最长的数组（不考虑等长情况）</p>
</blockquote>
<a id="more"></a>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="comment">//设定一个原数组</span></div><div class="line"><span class="keyword">var</span> orignalArray = [<span class="number">1</span>,<span class="number">2</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">7</span>,<span class="number">8</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">7</span>,<span class="number">8</span>,<span class="number">9</span>,<span class="number">2</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">78</span>,<span class="number">3</span>,<span class="number">2</span>,<span class="number">4</span>,<span class="number">6</span>,<span class="number">7</span>,<span class="number">9</span>,<span class="number">1</span>,<span class="number">2</span>,<span class="number">3</span>,<span class="number">4</span>,<span class="number">5</span>,<span class="number">6</span>,<span class="number">7</span>,<span class="number">8</span>,<span class="number">9</span>];</div><div class="line"><span class="keyword">var</span> idx = <span class="number">0</span>,result = &#123;</div><div class="line">  <span class="attr">max</span> : <span class="literal">null</span>,</div><div class="line">  <span class="attr">resultArray</span> : []</div><div class="line">&#125;;</div><div class="line"></div><div class="line"><span class="comment">//调用函数求出递增最长数组</span></div><div class="line">sortArray(orignalArray);</div><div class="line"></div><div class="line">  </div><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">sortArray</span>(<span class="params">orignalArray</span>)</span>&#123;</div><div class="line"></div><div class="line">  <span class="keyword">var</span> length = orignalArray.length;</div><div class="line"></div><div class="line">  <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>; i &lt; length; i++) &#123;</div><div class="line">    <span class="comment">//这里的i === length-1表示已经到最末尾的情况</span></div><div class="line">    <span class="keyword">if</span>(orignalArray[i] &gt; orignalArray[i+<span class="number">1</span>] || i === length - <span class="number">1</span>)&#123;           </div><div class="line"></div><div class="line">      <span class="keyword">var</span> tempArray = orignalArray.slice(idx,i+<span class="number">1</span>); </div><div class="line">      idx = i+<span class="number">1</span>;</div><div class="line"></div><div class="line">      <span class="comment">//如果切割出来的数组比原先存在result里面的数组长度要长，就替换掉原来result里面的数组</span></div><div class="line">      <span class="keyword">if</span>(tempArray.length &gt; result.max)&#123;</div><div class="line">        result.max = tempArray.length;</div><div class="line">        result.resultArray = [];  </div><div class="line">        result.resultArray.push(tempArray);</div><div class="line">      &#125;</div><div class="line">        </div><div class="line">    &#125;</div><div class="line">  &#125;;</div><div class="line">  <span class="comment">//打印出结果</span></div><div class="line">  <span class="built_in">console</span>.log(result)</div><div class="line">&#125;</div></pre></td></tr></table></figure>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;Question：已经一个由数字构成的数组，用Javascript求出数组里面递增最长的数组（不考虑等长情况）&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="技术研究" scheme="https://cnt1992.github.io/categories/%E6%8A%80%E6%9C%AF%E7%A0%94%E7%A9%B6/"/>
    
    
      <category term="javascript 算法" scheme="https://cnt1992.github.io/tags/javascript-%E7%AE%97%E6%B3%95/"/>
    
  </entry>
  
  <entry>
    <title>ie6下z-index解决tips</title>
    <link href="https://cnt1992.github.io/2013/11/03/ie6%E4%B8%8Bz-index%E8%A7%A3%E5%86%B3tips/"/>
    <id>https://cnt1992.github.io/2013/11/03/ie6下z-index解决tips/</id>
    <published>2013-11-03T01:26:00.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<p>IE6虽然已经被公认放弃，但是不少前端开发人员还是在苦逼的兼容IE6.我没有刻意去学习IE6的hack，只有在碰到的时候才会去兼容。至于z-index这个bug发生的条件有三个：</p>
<ol>
<li>父标签position属性为relative；</li>
<li>问题标签无position属性（不包括static）；</li>
<li>问题标签含有浮动(float)属性；</li>
</ol>
<p>那解决办法肯定是针对这三个条件，去除任何一个即可。给出的方法也有三个：</p>
<ol>
<li>position:relative改为position:absolute；</li>
<li>去除浮动；</li>
<li>浮动元素添加position属性（如relative，absolute等）。</li>
</ol>
<p>关于ie hack，个人的感想是懂得解决一类bug的方法，而不是针对某个bug的特定方法，而且最重要的还是学会如何解决，像3px bug、注释bug、盒子bug等等，这些bug其实都大同小异。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;IE6虽然已经被公认放弃，但是不少前端开发人员还是在苦逼的兼容IE6.我没有刻意去学习IE6的hack，只有在碰到的时候才会去兼容。至于z-index这个bug发生的条件有三个：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;父标签position属性为relative；&lt;/li&gt;
&lt;li&gt;问
    
    </summary>
    
      <category term="技术研究" scheme="https://cnt1992.github.io/categories/%E6%8A%80%E6%9C%AF%E7%A0%94%E7%A9%B6/"/>
    
    
      <category term="css hack ie6 z-index" scheme="https://cnt1992.github.io/tags/css-hack-ie6-z-index/"/>
    
  </entry>
  
  <entry>
    <title>数组去重的算法</title>
    <link href="https://cnt1992.github.io/2013/10/26/%E6%95%B0%E7%BB%84%E5%8E%BB%E9%87%8D%E7%AE%97%E6%B3%95/"/>
    <id>https://cnt1992.github.io/2013/10/26/数组去重算法/</id>
    <published>2013-10-26T14:40:00.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<p>Q：有一数组如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line">arr[</div><div class="line">    &#123;</div><div class="line">        <span class="attr">ID</span>:<span class="number">123</span>,</div><div class="line">        <span class="attr">name</span>:<span class="string">"jackket"</span></div><div class="line">    &#125;,</div><div class="line">    &#123;   </div><div class="line">        <span class="attr">ID</span>:<span class="number">132</span>,</div><div class="line">        <span class="attr">name</span>:<span class="string">"jacket"</span></div><div class="line">    &#125;,</div><div class="line">    &#123;   </div><div class="line">        <span class="attr">ID</span>:<span class="number">123</span>,</div><div class="line">        <span class="attr">name</span>:<span class="string">"tom"</span></div><div class="line">    &#125;</div><div class="line">]</div></pre></td></tr></table></figure>
<p>,用一算法去除ID同样的元素，保留其中一个，输出最后的数组。</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">removeSameID</span>(<span class="params">obj</span>)</span>&#123;</div><div class="line">    <span class="keyword">var</span> _arr = [];</div><div class="line">    _arr.push(obj[<span class="number">0</span>]);</div><div class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> i=<span class="number">0</span>;i &lt; obj.length;i++)&#123;</div><div class="line">        <span class="keyword">var</span> _num = <span class="number">0</span>;</div><div class="line">        <span class="keyword">for</span>(<span class="keyword">var</span> j=<span class="number">0</span>;j &lt; _arr.length;j++)&#123;</div><div class="line">            <span class="keyword">if</span>(obj[i].ID != _arr[j].ID)&#123;</div><div class="line">                _num ++;</div><div class="line">            &#125;</div><div class="line">            <span class="keyword">if</span>(_num == _arr.length)&#123;</div><div class="line">                _arr.push(obj[i]);</div><div class="line">            &#125;</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> _arr;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<blockquote>
<p>注：上面用到了underscore.js插件。</p>
</blockquote>
<p>不过，上面算法复杂度是N的二次方，如果数据相对较少那还可以忽略执行时间，但是数据一旦非常大，显然执行效率是非常差的，于是想到了另外一种解决思路，改进代码如下：</p>
<figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="function"><span class="keyword">function</span> <span class="title">removeSameID</span>(<span class="params">arr</span>)</span>&#123;</div><div class="line">    <span class="keyword">var</span> O = &#123;&#125;;</div><div class="line">    <span class="keyword">var</span> output =[];</div><div class="line">    _.each(arr,<span class="function"><span class="keyword">function</span>(<span class="params">o</span>)</span>&#123;</div><div class="line">        O[o.ID] = o;</div><div class="line">    &#125;);</div><div class="line">    <span class="keyword">for</span>(<span class="keyword">var</span> n <span class="keyword">in</span> O)&#123;</div><div class="line">        output.push(O[n]);</div><div class="line">    &#125;</div><div class="line">    <span class="keyword">return</span> output;</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>很明显，上面这种算法复杂度仅仅为N，执行时间大幅度减少。<br>通过这一个小小的需求，感悟就是：不论是java，c#，还是js，还是其它任何语言，最根本的还是算法，基础有多好，决定了能站多高。</p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Q：有一数组如下：&lt;/p&gt;
&lt;figure class=&quot;highlight javascript&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;arr[&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;    &amp;#1
    
    </summary>
    
      <category term="技术研究" scheme="https://cnt1992.github.io/categories/%E6%8A%80%E6%9C%AF%E7%A0%94%E7%A9%B6/"/>
    
    
      <category term="javascript 算法" scheme="https://cnt1992.github.io/tags/javascript-%E7%AE%97%E6%B3%95/"/>
    
  </entry>
  
  <entry>
    <title>常用的javascript语句</title>
    <link href="https://cnt1992.github.io/2013/09/22/%20%E5%B8%B8%E7%94%A8%E7%9A%84javascript%E8%AF%AD%E5%8F%A5/"/>
    <id>https://cnt1992.github.io/2013/09/22/ 常用的javascript语句/</id>
    <published>2013-09-22T10:16:29.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<blockquote>
<p>我只是大自然的搬运工…</p>
</blockquote>
<a id="more"></a>
<ol>
<li>document.write(“”); 输出语句</li>
<li>JS中的注释为//</li>
<li>传统的HTML文档顺序是:<br> document-&gt;html-&gt;(head,body)</li>
<li>一个浏览器窗口中的DOM顺序是:<br> window-&gt;(navigator,screen,history,location,document)</li>
<li>得到表单中元素的名称和值:<br> document.getElementById(“表单中元素的ID号”).name(或value)</li>
<li>一个小写转大写的JS:<br> document.getElementById(“output”).value=document.getElementById(“input”).value.toUpperCase();</li>
<li>JS中的值类型:<br> String,Number,Boolean,Null,Object,Function</li>
<li>JS中的字符型转换成数值型:<br> parseInt(),parseFloat()</li>
<li>JS中的数字转换成字符型:<br> (“” 变量)</li>
<li>JS中的取字符串长度是:<br>(length)</li>
<li>JS中的字符与字符相连接使用 号.</li>
<li>JS中的比较操作符有:<br>==等于,!=不等于,&gt;,&gt;=,&lt;.&lt;=</li>
<li>JS中声明变量使用:<br>var来进行声明</li>
<li>JS中的判断语句结构:<br>if(condition){}else{}</li>
<li>JS中的循环结构:<br>for([initial expression];[condition];[upadte expression]) {inside loop}</li>
<li>循环中止的命令是:<br>break</li>
<li>JS中的函数定义:<br>function functionName([parameter],…){statement[s]}</li>
<li>当文件中出现多个form表单时.可以用document.forms[0],document.forms[1]来代替.</li>
<li>窗口:<br>打开窗口window.open(), 关闭一个窗口:window.close(), 窗口本身:self</li>
<li>状态栏的设置:<br>window.status=”字符”;</li>
<li>弹出提示信息:<br>window.alert(“字符”);</li>
<li>弹出确认框:<br>window.confirm();</li>
<li>弹出输入提示框:<br>window.prompt();</li>
<li>指定当前显示链接的位置:<br>window.location.href=”URL”</li>
<li>取出窗体中的所有表单的数量:<br>document.forms.length</li>
<li>关闭文档的输出流:<br>document.close();</li>
<li>字符串追加连接符: =</li>
<li>创建一个文档元素:<br>document.createElement(),document.createTextNode()</li>
<li>得到元素的方法:<br>document.getElementById()</li>
<li>设置表单中所有文本型的成员的值为空:<pre><code>var form = window.document.forms[0]
for (var i = 0; i &lt; form.elements.length;i ){
    if (form.elements.type == &quot;text&quot;){
        form.elements.value = &quot;&quot;;
    }
}
</code></pre></li>
<li>复选按钮在JS中判断是否选中:<br>document.forms[0].checkThis.checked<pre><code>(checked属性代表为是否选中返回TRUE或FALSE)
</code></pre></li>
<li>单选按钮组(单选按钮的名称必须相同):<br>取单选按钮组的长度document.forms[0].groupName.length</li>
<li>单选按钮组判断是否被选中也是用checked.</li>
<li>下拉列表框的值:<br>document.forms[0].selectName.options[n].value <pre><code>(n有时用下拉列表框名称加上.selectedIndex来确定被选中的值)
</code></pre></li>
<li>字符串的定义:<br>var myString = new String(“This is lightsword”);</li>
<li>字符串转成大写:<br>string.toUpperCase(); 字符串转成小写:string.toLowerCase();</li>
<li>返回字符串2在字符串1中出现的位置:<br>String1.indexOf(“String2”)!=-1则说明没找到.</li>
<li>取字符串中指定位置的一个字符:<br>StringA.charAt(9);</li>
<li>取出字符串中指定起点和终点的子字符串:<br>stringA.substring(2,6);</li>
<li>数学函数:<br>Math.PI(返回圆周率),Math.SQRT2(返回开方),Math.max(value1,value2)返回两个数中的<br>最在值,Math.pow(value1,10)返回value1的十次方,Math.round(value1)四舍五入函数,<br>Math.floor(Math.random()*(n 1))返回随机数</li>
<li>定义日期型变量:<br>var today = new Date();</li>
<li>日期函数列表:<br>dateObj.getTime()得到时间,dateObj.getYear()得到年份,dateObj.getFullYear()得到四位的年份,<br>dateObj.getMonth()得到月份,dateObj.getDate()得到日,dateObj.getDay()得到日期几,<br>dateObj.getHours()得到小时,dateObj.getMinutes()得到分,dateObj.getSeconds()得到秒,<br>dateObj.setTime(value)设置时间,dateObj.setYear(val)设置年,<br>dateObj.setMonth(val)设置月,dateObj.setDate(val)设置日,<br>dateObj.setDay(val)设置星期几,dateObj.setHours设置小时,dateObj.setMinutes(val)设置分,<br>dateObj.setSeconds(val)设置秒 [注意:此日期时间从0开始计]</li>
<li>IFRAME的表示方式:<br>[window.]frames[n].ObjFuncVarName,frames[“frameName”].ObjFuncVarName,frameName.ObjFuncVarName</li>
<li>parent代表父亲对象,top代表最顶端对象</li>
<li>打开子窗口的父窗口为:opener</li>
<li>表示当前所属的位置:this</li>
<li>当在超链接中调用JS函数时用:(javascript :)来开头后面加函数名</li>
<li>在老的浏览器中不执行此JS:<!-- //--></li>
<li>引用一个文件式的JS:<script type="text/javascript" src="aaa.js"></script></li>
<li>指定在不支持脚本的浏览器显示的HTML:<noscript></noscript></li>
<li>当超链和onCLICK事件都有时,则老版本的浏览器转向a.html,否则转向b.html.<br>例:<a href="a.html" onclick="location.href='b.html';return false">dfsadf</a></li>
<li>JS的内建对象有:<br>Array,Boolean,Date,Error,EvalError,<br>Function,Math,Number,Object,RangeError,ReferenceError,<br>RegExp,String,SyntaxError,TypeError,URIError</li>
<li>JS中的换行:\n</li>
<li>窗口全屏大小:<script>function fullScreen(){ this.moveTo(0,0);this.outerWidth=screen.availWidth;
this.outerHeight=screen.availHeight;}window.maximize=fullScreen;</script></li>
<li>JS中的all代表其下层的全部元素</li>
<li>JS中的焦点顺序:document.getElementByid(“表单元素”).tabIndex = 1</li>
<li>innerHTML的值是表单元素的值:<br>如<p id="para">“how are <em>you</em>“</p>,则innerHTML的值就是:how are <em>you</em></li>
<li>innerTEXT的值和上面的一样,只不过不会把<em>这种标记显示出来.</em></li>
<li>contentEditable可设置元素是否可被修改,isContentEditable返回是否可修改的状态.</li>
<li>isDisabled判断是否为禁止状态.disabled设置禁止状态</li>
<li>length取得长度,返回整型数值</li>
<li>addBehavior()是一种JS调用的外部函数文件其扩展名为.htc</li>
<li>window.focus()使当前的窗口在所有窗口之前.</li>
<li>blur()指失去焦点.与FOCUS()相反.</li>
<li>select()指元素为选中状态.</li>
<li>防止用户对文本框中输入文本:<br>onfocus=”this.blur()”</li>
<li>取出该元素在页面中出现的数量:<br>document.all.tags(“div(或其它HTML标记符)”).length</li>
<li>JS中分为两种窗体输出:模态和非模态.<br>window.showModaldialog(),window.showModeless()</li>
<li>状态栏文字的设置:<br>window.status=’文字’,默认的状态栏文字设置:window.defaultStatus = ‘文字.’;</li>
<li>添加到收藏夹:<br>external.AddFavorite(“<a href="http://meil.cnblogs.com&quot;;,&quot;jaskdlf" target="_blank" rel="external">http://meil.cnblogs.com&quot;;,&quot;jaskdlf</a>“);</li>
<li>JS中遇到脚本错误时不做任何操作:<br>window.onerror = doNothing; 指定错误句柄的语法为:window.onerror = handleError;</li>
<li>JS中指定当前打开窗口的父窗口:<br>window.opener,支持opener.opener…的多重继续.</li>
<li>JS中的self指的是当前的窗口</li>
<li>JS中状态栏显示内容:window.status=”内容”</li>
<li>JS中的top指的是框架集中最顶层的框架</li>
<li>JS中关闭当前的窗口:window.close();</li>
<li>JS中提出是否确认的框:if(confirm(“Are you sure?”)){alert(“ok”);}else{alert(“Not Ok”);}</li>
<li>JS中的窗口重定向:window.navigate(“<a href="http://meil.cnblogs.com" target="_blank" rel="external">http://meil.cnblogs.com</a>“;);</li>
<li>JS中的打印:window.print()</li>
<li>JS中的提示输入框:window.prompt(“message”,”defaultReply”);</li>
<li>JS中的窗口滚动条:window.scroll(x,y)</li>
<li>JS中的窗口滚动到位置:window.scrollby</li>
<li>JS中设置时间间隔:setInterval(“expr”,msecDelay)或setInterval(funcRef,msecDelay)或setTimeout</li>
<li>JS中的模态显示在IE4 行,在NN中不行:showModalDialog(“URL”[,arguments][,features]);</li>
<li>JS中的退出之前使用的句柄:<br>function verifyClose(){event.returnValue=”we really like you and hope you will stay longer.”;}} window.=verifyClose;</li>
<li>当窗体第一次调用时使用的文件句柄:onload()</li>
<li>当窗体关闭时调用的文件句柄:onunload()</li>
<li>window.location的属性:<br>protocol(http:),hostname(www.example.com),port(80),host(www.example.com:80),<br>pathname(“/a/a.html”),hash(“#giantGizmo”,指跳转到相应的锚记),href(全部的信息)</li>
<li>window.location.reload()刷新当前页面.</li>
<li>window.history.back()返回上一页,window.history.forward()返回下一页,<br>window.history.go(返回第几页,也可以使用访问过的URL)</li>
<li>document.write()不换行的输出,document.writeln()换行输出</li>
<li>document.body.noWrap=true;防止链接文字折行.</li>
<li>变量名.charAt(第几位),取该变量的第几位的字符.</li>
<li>“abc”.charCodeAt(第几个),返回第几个字符的ASCii码值.</li>
<li>字符串连接:string.concat(string2),或用 =进行连接</li>
<li>变量.indexOf(“字符”,起始位置),返回第一个出现的位置(从0开始计算)</li>
<li>string.lastIndexOf(searchString[,startIndex])最后一次出现的位置.</li>
<li>string.match(regExpression),判断字符是否匹配.</li>
<li>string.replace(regExpression,replaceString)替换现有字符串.</li>
<li>string.split(分隔符)返回一个数组存储值.</li>
<li>string.substr(start[,length])取从第几位到指定长度的字符串.</li>
<li>string.toLowerCase()使字符串全部变为小写.</li>
<li>string.toUpperCase()使全部字符变为大写.</li>
<li>parseInt(string[,radix(代表进制)])强制转换成整型.</li>
<li>parseFloat(string[,radix])强制转换成浮点型.</li>
<li>isNaN(变量):测试是否为数值型.</li>
<li>定义常量的关键字:const,定义变量的关键字:var</li>
</ol>
]]></content>
    
    <summary type="html">
    
      &lt;blockquote&gt;
&lt;p&gt;我只是大自然的搬运工…&lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="技术研究" scheme="https://cnt1992.github.io/categories/%E6%8A%80%E6%9C%AF%E7%A0%94%E7%A9%B6/"/>
    
    
      <category term="javascript" scheme="https://cnt1992.github.io/tags/javascript/"/>
    
  </entry>
  
  <entry>
    <title>兼容IE6~IE9的CSS Hack</title>
    <link href="https://cnt1992.github.io/2013/09/22/%E5%85%BC%E5%AE%B9IE6~IE9%E7%9A%84CSS%20Hack/"/>
    <id>https://cnt1992.github.io/2013/09/22/兼容IE6~IE9的CSS Hack/</id>
    <published>2013-09-22T10:16:29.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<figure class="highlight"><table><tr><td class="code"><pre><div class="line">div &#123;</div><div class="line">  background-color:#f00\0; /* ie 8/9 */</div><div class="line">  background-color:#f00\9\0; /* ie 9 */</div><div class="line">  *background-color:#0f0; /* ie 7 */</div><div class="line">  _background-color:#00f; /* ie 6 */</div><div class="line">&#125;</div></pre></td></tr></table></figure>
<p>其中，‘\0’对ie8和ie9都支持，而‘\9\0’只支持ie9，所以顺序不可以颠倒。</p>
]]></content>
    
    <summary type="html">
    
      &lt;figure class=&quot;highlight&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;div class=&quot;line&quot;&gt;div &amp;#123;&lt;/div&gt;&lt;div class=&quot;line&quot;&gt;  background-color:#f00\0; /*
    
    </summary>
    
      <category term="技术研究" scheme="https://cnt1992.github.io/categories/%E6%8A%80%E6%9C%AF%E7%A0%94%E7%A9%B6/"/>
    
    
      <category term="css hack" scheme="https://cnt1992.github.io/tags/css-hack/"/>
    
  </entry>
  
  <entry>
    <title>Solr常用查询语句</title>
    <link href="https://cnt1992.github.io/2013/08/30/Solr%E5%B8%B8%E7%94%A8%E6%9F%A5%E8%AF%A2%E8%AF%AD%E5%8F%A5/"/>
    <id>https://cnt1992.github.io/2013/08/30/Solr常用查询语句/</id>
    <published>2013-08-30T14:40:00.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<p>下周开始要用nodejs配合Solr写mobile项目后台功能了，现在在熟悉Solr查询语句，摘自互联网的语句如下：</p>
<blockquote>
<p>q 查询字符串，必须的。Solr中用来搜索的查询。有关该语法的完整描述，请参阅 参考资料 中的 “Lucene QueryParser Syntax”。可以通过追加一个分号和已索引且未进行断词的字段的名称来包含排序信息。默认的排序是 score desc，指按记分降序排序。   </p>
</blockquote>
<a id="more"></a>
<ul>
<li>q=myField:Java AND otherField:developerWorks;</li>
<li>dateasc此查询搜索指定的两个字段并根据一个日期字段对结果进行排序。</li>
<li>start 返回第一条记录在完整找到结果中的偏移位置，0开始，一般分页用。</li>
<li>rows 指定返回结果最多有多少条记录，配合start来实现分页。</li>
<li>sort 排序，格式：sort=<field name="">+<desc|asc>[,<field name="">+<desc|asc>]… 。示例：（inStock desc, price asc）表示先 “inStock” 降序, 再 “price” 升序，默认是相关性降序。</desc|asc></field></desc|asc></field></li>
<li>wt (writer type)指定输出格式，可以有 xml, json, php, phps, 后面 solr 1.3增加的，要用通知我们，因为默认没有打开。</li>
<li>fq （filter query）过虑查询，作用：在q查询符合结果中同时是fq查询符合的，</li>
<li>fl field作为逗号分隔的列表指定文档结果中应返回的 Field 集。默认为 “<em>”，指所有的字段。“score” 指还应返回记分。例如 </em>,score<br>将返回所有字段及得分。用solrj的bean时，得在query中指定 query.set(“fl”, “*,score”);</li>
<li>q.op 覆盖schema.xml的defaultOperator（有空格时用”AND”还是用”OR”操作逻辑），一般默认指定</li>
<li>df 默认的查询字段，一般默认指定</li>
<li>qt（query type）指定那个类型来处理查询请求，一般不用指定，默认是standard。</li>
<li>indent – 返回的结果是否缩进，默认关闭，用 indent=true|on 开启，一般调试json,php,phps,ruby输出才有必要用这个参数。</li>
<li>version – 查询语法的版本，建议不使用它，由服务器指定默认值。</li>
</ul>
<p>hight:</p>
<ul>
<li>hl-highlight，h1=true，表示采用高亮。可以用h1.fl=field1,field2 来设定高亮显示的字段。</li>
<li>hl.fl: 用空格或逗号隔开的字段列表。要启用某个字段的highlight功能，就得保证该字段在schema中是stored。如果该参数未被给出，那么就会高 亮默认字段 standard handler会用df参数，dismax字段用qf参数。你可以使用星号去方便的高亮所有字段。如果你使用了通配符，那么要考虑启用 hl.requiredFieldMatch选项。</li>
<li>hl.requireFieldMatch:<br>如果置为true，除非该字段的查询结果不为空才会被高亮。它的默认值是false，意味 着它可能匹配某个字段却高亮一个不同的字段。如果hl.fl使用了通配符，那么就要启用该参数。尽管如此，如果你的查询是all字段（可能是使用 copy-field 指令），那么还是把它设为false，这样搜索结果能表明哪个字段的查询文本未被找到</li>
<li>hl.usePhraseHighlighter:<br>如果一个查询中含有短语（引号框起来的）那么会保证一定要完全匹配短语的才会被高亮。</li>
<li>hl.highlightMultiTerm<br>如果使用通配符和模糊搜索，那么会确保与通配符匹配的term会高亮。默认为false，同时hl.usePhraseHighlighter要为true。</li>
<li>hl.snippets：<br>这是highlighted片段的最大数。默认值为1，也几乎不会修改。如果某个特定的字段的该值被置为0（如f.allText.hl.snippets=0），这就表明该字段被禁用高亮了。你可能在hl.fl=*时会这么用。</li>
<li>hl.fragsize:<br>每个snippet返回的最大字符数。默认是100.如果为0，那么该字段不会被fragmented且整个字段的值会被返回。大字段时不会这么做。</li>
<li>hl.mergeContiguous:<br>如果被置为true，当snippet重叠时会merge起来。</li>
<li>hl.maxAnalyzedChars:<br>会搜索高亮的最大字符，默认值为51200，如果你想禁用，设为-1</li>
<li>hl.alternateField:<br>如果没有生成snippet（没有terms 匹配），那么使用另一个字段值作为返回。</li>
<li>hl.maxAlternateFieldLength:<br>如果hl.alternateField启用，则有时需要制定alternateField的最大字符长度，默认0是即没有限制。所以合理的值是应该为</li>
<li>hl.snippets * hl.fragsize这样返回结果的大小就能保持一致。</li>
<li>hl.formatter:一个提供可替换的formatting算法的扩展点。默认值是simple，这是目前仅有的选项。显然这不够用，你可以看看org.apache.solr.highlight.HtmlFormatter.java 和 solrconfig.xml中highlighting元素是如何配置的。<br>注意在不论原文中被高亮了什么值的情况下，如预先已存在的em tags，也不会被转义，所以在有时会导致假的高亮。</li>
<li>hl.fragmenter:<br>这个是solr制 定fragment算法的扩展点。gap是默认值。regex是另一种选项，这种选项指明highlight的边界由一个正则表达式确定。这是一种非典型 的高级选项。为了知道默认设置和fragmenters (and formatters)是如何配置的，可以看看solrconfig.xml中的highlight段。</li>
<li>regex 的fragmenter有如下选项：<br>hl.regex.pattern:正则表达式的pattern<br>hl.regex.slop:这是hl.fragsize能变化以适应正则表达式的因子。默认值是0.6，意思是如果hl.fragsize=100那么fragment的大小会从40-160.<br>这些值都可以在select中加入，也可以用solrj的api去设定，也可以配置在solrconfig.xml中配置。<br>示例如下：</li>
</ul>
<figure class="highlight"><table><tr><td class="code"><pre><div class="line">&lt;requestHandler name="search" class="solr.SearchHandler" default="true"&gt;</div><div class="line">    &lt;!– default values for query parameters can be specified, these</div><div class="line">         will be overridden by parameters in the request</div><div class="line">      –&gt;</div><div class="line">     &lt;lst name="defaults"&gt;</div><div class="line">       &lt;str name="echoParams"&gt;explicit&lt;/str&gt;</div><div class="line">       &lt;int name="rows"&gt;10&lt;/int&gt;</div><div class="line">       &lt;bool name="hl"&gt;true&lt;/bool&gt; </div><div class="line">       &lt;str name="hl.fl"&gt;title,content&lt;/str&gt;  </div><div class="line">       &lt;str name="f.content.hl.fragsize"&gt;200&lt;/str&gt;</div><div class="line">       &lt;str name="mlt.qf"&gt;</div><div class="line">         id^10.0 title^10.0 content^1.0</div><div class="line">       &lt;/str&gt;</div><div class="line">     &lt;/lst&gt;</div><div class="line">&lt;/requestHandler&gt;</div></pre></td></tr></table></figure>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;下周开始要用nodejs配合Solr写mobile项目后台功能了，现在在熟悉Solr查询语句，摘自互联网的语句如下：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;q 查询字符串，必须的。Solr中用来搜索的查询。有关该语法的完整描述，请参阅 参考资料 中的 “Lucene QueryParser Syntax”。可以通过追加一个分号和已索引且未进行断词的字段的名称来包含排序信息。默认的排序是 score desc，指按记分降序排序。   &lt;/p&gt;
&lt;/blockquote&gt;
    
    </summary>
    
      <category term="个人学习" scheme="https://cnt1992.github.io/categories/%E4%B8%AA%E4%BA%BA%E5%AD%A6%E4%B9%A0/"/>
    
    
      <category term="solr node.js 查询语句" scheme="https://cnt1992.github.io/tags/solr-node-js-%E6%9F%A5%E8%AF%A2%E8%AF%AD%E5%8F%A5/"/>
    
  </entry>
  
  <entry>
    <title>原生JS实现li点击弹出索引</title>
    <link href="https://cnt1992.github.io/2012/10/10/%E5%8E%9F%E7%94%9FJS%E5%AE%9E%E7%8E%B0li%E7%82%B9%E5%87%BB%E5%BC%B9%E5%87%BA%E7%B4%A2%E5%BC%95/"/>
    <id>https://cnt1992.github.io/2012/10/10/原生JS实现li点击弹出索引/</id>
    <published>2012-10-09T18:57:29.000Z</published>
    <updated>2016-03-31T00:46:55.000Z</updated>
    
    <content type="html"><![CDATA[<p>两种方法原生JS实现li点击弹出索引</p>
<ol>
<li>第一种方法：直接将索引i寄存在对象中，代码如下：</li>
</ol>
<p>Html结构：<br><figure class="highlight html"><table><tr><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">"test"</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>0<span class="tag">&lt;/<span class="name">li</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>1<span class="tag">&lt;/<span class="name">li</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>2<span class="tag">&lt;/<span class="name">li</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>3<span class="tag">&lt;/<span class="name">li</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>JS：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="built_in">window</span>.onload = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> oUl = <span class="built_in">document</span>.getElementById(<span class="string">'test'</span>),</div><div class="line">        oLi = oUl.getElementsByTagName(<span class="string">'li'</span>);</div><div class="line">    <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>,len = oLi.length; i &lt; len; i++) &#123;</div><div class="line">        oLi[i].idx = i;</div><div class="line">        oLi[i].onclick = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">            alert(<span class="keyword">this</span>.idx)</div><div class="line">        &#125;</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
<ol>
<li>第二种方法：利用JS闭包实现，代码如下：</li>
</ol>
<p>Html结构：<br><figure class="highlight html"><table><tr><td class="code"><pre><div class="line"><span class="tag">&lt;<span class="name">ul</span> <span class="attr">id</span>=<span class="string">"test"</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>0<span class="tag">&lt;/<span class="name">li</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>1<span class="tag">&lt;/<span class="name">li</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>2<span class="tag">&lt;/<span class="name">li</span>&gt;</span></div><div class="line">    <span class="tag">&lt;<span class="name">li</span>&gt;</span>3<span class="tag">&lt;/<span class="name">li</span>&gt;</span></div><div class="line"><span class="tag">&lt;/<span class="name">ul</span>&gt;</span></div></pre></td></tr></table></figure></p>
<p>JS：<br><figure class="highlight javascript"><table><tr><td class="code"><pre><div class="line"><span class="built_in">window</span>.onload = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">    <span class="keyword">var</span> oUl = <span class="built_in">document</span>.getElementById(<span class="string">'test'</span>),</div><div class="line">        oLi = oUl.getElementsByTagName(<span class="string">'li'</span>);</div><div class="line">    <span class="keyword">for</span> (<span class="keyword">var</span> i = <span class="number">0</span>,len = oLi.length; i &lt; len; i++) &#123;       </div><div class="line">        (<span class="function"><span class="keyword">function</span> (<span class="params">n</span>) </span>&#123;</div><div class="line">            oLi[i].onclick = <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</div><div class="line">                alert(n)</div><div class="line">            &#125;</div><div class="line">        &#125;)(i)</div><div class="line">    &#125;</div><div class="line">&#125;</div></pre></td></tr></table></figure></p>
]]></content>
    
    <summary type="html">
    
      &lt;p&gt;两种方法原生JS实现li点击弹出索引&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;第一种方法：直接将索引i寄存在对象中，代码如下：&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Html结构：&lt;br&gt;&lt;figure class=&quot;highlight html&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;co
    
    </summary>
    
      <category term="技术研究" scheme="https://cnt1992.github.io/categories/%E6%8A%80%E6%9C%AF%E7%A0%94%E7%A9%B6/"/>
    
    
      <category term="javascript" scheme="https://cnt1992.github.io/tags/javascript/"/>
    
  </entry>
  
</feed>
